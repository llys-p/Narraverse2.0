# 叙界 Narraverse + Denova 平台模块功能总览

> 用途：一页看清当前项目（`<project-root>`）两大组成部分——**叙界 Narraverse（前端 SPA 文字冒险平台）**与 **Denova（Go 后端 + React 前端的 AI 创作平台）**——各自拥有的模块与功能，以及二者的整合关系。
> 主要依据：`代码指南.md`、`项目协作日志.md`、`denova-src/README.md`、`denova-src/DESIGN.md`、目录结构。

---

# 第一部分：叙界 Narraverse（前端 SPA 文字冒险平台）

无构建步骤的纯静态单页应用，整页逻辑集中在 `app/app.js`（约 415KB），由 `index.html` 加载、`style.css` 着色。既可 standalone 运行，也可作为 **Denova 的第三顶层模式**从 `/narraverse/index.html?embedded=denova` 同源嵌入。

## 1. 文件地图（核心模块）

| 模块 | 文件 | 职责 |
|---|---|---|
| 核心逻辑 | `app/app.js` | 状态机、提示词构建、渲染、交互、文件解析（90% 功能改动在此） |
| DOM 骨架 | `app/index.html` | 顶栏、左主栏、各 Modal、脚本加载顺序 |
| 主样式 | `app/style.css` | 全部样式 + 主题变量 |
| 重设计层 | `app/redesign.css` | dark-game 玻璃拟态风、命令面板、右栏 Tab 化、嵌入态去重（不改原 style.css） |
| 中性主题 | `app/theme-neutral.css` | 第三套中性基线（`body[data-theme="neutral"]`） |
| 知识库数据 | `app/local_library.js` | **自动生成、禁手改**；设定书/角色卡源数据（`books`） |
| 离线小说引擎 | `app/game_engine.js` | 事件图 + 检定 + 战斗 + 结局，含可选 AI 增强层 |
| 内容包 | `app/packs/limitless.js` | 离线线内容（永生题材） |
| 火柴人 | `app/stickman.js` | 右侧「火柴人」可视化（`ACTIONS` 关键帧 + 插值） |
| 场景预设 | `app/presets.js` | 5 套场景预设 + 6 个右栏 Tab 引擎 |
| 宿主协议 | `app/bridge.js` | Denova 宿主 v1 信封（`ready/switch-mode/theme-changed/locale-changed`） |

脚本加载顺序（index.html 末尾）：`stickman.js → game_engine.js → packs/limitless.js → local_library.js → app.js → presets.js → bridge.js`。

## 2. 两条玩法线（共用同一套状态与渲染）

- **在线冒险线**：`callLLM` SSE 流式，解析固定协议 `[NARRATIVE][STATE][CHANGES][CHOICES][QUESTS][COMBAT][CHARACTERS][PLOT]`，驱动叙事/角色/世界。
- **离线小说线**：`GameEngine.open()` 入口，**默认完全离线、可选 AI 增强**。界面「✨ AI 增强」开关（默认关），开启且配置好 endpoint/key/model 时复用全局 `callLLM` 扩写事件叙事/选项结果/战斗结局/副本序章；解析回复末尾 `<action:xxx>` 驱动火柴人（`idle/walk/run/attack/cast/hit/jump/dodge/fall/win/search/talk`）。数值/检定/战斗/结局逻辑始终离线，AI 失败自动回退；事件级 AI 文本按 `pack.id+事件id` 缓存到 localStorage。

## 3. 提示词构建管线（最重要扩展点）

每回合 `sendMessage → updateSystemPrompt → 重建 conversationHistory[0]`：

- `buildSystemPrompt`（在线）/ `buildTavernSystemPrompt`（酒馆线，无战斗骰子）
- 注入顺序：世界设定 → NPC 角色卡 → 在场群像 → 角色专属 system → World Info → 玩家自定义规则 → 曼陀罗人物 → 玩家状态 → 回复格式契约 → 22 条规则 → 置尾指令
- **提示词宏** `{{user}}{{location}}{{chapter}}{{level}}{{hp}}{{mood}}{{date}}{{random}}…`（未知宏原样保留，可开关）
- **World Info 化** `buildLorebookBlock`：概览常驻 + 近 N 回合命中关键词才注入（预算/深度可调）

## 4. 冒险全生命周期

- `createAdventure` 构造对象 + 生成系统提示词
- **三步创建向导**：① 选择素材（角色卡/世界设定卡/偏好）② AI 生成 **3 套差异化故事方案** ③ 确认开局（标题/背景/身份/地点/目标/开场钩子）
- V3 多开局（`first_mes` + `alternate_greetings` 选择）
- 发送主回合、解析协议、落库、左栏拖拽重排、切换当前冒险
- 自定义职业硬约束（防回落默认剑士）

## 5. 角色卡系统（NPC，V3 字段）

- `normalizeCardObject` 归一化（PNG/TXT/MD/JSON 统一）
- 字段：name/appearance/personality/relationship/scenario/tags/first_mes/alternate_greetings/character_note/system_prompt/post_history_instructions/talkativeness/present
- 导入：`parseFileToCards`（PNG 走 `extractCharaFromPNG` 读 `tEXt` 块；文本走 `parseCharacterCardsFromFile`）
- 群像在场过滤 + 话痨度权重

## 6. 主题 / 冒险类型

- 内置主题：奇幻/科幻/恐怖/末日/武侠/悬疑
- **26 个冒险类型**（题材×玩法模式），网格内无编辑工具
- 自定义主题 CRUD（删除内置=隐藏，同名重建恢复）
- 随机冒险（含冒险类型池）

## 7. 扩展生态（内嵌于 AI 气泡，无独立弹窗）

- 🔊 **TTS 朗读（可点停）**：`native` 浏览器原生 / `cosyvoice` 阿里云（经 `tools/cosyvoice_proxy.py` 本地中继 `ws://localhost:8787` 代注 Authorization 头，失败回退原生）
- 🖼 **AI 生图**：每条叙事内「生成场景图」键（DashScope Qwen-Image，图渲染进气泡）

## 8. 存储层

- 主存 `IndexedDB`（`adventureAI_db`），localStorage 仅做小数据镜像（>4MB 不写）
- 写入策略分级（<1MB 双写 / 1~4MB 单写 / >4MB 不写）
- 「存储体检」显示真实用量
- **永久对话档案** `conversation-archive v1`（独立存 IndexedDB，压缩/回溯不删除）

## 9. 知识库管线（`knowledge-base/` + `tools/`）

- 根目录 `knowledge-base/`（世界观设定/角色卡/归档/草稿）
- `gen_local_library_v2.py` → 生成 `local_library.js`（含 summary/tags/category）
- `rebuild_meta_categories.py` → 补 category 字段、清理、修复坏 JSON
- `kb_sync.py` + `kb-summary-sync` skill → 中文简介同步三处（元数据/说明文档/Obsidian）
- **chub.ai 接口**：搜索/下载设定书与角色卡（不支持中文关键词，需完整 UA）
- 平台加载界面按 category 分组，NSFW 默认隐藏

## 10. Pixiv 搜图（本地桥接）

- `tools/pixiv_bridge.py`（127.0.0.1:8098，pixivpy3，CORS 放开）
- 接口：/status /search /ranking /illust /user /img(代理带 Referer) /download /ugoira(动图) /bookmark
- 前端：搜索/排行榜/网格/详情多图/翻页/收藏/动图播放/换一个/设为场景图/搜图参考/封面
- NSFW 按 `x_restrict`/`sanity_level` 过滤，R18 角标

## 11. Denova 互通（`denova/` + `tools/`）

- `denova_bridge.py`（8097）轻量同步路由 + 全量任务控制
- `denova_full_export.py` — `export-bundle v4` + HY-MT 串行翻译队列（分块 ≤8MiB，原子提交英文原件，冲突写入 `.narraverse/conflicts/`）
- 前端「🚀 导出 Denova」`exportToDenova()`：读永久档案 → 分片上传（清点/回源/恢复/翻译/原子写入/验收），**不调 callLLM**
- 双向同步 `buildAdventureSyncPayload` / `pushAdventureSync` / `pullAdventureSync`（防抖 1.2s，避免循环）
- P1 加载库接入 Denova 工程（拉取 lore 转设定书）
- 本地翻译模型 HY-MT（Ollama），单条/批量翻译，术语表 `translation_glossary.json`
- 本地管家模型 `qwen2.5:7b`（`qwen25-local` profile）
- `denova_lore_sync.js` 平台设定书 ↔ Denova 工程互转

## 12. UI 重设计 / 统一外壳

- 顶栏收口：3 常驻（存档/导出/离线游戏）+「✎ 创作 ▾」下拉 + 视图图标簇 + 🔍 命令面板
- **命令面板** `openCommandPalette`（`Ctrl/Cmd+K`，↑↓/Enter/Esc，按场景过滤）
- **右栏 Tab 化**：常驻状态速览 + Tab（状态/属性/技能/背包/关系/任务）
- **AI 写手思考过程**：捕获 `reasoning_content`，可折叠 `💭 AI 写手思考过程`（区别于角色独白）
- **场景预设引擎** `presets.js`：5 套（探索/严肃长篇/互动游戏/快速试玩/世界观构建）+ 6 右栏 Tab + 按场景过滤命令
- 本地 Profile（nickname/主题/NSFW/默认预设），无云端
- iframe 嵌入 + postMessage 桥接（`bridge.js`，白名单 origin）

## 13. 写作工程化 / 时间轴

- **大纲/细纲**：`novelPlan{outline,groups}`，`generateNovelOutline`，一键按大纲写作
- **版本账本**：`versionLedger`（上限 50）+ `snapshotDiff` 对比最近两次快照
- **时间轴·回溯中心**：🗺 分支图（Denova 风格泳道 SVG）/ 📋 列表；回溯点（快照/分支/IF 线）；开新分支、进入节点、IF 线进出（`saveLineResume`/`applyLineResume`）
- **可选小说化**：`buildNovelChapters` 按消息边界分卷，检查点无损

## 14. 启动 / 运维

- `Launch-Narraverse.cmd`（用户双击入口）→ `tools/start_narraverse.ps1`（纯 ASCII，探测/启动 8097→8098→Denova `:8080`）
- 本地预览 `python -m http.server 8099`
- 校验：`node --check app/app.js` + 无头回归（adv_harness / test_adventure_types 28/28 / test_new_features 15/15 / test_pixiv 17/17 / test_denova_*）
- 三处同步铁律：代码改动 → `代码指南.md` + `项目协作日志.md`

---

# 第二部分：Denova（Go 后端 + React 前端的 AI 创作平台）

当前版本 **v0.3.3 Beta**。Go 1.26+ 后端 + Node.js 20+ 前端，Apache-2.0。**写作**与**游戏**两个并列工作台共享资料库 / 方案预设 / 模型与 Agent 配置 / Skills / 版本管理 / 基础设置。

## 1. 后端内部模块（`denova-src/internal`，Go 包）

**核心应用与 API**
- `app` / `api` — 应用层与 HTTP 接口
- `session` / `messages` — 会话与消息流
- `book` / `bookcover` — 书籍/工程与封面生成
- `workspacechange` / `workspacepath` / `webfs` — 工作区变更账本、路径、Web 文件服务
- `restart` / `buildinfo` / `observability` / `keyedlock` — 重启、构建信息、可观测性、键锁

**AI 创作引擎**
- `agent` — 创作 Agent 引擎（工具调用、SubAgent、Diff 审阅）
- `skills` — 技能运行时
- `prompts` — 提示词构建
- `providercompat` — 多供应商（OpenAI 兼容）适配

**游戏 / 互动**
- `interactive` — 游戏模式、分支、故事线、导演
- `interactiveimage` — 互动图像

**资料库 / 设定**
- `loreimage` — 设定书图像
- `styleref` — 风格参考
- `revisionfile` — 修订文件

**图像创作**
- `imagegen` / `imagepreset` / `illustration` — 图像生成、图像方案、章节插画

**自动化与审阅**
- `automation` — 定时任务 / Review / 自动续写 / 自定义 Prompt 工作流
- `documentreview` — 可信审阅意见
- `autosaveconflict` — 自动保存冲突处理

**本地化 / 更新**
- `i18n` — 中英文
- `update` — 版本更新

## 2. 前端特性模块（`denova-src/web/src/features`，React）

| 模块 | 功能 |
|---|---|
| `chapters` | 写作模式正文：Markdown 编辑、多 Tab、正则查找替换、工作区全局替换、章节统计、大纲/细纲、进度追踪、正文评论 |
| `document-review` | 可信审阅意见接入 Agent |
| `changes` | Change Review（累计 Diff 审阅、评论、撤销） |
| `agents` | 创作 Agent 工作台（选区/文件/资料库/审阅读取，工具调用，Skills/SubAgents） |
| `interactive` | 游戏模式：玩家输入、剧情分支、故事线切换、导演运行策略、行动建议、AI 回复修正、回合历史检索、Actor State、自定义状态布局、导演台（目标/压力/代价/事件卡包/规则检定） |
| `skills` | 技能浏览与管理 |
| `automations` | 自动化任务配置与运行 |
| `versions` | 版本管理：本地 Git 版本、Diff、恢复、变更账本 Undo/Redo |
| `translation` | 本地翻译（HY-MT 串行队列，术语表/占位符保护） |
| `narraverse` | **Narraverse（叙界）嵌入宿主**——把叙界作为 Denova 的第三顶层模式（`NarraverseWorkspace`，postMessage 桥接、可见性同步、导入工程导演策略） |
| `settings` | 通用/写作/游戏/远程访问/模型与 Agent 配置 |
| `onboarding` | 新手引导 |
| `notices` | 通知 |
| `messages` | 消息流渲染 |
| `motion` | 动效 token（novaEase/novaSpring，尊重 reduced/off） |

**组件层**（`web/src/components`）：`Editor`（TipTap/Monaco 正文）、`Chat`、`Sidebar`、`layout`（WorkspaceLayout + react-resizable-panels）、`navigation`、`resource-directory`、`review`、`Versions`、`workbench`、`Home`、`ai-elements`、`forms`、`ui`（shadcn/Radix 原语）、`common`。

**API 客户端**（`web/src/lib/api-client`）：`books` `chat` `config-manager` `lore` `material-library` `skills` `versions` `workspace` `automations` `local-translation`。

## 3. 内置技能（20 个，`denova-src/skills`）

- 写作类：`novel-heavy` / `novel-standard` / `novel-lite` / `outline` / `rewrite` / `continue` / `group-plan` / `orchestrate-projects`
- 设定/资料：`lore` / `lore-init` / `teller-config`（故事讲述者预设）
- 图像：`chapter-illustration` / `image-preset-config` / `interactive-image`
- 导演/自动化：`story-director-config` / `automation-config` / `agent-config`
- 工具：`skills-creator`

## 4. 跨模块核心能力

1. **写作模式**：Markdown 多 Tab 编辑、正则查找替换、全局替换自动备份、章节统计、大纲+章节组细纲、进度追踪、正文评论、Change Review、已有小说导入。
2. **创作 Agent**：读选区/文件/资料库/审阅 → 调用工具生成或修改章节 → Skills/SubAgents 适配文风工作流 → 累计 Diff 审阅/评论/撤销。
3. **游戏模式 + 故事导演**：首个场景前结合开局+资料库排布舞台/角色/线索/分支；运行时持续调向；资料库重要角色/地点/势力/规则优先入戏；可插拔组合「叙事风格 / 事件包 / TRPG 检定 / 状态系统 / 图像方案」，**每模块可独立关闭**；状态系统提供可复用词条库 + 模板决定 Actor 抽取规则；历史事实以已提交 Turn 为真源，当前事实归 Actor State，稳定设定归资料库，未来意图归 `director.md`。
4. **资料库与预设**：沉淀角色/世界观/地点/势力/规则/物品；叙事风格管文风+提示词槽位+场景风格；故事导演预设可插拔组合。
5. **图像创作**：章节插画 / 互动图像 / 书籍封面，复用 OpenAI 兼容图像模型配置，界面预览管理。
6. **上下文管理**：渐进式组织可见上下文，来源展开与一键复制，回合来源历史检查点，缓存优化，有界工具结果（降噪降 token）。
7. **版本与恢复**：本地 Git 版本 + Diff + 恢复；工作区变更账本跨重启 Undo/Redo；定时与 Agent 批量输出后自动保存。
8. **自动化**：定时任务、Review、自动续写、自定义 Prompt 工作流。
9. **产品化**：中英文、浅/深主题、OpenAI 兼容模型配置、远程访问（局域网 + 反向代理 HTTPS）、PWA 手机、Win/Mac/Linux 全平台。

---

# 第三部分：叙界 ↔ Denova 整合关系

- 叙界通过 `features/narraverse` 作为 **Denova 的第三顶层模式** 嵌入（`?mode=narraverse` / `/narraverse/index.html?embedded=denova`），同源 `bridge.js` postMessage 握手，可见性变化触发 `pullCurrentAdventureSync()`。
- **叙界 → Denova**：`tools/denova_bridge.py`（8097）、`denova_full_export.py` 全量导出 + HY-MT 翻译、资料库导入（`material-import.go`）；Denova 工程的真源落在 `.narraverse/source/`，清单 `.narraverse/import-manifest.json`。
- **Denova → 叙界**：回写标题 / 四段设定 / 摘要 / 章节 / 聚合 lore 设定书（不覆盖叙界原始对象，避免丢失 `alternate_greetings`、外貌、关系等字段）。
- **导入工程导演策略**：叙界导入工程采用 `director_run_policy=manual` 首次收起右侧导演区（Denova 自身导演仍保留）。
- **启动入口**：用户只运行 `<project-root>\Launch-Narraverse.cmd` → `tools/start_narraverse.ps1` 探测/启动 8097（桥接）→ 8098（Pixiv）→ Denova `:8080`。

---

# 第四部分：关键端口与服务速查

| 端口 | 服务 | 说明 |
|---|---|---|
| 8080 | Denova 后端 | Go 服务，主体功能 API |
| 8097 | `denova_bridge.py` | 叙界↔Denova 轻量同步 + 全量导出 + 翻译队列 |
| 8098 | `pixiv_bridge.py` | Pixiv 搜图本地桥接（pixivpy3，CORS 放开） |
| 8099 | 叙界静态服务 | `python -m http.server 8099`（或 Denova 内嵌同源） |
| 8787 | `cosyvoice_proxy.py` | CosyVoice TTS 本地中继（代注 Authorization 头） |
| 11434 | Ollama | 本地翻译 HY-MT / 管家模型 qwen2.5:7b |

---

> 维护提示：叙界侧代码与功能改动须同步更新 `代码指南.md` 与 `项目协作日志.md`（三处同步铁律）；Denova 侧遵循 `DESIGN.md` 设计系统（语义 token、双语、可访问性、动效偏好）。

const runtime = {
  'runtime.title': 'Denova frontend render error',
  'runtime.description': 'The crash reason was recorded in the browser console and localStorage.nova.runtime.logs.',
  'runtime.reload': 'Reload Page',
} as const

export default runtime

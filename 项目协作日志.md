# 叙界 Narraverse · 项目协作日志
## 2026-09-11 14:28 · Codex：Phase 3 v2.7 完成 3.0B 设计冻结收口（仅文档，未编码）

- 修订 `docs/plans/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md` 为 **v2.7 / Phase 3.0B Design Freeze**，未修改 Snapshot schema（§4.7～§4.12）及任何生产代码。
- **Capability 边界纠偏**：确认当前同源、无 sandbox 的 iframe 与顶层浏览器宿主不存在可由服务端验证的信任根；v2.6 的一次性 bootstrap secret 与 A/B/C 多选方案降级为历史候选。Capability、iframe bind/resume 与受控模型入口整体移至 **3.2-C0 独立安全 Design Freeze**；3.0B 明确禁止创建相关 token、路由、origin/sandbox 或数据迁移代码。
- **analysisHandle 闭环**：同一 `(consumer, sessionKey, contextFingerprint)` 只有一个未消费 handle；注册表以锁/CAS 执行 `pending → claimed → consumed`；先分配运行身份并迁移 runContext，再启动 Task goroutine。失败不得启动模型或遗留半成品映射；handle 保持短期、单次、非身份、非阻断降级。
- **InteractiveRun 对齐真实 Task 时序**：新增 `TaskRunIndex`（Task ID 分配后、goroutine 启动前写入）与 `TurnRunIndex`（仅 `interactive_turn_persisted` 后写入）；regenerate 先由 turn 反查原 InteractiveRun，再创建新 Task；失败/abort 不伪造 turn 映射，未命中不猜 run。
- 同步清理 3.0B 中残留的 iframe `run_context_id`、宿主映射与 capability 现行表述；Phase 3.2 顺序冻结为 **3.2-A 写作 → 3.2-B 游戏 → 3.2-C0 安全设计与隔离验证 → 3.2-C Narraverse → 3.2-D Module4**。
- 文档级验证已完成：`git diff --check` 通过；v2.7 必需契约与 3.0B 禁止项关键词扫描通过；tracked 改动仅为本计划与协作日志。不运行代码测试或构建（本轮无代码改动）。

## 2026-09-11 12:25 · Workbuddy：Phase 3 架构方案修订为 v2.6 Design Freeze Candidate（仅文档，未编码）

- 依据 Codex Phase 3 v2.5 Design Freeze Review + 短复审（12:18–12:21）的 NEED REVISION 三个阻塞点，修订 docs/plans/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md → v2.6（1413 → 1609 行；§0 新增 v2.4→v2.6 变更对照，v2.5 摘要降级为 §0.1 历史）。**Snapshot schema（§4.7～§4.12）复审已通过，本版未改动。**
- ①**Capability 形成真实信任根**（§7.6.1 重写 + §7.6.0 强化）：
  - **承认 v2.5 缺陷**：宿主实为浏览器 React 页面，仓库中不存在「宿主进程凭据 / 本地监听令牌」；iframe **同源且无 sandbox** ⇒ 顶层宿主与 iframe 能发出完全一样的 HTTP 请求（同 cookie/header/Origin/Sec-Fetch-Site）⇒ **纯 HTTP bootstrap 无法区分**；仅声明「iframe 不能申请」**不构成技术边界**。
  - **冻结具体可信来源**：**Denova 进程生成一次性 bootstrap secret**（`denovaBootstrapSecret`，CSPRNG ≥32B base64url；**一次性**——签发成功即作废；**≤5 分钟**未用失效；**仅内存**；**只经进程外通道**注入顶层宿主，如启动器注入 `window.__DENOVA_BOOTSTRAP_SECRET__` / 本地临时文件 / 本地 socket；**禁止**写入 localStorage/sessionStorage/cookie/IndexedDB、**禁止**经 HTTP 响应体下发）；**capability 签发端点强制校验**该 secret。
  - **给出「iframe 为何取不到」四条冻结理由**（不参与注入通道 / 不落 Web 存储 / 不经 HTTP 下发 / 配套后传输层可独立验证）。
  - **强制配套三选一（必须同启其一）**：**A·iframe `sandbox` 且不含 `allow-same-origin`**（推荐，⇒ 独立 origin，服务端可凭 `Origin`/`Sec-Fetch-Site: cross-site` 拒绝非顶层宿主 bootstrap）／**B·独立 origin**／**C·native bridge**。**未实现任一 ⇒ 不得声称已建立信任根，且 3.0B 中依赖 capability 的部分不得开工。**
  - bootstrap 流程表更新：步骤②增加「**强制校验 secret**（一次性）+ 校验传输层信号」，步骤③签发后 **secret 立即作废**。
- ②**analysisHandle 流程闭合**（新增 §6.7.1～§6.7.4）：
  - **请求字段契约（§6.7.1）**：`/api/chat` 与 `/api/interactive/chat` **新增可选 `analysis_handle?: string`**（opaque）；受控入口 `/api/world-context/*/model-call` **不新增**（iframe 路径无 analysis 阶段）。**这是唯一合法提交位置**——查询串/header/iframe 消息/其它字段出现的 handle 一律忽略+审计。与 `world_context` 并存时：fingerprint 不一致 → 以 Ref 优先、handle 作废。
  - **严格解码（§6.7.2）7 项**：类型 string / 空值（`""` = **未携带**而非错误）/ 字符集 `[A-Za-z0-9_-]` / 长度 32–128 / **不得可解出结构**（非 JWT/JSON，服务端只查表）/ 查表命中 / consumer 一致。任一不满足 → 忽略 handle、回落常规解析、**不阻断** chat、**不用非法 handle 迁移**。
  - **消费顺序（§6.7.3）固定六步**：①严格解码 → ②校验（存在/未过期/未消费/consumer 一致/fingerprint 不冲突）→ ③**按 consumer 分叉建运行身份**（writing=`StartTask`→task.ID()；**game=先建 InteractiveRun、再在其下建 Task**）→ ④runContext 迁移（沿用同一 id/`runSalt`/ModelView 字节）→ ⑤标记 `consumedAt` → ⑥执行 chat。
  - **game 侧关键修正**：runContext **锚定到 InteractiveRun 而非某个 Task**（否则后续 regenerate 新建 Task 会丢失背景）。
  - **失效语义（§6.7.4）四个原因码**：`analysis_handle_invalid` / `_expired` / `_consumed` / `_conflict`；**一律非阻断**——忽略 handle + 常规解析，响应附 `analysis_handle_status`（`consumed` / `ignored_invalid` / `ignored_expired` / `ignored_consumed` / `ignored_conflict`）；**不得**用失效 handle 迁移、**不得**因曾有 handle 伪造上下文、**不得**混入 §9.2 的 `context_state` 降级判定。
- ③**InteractiveRun 残留清理 + 两条新规则**：
  - 三处残留改写：§6.5「凭既有 **story/branch/task** 关联取回 runContext」→ `story_id/branch + interactiveRunId`（**不含 task**）；§7.1「game scope 由 `story_id`/`branch` + 服务端 **task** 派生」→ + 服务端 **`interactiveRunId`**；§14 测试「game 绑定到 `story_id+branch(+task)`」→ `story_id+branch+interactiveRunId`。
  - §6.5 的 `context-analysis` 行同步（原「无目标 task 时 none」→ 改为 pending context + handle 语义）。
  - **新增 §6.3.3 `turnId → interactiveRunId` 服务端内存映射**：`TurnRunIndex: Map<turnId, interactiveRunId>` + `RunMeta: Map<interactiveRunId, {storyId, branchId, turnIds, createdAt, lastUsedAt}>`；冻结写入时机 / regenerate 反查 / 新回合不查表 / branch 切换 / 按 `(storyId, branchId)`+consumer 隔离 / 仅内存受 §6.5 四档清理 / **未命中不新建 run 去"猜"**（回落常规解析 ⇒ 无绑定则 bare + 审计）/ 客户端不可提交。
  - **新增 §6.3.2 规则 7**：**普通（非 regenerate）的下一回合必须创建新 InteractiveRun**（新 runContext / 新 `runSalt`），理由 = runContext 生命周期跟随「回合」而非「故事/分支」，否则跨回合误复用 `runSalt` 与 `sourceRef`，违背「final ModelView 每 run 独占」；旧 runContext 不立即销毁（按 TTL/LRU 回收，支持切回 regenerate）。
  - **清理测试章节旧规则**：§13 风险行「analysis 必须锚定 task/run：无目标 task 不建 runContext」改写为 pending+迁移语义；§14 测试行同款旧规则改为「无需 task 锚点」并新增 ①②③④ 断言。
- §13 风险表新增 4 行（handle 失效被静默降级/用失效 handle 迁移、trust root 未真实建立、映射未命中"猜"run、新回合误复用）、改写 1 行；§14 3.0B DoD 新增 4 组测试（`analysis_handle` 契约与消费 / turnId 映射 / 新回合规则 / bootstrap secret）；§15 明确不做新增 11 条禁令；§17 裁定更新为 v2.6 + 三项核对点 + 明示「Snapshot schema 已通过，无需再审」。
- 保持不放松：World 单一真源 · Snapshot 双投影 · 四模式只读 · 不回写 World · **Timeline 去 Canon** · Knowledge/Obsidian 只读投影 · sidecar metadata · ModelView schema 稳定性 · bare 规则 · 降级分级。
- **未写任何生产代码**；未 git add/未 commit；等 Codex 短复审确认三项关闭。

## 2026-09-11 12:00 · Workbuddy：Phase 3 架构方案修订为 v2.5 Design Freeze Candidate（仅文档，未编码）

- 依据 Codex Phase 3 v2.4 Final Design Freeze Review 的 NEED REVISION 三项必补，修订 docs/plans/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md → v2.5（1258 → 1413 行；§0 新增 v2.4→v2.5 变更对照表，v2.4 摘要降级为 §0.1 历史）；保留 v2.4 已生效的全部冻结结论。
- ①**Capability 信任根**（§7.6 重写，拆为 §7.6.0～§7.6.3）：
  - **iframe 不可信且不能申请 capability**：服务端**不提供 iframe bootstrap 端点**；`iframeInstanceRef` 只能由**宿主** bootstrap 时上报，服务端不接受 iframe 自行提交的同名字段（出现即忽略 + 审计 + 403）。iframe 无宿主身份 ⇒ **结构上不可能**完成 bootstrap（不再只靠「约定不下发」）。
  - **host bootstrap 流程**（§7.6.1）：①挂载 → ②宿主 bootstrap（服务端校验宿主身份、分配 `hostInstanceId`、记录 `iframeInstanceRef`）→ ③服务端签发并**只回 opaque token** → ④宿主内存保存 → ⑤代理调用注入、服务端反查逐项校验。一个宿主可为多个 iframe 实例 bootstrap，卸载即 revoke。
  - **不透明令牌**（§7.6.2）：浏览器（含宿主 JS 与 iframe）只见**不可解析随机串** + 宿主额外可见 `expiresAt`/`consumer`/`usage`；**不得**编码 `capabilityId`/`hostInstanceId`/`iframeInstanceRef`/`revoked`，**不得**用 JWT 等可解码载体。给出「内部字段可见性表」（6 项不可见 / 3 项宿主可见）。服务端只存 `tokenHash`，不存明文。
  - **`consumer_not_trusted` 错误归属**（§7.6.3）：新增**归属列** + 归属原则——本条只表「调用方不被信任为某 consumer 的合法代理」（授权维度），**不表**世界/selection/revision/预算问题；capability 通过后的 `revision_conflict`/`selection_invalid`/`budget_exceeded`/`world_not_found` 一律归 §9.2。新增规则 2′（iframe 试图申请 capability）。规则 3 改为「部分归属」（未用于绑定时忽略+审计，不一律 403）。新增：revoke 与「从未签发」返回**同一响应**防探测。
- ②**analysisHandle 生命周期改为「先分析后建 task」链**（§6.7 重写 + §6.6 analysis 段同步）：
  - 新链路：`context-analysis` → **pending analysis context** → opaque `analysisHandle` → **首次 chat 消费 handle** → 创建 task → **runContext 迁移**（pending → 锚定 task）。
  - **v2.5 取消 v2.4 的「analysis 必须有 task 锚点」前置**——「先分析再开始写」不再需要伪造 task；pending context 与迁移后是**同一条记录的两个阶段**（迁移只改归属锚点，**不换 id / 不换 runSalt / 不换 ModelView 字节**）。
  - 明确：**短期**（≤10 分钟、不续期）、**不进任务日志**（pending→task 迁移也不写，因属内存归属变更非业务事件）、**不作为长期身份**（不可 resume、不可替代 runContextId、不可跨 task 复用、不可作授权凭证）。
  - **前端可见面最小化**：新增 `AnalysisHandleViewV1`，前端**只能看到 `{ analysisHandle, expiresAt }`**；`analysisHandleId`/`pendingContextRef`/`contextFingerprint`/`consumedAt` 一律不下发。handle **单次消费**即作废。
- ③**InteractiveRun 模型**（§6.3.2 重写）：
  - 冻结层级 `InteractiveRun ⊃ {Task#1(initial), Task#2(regenerate), Task#3(regenerate)}`；**Task = 一次执行尝试**（每次生成都新建 Task）。
  - **regenerate 经 `regenerate_from_turn_id` 找回同一 InteractiveRun** ⇒ 复用同一 runContext / runSalt / ModelView 字节；**新建的是 Task，不是 InteractiveRun**。
  - **abort / 取消只结束当前 Task** ⇒ InteractiveRun 与其 runContext **保留**（可继续 regenerate）。
  - **branch 切换创建新 InteractiveRun** ⇒ 新 scopeKey ⇒ 新 runContext。
  - 写作侧无 InteractiveRun（regenerate 沿用同一 task），本节模型**只适用 game**。
- ③′**清理 v2.4 三处错误表述**（§6.3.2 给出对照表）：①「`story+branch+task` 作为 run 身份」→ run 身份**不含 task**（§6.3 表 game 行、§6.3.1 确定性表述同步）；②「同一个 task regenerate」→ regenerate 必然**新建 Task**、沿用同一 InteractiveRun（§6.6 五态表 regenerate 行同步）；③「task 结束即销毁 run」→ 只有 **InteractiveRun 结束 / branch 切换 / 实例级 scope 事件**才解绑（§6.5 档 1、§6.6 destroy 行、§6.6 边界规则新增一条同步）。
- §13 风险表新增 7 行（iframe 自行申请 capability / 令牌可解码泄露内部字段 / 撤销状态被探测 / 错误归属错误 / pending 迁移时重建 runContext / 前端拿到 handle 内部字段 / 把 task 完成当销毁条件），并改写 2 行；§14 3.0B DoD 三组测试重写为 **bootstrap + opaque token / 错误归属 / handle 与 pending 迁移 / InteractiveRun 模型**；§15 明确不做新增 10 条禁令；§17 裁定更新为 v2.5 + 三项核对点。
- 保持不放松：World 单一真源 · Snapshot 双投影（body 与 final ModelView 分离）· 四模式只读 · 不回写 World · **Timeline 去 Canon** · Knowledge/Obsidian 只读投影 · sidecar metadata · ModelView schema 稳定性 · 线上无 scopeKey · bare 规则 · 降级分级。
- **未写任何生产代码**；未 git add/未 commit；等 Codex 只核对 v2.4 → v2.5 三项 diff。

## 2026-09-11 11:32 · Workbuddy：Phase 3 架构方案修订为 v2.4 Design Freeze Candidate（仅文档，未编码）

- 依据 Codex Phase 3 v2.3 Review 的 NEED REVISION 五项必补，修订 docs/plans/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md → v2.4（1070 → 1258 行；§0 新增 v2.3→v2.4 变更对照表 + 不变量清单）；保留 v2.3 已生效的全部冻结结论。
- ①**iframe capability 边界**（新增 §7.6 整段）：
  - **iframe 不可信**：不持能力凭证、不触发世界上下文加载、不影响绑定。
  - **`worldId` / `revision` 不作为授权**：仅用于「目标定位 + 一致性比对 + 显示」，请求声明不产生任何权限。
  - **host capability 服务端签发**：`HostCapabilityV1`（CSPRNG capabilityId、绑定 consumer+hostInstanceId、≤15 分钟、usage 最小化、refresh/revoke 全路径；仅内存、不下发 iframe、与 Ref/runContextId/scopeKey/sourceRef/runSalt 同级秘密）。
  - **`consumer_not_trusted` 触发规则穷举 6 条**（§7.6 表）：①通用网关带世界字段 → 403；②受控入口缺/过期/已 revoke/consumer 不匹配/usage 不覆盖的 capability → 403（**iframe 直呼天然落入此条**）；③自造 consumer/scopeKey/interactiveRunId/analysisHandleId → 忽略+审计；⑤提交 Snapshot/ModelView/系统 Prompt/字段白名单扩展 → 403；⑥capability 校验通过但 worldId/revision 不符走 §9.2；不与 bare 混淆，**拒绝 ≠ 降级**。
- ②**analysisHandle 生命周期**（新增 §6.7 整段）：明确 `analysisHandle → runContext → task/run` 三者关系；handle 仅作「analysis ↔ runContext」短期引用（`expiresAt = min(10min, task/run 剩余)`、不续期）；**不进任务日志/存档/导出**；**不作为长期身份**（不可用于 resume、不可替代 runContextId、不可作为授权凭证、不可跨 task/run 复用）；同 `(consumer, taskRef, fingerprint)` 重复 analysis 幂等复用同一 handleId；不下发 iframe；仅内存。
- ③**interactiveRunId 设计**（新增 §6.3.2 整段，§6.3 表 game 行改写）：
  - **冻结 `task ≠ run`**：task 是长链执行体；`interactiveRun`（interactiveRunId）是回合级身份，服务端生成。
  - **game scope 不再用 task**：从 `task:<serverTaskId>` 改为 `run:<interactiveRunId>`，**branch** 仍参与 scopeKey。
  - **regenerate 复用同一 `interactiveRunId`** ⇒ 不新建 run、不重建 runContext、不重随机 runSalt ⇒ finalModelViewBytes 恒定（对应 §6.6 regenerate）。
  - **branch 切换创建新 `interactiveRunId`** ⇒ 新 scope ⇒ 新 runContext（旧按 §6.5「scope 事件」解绑）。
  - 写作侧无 interactiveRun：regenerate 沿用同一 task；writing scope 仍为 `task:<serverTaskId>`。
  - 客户端自造字段 → 忽略 + 审计（§7.6）。
- ④**删除 Snapshot 自引用 `stats.bodyBytes`，改 sidecar metadata**（§4.8 stats 字段移除 bodyBytes + §4.11(b) 同步）：
  - **SnapshotSidecarMetaV1**（独立对象）：worldId/worldRevision/consumer/fingerprint/generatedAt(scope+RFC3339 诊断用)/snapshotBodyBytes/projectionBodyBytes/finalModelViewBytes/counts/budget。
  - sidecar **不进 Snapshot/UIView/ModelView/任何请求或响应体**；**不参与** `contextFingerprint` 计算、ProjectionBody 缓存键与任何等值判断；**同输入同字节的纯函数性质**严格恢复。
  - sidecar 受 §9.3 禁令同等级别：不入通用日志 / 任务日志 / 故事存档 / 导出包；日志只允许其派生标量（字节数、计数、estimatedTokens）。
- ⑤**ModelView 固定字段契约 / schema 稳定性**（§4.10 增「schema 稳定性契约」小节 + §9.1 `sections` 定义同步）：
  - 六个数组（`characters/locations/factions/timeline/materials/sources`）**恒为数组**，无内容时输出 `[]`，**不允许省略键、不允许 `null`**。
  - 空 `sources` 合法（极简上下文），必须是 `[]`，不得省略；`sources` 不得含重复 `ref`。
  - 可选标量缺失即**不输出该键**（不写 `null`、不写 `""`）；`identity` 恒存在且 `name` 必填非空；`setting` 可选键，存在时 `rules` 恒为数组。
  - `schemaVersion` 恒为 `1`；v1 内字段结构与命名**不可变**；变更需 `schemaVersion: 2` + 双读兼容期（本期不做）。
  - `sections` 重定义为 `1(identity) + (setting 存在 ? 1 : 0) + [characters/locations/factions/timeline/materials 中 length>0 的段数] + (sources.length>0 ? 1 : 0)`，**空数组段不计数**；§9.1 token 公式与黄金样例同步。
  - 黄金样例除断言 token 数外必须断言**顶层键集合与各数组元素键集合**，防 schema 漂移。
- 保持不变（v2.3 已冻结）：World 单一真源 · UI/Model 双投影（body 与最终 ModelView 分离）· 四模式只读 · 不回写 World · **Timeline 去 Canon** · Knowledge/Obsidian 只读投影 · 线上无 scopeKey · consumer 由路由固定 · `context_state` 四值 · bare 规则 · selection 五步检测顺序 · token 确定性公式 · fingerprint 规范化 · `sourceRef` 不可寻址 · 降级分级 · **§7.6 capability 之前的所有 §7 内容**。
- §13 风险表新增 6 行（worldId/revision 当授权 / iframe 直呼 / capability 泄漏 / analysisHandle 当长期身份 / task 与 run 混用 / Snapshot 自引用 / ModelView 段缺失）；§14 3.0B DoD 新增 4 组测试（host capability 路径 / analysisHandle / interactiveRunId / sidecar / ModelView schema 稳定性）；§17 裁定更新为 v2.4 并列出五项核对点。
- **未写任何生产代码**；未 git add/未 commit；等 Codex 只核对 v2.3 → v2.4 五项 diff。

## 2026-09-11 11:05 · Workbuddy：Phase 3 架构方案修订为 v2.3 Design Freeze Candidate（仅文档，未编码）
- 依据 Phase 3.0B Architecture Review 的 NEED REVISION 五项必补，修订 docs/plans/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md → v2.3（796 → 1070 行；§0 新增 v2.2→v2.3 变更对照表 + 不变量清单；保留 Codex 已生效的 §6.3.1/§7.5 与两次快速复审结论）。
- ①**完整 Snapshot 契约**（§4.7～§4.11）：`WorldContextSelection` / `WorldContextSnapshotV1` / `WorldContextUIViewV1` / `WorldContextModelViewV1` 逐字段必填·可选·上限·空值语义，并给出「空值语义 / 数组与文本限制 / revision 校验」三张总表；ModelView 硬禁止项含内部 id/bindingId/masterItemId/路径/omissions/runSalt/scopeKey/runContextId/fingerprint；`sources[].ref` 为本 run 的 sourceRef。
- ②**修正缓存设计**（解决 Codex 09:36 记下的定点冲突）：**取消最终 `modelViewBytes` 共享**。改为 `ProjectionBodyCacheEntry`（**source-neutral** body ≤96KiB，按 fingerprint 共享、`refCount` 计数）+ `RunContext`（每 run 独立 `runSalt` / `sourceRefTable` / `finalModelViewBytes`）；新增物化链路 `body → runSalt → sourceRef → final ModelView`；同步 §4.1（单一序列化→两级产物）、§6.2、§6.3 并存规则、§6.5 清理粒度、§9.1 比较顺序（新增 body/ModelView JSON 层）、§9.3 禁令命名、§10.1、§11 隔离矩阵。
- ③**runContext 生命周期**（新增 §6.6）：**analysis run 必须锚定 task/run**（不产生游离 runContext；先分析后聊天允许 `origin='analysis'` bind，随后 chat 复用；展示与模型同一份字节）；五态 **create / reuse / reconnect / regenerate / destroy** 与易错边界（regenerate≠换背景、reconnect 不重建、bare≠destroy、create 幂等）；§6.5 表补 analysis 行。
- ④**iframe 边界**（§6.4 新增「iframe 不得直接调用受控入口」+ §7.1/§7.3）：一律宿主代理；**秘密不落 iframe**（Ref / runContextId / scopeKey / sourceRef / runSalt 均不下发）；iframe 直呼只能落到 bare（无世界数据）并记 `context_call_source=iframe_direct`；宿主必须重新构造请求、丢弃越权字段。
- ⑤**错误降级分级**（§9.2 重写为分级表）：**可降级 5 类**（`world_unavailable` / `context_unavailable` / `projection_failed` / 窗口类 `budget_exceeded` / `world_not_found`）→ `context_state='degraded'` 且模式可继续；**不可降级 6 类**（`selection_invalid` / `revision_conflict` / `world_archived` / 其余 `budget_exceeded` / `consumer_not_trusted` / `invalid_request`）必须阻断并让用户显式处理；**主动 bare = `none`，非降级**。
- 保持不放松：World 单一真源 / UI·Model 双投影 / 四模式只读 / 不回写 World / 不建立 Canon / Knowledge·Obsidian 只读投影。
- §13 风险表新增 4 行（body 误缓存导致跨 run 复用 sourceRef、analysis 游离绑定、iframe 直呼、不可降级错误被降级放行）；§14 3.0B DoD 增补契约 schema / body↔final 差异 / analysis 绑定 / 降级分级 / iframe 边界五组测试；§17 裁定更新为 v2.3 并列出五项核对点。
- 未写任何生产代码；未 git add/未 commit；等 Codex 只核对 v2.2 → v2.3 五项 diff。

## 2026-09-11 10:12 · Workbuddy：Phase 3.0A Final Commit 与推送（未改源码）
- 按用户指令精确暂存 13 个文件（3.0A Timeline 源码 4 + 前端 4 + 测试 2 + i18n 2 + 协作日志 1），**未用 `git add .`**，暂存前自检无验收目录/截图/exe/dist/本地配置；范围自检 `Snapshot|runContext|WorldContext|projectFor|fingerprint|selection` 关键词零命中（确认未混入 3.0B）。
- 提交前复跑门禁全绿：`go test ./internal/world` ok、`tsc --noEmit` 通过、i18n 3764 键对齐、World Workspace 15 文件 122 测试通过。
- 提交 `f87d79527f31bebe2453832a9f7c5e13d8e11d21` · `feat(world): add timeline classification compatibility`（13 files changed, 478 insertions(+), 31 deletions(-)）；随后按用户确认推送：`bd7276b..f87d795 main -> main`，`git ls-remote` 核验远端 main = `f87d795`（同时带上未推送的 `b017fbe` v2.2 freeze）。
- 环境踩坑：首次 `git push` 挂起 3m20s（GCM 等交互，无 TTY）；`GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=never timeout 75` 重试成功。另：本沙箱写 `.git/refs/remotes/**` 被静默丢弃，本地 `origin/main` 可能长期陈旧 → **判断推送以 `git ls-remote` 为准**。

## 2026-09-11 09:57 · Codex：Phase 3.0A Timeline 兼容实现与门禁完成
- 按 Phase 3 执行计划仅实现 3.0A，未开始 3.0B Snapshot、3.1 控制台增强或四模式接入。后端新增 `TimelineEntry` 自定义 JSON 编解码与 CAS 旧值对照：旧 `canon`、未知非空、空串、`null`、字段缺失在未编辑时按原始磁盘语义保留；新建/显式编辑仅允许 `background`、`historical`、`planned`，空分类新建默认 `background`。
- 前端新增时间线分类展示归一化与遗留值提示，三种新分类替换旧 `canon` 选项；补充 12 个分类纯函数测试。改动范围限于 `internal/world`、World Workspace 时间线组件/类型/工厂/i18n 与对应测试。
- 验证：临时解压官方 Go 1.27.0 仅用于本轮命令；`go test ./internal/world ./internal/api/handlers -count=1`、作用域 `go vet`、`go build ./cmd/denova`、`tsc --noEmit`、i18n 3764 键对齐、World Workspace 15 文件 122 测试、`vite build`、`git diff --check` 均通过。Vite 仅有既有大 chunk 警告。
- 当前未提交；未跟踪验收/构建产物继续排除，未执行 `git add .`。下一步需由用户/当前模型决定是否精确暂存提交；本阶段完成后强制停在 3.0A。

## 2026-09-11 09:36 · Codex：完成 Phase 3 分阶段执行规划（仅文档，未编码）
- 新增 `docs/plans/WORLD_WORKSPACE_PHASE3_EXECUTION_PLAN.md`，基于 `b017fbe` 与 Phase 3 Architecture Plan v2.2 Design Freeze，把后续工作拆为严格阶段门：3.0A Timeline 兼容 → 3.0B Snapshot/双投影/runContext → 3.1 控制台增强 → 3.2-A 写作 → 3.2-B 游戏 → 3.2-C0 iframe 受控入口 → 3.2-C Narraverse → 3.2-D Module4。
- 每阶段列明目标、精确改动面、数据/安全边界、自动门禁、正式 executable 验收、建议提交信息与强制停止点；要求一次只执行一个阶段、精确暂存、不得自动滚入下一阶段。
- 3.0A 选择 TimelineEntry 自定义 JSON 编解码 + CAS 内旧值对照，保证 canon/未知/空串/null/缺失未编辑保真且新写只允许 background/historical/planned；3.0B 开工前另设一个定点核对，解决“共享 ModelView bytes”与“sourceRef 跨 run 不可复现”的实现冲突，不重新打开总架构。
- 本轮未修改生产代码、未运行构建或测试；仅做计划内容、真实路径和 `git diff --check` 自检。计划产物保持未提交，等待用户在当前任务切换模型后从 3.0A 开始执行。

## 2026-09-11 09:22 · Codex：Phase 3 v2.2 两项快速复审通过并完成 Design Freeze（仅文档）
- 按用户限定只核对并自行收口两项契约：① `scopeKey` 规则；②受控入口无上下文（bare）规则。未修改生产代码、未创建 API、未开始 Phase 3 实现。
- `scopeKey` 结论：线上协议不接收该字段；writing/game 由服务端既有 task/story/branch 派生内部键，Narraverse/Module4 由服务端生成 `runContextId` 并使用内部 `rc:<id>`，宿主只保存 iframe 场景的 id 映射。同步清除 Module4 `scopeKey=adventure|instance` 与写作“宿主持有 runContextId”两处旧表述。
- bare 结论：改为由服务端受控适配层解析，而非机械判断请求是否缺少 `world_context/run_context_id`；原生续接通过 task/story/branch 恢复 active，iframe 续接由宿主提交 `run_context_id`，真正 bare 返回 `context_state=none` 且不隐式解绑；错误降级继续单列 `degraded`。
- 快速复审结论：**PASS**。`WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md` 状态更新为 `DESIGN FREEZE PASS`；可以进入 3.0A 实施计划，但本次裁定不授权编码。仅进行文档一致性检查与 `git diff --check`，未运行构建或代码测试。

## 2026-09-10 23:30 · 豆包：Phase3 v2.2 第二轮快速复审修订——scopeKey 上线通道（仅文档）
- 复审指出：受控入口请求体只有 world_context?/run_context_id?/messages，未定义首请求如何交付 scopeKey，“按 scopeKey 复用”无法实现。先核实现有调用链：POST /api/chat 的 agent.ChatRequest 无 task 字段，task 由 StartTask 服务端生成 task.ID()；/api/interactive/chat 请求体本就带 story_id+branch、task 由 StartInteractiveTask 服务端创建。据此按复审给的二分方案重写，不写代码。
- §6.3 表+§6.3.1 重写为“线上无 scopeKey 字段”：A 类 writing/game 由服务端从 task.ID()/story_id/branch 内部派生（writing=task:<id>，game=story|branch|task），不新增请求字段；B 类 narraverse/module4 首次 bind 只带 Ref，服务端 CSPRNG 生成 runContextId（内部 scopeKey=rc:<id>）并仅在响应回宿主，宿主用本地内存 iframeInstanceId→runContextId 路由、不上线不持久化。请求体/查询/iframe 消息出现自造 scopeKey/scope/run_scope 一律忽略+脱敏审计、不作绑定或授权凭证，通用 /api/model/chat 出现即 consumer_not_trusted。
- 联动：§6.2 结构注释、§6.4 宿主本地映射、§7.1 入口表与首/续调用、§7.3 越权保留字段加 scopeKey、§11 链路措辞、§13 新增 P0“客户端自造 scopeKey”并改写稳定性风险、§14 3.0B 测试改五项（线上无字段/服务端派生/服务端生成/幂等替换/缺标识 400）、§15 禁止项、§0 增第⑧项、§17 复核清单加⑧。仍未编码，等下一轮 diff 核对。

## 2026-09-10 23:20 · 豆包：Phase3 架构计划 v2.2 补强两项规则（仅文档，未编码）
- 按用户第一步指令，在 WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md（仍标 v2.2，不另升版）补：⑥scopeKey 形式化规则、⑦受控入口无上下文(bare)规则；§0 增“v2.2 本次补强”对照，§17 Codex diff 清单同步加⑥⑦。
- §6.3.1 新增 scopeKey 规则：唯一生成者=宿主（服务端不生成/不解析/不授权）；语法 [a-z0-9:|._-]、段 kind:id、长度1~160、非法→400 不建 runContext；确定性（禁随机/时间戳/渲染自增）；consumer 命名空间隔离；bind(upsert: 无则建/相同幂等复用不重复 refCount/不同替换)/resume(带 run_context_id，冲突 Ref→context_ref_mismatch)/unbind 三操作；缺 scopeKey 拒绝且禁每请求临时解析；iframeInstanceId 挂载期稳定、卸载即 unbind；scopeKey 与 runContextId 同级脱敏只记哈希。同步修正 §6.3 原“重复首请求不同→mismatch”表述为“bind 不同→替换、mismatch 仅 resume”。
- §7.5 新增 bare 规则：受控入口三请求形状 bind/resume/bare；响应统一 context_state=bound/active/none/degraded；bare 一等支持非错误，不建 runContext、不读 World、不占名额、不触发 Master；不隐式注入也不隐式解绑（清除必须显式 unbind）；可逐请求交替；context-analysis 无绑定返回 none 非 4xx；带 Ref 命中 selection/revision/非窗口 budget/archived 不得吞成 bare；/api/model/chat 永远 bare、带世界字段即 consumer_not_trusted。§7.1 改为引用 §7.5，§9.2 补“主动 none≠降级 degraded”，§13 加两条 P1 风险，§14 给 3.0B/3.2 各补一组定向测试。未写生产代码，等 Codex 核对本版 diff。

## 2026-09-10 19:40 · 豆包：整合阶段进度交接报告给 GPT（仅文档）
- 核对 git：HEAD=bd7276b；Phase1/2A/2A.1/2A.2/2B.1(c48b20e)/2B.2(37d6bfc,55fc07a) 均已提交；当前 tracked 未提交仅 Phase3 架构计划 v2 与本日志，其余为 untracked 非源码（禁止 git add .）。
- 核实真实验收 P1（世界资料多选器误纳 entity 类 Master）已在 c48b20e 闭环：BindingPicker 导出 WORLD_MATERIAL_SEMANTIC_TYPES(world/rule/item/other) 与创世向导白名单，首屏+loadMore 客户端过滤，后端 entity 零引用 400 作为纵深防御。
- 产出 docs/交付/WORLD_WORKSPACE_STATUS_HANDOFF_FOR_GPT.md：提交地图、各阶段事实口径、P1 状态、Phase2 收口三跟进项（真实 DeepSeek E2E/真实 PNG 头像/有权限环境全量 Go）、Phase3 当前卡在“v2 等 Codex diff 核对、零生产代码”、铁律与文件指针，并列出请 GPT 判断的 5 个问题。

## 2026-09-10 20:35 · Workbuddy：Phase 3 架构方案修订为 v2.2 Design Freeze（仅文档，未编码）
- 依据 Codex Phase 3 架构复审 NEED REVISION 的五项必加，整体重写 docs/plans/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md → v2.2（742 行；§0 新增 v2.1→v2.2 变更对照表 + 保持不变清单）；未改任何生产代码。
- ①runContext 归属（§6.4 重写）：`runContextId` 只存在于服务端与**宿主**内存，**iframe 不接收、不持有、不回传**；宿主按「已挂载 iframe + 来源 window」派生 `iframeInstanceId` 并维护 `iframeInstanceId → scopeKey → runContextId`；`model-call-request` **只传 messages**，出现 runContextId/worldId/revision/selection/consumer 任一 → 宿主丢弃并记审计（§7.3）；`world-context-changed` 只下发只读展示摘要（不含 runContextId 与正文）。
- ②runContext 生命周期（§6.2/§6.3/§6.5）：拆**两级注册表**——`RunContext`（scope 绑定，≤64 条）+ `ModelViewCacheEntry`（按 fingerprint **共享字节**，≤32 条且 ≤8MiB，refCount）；冻结 scopeKey 映射（writing=`book|session|task`、game=`story|branch|run`（**分支切换 = 新 scopeKey**）、narraverse=`iframe:<instanceId>`、module4=`adventure|instance`）；冻结**四档清理粒度**（scope 事件即时解绑 → 空闲 30min / 绝对 6h TTL → refCount==0 且空闲 30min 才释放共享字节 → 进程重启全丢）。
- ③Timeline 空值保真（§8.1）：新增**八态冻结表**（三值 / canon / 未知非空 / `""` / `null` / 字段缺失），读取归一化**只用于展示**，保存**逐字节保真**（缺失键不新增、null 写回 null、空串写回空串）；新增**解码侧四态区分强制约束**（自定义 UnmarshalJSON 或内存 sidecar，禁止 `string+omitempty`，禁止新增持久化字段）；§8.2 改为字节级（除 updatedAt）断言。
- ④预算窗口加固（§9.1/§9.2）：新增 `consumerContextWindowTokens <= 4096` → **直接 `budget_exceeded`**（入口层执行，先于 token 估算）；§9.2 把该原因列为**允许「无上下文继续」降级**的例外之一（用户无法通过缩减选择修复）。
- ⑤Knowledge / Obsidian（§12 重写）：冻结 `KnowledgeExcerptRefV1 { sourceKind, sourceId, sourceRevision, locator }`；locator 白名单（obsidian_vault/book/master_asset/user_paste）；**禁止绝对路径**（盘符 / UNC / 前导斜杠 / `..` / `file://`），`vaultId` 由服务端注册表映射本地根；明确 **Obsidian 关系图只是只读投影、不是 World 真源**、不写 World、不构成 Canon；本期只冻形状不实现。
- 保持不放松：World 单一真源 / UI Projection·Model Projection 双投影 / 四模式只读 / 不回写 World / 不建立 Canon。
- 未 git add/未 commit；等 Codex 只核对 v2.1→v2.2 五项 diff（§17 已列核对点）。

## 2026-09-10 19:55 · Workbuddy：Phase 3 架构方案修订为 v2.1（仅文档，未编码）
- 依据 docs/reviews/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_V2_DIFF_REVIEW.md（NEED REVISION）修订 docs/plans/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md → v2.1，落实 P0/P1 五项修订 + 复审 §4 五项补强；未改任何生产代码。
- P0 网关边界（用户拍板「新增服务端受控模式入口」）：§7 冻结新增 POST /api/world-context/{narraverse|module4}/model-call，consumer 由**路由**固定；iframe 不再直连网关，改由宿主 postMessage 代办（world-context-changed / model-call-request / model-call-result）；通用 POST /api/model/chat 降级为「仅旧 iframe 兼容」，出现 world_context/consumer/run_context_id 任一即 consumer_not_trusted；新增 3.2-C0 强前置并同步 §11/§14/§16/§17。
- runContext 生命周期：§6.2 冻结注册表——仅进程内存、字段集（含 runSalt/modelViewBytes）、空闲 TTL 30min、绝对 TTL 6h、单条 ≤96KiB、注册表 ≤64 条且 ≤8MiB LRU 淘汰、**重启即全丢 → context_unavailable 无上下文降级**、禁止进入通用日志/任务日志/故事存档/导出。
- Timeline 未知值保真：§8.1 由「保存时归一化写入」改为**未编辑即保真**（canon 与未知值保存后磁盘原值字节不变，仅用户显式改分类才写新三值；服务端按磁盘原值比对判定未编辑）；§8.3 精确回退措辞 + 升级栅栏（备份指引/禁旧二进制回写/旧版本打开的确定行为）。
- token 估算：§9.1 冻结实际公式——规范化(NFC→换行统一→去行尾空白→折叠空行→去首尾)+分段计量(ascii/非 ascii)+`ceil(ascii/4)+nonAscii*1+sections*8`+headroom consumerReserve=4096+四层比较顺序+7 例黄金样例（含边界等值）。
- selection 重复语义：§5.1/§5.3 取「普通重复幂等去重、不报错」，selection_invalid 仅限不可解析/不属于当前 World/跨 World/越权(entity 绑定走 bindingIds)/越界，并冻结五步检测顺序。
- §4/§9 补强：§4.6 fingerprint 规范化（`v1|` 前缀 + schemaVersion + consumer + worldRevision + canonicalSelection）、§4.2 omissions 审计（内部 Snapshot 保留 / UIView 可见 / ModelView 不可见）、§4.4 sourceRef 改为 HMAC-SHA256 + 每 run 随机盐（不可寻址、跨 run 不可复现）、§9.2 错误码映射层（World API 404/CAS409 与 Context 错误分层）+ 新增 context_unavailable(410)、§4.1/§9.1 ModelView 单一序列化（analysis 与模型提示同源字节）。
- 未 git add/未 commit；等 Codex 再做一次文档 diff 复审（§17 已列六项核对点）。

## 2026-09-10 19:36 · Codex：Phase 3 架构计划 v2 diff 复审（NEED REVISION，仅文档）
- 核对 v2 文档后确认上轮六项主题均已补入：双投影、Ref/run 生命周期、consumer 边界、Timeline 3.0A、selection 闭包、预算/错误/日志。
- 仍发现一个真实契约冲突：当前 `app/ai-client.js` / `app/app.js` 仍由浏览器直接调用 `/api/model/chat` 并提交客户端 `module`，与 v2 “通用网关拒收浏览器自由 consumer”不一致；此外需补 runContext 存活/清理、Timeline 未知值保真、token 估算公式、selection 重复值语义。
- 结论：3.0A 可在修订兼容措辞后独立实施计划；3.0B 与四模式接入暂不允许。复审文档：`docs/reviews/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_V2_DIFF_REVIEW.md`。

## 2026-09-10 19:34 · 豆包：WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md 修订为 v2（仅文档，未编码）
- 依据 docs/reviews/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_REVIEW.md 第 9 节七项阻塞，整体重写 Phase3 架构计划为 v2，文首加 v1→v2 变更对照表供 Codex 只核 diff。
- 必加六项已落章：①§4 UI Projection/Model Projection 双投影（内部 Snapshot 一次生成、两个纯序列化函数，模型投影剥离内部 id/路径、用不可寻址 sourceRef、悬空引用闭包裁剪）；②§6 WorldContextRef 生命周期，冻结“Ref 绑定到服务端 run 非 World 元数据 + contextFingerprint”，覆盖 chat 首请求/SSE stream/reconnect/active/context-analysis/regenerate 与 iframe，续接复用同一绑定、不一致报 context_ref_mismatch；③§7 consumer 可信边界，consumer 由受控入口服务端固定、通用 /api/model/chat 拒收自由 consumer/客户端快照、跨入口伪造 consumer_not_trusted、iframe 同源与方向控制；④§8 Timeline 3.0A 独立提交，回答读层映射/写升级/旧二进制单向升级/CAS/默认值五问，canon→historical、空→background；⑤§5 selection 闭包：归一化去重排序、ruleIndexes 校验、entity 绑定自动派生、world 绑定单独可选、关系/总部/角色外键闭包、非法整体拒绝不返回部分；⑥§9 预算/错误/日志契约：96KiB/48000 码位/12000 token 确定性估算与黄金样例、九类稳定错误码、日志脱敏不存正文路径、无 world_context 旧请求与旧 iframe 兼容。
- 结构调整：原 3.0 拆为 3.0A（Timeline 兼容，独立可回退）与 3.0B（Snapshot 契约）；3.1/3.2 保留并声明依赖；实施顺序写作→游戏→叙界→Module4；明确不做全局 Canon/事件总线/快照库/自动模拟/Knowledge 实现。未写任何生产代码，等待 Codex v2 diff 快速核对。

## 2026-09-10 18:57 · Codex：Phase 3 架构审查（NEED REVISION，仅文档）
- 基于 `main` / `55fc07a`、`WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md` 与当前 World/Master/四模式实际代码完成只读审查；未修改生产代码、未创建 API、未运行模型或构建。
- 结论为 **NEED REVISION**：服务端“引用→Snapshot”方向、World 不变、四模式写作→游戏→Narraverse→Module4 顺序均可保留，但进入编码前必须补齐 Snapshot 的 UI/模型双投影与引用闭包、world context 在任务/流重连/context-analysis/iframe 中的生命周期、通用模型网关可信边界、`canon→historical` 混合版本策略，以及预算/错误/日志脱敏契约。
- 建议将 3.0 拆为可回退的 3.0A Timeline 兼容与 3.0B Snapshot 契约；3.1/3.2 依赖二者通过后再实施。审查报告：`docs/reviews/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_REVIEW.md`。

## 2026-09-10 18:31 · Codex：完成 Phase 3.0～3.2 架构规划（仅文档，未修改生产代码）
- 新增 `docs/plans/WORLD_WORKSPACE_PHASE3_ARCHITECTURE_PLAN.md`，基于 `main` 当前 `55fc07a`、Phase 2 Closeout、World/Master/四模式实际入口与 iframe v1 协议制定 Phase 3 架构方案。
- 推荐采用服务端“引用→快照”单一解析器：客户端只提交 `worldId + expectedWorldRevision + 用户选择的实体 ID`，四模式复用既有模型/运行链路并只读消费 `WorldContextSnapshotV1`；禁止客户端提交完整 World、模式剧情回写、全局 Canon、自动事件同步、快照持久化和第二套模型链。
- 实施顺序冻结为：Timeline `canon→historical` 兼容收口 → Phase 3.0 Snapshot 契约 → Phase 3.1 控制台上下文准备/健康与引用解释 → Phase 3.2 写作、游戏、叙界、Module4 分段只读连接。Knowledge Workspace 仅预留用户主动片段的引用原则，本阶段不开发。
- 本轮仅新增方案文档并更新协作日志；未修改生产代码、未创建 API、未运行模型、构建或页面验收。

## 2026-09-10 17:56 · Codex：Phase 2 Closeout Review（仅审查，未修改生产代码）
- 基于 `main` 当前 `55fc07a`、Phase 2B.1 `c48b20e`、Phase 2B.2 实现/验收报告和 World/Master/Proposal 实际契约完成收口审查。
- 结论：Phase 2B 可以关闭为开发阶段，状态为 **CLOSED WITH ACCEPTANCE FOLLOW-UPS**；无新的 P0/P1。真实 DeepSeek E2E、带 `avatar_url` 的 PNG 视觉验收、全量 Go 权限环境复跑仍是产品完整验收跟进项。
- 允许进入 Phase 3.0 规划；推荐顺序：收口与 `WorldContextSnapshot` 契约 → World Console 增强 → 四模式只读连接 → Knowledge Workspace 拆书。明确禁止借 Phase 3 扩张为世界模拟、全局 Canon、自动事件、Agent 自治、长期记忆、全文自动上传或 Module3/4 重构。
- 评审文档：`docs/reviews/WORLD_WORKSPACE_PHASE2_CLOSEOUT_REVIEW.md`。本轮未修改生产代码。

## 2026-09-10 16:53 · Codex：Phase 2B 正式验收（条件通过，未修改生产代码）
- 在隔离目录 `.phase2b-acceptance-codex-20260910-1414` 使用正式 Denova executable 完成闭环：创建世界→从隔离副本中的真实 Master Library 数据选择角色卡→mock OpenAI-compatible Proposal→用户采纳→原子创建 World→角色档案→人工推进 Master revision→识别 stale→刷新→保存→停服重启后验证世界、绑定与 revision 仍在。
- 同时完成 Vite 8174→Denova 8124 `/api` 代理对照；Vite 页面与 executable 同源页面均显示验收世界。未使用本地 qwen2.5，未读取/写入/输出 DeepSeek API Key。
- 自动门禁：Proposal/World Go 定向测试、vet、build、World Workspace 113 测试、workbench/stores 41 测试、tsc、i18n、Vite build、git diff --check 均通过；全量 Go 仍仅受既有 Windows 符号链接权限测试阻断。
- 结论：无新的 P0/P1，Phase 2B.2 代码与隔离运行验收 CONDITIONAL PASS；真实 DeepSeek E2E、带 `avatar_url` 的 PNG 头像目视验收、全量 Go 权限环境复跑和当前未提交修复仍是进入 2B.3 前的条件。详见 `docs/acceptance/WORLD_WORKSPACE_PHASE2B_FINAL_ACCEPTANCE.md`。

## 2026-09-10 14:05 · Workbuddy：Phase 2B.2 修复任务 C — 服务端 AI Proposal 契约（未提交）
- ①输入预算冻结公式：effectiveBudget = min(16000, contextWindowTokens - 6144)，<2048 时在调用模型前拒绝（input_too_large 413）；contextWindowTokens 取 config.ResolveAgentModel(cfg, AgentKindInteractiveStory)；token 估算改用 effectiveBudget 比较而非固定 16000。
- ②ModelStructureProposal 输出限制（enhanceStructureProposal 阶段，全部 invalid_model_output 422，禁止静默截断）：名称 ≤100、描述 ≤4000、worldNote ≤4000、tone ≤200、rule ≤2000、tags ≤50 个且单个 ≤100；数量上限沿用（rules30/chars20/locs30/factions20/total60）。
- ③新增真实 handler 路径测试 internal/api/handlers/handler_world_proposal_test.go（4 例）：临时目录构造最小可用 Master Library（manifest + .narraverse/master/items/<id>.json + originals 原件归档）+ httptest 假 OpenAI-compatible 上游（不调用真实模型）+ 真实 app.New。覆盖：请求→mock 模型→严格解析→增强→返回 Proposal（含 sourceRefs/bindingCandidates/盖章字段断言、上游恰好 1 次）；严格解码拒未知字段/尾随 JSON；模型未配置→not_configured；上下文 8191→input_too_large 且模型调用 0 次。
- 测试与门禁：app proposal 10 个测试函数全 PASS（含新增预算边界 8 例、输出限制 11 例）、handlers 全 PASS、world PASS；go vet 通过；go build ./cmd/denova 通过；本轮 4 个 Go 文件已 gofmt -w（此前 gofmt -l 提示的 3 个文件即本轮新增文件）。internal/app 整包仅既有 Windows 符号链接测试失败（无关）。
- 报告补 §8。未 git add/未 commit。附注：gofmt -l internal/ 全仓仍有既有未格式化文件（internal/agent/* 等），属历史债，未扩大范围处理。

## 2026-09-10 13:55 · Workbuddy：Phase 2B.2 修复任务 B — Proposal 应用语义（未提交）
- 只修前端应用语义，未改 AI 输入契约（请求 schema/字段白名单/预算均未动）。
- ①不覆盖用户草稿：world-proposal.ts 新增 mergeProposalIntoDraft(draft, input)（binding 按 masterItemId 去重、重复时复用既有 bindingId 并重映射 AI 实体 bindingId 防悬空、实体按 bindingId|名称 去重追加、tone 用户优先、rules 并集去重、空占位行丢弃、重复应用不产生重复实体）；WorldCreatePage.applyProposal 由整体覆盖改为合并。
- ②setting 采纳门控：仅当 setting 提案项被采纳才写 tone/rules；未采纳 setting 时 worldSetting 整体 undefined（settingOverride 一并不生效）；采纳后 settingOverride.tone 才可覆盖，rules 仍逐条门控。
- ③多 source 实体绑定：实体自动绑定要求 sourceRefIds 解析出的 distinct masterItemId 恰为 1，多个来源综合实体不生成 bindingId，其 entity 绑定候选亦不发出。
- 测试：新增 __tests__/WorldCreatePage.test.tsx（创建向导集成：手动角色+AI角色同时存在、绑定合并、tone/rules 采用 AI 值）、world-proposal.test.ts +6 例并改写 1 例（原 settingOverride 旧语义与新门控冲突）。
- 验证：vitest world-workspace 14 文件 113 测试通过（任务 A 后为 13/103）、tsc --noEmit 通过。报告补 §7。未 git add/未 commit。
- 仍未处理（Codex 13:33 复审其余项）：AI 应用覆盖手动草稿已完成，其余如上下文窗口 token 下限、Proposal 输出字段限制、gofmt 3 个 Go 文件等仍待后续任务。

## 2026-09-10 13:45 · Workbuddy：Phase 2B.2 修复任务 A — 前端 AI 结构提案稳定性（未提交）
- 仅前端稳定性修复，未触碰后端 token 预算 / Proposal schema / 绑定逻辑。
- ①AiStructureAnalyzer.tsx Hook 顺序：原 `if (!open) return null` 位于 `useMemo` 之前（条件 return 后仍有 Hook）；已把 selectedCount(useMemo) 上移，新增的 resetProposal(useCallback) 与三个 useEffect 也全部置于提前 return 之前（现所有 Hook 40–122 行，提前 return 在 179 行）。
- ②请求生命周期：新增 requestSeq(请求代)+AbortController；每次 analyze 自增 seq、abort 旧请求，新 signal 透传 world-api.analyzeWorldStructure(input, signal?)→requestJSON→fetch；响应/异常按请求代丢弃并忽略 AbortError，finally 仅在当前请求时复位 analyzing；关闭弹窗与组件卸载均令在途请求失效。
- ③关闭清理：open→false 调 resetProposal() 清理临时 proposal（proposal/decisions/adoptedBindingIds/edits/toneOverride/error/analyzing），「重新分析」复用同一函数；仅作用于弹窗内部状态，父组件 WorldCreatePage 手动创建流程不受影响，关闭不触发 onApply。
- 新增 __tests__/AiStructureAnalyzer.test.tsx（4 用例：关闭→重开无 Hook 错误 / 关闭清理且不 onApply / 关闭使在途请求失效旧响应不写回 / 关闭中止在途 signal.aborted）。
- 验证：vitest 单文件 4/4 通过、world-workspace 13 文件 103 测试通过（原 12/99）、tsc --noEmit 通过。未 git add/未 commit。报告已补 §6 修复任务 A。

## 2026-09-10 13:33 · Codex：记录 Phase 2B.2 独立代码复审结论（待修复，未提交生产修复）
- 新增 `docs/reviews/WORLD_WORKSPACE_PHASE2B2_CODE_REVIEW.md`，供 GPT/Codex 复核当前 Phase 2B.2 实现。
- 复审结论为 `CHANGES REQUESTED`：发现 AI 分析弹窗 React Hook 顺序 P0；请求取消/生命周期、AI 应用覆盖手动草稿、未采纳 setting 仍写入 tone、多来源实体绑定错误、上下文窗口 token 下限、Proposal 输出字段限制等 P1；另记录失败降级入口、来源证据展示、预算提示和完整服务路径测试等 P2。
- 本轮仅新增评审文档与本日志记录，未修改生产代码、未调用真实模型、未开始 Phase 2B.3。
- 独立门禁已复跑：World Proposal 相关 Go 测试、作用域 vet/build、World Workspace Vitest、tsc、i18n、隔离 Vite build、git diff --check 均通过；`gofmt -l` 仍提示 3 个 Go 文件，已在评审文档中列为修复项。
- 未暂存或提交构建产物、截图、验收目录、`.codex-review-2b2-dist`、`denova.exe` 等无关文件；API Key 未写入任何项目文件。

## 2026-09-10 13:05 · Workbuddy：实现 World Workspace Phase 2B.2 创建向导 AI 结构提案（未提交）
- 范围：仅创建向导 AI 结构提案，严格按 v1.2 设计；未实现 Timeline/既有世界合并/Agent/长期记忆/世界模拟/Canon/Module3·4 接入，未开始 2B.3。
- 后端：新增受控入口 POST /api/world-proposals（internal/app/world_proposal.go 服务 + internal/api/handlers/handler_world_proposal.go + routes.go 注册）。服务端严格解码请求（拒未知字段/尾随 JSON）、重读 Master 校验存在/usable/revision/字段白名单、输入预算（资产8/字段32/片段4/单字段8k/总量48k/输入token≤16k/输出4096）、固定 App.GenerateModel(module=narraverse)→agent.GenerateOneShot（120s 超时、无工具/无 Session/无 Task）、严格解析 ModelStructureProposal（拒 markdown/未知字段/多JSON/尾随/timeline）、服务端增强为 StructureProposal（盖章 proposalItemId/sourceRefs/bindingCandidates/generatedAt）；错误码按 ModelGatewayError.Code 映射 source_not_found/model_not_found 等。
- 前端：types/world-api 增分析请求；新增纯函数 world-proposal.ts（proposalToWorldCreateInput：bindingCandidateId 与 bindingId 区分、master 信息服务端盖章、entity 绑定须有采纳实体引用）；新增 AiStructureAnalyzer 组件（选资料→字段→片段→分析→逐项 adopt/discard/编辑→应用）；WorldCreatePage 步骤3 接入 + locations/factions 入创建入参；i18n zh/en +23 键。
- 验证：go test 新增 proposal 全过（app 包仅既有 Windows 符号链接测试 TestActiveAutomationReservation… 失败，与本轮无关）；go vet/go build ./cmd/denova 通过；tsc --noEmit 通过；vitest world-workspace 12 文件 99 通过；check-i18n-keys 3761 对齐；vite build 通过；git diff --check 通过。
- 产出 docs/reviews/WORLD_WORKSPACE_PHASE2B2_IMPLEMENTATION_REPORT.md。未 git add/未 commit；等 Codex 复审 + 真实 executable 验收（本环境无可用模型，模型调用未跑真实 E2E）。

## 2026-09-10 12:25 · Workbuddy：Phase 2B.2 实施计划修订为 v1.2（仅文档，未编码）
- 依据 Codex v1.1 复审（NEED REVISION，范围很小）修三阻塞点：①拆分 ModelStructureProposal（模型原始输出，不含 proposalItemId/sourceRefs/bindingCandidates/generatedAt/schemaVersion）与 StructureProposal（服务端增强后）；②ProposalChoices.edits 收紧为仅世界可编辑字段 WorldEditable*，adoptedBindingIds 改 adoptedBindingCandidateIds 引用独立 bindingCandidateId；③严格 JSON 扩展为服务端严格解码（HTTP 请求体也拒未知字段与尾随 JSON）。
- 另落实两条建议：领域 handler 按 ModelGatewayError.Code（而非 HTTPStatus()，其除 invalid_request 外统一 502）映射 not_found→model_not_found(404)/unauthorized(401)/rate_limited(429)/timeout(504)/其余 502。
- 未写生产代码、未改 2B 设计稿 v2。等待 Codex 核对三处后转 PASS。另已修正协作日志排序（11:43 条目原被置顶，现按最新置顶规则重排）。

## 2026-09-10 12:18 · Workbuddy：Phase 2B.2 实施计划修订为 v1.1（仅文档，未编码）
- 修订 docs/plans/WORLD_WORKSPACE_PHASE2B2_IMPLEMENTATION_PLAN.md → v1.1，落实 Codex 方向审查后的 7 项契约问题，达 Design Freeze 条件。
- 七项：①ProposedBindingCandidate 增 recordKind/semanticType，模型禁生成 masterItemId/bindingId/revision（服务端盖章）；②角色枚举改现有契约 protagonist|major|minor|npc（锚点 web types.ts:18）；③所有 Proposal 对象含 setting 与每条 rule 均带 sourceRefIds+confidence（rule 升级为 ProposedRule）；④删除 timeline 提案字段，模型返回即 invalid_model_output 禁止静默丢弃；⑤新增 ProposalChoices：Proposal→用户选择→ProposalChoices→proposalToWorldCreateInput→POST /api/worlds；⑥错误码区分 source_not_found / model_not_found 不混用 not_found；⑦AI 输出严格 JSON（拒 markdown 包裹/未知字段/尾随 JSON/多 JSON 对象）。
- 未写任何生产代码，未改 2B 设计稿 v2。等待 Codex 快速复审（文档 diff）转 PASS 后进入编码。

## 2026-09-10 12:05 · Workbuddy：产出 Phase 2B.2 实施计划（仅文档，未编码）
- 新增 docs/plans/WORLD_WORKSPACE_PHASE2B2_IMPLEMENTATION_PLAN.md：依据 docs/reviews/WORLD_WORKSPACE_PHASE2B2_AI_ARCHITECTURE_REVIEW.md（NEED REVISION 七项）把 2B.2 收敛为「创建向导 AI 结构提案」的可编码契约。
- 冻结要点：①服务端受控入口，前端禁直调 /api/model/chat，走 App.GenerateModel(module=narraverse)→agent.GenerateOneShot（无工具/无 Session/无 Task）；②AI 输入契约=勾选 usable 资产字段白名单+用户片段，禁自动全文/自动扫描/Module3·4 状态读取，资产8/字段32/片段4/单字段8k/正文48k/输入token≤16k/输出4096 硬上限服务端拒绝；③AI 只产 StructureProposal，Proposal→人确认→proposalToWorldCreateInput→POST /api/worlds，模型不写 World；④会话级草稿可丢、不建任务/后台恢复；⑤第一版不生成 Timeline Proposal（等独立 Timeline 任务，现枚举仍 canon|planned）；⑥失败降级矩阵（未配置/超时/取消/429/5xx/invalid JSON），手动创建恒可用；⑦范围仅创建向导，既有世界 AI 合并/自动生成/模拟/Canon/Agent 自治均排除。
- 计划含代码锚点（model_gateway.go:198/269、agent/model.go:52、routes.go:244、world types.go:65-69 等）、请求/响应/错误 schema、测试矩阵与 Codex 复审核对清单。未写任何生产代码、未改 2B 设计稿 v2。等待 Codex 文档 diff 复审。

## 2026-09-10 11:43 · Codex：完成 Phase 2B.2 AI Design Freeze 架构审查（NEED REVISION，仅文档）
- 新增 `docs/reviews/WORLD_WORKSPACE_PHASE2B2_AI_ARCHITECTURE_REVIEW.md`，基于 `c48b20e1063d8803639bbaed7557213db3a08d0b`、Phase 2B v2 方案、当前 World/Master Library、共享 Model Gateway、Agent/Task/Session 真实代码完成审查；本轮未改生产代码、未创建 API、未调用模型、未开始 AI 实现。
- 可保留：AI 只产 StructureProposal、用户确认后原子创建、Proposal 会话级可丢失、手动路径独立可用、World 背景共享而四模式剧情隔离、2B.2 只做 AI 辅助分析与结构提案。
- 必须修订：浏览器不得直接拼通用 `/api/model/chat`；未来用一个服务端 World Proposal 受控适配层重读资产/校验 usable+revision+白名单+预算，再固定复用 `App.GenerateModel(module=narraverse)` → `agent.GenerateOneShot`，不用 Master Agent/Task/SSE/持久 Session，不新增模型配置链。冻结 8 资产/32 字段/4 片段/48k 字符/16k 输入 token/4096 输出 token/64KiB 请求体与 120 秒超时等第一版上限；补严格 Proposal schema、逐项 sourceRef、确定性转换、失败降级和日志脱敏。
- 识别硬阻塞：当前前后端 Timeline 仍为 `canon|planned`，不能接收方案中的 `background|historical|planned`；2B.2 开工前应先做独立兼容提交，或从第一版 Proposal 移除 timeline。另将 2B.2 收窄为创世向导，既有世界增量 AI 合并留 2B.3。裁定 NEED REVISION；七项写入实施计划后只需 Codex 快速核对文档 diff，通过后才能编码。

## 2026-09-10 11:11 · Codex：修复 Phase 2B.1 世界资料错误绑定 P1（未提交）
- 修复范围严格限定为前端资料选择边界：`BindingPicker.tsx` 新增可选语义白名单与语义归一（API 的 `lorebook` 映射为 `other`），并以服务端原始条目数维持分页 offset；`WorldConsolePage.tsx` 的“世界资料”只允许 `world|rule|item|other`，`WorldCreatePage.tsx` 只允许角色及同类世界资料，地点/势力仍从各自专用入口创建实体。未改后端、路由、Module3/4，也未新增依赖。
- 补充组件/集成断言：世界资料筛掉角色卡、lorebook 仍可选、过滤后“加载更多”仍按服务端原始 offset 请求；控制台入口断言传入同一白名单。修复了先前真实角色卡可被世界资料选择器选中，进而生成无实体引用的 `entity` binding 并在保存时报 400 的路径。
- 验证：`pnpm exec vitest run src/features/world-workspace --reporter=verbose` 11 文件 92 测试通过；`pnpm exec tsc --noEmit` 通过；`node scripts/check-i18n-keys.mjs` 确认 3738 键 zh/en 对齐；`pnpm exec vite build --outDir .phase2b-flow-20260910-0810\\web-dist-fixed` 通过（仅既有大 chunk 提示）；`git diff --check` 通过。本轮无后端改动，未在当前 PowerShell PATH 缺失 Go 的情况下伪称重跑 Go 测试。
- 正式 executable 复验：在既有隔离目录 `.phase2b-flow-20260910-0810`，用新构建静态包、`denova-src\\denova.exe --no-open --port 8103` 实测。世界资料弹窗仅显示 3 个 lorebook（含已绑定 1 个），4 张角色卡均不显示；选择 `导出` → 添加 → 保存得到“世界设定已保存”。停止并重启同一 executable 后，浏览器重新进入控制台的“世界资料”仍显示 `僵尸末日生存、导出`，`/api/worlds/:id` 同时确认测试世界仍在，全部绑定为 `僵尸末日生存、喜多川海梦、导出` 共 3 条。服务已停止，隔离数据保留；未触及用户运行数据。

## 2026-09-10 08:09 · Codex：Phase 2B.1 正式 executable 闭环验收（发现 P1，未改源码）
- 在全新隔离目录 `.phase2b-flow-20260910-0810`（独立 `.denova`、仅复制既有隔离 Master Library 种子）以 `denova-src\denova.exe --no-open --port 8101` 和 `DENOVA_WEB_DIR=denova-src\web\dist` 实测；未触及用户现有世界、总资料库或 Module3/4，服务已停止，隔离证据保留。
- 通过：正式页面创建测试世界 → 进入控制台 → 世界资料选择真实 lorebook Master（`僵尸末日生存`）→ 绑定 → 保存 → 按需刷新 → 再保存 → 两次完整重启；重启后 UI 与 `/api/worlds/:id` 均确认世界、`other:world` lorebook 绑定、revision 仍在。角色正确入口也通过：真实 `character_template`（`喜多川海梦`）绑定后自动创建角色实例，档案显示“已是最新”，刷新保存并二次重启后仍保留 `character:entity` 绑定及角色计数。
- P1：世界资料多选器没有排除 `character/location/faction` 语义资产；真实角色条目可被选中，却由 `toBinding` 生成 `scope=entity` 且不会创建实体引用，保存稳定返回 `400 字段 bindings[0].scope：实体作用域绑定缺少实体引用`。根因是 `WorldConsolePage` 的 materials picker 未传语义过滤、`BindingPicker` 仅支持单个可选 semanticType。最小修复：世界资料模式仅展示/允许 `world|rule|item|other`（含当前 API 返回的 `lorebook` 归一为 other）资产，并补真实组件集成测试；创世向导若继续支持地点/势力也应同步创建相应实体或限制可选类型。
- 定向验证：`pnpm exec tsc --noEmit` 通过；world-workspace 定向 Vitest 5 文件 45 测试通过；`git diff --check` 通过。当前 PowerShell PATH 未发现 `go`，本轮未重跑 Go 测试；使用刚构建的正式 executable 完成实际运行验证。

## 2026-09-10 07:48 · 豆包：实现 World Workspace Phase 2B.1（非 AI 资料库深度绑定底座，未提交）
- 范围：仅 2B.1，不进 2B.2；无模型调用、不读全文小说/完整互动故事、不建任务系统、不改 Module3/4、不加 npm 依赖/后端路由。完整保留 F-01~F-06 与 2A/2A.1/2A.2 契约。
- 后端 internal/world：AssetBinding 增 scope（entity|world，omitempty，旧数据缺省）；validate.go 新增 effectiveBindingScope（显式优先，缺省按 character/location/faction=entity、其余=world 推导，读时不回写）、masterItemId 同世界唯一、scope 枚举白名单、地点只能引用 location 语义/势力只能引用 faction 语义绑定、entity 作用域绑定零实体引用拒绝持久化；新增 scope_test.go（旧无 scope 兼容、entity 零引用拒、world 零引用留、重复 masterItemId、非法 scope、地点/势力语义匹配、scope CAS、推导表）。
- 前端 world-workspace：types 增 BindingScope/scope?；world-ops 增 bindingScopeOf/worldScopedBindings/addWorldBinding(同 master 去重)/removeWorldBinding(解绑并解除实体引用)，pruneOrphanBindings 改为以 scope 为准；world-factory 增 locationFromBinding/factionFromBinding；BindingPicker 的 toBinding 显式写 scope，并新增 multi 多选模式（跨页勾选、一次性 onBindMany、搜索代失效保护沿用）；新增 components/sections/WorldMaterialsSection.tsx（world 作用域资料查看/逐条按需刷新复用 fetchMasterAsset+applyRefreshedBinding/显式移除，列表加载零详情扇出、请求序号+卸载保护、无 AbortController）；WorldConsolePage 新增“世界资料”分区与 location/faction/materials 三个选择器，地点/势力只绑定对应语义顶层资产、无则真实空状态。
- i18n：zh/en 同步 +11 键（console.materials/bindLocation/bindFaction/bindMaterial/emptyMaterials、materials.refresh/remove/removeConfirm、bindingPicker.select/selected/addSelected），共 3738 键对齐。
- 测试：world-workspace 11 文件 91 测试全绿（新增 world-ops scope 用例、toBinding scope 必测、world-factory 地点/势力、BindingPicker 多选、控制台三入口/世界资料刷新成功·404·5xx·移除确认）；workbench+stores 12 文件 41 测试回归通过。
- 验证命令与结果：go test ./internal/world ./internal/api/handlers -count=1 通过；作用域 go vet 通过；gofmt -l 无输出；tsc --noEmit 通过；check-i18n-keys 3738 对齐；vitest world-workspace 91 通过、workbench/stores 41 通过；vite build 成功（仅既有 chunk>500kB 告警）；go build ./cmd/denova 通过。
- 边界与后续：Timeline 枚举 background/historical/planned 按 v2 要求留作独立提交，本轮未动（仍 canon|planned）；嵌套 lorebook 条目独立身份契约、StructureProposal/AI 白名单均属 2B.2，本期不做。未做正式 executable 页面验收，建议交 Workbuddy 按隔离 cwd+DENOVA_WEB_DIR 流程做真实验收。未 git commit、未 git add .。

## 2026-09-10 07:19 · 豆包：Phase 2B 设计文档修订为 v2（仅设计，未编码）
- 依据 docs/reviews/WORLD_WORKSPACE_PHASE2B_ARCHITECTURE_REVIEW.md（基线更新为已提交的 01253b9）整体重写 docs/plans/WORLD_WORKSPACE_PHASE2B_DESIGN_PLAN.md 为 v2，文首加 v1→v2 变更摘要供 Codex diff 核对。
- 落实冻结决策：①Binding 只保留 scope（生命周期策略 entity 有引用才留/world 零引用也留），删除 ownerId、不加持久化 references[]，引用由实体 bindingId 即时派生，一 World 一 masterItemId 一 binding，旧 binding 无 scope 按 semantic 推导、读时不写不报损坏；②StructureProposal 定为一次性会话草稿，刷新/关闭/重启可丢，不建任务系统/后台恢复/localStorage，SourceRef 去 path、去掉 model 字段；③时间线枚举改 background/historical/planned，旧 canon 仅兼容读取按 historical 展示、不迁移，且作为独立提交；④AI 输入只用勾选 usable 资产的结构化字段白名单+用户主动片段，禁止自动全文小说/完整故事/未选详情，来源数与字符数硬上限、服务端最终拒绝、不靠放宽 1MiB、原文不入日志；⑤2B.1 收窄为非 AI 绑定底座，只用 /api/library/assets 真实 usable 顶层资产，不假设 worldbook_entry、不把 lorebook 嵌套条目伪装成地点/势力，无真实来源给空状态，含隐藏依赖表/开工门槛/测试矩阵。
- 已拍板项移入“冻结决策”，开放问题只留 4 项（地点势力真实来源、世界资料 UI 位置、多选上限、未来嵌套条目身份契约）。结论：2B.1 待 v2 diff 快速核对后 Conditional Go；2B.2 需完成 2B.1 并单独冻结模型契约。未写任何生产代码。

## 2026-09-10 07:03 · Codex：完成 World Workspace Phase 2B 架构审查（仅文档，未编码）
- 新增 `docs/reviews/WORLD_WORKSPACE_PHASE2B_ARCHITECTURE_REVIEW.md`，基于 main `01253b9`、Phase 2B v1 方案与当前 World/Master Library 源码契约完成审查；结论为 Changes requested，v1 可作修订基础但不能直接作为编码任务书，修订为 v2 并快速核对后对 2B.1 Conditional Go，2B.2 暂不允许开工。
- 冻结决策：World Binding 采用 `scope + 实体 bindingId 派生引用`，不持久化 `ownerId` 或 `references[]`；AI StructureProposal 是允许刷新/重启丢失的一次性会话草稿；新产品与数据契约去 Canon 化（旧 `canon` 只兼容读取）；AI 仅接收 Master 结构化白名单与用户主动选择片段，禁止自动读取全文小说/完整故事。
- 识别关键隐藏依赖：`/api/library/assets` 当前排除 `worldbook_entry`，真实总库也没有独立 location/faction 顶层资产，因此 2B.1 只绑定 API 实际返回的 usable 顶层资产；角色模板与 world-scope lorebook 可先交付，地点/势力无来源时显示空状态，嵌套条目若要独立绑定必须另立 identity/revision 契约。主书/主故事在 2B.1 仍只是模式目标指针，不读取正文；不新增 AI 路由、任务系统或 Module3/Module4 改动。
- 本轮未修改生产代码、未运行构建/测试、未进入 Phase 2B 实现；保留工作区既有未提交文件与隔离验收目录不动。

## 2026-09-10 00:06 · 豆包：产出 Phase 2B 设计方案（仅设计文档，未编码）
- 新增 docs/plans/WORLD_WORKSPACE_PHASE2B_DESIGN_PLAN.md：主题=真实资料库深度接入。含 2B 目标与 2B.1(无AI结构化导入底座)/2B.2(AI分析提案)/2B.3(克制联动) 建议子阶段；用户流程（选来源→AI分析→结构提案→人确认→原子创建→控制台），草稿仅组件状态、AI 只产待确认提案、不复制正文；四层数据模型 Master Asset/World Binding/World Entity/Runtime State 及增量字段建议（binding 增 scope/owner?、运行时 StructureProposal 不进 World、地点/势力绑定化复用、时间线不直接绑定）。
- Binding Ownership 给 A 引用型/B 拥有者型/C 混合型三案利弊对照并推荐 C：entity 作用域零引用才清、world 作用域恒保留，旧数据按 semantic 推导兼容；标注最终字段需 Codex 拍板（回应复审债务 D-2）。四模式关系明确“世界背景共享、剧情隔离”，列出共享/隔离清单，沿用 2A.2 单次 preflight、不回写 worldId。明确禁止世界自动模拟/AI自主事件/全局Canon/大规模事实系统/地图引擎/关系图谱，并不改 Module3/4、不轮询、不复制正文、不建 index。列了契约衔接点与 6 个开放问题。

## 2026-09-09 23:00-23:50 · Workbuddy：执行 Phase 2A Final Acceptance（只验收与取证；未改源码/未提交/未动 Module3/4；数据与证据仅写入 .acceptance-2a-final 隔离目录）
- 结论：**A/B/C 与 4.x 顺带回归全部 PASS，无 P0/P1，允许正式关闭 Phase 2A**。正式 executable（8095 + 隔离 web-dist 同源装配）主验收；Vite dev(5174) 对照明确未做。验收记录已逐项填入 docs/acceptance/WORLD_WORKSPACE_PHASE2A_FINAL_ACCEPTANCE_WORKBUDDY.md 第 7 节。
- 构建/环境：vite build 原地重建 web/dist 被沙箱批量删除保护拦截（非代码问题），改 `--outDir .acceptance-2a-final\web-dist`（同一工作树产物）✓built 8.60s；go build exit0 生成 .acceptance-2a-final\denova.exe；cwd=runtime → DataDir=runtime\.denova，复制 narraverse-master-library +《反派：我的母亲是大帝》为种子并改写 books.json；8095 /api/status、/api/library/assets 正常（真实资产 7：4 character_template + 3 lorebook_template）。浏览器驱动：agent-browser CLI 未预装，用 playwright-core + 系统 Edge（仅装在隔离验收目录，仓库零新增依赖）。
- A·M1（36 断言全 PASS）：写作/游戏/叙界/沙盒四入口 dirty→点击→**原生 confirm 仅弹一次**→取消零副作用（不切书/不切模式/无任何写请求/草稿保留/卡片不转圈不禁用）；确定才切换进入对应工作台；游戏模式取消无 `POST /api/interactive/stories/:id/select`、确定后才 select 并进入游戏工作台；叙界/沙盒单次确认、取消不打开/不切 Module4；返回后卡片可再次点击。注：worlds 为 hidden keepalive，进入模式后返回未保存草稿仍在（与 M1 设计一致，非缺陷）。
- B·M2（18 断言）：删绑定角色（喜多川海梦）→confirm 一次→保存 PUT；characters 空、角色 binding 被删、两条世界作用域 binding（lorebook，落库 semanticType=other）保留；反向对照经 JSON 注入“有绑定无实体”的孤儿 char binding，删除任一实体触发级联后孤儿被清而同世界作用域 binding 保留=修复精确。注：真实总资料库无 rule/world 类资产，世界规则/lore 位以 lorebook 充当（环境约束如实记录）。
- C·闭环（28 断言）：打开档案=只读检查“已是最新”、无 PUT、保存禁用；首次刷新提示“已更新本地资料摘要，保存后才会生效”且无 PUT；保存才 PUT；停服改写 masterRevision=sha256:stale-old 后重开=“原件有更新”，刷新+保存恢复原件 nameSnapshot/masterRevision 且世界内 displayName/worldNote 不被覆盖；完全重启后世界/绑定/角色/revision 持久化并恢复“已是最新”，列表无损坏 warning。
- 4.x（11 断言）：空名称地点/势力、空标题时间线保存均被前端拦截并提示、零请求零 4xx；干净控制台不显示保存按钮、档案无 dirty 禁保存；409 冲突横幅+“重新加载”，取消保留本地草稿、确认以服务端为准并清横幅。
- 取证：截图 .acceptance-2a-final\shots\（A1/A2g/A2n/A2s 分支、B-deleted-saved、B6-after-save、C-open-latest/C-stale/C-reopen-latest/C6-restart-latest、4x1~4x5）；网络与 JSON 证据 .acceptance-2a-final\evidence\（A1/A2g/A2n/A2s/B/regress results、B-before/after、B6-after、C-phase1-final/phase2-final、C-meta）。服务已停、runtime 保留待确认后清理。
## 2026-09-09 20:43 · 豆包：编写 Phase 2A Final Acceptance 验收任务书（交付 Workbuddy，未改源码）
- 新增 docs/acceptance/WORLD_WORKSPACE_PHASE2A_FINAL_ACCEPTANCE_WORKBUDDY.md：面向不熟悉项目的 Workbuddy，含环境准备（非 PATH 的 go/vite 命令、隔离 runtime 启动、正式 executable 为主 + Vite 5174 对照）、A=M1 模式跳转（写作单次确认/取消零副作用/确定进入返回不转圈，游戏取消不得发 stories/select、叙界单次、沙盒单次）、B=M2（删角色只清角色绑定，世界规则/lore 绑定以 world JSON 为准保留；实体作用域孤儿反向对照）、C=基础闭环（创建→绑定→打开只读检查→刷新→制造 stale 推荐改 world JSON 的 masterRevision→刷新→重启持久化恢复“已是最新”），以及空名称拦截/无修改禁保存/冲突重载顺带回归、取证要求、纪律（不改源码不提交不动 Module3/4、只写隔离目录）和验收记录模板。

## 2026-09-09 20:23 · 豆包：World Workspace Phase 2A.2 正确性修复（依据 2A.1 复审报告，未提交，待 Codex 节点复审）
- 范围纪律：不进 Phase 2B、不重构、不动 Module3/4、无新增 npm 依赖/后端路由；Go 代码本轮零改动；完整保留 F-01~F-06 与 2A/2A.1 契约。
- M1（P1）模式入口统一单次 preflight：ModeEntries 新增 confirmLeave()，写作/游戏/叙界/沙盒四个入口都在最顶部确认一次，取消即无副作用中止；游戏 selectInteractiveStory（服务端 POST）移到确认之后；消除写作/叙界双重确认与“第二次取消后书已切走/故事已选”。写作、游戏用 try/finally 复位 pending，修掉 worlds hidden 保活返回后卡片永久转圈（B2）；取消切书不再误报失败（B6）。WorldConsolePage 删除 guardedSetMode/guardedQuickSwitchBook/guardedOpenModule4/guardedCloseModule4，改为单个 confirmLeave + 透传原始回调，页面内返回/打开角色仍走 guardLeave。
- M2（P1）orphan 清理保守化：world-ops.pruneOrphanBindings 只对 semanticType∈{character,location,faction} 的实体作用域绑定做“无引用即清理”，world/rule/item/other 等世界作用域绑定恒保留，修掉创世向导自由绑定在删除任意实体时被静默误删；完整世界级 binding ownership 仍留 2B（债务 D-2）。
- B1（P2）保存前统一草稿校验：world-ops 新增纯函数 findDraftIssue（角色 displayName/地点/势力 name、时间线 title 空值，按序返回首个问题）；WorldConsolePage.save 命中则跳转对应分区并 toast、不发请求；CharacterProfile.save 拦空角色名。
- B5（P3）CharacterProfile 无 dirty 时禁用保存、save 内 !dirty 直接返回，消除空转 PUT。B7（P3）删除 selectors.masterAvatarURL 死代码及其测试，杜绝走回“凭 id 盲请求头像”。
- i18n：zh/en 同步新增 console.draftInvalid.{character,location,faction,timeline}，键总数 3723→3727 全等。
- 测试：新增 ModeEntries.test.tsx(9)；world-ops +4（M2 两条/findDraftIssue）；WorldConsolePage +1（B1 拦截）；CharacterProfile +2（B5）并把 3 个依赖“干净态也能保存”的旧用例改为先制造 dirty；selectors 删 1。
- 验证结果：tsc --noEmit 通过；check-i18n-keys 3727 对齐；world-workspace 11 文件 71 测试通过；workbench+stores 回归 12 文件 41 测试通过（仅既有 act/a11y warning）；go test ./internal/world ./internal/api/handlers 通过、作用域 go vet 通过；vite build 通过（仅既有 chunk 体积提示）；go build ./cmd/denova 通过。
- 改动文件（精确，未 git add、未 commit）：world-ops.ts、components/ModeEntries.tsx、components/CharacterProfile.tsx、pages/WorldConsolePage.tsx、selectors.ts、i18n zh/en worldWorkspace.ts，及 __tests__ 下 world-ops/selectors/WorldConsolePage/CharacterProfile 与新增 ModeEntries.test.tsx。页面实机验收（Vite dev 与正式 executable 两条路径）待 Codex 复审后按需进行；本轮以 jsdom 组件测试覆盖深层交互。

## 2026-09-09 19:20 · 豆包：World Workspace Phase 2A.1 独立代码复审（只读，未改生产代码）
- 基线：main HEAD 8623b64；复跑 world-workspace 前端 10 文件 56 测试通过、go test ./internal/world ./internal/api/handlers 通过。产出 docs/reviews/WORLD_WORKSPACE_PHASE2A1_DOUBAO_REVIEW.md。
- 结论：2A 核心契约（健康六态/CAS/脱敏/F-01~F-06/级联纯函数）无回退，但有 2 个 P1 主线问题，建议做小版本 2A.2 后再正式关闭 2A、进入 2B；均无需 Codex 立即介入。
  - M1(P1,CONFIRMED)：ModeEntries 缺统一 preflight——写作双重确认且第二次取消后书已被切；游戏 selectInteractiveStory（服务端 POST）先于唯一确认，取消后当前故事已被切；叙界双重确认。次生：成功进入后 pending 未复位 + worlds 为 hidden 保活，返回后写作/游戏卡片永久转圈(P2)；取消被误报切换失败(P3)。
  - M2(P1,CONFIRMED，当前即可触发)：创世向导第3步 BindingPicker 未传 recordKind/semanticType，可绑定 world/rule/item/lore 等无实体引用的“世界级绑定”（后端允许）；此后在控制台删除任意实体，removeWorldEntity→pruneOrphanBindings 只统计 char/loc/fac 引用，会把该自由绑定当 orphan 静默删除。最小修法：prune 按语义作用域保守化（只清 character/location/faction 且无引用者），完整 ownership 留 2B 由 Codex 拍板。
  - A-F：A CONFIRMED、B CONFIRMED(空地点/势力/时间线必然400，P2)、C CONFIRMED 但属契约内取舍(P3)、D CONFIRMED 即 M2、E NOT REPRODUCED（reload 的 checkNonce+cleanup 已使在途 refresh 失效，不会 apply/置脏）、F DESIGN RISK（1MiB 与领域上限不自洽，MVP 难触发，登记债务）。
  - 分支 P3：角色档案无改动也可保存产生空转 PUT；masterAvatarURL 成仅测试引用的死代码；列表头像、归档409无重载、refresh晚于save 等登记后续债务。全局活动栏切模式绕过页面 guard（D-3），但 worlds hidden 保活不丢草稿。
- 下一步：Workbuddy 按文档复现 6 步；豆包可在 2A.2 一次性修 M1/M2/B1/B2/B5/B7 并补测试；Codex 仅做快速节点复审，2B 定义 binding ownership 时再深入。未提交、未 git add。

## 2026-09-09 14:32 · 豆包：World Workspace Phase 2A.1 Correctness Fix（暂停 2B，纯正确性修复，未提交，待 Codex 复审）
- 范围：GPT 提出的 6 类正确性问题 + Codex 真实环境验收的 2 项 P1；不重构、不替换 Module3/4、不新增后端路由/npm 依赖/批量扫描，完整保留 F-01～F-06 与 Phase 2A 契约。
  1) 离开保护：WorldConsolePage 新增 guardLeave，覆盖返回列表、打开角色、FeaturePageShell 关闭、ModeEntries 进入四模式/快速切书/Module4 打开与关闭（取消则留在本页）；CharacterProfile 新增 dirty，编辑或显式刷新置脏、保存/重载清脏，返回前确认。
  2) 引用完整性：新增纯函数 world-ops.ts（referencedBindingIds / pruneOrphanBindings / removeWorldEntity），删角色/地点/势力时级联解除入向引用（关系、locationId、factionId、headquartersLocationId）并只清理无人引用的 orphan binding（共享绑定不误删）；LocationSection/FactionSection 增加 onRemove，控制台确认后走级联，杜绝后端必然 400 的草稿。
  3) BindingPicker：loadMore 捕获搜索代 requestSeq，等待期间发起新搜索则丢弃旧分页结果，loadFirstPage 重置 loadingMore，旧分页不再混入。
  4) 后端脱敏：HandleWorldGet 不再直接输出 err.Error()，统一走 writeWorldError；损坏文件包裹的本机路径返回固定“世界数据操作失败”500，非法 id/不存在保留安全文案与 400/404。
  5) P1-01 头像单一契约：BindingAvatar 仅在传入 avatar_url（总资料库摘要，仅已归档 PNG 才有）时渲染 img 并 onError 回退，否则直接图标、不再凭 masterItemId 盲发 /avatar（消除真实卡全 404）；角色档案传 source.summary.avatar_url，Picker 行传 asset.avatar_url，控制台列表只有 id→图标（零额外请求）。
  6) P1-02 手动新建：emptyCharacter(name?) 支持默认名，控制台生成“新角色”并对重名追加序号，保证 displayName 非空可保存；角色档案找不到角色改为中性“尚未保存或已删除”并提供返回，不再误报“世界不存在或已被移除”。刷新成功文案改为“已更新本地资料摘要，保存后才会生效”，不暗示已持久化。
- 精确改动文件：新增 web/src/features/world-workspace/world-ops.ts、__tests__/world-ops.test.ts、__tests__/BindingPicker.test.tsx；改 types.ts、world-factory.ts、components/{BindingAvatar,BindingPicker,CharacterProfile}.tsx、components/sections/{LocationSection,FactionSection}.tsx、pages/WorldConsolePage.tsx、__tests__/{BindingAvatar,CharacterProfile,WorldConsolePage}.test.tsx、i18n zh-CN/en-US worldWorkspace.ts（+6 键，3717→3723 对齐）；后端 internal/api/handlers/handler_world.go 与 handler_world_test.go。
- 验证（全部通过）：tsc --noEmit=0；check-i18n-keys=3723 zh/en 对齐；vitest world-workspace 10 文件 56 测试通过、workbench/stores 12 文件 41 测试通过；go test ./internal/world ./internal/api/handlers -count=1 通过、作用域 go vet 通过；go build ./cmd/denova 通过；vite build 通过。
- 边界说明（需 Codex 知悉）：全局顶栏/左侧活动栏切模式在 ModeRouter/App 层，会绕过页面内组件，页面级 guard 无法拦截；本轮按“不重构/最小修复”只覆盖页面内四个出口，全局拦截若需要应另立任务。未自行 git commit，未 git add 任何文件，交 Codex 节点复审。


## 当前状态（最近更新：2026-09-09 14:00 · Codex）
- 2026-09-09 14:00 · Codex：**完成 World Workspace Phase 2A 真实环境验收；未进入 Phase 2B，未修改生产源码。** 在独立运行副本中使用当前 `main` commit `6a3f4422a15bd35ed5905aaa1169904de8b27ff2` 构建的 Denova executable + 真实 Master Library / 当前书籍副本，创建并验证独立测试世界；所有修改、故障注入和损坏 JSON 均只在 `D:\Narraverse2.0\.acceptance-phase2a-real-env`，待验收完成清理。
  - 通过：创建并绑定真实角色卡、详情和 revision 健康、真实 Master 改版→stale→刷新→保存、重启持久化、404、代理注入 5xx/空 revision、CAS 409→重新加载、主书/故事失效显示、损坏 world JSON warning、快速切换角色和组件卸载后的迟到响应、Vite proxy 与 executable 静态装配对照；门禁复验 go test/vet、tsc、3717 i18n 键、World Workspace 41 测试、workbench/stores 41 测试及 vite build 通过。
  - P1-01：隔离真实总资料库中 4/4 张可用角色卡 `/avatar` 均 404；当前 handler 只服务已归档 PNG，UI SVG 回退正常，但“真实头像”闭环未成立。P1-02：控制台“手动新建”插入空 `displayName`，后端正确拒绝保存，临时项详情误报世界已移除。结论：核心 Phase 2A 契约成立，但这两项 P1 修复前不建议进入 Phase 2B。
  - 详见新增报告 `docs/acceptance/WORLD_WORKSPACE_PHASE2A_REAL_ENV_ACCEPTANCE.md`。本轮仅新增验收报告和本条日志；不提交、不暂存无关文件。

## 当前状态（最近更新：2026-09-09 12:29 · Doubao）
- 2026-09-09 12:29 · Doubao：**完成 Codex 对 Phase 2A 节点审查的 Changes requested 最小修复（不重构、不进 2B），未提交，等 Codex 快速复审**。只动 CharacterProfile 一处源码，新增 3 个组件/集成测试文件；F-01～F-06、后端兼容与既有测试全保留，未加依赖、未改 Module3/4、未批量扫描、未自动删绑定。
  - 修复①刷新边界（components/CharacterProfile.tsx）：显式刷新时若 `detail.summary.master_revision` 为空，健康保持 unavailable、**不调用 applyRefreshedBinding、不改名称/标签/masterRevision、不弹成功**，改 toast `bindingHealth.refreshUnavailable`（“暂时无法检查，未改动资料”）。
  - 修复②异步失效（同文件，仍只用请求序号、无 AbortController）：检查 effect 在依赖切换（换角色）/切到无绑定角色/组件卸载的 cleanup 中 `inspectSeq.current += 1` 使旧在途请求失效，旧角色晚返回不再覆盖新角色或无绑定态。
  - 新增测试（world-workspace/__tests__）：`CharacterProfile.test.tsx`（8：打开检查不改 world/不发 PUT；刷新成功只改绑定三字段且角色不变、保存才 PUT；404=missing 保留不删；5xx=unavailable；空 rev 不 apply；切换角色迟到请求不覆盖；切无绑定旧请求失效；409 重新加载取消/确认）；`WorldConsolePage.test.tsx`（4：失效主书/故事保留 disabled 原值选项且仍选中、主动重选才替换；角色列表 fetchMasterAsset 0 次且 books/stories 各 1 次；409 重载取消/确认）；`BindingAvatar.test.tsx`（2：img onError 回退 svg、无 id 直接回退）。world-workspace 合计 8 文件 41 测试全过（原 27 + 新 14）。
  - 门禁全绿：`tsc --noEmit` 0；`check-i18n-keys` 3717 键 zh/en 全等；vitest world-workspace 8 文件 41 过；workbench+stores 12 文件 41 过（仅既有 act/a11y 警告）；`go test ./internal/world ./internal/api/handlers -count=1` 两包 ok、作用域 `go vet` 0；`vite build` 成功（仅既有 >500kB chunk 提示）；`go build -o denova.exe ./cmd/denova` 0。
  - 页面验收按要求**分两类**（隔离数据目录，验收后已停服并删除临时目录，未污染真实数据）：(a) **Vite 开发/代理**：vite v8 dev 127.0.0.1:5174（DENOVA_BACKEND_PORT=8097，/api 经 vite proxy 转发），浏览器直连 5174/api/worlds 经代理拿到世界，?mode=worlds 正常渲染列表与世界卡（shot-dev）；(b) **正式 Denova executable 静态装配**：刚构建的 denova.exe -port 8097 以 DENOVA_WEB_DIR 指向 web/dist 同源提供构建产物与 /api，POST 建世界 201，浏览器访问 8097/?mode=worlds 正常渲染同一列表（shot-list）。两者均确认四模式顶栏、左侧世界工作区导航、中文无乱码。注：无头 Edge 截图须用 --virtual-time-budget 等网络空闲，仅 --timeout 会在异步 fetch 前截到“加载中”（上轮卡点是截图时机而非缺陷）。控制台/角色档案为组件内视图、无 URL 深链，且环境无本地 Playwright、本轮禁止加依赖，故其健康徽章/刷新/失效选项/冲突重载交互由上述 jsdom 组件测试覆盖，未在无头浏览器逐一点击，覆盖边界已在此说明。
  - 本轮精确文件：改 `web/src/features/world-workspace/components/CharacterProfile.tsx`；新增 `web/src/features/world-workspace/__tests__/{CharacterProfile.test.tsx,WorldConsolePage.test.tsx,BindingAvatar.test.tsx}`。提交时只精确暂存这些 + Phase 2A 既有文件，禁止 git add .，排除 denova.exe、web/dist、截图、临时目录等。

## 当前状态（最近更新：2026-09-09 10:10 · Doubao）
- 2026-09-09 10:10 · Doubao：**完成 World Workspace Phase 2A「真实资料库绑定健康与刷新」编码（v2 方案经 Codex Approved），未提交，等 Codex 节点审查**。严格按 v2 与两条附加约束实现，完整保留 F-01～F-06，未新增后端路由/npm 依赖/hook，未批量扫描/轮询，未改 Module3/Module4，未回写总库/书籍/故事。
  - 后端（internal/world，加性改动）：`types.go` AssetBinding 增 `MasterRevision string json:"masterRevision,omitempty"`（旧文件缺字段=空=尚未检查，不判损坏，SchemaVersion 仍 1）；`validate.go` 增 `maxMasterRev=100`，绑定循环只做长度上限、空串允许，validateStoredWorld 严格度不提升；`store_test.go` 增 TestBindingMasterRevision（旧值兼容无 warning、往返+CAS、超长 400）。
  - 前端新增：`binding-health.ts`（classifyBindingHealth 严格按 Codex 指定优先级 loading→error(404 missing/其它 unavailable)→当前 rev 缺失 unavailable→存储空 unchecked→相等 latest/不等 stale；applyRefreshedBinding 只改绑定三字段且角色等集合引用不变；classifyTargetValidity）；`components/BindingAvatar.tsx`（复用 /avatar，onError 回退 UserRound，不存头像）；`components/BindingHealthBadge.tsx`；测试 `__tests__/binding-health.test.ts`（14，含“旧绑定空版本+404 必须 missing”）、`__tests__/toBinding.test.ts`（3，必测 masterRevision 写入/缺省回退）。
  - 前端修改：`types.ts`（masterRevision? + BindingHealthState 唯一定义处）；`BindingPicker.tsx`（导出 toBinding 并写 masterRevision、头像换 BindingAvatar）；`CharacterProfile.tsx`（打开即复用现有 fetchMasterAsset 做**只读检查**绝不改 world，请求序号防竞态不用 AbortController；显式“刷新资料摘要”成功才用 applyRefreshedBinding 更新本地绑定三字段、不碰角色、无 dirty、走既有保存+CAS；404/其它错误不改不删；冲突改**页面局部横幅+“重新加载”按钮**，confirm 放弃未保存后 load 覆盖 world/revision 清 conflict，未改共享 FeaturePageShell）；`WorldConsolePage.tsx`（books/stories 增 loadState，失败=暂时无法检查；概览主书/故事有效性标签，失效时 select 内保留 disabled“已失效：原值”选项、主动重选才替换；局部冲突重载；角色卡头像换 BindingAvatar；**角色列表不加健康/刷新控件、加载零扇出**）；zh/en `worldWorkspace.ts` 各 +17 键（bindingHealth.* / targetValidity.*）等键对齐。
  - 验证全绿：`go test ./internal/world ./internal/api/handlers -count=1` 两包 ok；作用域 `go vet` 0；`go build ./cmd/denova` 0；前端 `tsc --noEmit` 0；`check-i18n-keys` 3717 键 zh/en 全等；vitest world-workspace 5 文件 27 测试 + workbench/stores 12 文件 41 测试全过（仅既有 act/a11y 警告）；`vite build` 成功（仅既有 >500kB chunk 提示；pnpm 不在 PATH，改用本地 vite.cmd）。
  - 真实 HTTP 验收（隔离临时数据目录 tmp-accept + 刚构建 dist，端口 8099，验收后已停服并删除该目录，未污染真实数据）：POST 现代世界（带 masterRevision）与旧世界（不带该字段）均 201，旧世界盘上 UTF-8 中文完好、列表 warnings 为空=旧文件兼容；PUT 错 revision→409（仅 expected/actual、无路径泄漏）、正确 revision 刷新绑定三字段→200 且角色 displayName/worldNote 与 createdAt 原样保留、updatedAt 刷新；masterRevision 101 字符→400 字段级报错。无头 Edge（独立 user-data-dir）截图确认外壳/左侧“世界工作区”导航/四模式（写作/游戏/叙界/开放沙盒）完好零回归。说明：空数据实例下外壳全局引导停在“加载中”（根页面同样，缺书籍 workspace 所致，与本次无关；Phase 1 已在真实数据验证过列表渲染），故角色档案健康徽章/刷新按钮、概览失效选项的深层交互由上述单测+HTTP 覆盖，建议在你真实环境再做一次目视确认。
  - 精确文件（待 Codex 审查后再暂存，禁止 git add .，排除 denova.exe、shot-*.png、web/dist-verify-*、dist、.obsidian、docs 原型）：后端 internal/world/{types.go,validate.go,store_test.go}；前端 features/world-workspace/{binding-health.ts,types.ts,components/BindingPicker.tsx,components/CharacterProfile.tsx,components/BindingAvatar.tsx,components/BindingHealthBadge.tsx,pages/WorldConsolePage.tsx,__tests__/binding-health.test.ts,__tests__/toBinding.test.ts}、i18n/locales/{zh-CN,en-US}/worldWorkspace.ts；方案 docs/plans/WORLD_WORKSPACE_PHASE2A_PLAN.md。

## 当前状态（最近更新：2026-09-09 08:39 · Doubao）
- 2026-09-09 08:39 · Doubao：**按 Codex 三审“Changes requested”把 Phase 2A 方案修订为 v2（仅改方案、未编码），待 Codex 快速核对文档 diff**，覆盖 `docs/plans/WORLD_WORKSPACE_PHASE2A_PLAN.md`（文首加 v1→v2 对照表）。六点修订：①两页 conflict 从“只有文案”改为新增显式“重新加载”按钮，点击先 confirm 放弃未保存修改、确认后重新 getWorld 覆盖本地并更新 revision/清 conflict（控制台同时清 dirty），补测试；②**检查与刷新分离的最小实现**——控制台角色列表不加任何健康检查/刷新请求或控件，仅打开 CharacterProfile 时复用其现有 fetchMasterAsset 做一次只读检查（绝不改本地 world），档案页显示健康态，只有显式点“刷新资料摘要”才重读并更新绑定三字段；③删除 useBindingHealth hook 与 AbortController/signal 承诺（fetchMasterAsset 不收 signal，唯一消费者是档案页，逻辑内联，用现有 cancelled 布尔+请求序号防竞态），新增前端文件由 4 改为 **3**（binding-health.ts、BindingAvatar、BindingHealthBadge）；④CharacterProfile 本无 dirty，删除“刷新置 dirty”及相关测试，刷新只改其本地 world、持久化走既有保存按钮+CAS，控制台 dirty 仅服务自身编辑；⑤主书/故事失效时在 select 内保留 disabled 的“已失效：原路径/ID”选项，不呈现为未选择，主动重选才替换；⑥一致性：BindingHealthState 只在 types.ts 定义一次，统一为“四个结果态 latest/stale/missing/unavailable + 遗留 unchecked + 瞬时 checking”，toBinding 写入 masterRevision 改为**必测**（导出纯函数并断言，含缺省回退空串）。
- 不变项：不新增后端路由/npm 依赖、不改 Module3/4、不批量扫描/轮询、不自动删绑定、不回写总库/书籍/故事，完整保留 F-01～F-06；后端仍仅 types.go 加可选 masterRevision（omitempty，旧文件缺字段=unchecked 不判损坏，SchemaVersion 仍 1）+ validate 长度上限 + store_test。下一步：交 Codex 核对 v2 diff，通过后再按文档第 11 节顺序编码。

## 当前状态（最近更新：2026-09-08 22:53 · Doubao）
- 2026-09-08 22:53 · Doubao：**先核对 Codex 的 F-01～F-06 修复，再产出 Phase 2A（真实资料库绑定健康与刷新）实施方案，本轮只出方案、未编码，待用户与 Codex 审查**。新建 `docs/plans/WORLD_WORKSPACE_PHASE2A_PLAN.md`。开工前把当前工作树当唯一事实来源，逐处读代码确认六处修复真实落地（未回退/未重做）：`handler_world.go` 双层必填 expected_revision+archived `*bool`、1MiB 限制与冲突脱敏；`store.go` Replace/Archive 拒空 revision、读取走 validateStoredWorld；`validate.go` 角色绑定类型校验、`maxNote=4000`、validateStoredWorld 校验文件名/内容 id/schemaVersion/时间戳/结构；前端 `characterFromBinding` 两端复用与移除联动、BindingPicker 限定 character_template/character、CharacterProfile 备注 maxLength=4000。并独立复跑：`go test ./internal/world ./internal/api/handlers -count=1` 两包 ok、作用域 vet 0、tsc 0、i18n 3700 键对齐、vitest 定向 7 文件 29 测试全过（与 Codex 自述一致），临时验证日志已清。
- Phase 2A 方案要点（实读为据）：核对到 `MasterItem.Revision`（master_library.go）是 `sha256:` 内容哈希、内容变才变，可等值比较；前端 `MasterAssetSummary.master_revision` 在列表/详情均已携带，新绑定记录版本零额外请求；CharacterProfile 已按需单取原件、控制台已一次性加载 books/stories。方案：①`AssetBinding` 加**可选** `masterRevision`（omitempty，空串=遗留“尚未检查”，SchemaVersion 仍为 1，不迁移不重写，validate 只加长度上限、绝不把缺字段判损坏，完整保留 F-01～F-06）；②健康六态 unchecked/checking/latest/stale/missing(404)/unavailable，纯函数落新 `binding-health.ts`，**世界加载零请求、只在查看单个绑定/手动点刷时拉一个，不做批量与轮询**；③单个绑定刷新只改 nameSnapshot/tagsSnapshot/masterRevision，绝不改角色实例，统一走既有 PUT+expected_revision CAS，409 走现有冲突重载，404 保留不删；④新增 BindingAvatar 补头像 onError fallback，复用既有头像接口，不存头像；⑤主书/主故事用已加载列表判 可进入/已失效/暂时无法检查，books/stories 区分 loadState 防把加载失败误判失效，可重选、不反向写 worldId。无新增后端路由/服务方法、无 npm 依赖。文档含字段迁移兼容矩阵、状态机、接口复用、UI 落点、错误处理、精确改动文件（后端 3+前端新增3改6+i18n+测试）、测试矩阵、风险表与提交纪律（只精确暂存，不 git add .，不混入 Demo/截图/dist/.obsidian）。下一步：等用户与 Codex 审查该方案，通过后再按文档第 13 节顺序编码。

## 当前状态（最近更新：2026-09-08 19:00 · Codex）
- 2026-09-08 19:00 · Codex：**完成 World Workspace Phase 1 节点审查修复，原审查问题统一编号 F-01～F-06，不进入 Phase 2 范围、不新增依赖、不改 Module3/Module4**。F-01：`internal/world.Store.Replace/Archive` 与 HTTP Handler 双层拒绝空 `expected_revision`，消除绕过 CAS 的盲写；archive 的 `archived` 改为必填布尔值。F-02：三个 World 写端点统一限制请求体 ≤1 MiB，超限返回 400。F-03：创世绑定角色模板时同步生成独立 `WorldCharacter` 实例，移除绑定时同步移除对应初始角色；共用 `characterFromBinding`，避免控制台与创世逻辑分叉。F-04：角色区 BindingPicker 仅请求 `character_template + character`，后端同时校验角色 binding 的类型，阻止设定/地点等资料被实例化为角色。F-05：列表、详情、替换、归档读取磁盘文件时校验文件名 id、内容 id、schemaVersion、时间戳与完整业务结构；合法 JSON 但语义损坏的文件进入 warnings/500，不再伪装成空世界；错误响应不再泄露本机 DataDir 路径，409 仍保留 expected/actual。F-06：CharacterProfile 的 worldNote/growthNote 前端上限由 8000 统一为后端契约 4000。
- 新增回归：`internal/api/handlers/handler_world_test.go` 覆盖 1 MiB 边界与冲突错误脱敏；`internal/world/store_test.go` 覆盖空 revision、语义损坏 JSON、错误路径脱敏、角色绑定类型及中文 UTF-8 往返；新增 `world-factory.test.ts` 覆盖角色绑定生成独立实例。验证真实执行：`go test ./internal/world ./internal/api/handlers -count=1`、作用域 `go vet`、TypeScript `tsc --noEmit`、i18n 3700 键对齐、7 个前端测试文件 29 项全部通过；`pnpm run build` 与 `go build ./cmd/denova` 均成功，Vite 仅保留既有大 chunk 提示。未读取或输出 API Key；未触碰其它未提交文件。

## 当前状态（最近更新：2026-09-08 18:05 · Doubao）
- 2026-09-08 18:05 · Doubao：**World Workspace MVP（Phase 1）编码完成并通过定向验证，待 Codex 关键节点审查**。严格按已批准 v3.1 契约实现，未重构、未替换 Module3/Module4，未新增 npm 依赖。开工前先做了用户批准的 4 处机械文字修正（archive 请求体补 expected_revision；bindingId 明确客户端生成+服务端校验、删除“必要时重建”；类型定义删 `| string` 且 semanticType 用精确联合；设计参数笔误 4/3/10 改回 4/3/7）。
- 后端（Go，全在新目录 `internal/world`，不归 internal/book；数据落全局 `cfg.DataDir()/worlds/`，跨书）：`types.go`（领域结构+枚举+SchemaVersion=1，Summary 派生 coverColor）、`validate.go`（字段上限、客户端 id 正则 `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`、世界 id `^[a-z0-9]{12,24}$`、枚举白名单、唯一与跨集合引用完整性）、`store.go`（每世界一个 world-<id>.json、**无 index.json**、扫描即时派生 Summary 按 updatedAt 倒序、损坏文件进 warnings、crypto/rand 服务端 id、time.RFC3339Nano 避免同秒 CAS）、`store_test.go`；`internal/app/world_app_service.go` 门面；`internal/api/handlers/handler_world.go` 5 处理器（POST 出现 id/status/createdAt/updatedAt/schemaVersion/revision 直接 400，成功信封 {world,revision}，错误映射 400/404/409/500）；`internal/api/routes.go` 在认证 /api 组注册 GET/POST /api/worlds、GET/PUT /api/worlds/:id、POST /api/worlds/:id/archive（无 DELETE，仅归档+撤销）。CAS 复用 `internal/revisionfile` 内容哈希，PUT body {expected_revision,world}、archive body {archived,expected_revision}。
- 前端新 feature `web/src/features/world-workspace/`：数据层 types/selectors/world-factory/world-api（与 Go json tag 对齐，零 localStorage，创世草稿纯 React state）；编排器 WorldWorkspace（list/create/console/character 四视图）；页面 WorldListPage（loading/error/ready、active/archived/all 过滤、warnings 横幅、归档走 CAS+sonner 撤销）、WorldCreatePage（四步创世、未保存离开 confirm、submitting 防重复、一次原子 POST、主书 getBooks/主故事 getInteractiveStories）、WorldConsolePage（装载/脏态/CAS 保存/409 冲突提示、6 section、角色绑定与手建）；组件 BindingPicker（防抖搜索+分页+五态，只读 listMasterAssets availability=usable）、CharacterProfile（世界实例可编辑 + fetchMasterAsset 原件只读双层，原件失败不堵编辑）、ModeEntries（**四**入口：写作先 await onQuickSwitchBook(primaryBookPath) 成功才切 ide、游戏 feature 内 import selectInteractiveStory 成功才切 interactive、叙界 onCloseModule4 后切 narraverse、开放沙盒仅 onOpenModule4 不存 module4AdventureId）；sections 下 Location/Faction/Timeline **三独立文件**未三合一。绑定单一真源：只在 World.bindings 存一份，角色/地点/势力仅存 bindingId，stats 即时算、头像按 masterItemId 构造 URL 不持久化、无 stale。
- 既有文件最小对称改动：`stores/workspace-store.ts`（WorkspaceMode/isWorkspaceMode 加 worlds；persistMode 守卫本就只对 ide/interactive/narraverse 写 nova:content-mode，故 worlds 天然不污染 content-mode）、`components/workbench/ModeRouter.tsx`（lazy WorldWorkspace + worlds MainRouteLayer，透传 onClose/onSetMode/onQuickSwitchBook/onOpenModule4/onCloseModule4）、`components/workbench/WorkbenchShell.tsx`（Globe2 图标、ActivityItemId、两套默认活动栏在 versions 后 books 前插 worlds、sharedMenuActive/fullWorkspacePanelVisible/modeLabel/navigationMode/navigationModelModule/openVersions 共享清单、worldsActive/openWorlds 再点返回、sharedActivityItems 与移动端）、`App.tsx`（三处共享清单，含无书可进入 worlds 的例外）；i18n 新建 zh-CN/en-US worldWorkspace.ts（约 130 扁平点号键，zh/en 全等）并在两聚合器注册，zh/en workbench.ts 各加 activity.worlds/mode.worlds。
- 验证（真实执行，非推断）：①`go build ./...`、`go vet` 通过；`internal/world -count=1` ok；`go test ./...` 30 包 ok，9 个失败**全部**是仓库已知的 Windows 符号链接权限类（建 symlink 报 “A required privilege is not held by the client”，含名字不含 symlink 但 setup 建链的 TestActiveAutomationReservation…ConcurrentCaller，单独 -count=3 复现同一权限错误），与本次无关，Codex 15:40 日志亦记录过同类。②前端 `tsc --noEmit` exit0、`check-i18n-keys` 3700 键对齐、`vite build` 成功（仅既有大 chunk 体积提示）。③vitest：新增 selectors(4)+world-api(5) 共 9 过、workspace-store 新增“worlds 不覆盖 content-mode”用例过，ModeRouter/WorkbenchShell 单跑全过（共 28）；全量并行的 22 个 “5000ms timed out” 经 `--no-file-parallelism --testTimeout=20000` 单跑全部通过（重组件高负载假失败，另抽 AgentsView+AutomationsView 25 过）；`src/main.test.tsx` 单独跑也失败，经 `git stash` 干净基线复现同样失败，根因是 main.tsx 的 localhost→127.0.0.1 重定向守卫在 jsdom(localhost) 下跳过 render，属既有环境问题、非本次回归。④真实 HTTP 验收（内置 go 起 127.0.0.1:8099）：空列表、UTF-8 原子创建（服务端生成 id、返回 {world,revision}）、列表派生 Summary、详情信封、错误 revision→409、正确 revision→200 且哈希变化、POST 带 id/空 name→400、非法/过短 id→400、格式合法但缺失→404、`..%2f` 穿越被路由归一化拦截到 SPA、非法 status→400、归档→过滤正确→恢复、注入损坏 world 文件列表返回 warnings(file/id/reason) 且健康世界仍在、删除损坏文件后 warnings 清空。⑤Edge headless（独立 user-data-dir）对 `?mode=worlds` 与默认写作视图真实截图：世界工作区列表从真实后端渲染、左侧“世界工作区”位于版本管理与书籍管理之间且高亮、顶部仍是**四**模式入口；默认写作视图《摘心人》目录/创作Agent/页脚完好，零白屏零回归。⑥网络审计：world-workspace 对总库仅 listMasterAssets/fetchMasterAsset/头像 GET 与 getBooks/getInteractiveStories GET，唯一 POST 是游戏入口既有 selectInteractiveStory，无模型/Key/chat 请求。临时脚本/日志/Edge profile/冒烟世界文件已清理，仅留 shot-worlds.png、shot-ide.png 两张验收截图。
- 踩坑：①既有 CRLF 源码（WorkbenchShell/App/i18n 聚合器）与本日志用多行 old_string 调 Edit 会报 Native execution failed，须单行唯一锚点编辑；本日志用 .NET ReadAllLines/WriteAllLines（UTF8Encoding($false) 无 BOM、自动 CRLF）拼接。②vitest 4.1.6 CLI 不接受 --poolOptions.forks.singleFork，用 --no-file-parallelism。③PowerShell 把含中文/引号的 JSON 作为 curl.exe 内联参数会被吞引号或按 GBK 误编码导致 400/乱码，须写入 UTF-8 文件用 --data-binary @file（服务端本身中文完好）。④无头 Edge 必须加独立 --user-data-dir，否则并入已开实例导致不出图。⑤go 不在 PATH，用内置 D:\Narraverse2.0\tools\go\bin\go.exe 且 GOTOOLCHAIN=local。下一步：交 Codex 做 Phase1 节点审查（建议重点看 internal/world 边界、模式接入共享清单、CAS 与归档撤销），审查通过后再进入 Phase2 真实资料库接入。

## 当前状态（最近更新：2026-09-08 15:40 · Doubao）
- 2026-09-08 15:40 · Doubao：**Codex 二审架构方向通过，按其 5 处契约问题把 World Workspace 实施计划修订为 v3（仍未编码；按约定不再三审，仅需 Codex 核对文档 diff）**，覆盖 `docs/plans/WORLD_WORKSPACE_IMPLEMENTATION_PLAN.md`。修订前复核代码：通读 `internal/revisionfile/revisionfile.go`（内容哈希 CAS：Revision=sha256、Read/Mutate/ReplaceIfRevision、ErrRevisionConflict、MissingRevision）；grep 确认 `onCloseModule4` 已在 ModeRouter(L96/196) 可得、WorkbenchShell 进 narraverse 时已自动 onCloseModule4(L243)、App handleCloseModule4(L620→L790)。
- 五处契约修订：①**创世一次原子落库**——扩展 WorldCreateInput 携带第3步所选 bindings/初始角色/primaryBookPath/primaryInteractiveStoryId，POST 建成完整世界，取消「先建空壳再 PUT」；②**CAS 改用 revisionfile 内容哈希而非 updatedAt**——单世界响应信封 {world,revision}、PUT 带 expected_revision、冲突 409 带 expected/actual，updatedAt 仅展示/排序，revision 不写进 World；③**取消 index.json**——每世界一个 world-<id>.json，GET /api/worlds 扫描目录即时派生摘要（消除「文件成索引败→世界消失」双真源），损坏文件不静默跳过：列表带 warnings、详情区分 404 缺失与 500 损坏并给可操作信息；④**补叙界回调链**——ModeRouter 把其已有的 setMode/onQuickSwitchBook/onOpenModule4/onCloseModule4 直接透传 WorldWorkspace（App 不加 props），selectInteractiveStory 由 world feature 内直接 import、不做 prop drilling；⑤**安全与字段语义**——世界 id 仅服务端生成并按 ^[a-z0-9]{12,24}$ 白名单校验后才进文件路径（filepath.Clean 防穿越，POST 带 id 直接 400），给出请求体≤1MiB 与各数组/文本上限表，创世草稿统一为**纯组件状态、删除 localStorage 表述**，**删除 lastVisitedAt**（避免读操作写盘），列表按 updatedAt 倒序。
- 同步更新数据模型、API 表、后端文件职责、模式接入清单、Phase1 任务序、DoD、风险表（新增路径穿越/超大请求 R9、半成品 R10，重写 R7）、决策与代码锚点，文首加 v2→v3 diff 对照表。一审七项（四模式/服务端真源/绑定单一真源/模式接入清单/归档撤销/状态契约/v3 范围缩减）按 Codex 确认保留。本轮仍只改计划文档与本日志，无生产代码改动、未运行构建。

## 当前状态（最近更新：2026-09-08 15:15 · Doubao）
- 2026-09-08 15:15 · Doubao：**按 Codex 第一轮「Changes requested」将 World Workspace 实施计划修订为 v2（仍未编码，待 Codex 第二轮快速审查）**，覆盖更新 `docs/plans/WORLD_WORKSPACE_IMPLEMENTATION_PLAN.md`。修订前先逐条复核了 Codex 引用的代码事实（非照搬）：`README.md:3-8` 与 `WorkbenchShell.tsx:512-553` 确认正式平台是**四**模块（写作/游戏/叙界/开放沙盒，v1 误把叙界并入沙盒）；`docs/交付/Narraverse2.0交接指南.md:60` 确认端口会从 8080 自动顺延且 localhost/127.0.0.1 与不同端口 localStorage 不互通；`features/interactive/api.ts:56` 存在 `selectInteractiveStory`；`config/config.go:437` 为全局 `cfg.DataDir()`；`internal/interactive/story_storage.go` 提供 index.json+单实体的 JSON 落盘范式；`WorkbenchShell.tsx:976 navigationModelModule` 的共享清单若漏加 worlds 会从游戏/叙界进入时误判为 writing 模型上下文。
- 七项修订：①运行入口改回**四个**并补独立「叙界」入口；②正式 MVP 不以 localStorage 为真源（该风险升为**高**），改为新增最小后端 `internal/world`、数据落全局 `cfg.DataDir()/worlds/`（跨书、不归 internal/book），前端从第一天走真实 HTTP，localStorage 仅存创世向导临时草稿；③模式入口必须进入绑定实例——写作先 await `onQuickSwitchBook` 切书成功、游戏先 await `selectInteractiveStory`，失败留页，沙盒受 iframe postMessage v1 限制**不再持久化不可用的 module4AdventureId**；④绑定单一真源——只在 World.bindings 存一份快照，角色/地点/势力仅存 bindingId，stats 即时计算不持久化，头像按 masterItemId 构造 URL，去掉未实现的 stale；⑤不提前冻结 API、删除「接口+双实现+工厂」，按真实动作定 5 个最小端点（列表/创建/详情/整文档 PUT 带 updatedAt 乐观锁 409/archive 单端点兼归档恢复，无 DELETE），不再承诺「UI 零改动」；⑥补全模式接入清单（navigationModelModule、ActivityItemId、两套默认活动栏排序、openWorlds 再点返回、桌面+移动测试、`?mode=worlds` 不覆盖 nova:content-mode）；⑦补全页面状态契约（BindingPicker 五态、角色原件加载失败不堵世界实例编辑、创世未保存离开/提交失败/重复提交/恢复、归档+撤销无删除确认），地点/势力/时间线拆为三个独立组件不做三合一。
- 采纳 Codex 全部拍板建议：共享菜单模式、创世草稿仅会话态、字段命名 primaryBookPath/primaryInteractiveStoryId（一本主书+一个主故事，不做多元实例假象）、手建条目升格总库延后、仅归档可撤销、新增 internal/world、首页 0.5 节明确这是相对 Demo v3 的主动范围缩减（事实提案/任务健康/沙盒推演/AI 生成/图谱地图均移到 Phase2/3，Phase1 不是完整「世界上下文与事实记忆层」）、沿用 Denova 设计系统（视觉冒险4/动效3/信息密度7，不另出视觉）。本轮仍只改文档与本日志，未改任何代码、未运行构建，工作区为本地未提交改动。

## 当前状态（最近更新：2026-09-08 14:18 · Doubao）
- 2026-09-08 14:18 · Doubao：**产出 World Workspace MVP 正式开发前实施计划（未编码，待 Codex 审查）**，新建 `docs/plans/WORLD_WORKSPACE_IMPLEMENTATION_PLAN.md`（约 24.8KB）。按既定协作流程（产品方向已定 → 豆包读项目+制定计划 → Codex 审查代码影响/风险 → 用户拍板 → 豆包编码 → Codex 关键节点审查），本轮只做方案，不改任何源码。
- 计划基于实读代码（非印象）：前端 React19+Vite+TS+zustand，模式机制在 `stores/workspace-store.ts`、`components/workbench/ModeRouter.tsx`、`WorkbenchShell.tsx`、`App.tsx`；Module3/4 为 `NarraverseWorkspace.tsx` iframe 载入 `/narraverse/`（来自 app/，同步到 web/public/narraverse），明确不碰；后端 `internal/api/routes.go` 当前**无 World API**，可复用只读端点 `GET /api/books`、`GET /api/library/assets`（仅 MasterAssetSummary 摘要）、`/api/library/assets/:id`（按需详情）、`/api/interactive/stories`；确认语义分类词表 `character|world|location|faction|rule|item|other`（internal/book/lore.go:1082）供世界地点/势力/物品复用。
- 核心方案：①新增共享菜单模式 `worlds`（对齐 library，不做第四内容模式，不动 ide/interactive/narraverse 三选一）；②本期纯前端、不写 Go，定义 `WorldWorkspaceApi` 接口 + `LocalWorldWorkspaceApi`（localStorage 键 `nova:world-workspace:v1`）+ 单点工厂，未来换 `HttpWorldWorkspaceApi` 对接冻结命名的 `/api/worlds*` 时 UI 零改动；③不复制总资料库——世界只存绑定（id+薄摘要+世界内实例状态），原始资料按需只读、永不回写；④世界背景共享、剧情不强制同步（只存背景正典 + 各模式 links，事实提案机制推迟 Phase3）。数据模型给出 World/WorldAssetBinding/WorldCharacter/WorldCodexItem/WorldFaction/WorldTimelineEntry 契约；新增隔离目录 `web/src/features/world-workspace/` 与 zh/en 成对 i18n；仅对称最小改动 4 个既有文件（store/ModeRouter/WorkbenchShell/App）+双语 workbench 标签。含 Phase0-3 拆分、DoD、12 项风险表、验证计划、6 个待拍板开放问题与代码锚点附录。
- 本轮未编码、未运行构建；除新建计划文档与本日志外无其它文件变更，未读取/输出 API Key。下一步：交 Codex 审查该计划，用户拍板后再进入 Phase0/Phase1 编码。

## 当前状态（最近更新：2026-09-08 12:23 · Doubao）
- 2026-09-08 12:23 · Doubao：**完成 Narraverse 世界工作区 Demo v3**，位于 `design-previews/2026-09-08-narraverse-world-demo-v3/`（`index.html` 单文件约 150KB，零依赖零构建，`file://` 双击即开；`说明-v3.md`；`_shots/` 11 张验证截图）。核心纠偏：世界工作区=世界的舞台/资料上下文/长期记忆，现有 Denova 侧栏=创作和运行的操作系统，V3 展示二者接入而非新造平行系统。三层信息架构：平台控制平面（版本/书籍/总资料库/方案预设/Skills/Agents/自动化/设置，左侧 rail+抽屉，仅展示入口与映射，文案明确"不重建不模拟"）→ 当前世界工作区（概览/资料绑定/角色/时间线/世界事实/任务与健康）→ 四运行模式（写作/结构化游戏/叙界/开放沙盒）。顶栏三 seg（工作区《黑月挽歌》/世界 黑月大陆/模式）+ 事实提案徽章 + 世界版本 r17；四模式页均带统一"世界上下文条"（当前世界/4 规则·3 角色·12 事实/版本/查看上下文/返回工作区），模式内不可改模型/预设/Skills/Agent 权限。
- 关键机制：**世界事实提案**——草稿→事实提案→用户确认→已确认世界事实→时间线/角色/势力更新；事实分六类（已发生/角色已知/玩家已知/未来计划/提案/被拒绝）；内置"林夜加入影裔教团"剧情 Agent 提案与"黑森林远古遗迹"沙盒提案，采纳后真实改写角色势力/势力影响力/地区稳定并追加时间线。**任务与健康中心**取代 V2"四 Agent 永远运行中"：七种状态（待命/排队/运行/等待确认/完成/失败/停止）+ 6 任务，来源模块全部映射既有能力（总资料库+配置管理 Agent、/master-library、写作 Agent+/continue、后台导演 Agent+Story Director、Module4 运行时+自动化、自动化·定时任务），含停止/重试/确认写入。**沙盒**：推进 1 天/7 天/1 季→生成事件候选→展示影响预测（谁响应/势力变化/地区受影响）→确认应用→更新世界状态；补齐暂停/停止/重试、规则四滑杆含含义/恢复默认/修改历史、三身份真权限（观察者只读锁定介入卡片/导演全权/新角色建世界实例不改写原始角色卡）、事件注入先成提案。
- 验证：内联 JS（约 78KB）`node --check` 通过；Edge 无头独立 profile 对 11 个视图（概览/绑定/角色中心/角色档案/时间线/事实/任务/写作/游戏/叙界/沙盒）逐一截图确认渲染；页面内冒烟 `?smoke=1` 17 项全过（概览/控制平面抽屉/绑定解析/角色双层/提案采纳/拒绝/写作提案/游戏分支/叙界对话/沙盒推进评审/应用候选/观察者权限拦截/角色介入/规则调整/任务完成/任务待确认/确认写入）。踩坑记录：① PowerShell 5.1 不支持 `&&`，命令串联用分号；② 无 puppeteer/playwright，验证用 Edge `--headless --user-data-dir=<临时profile> --dump-dom` + 页内 smoke、`--screenshot` 逐视图截图；③ 未加 `--user-data-dir` 时无头 Edge 会并入已运行实例导致截图内容错乱；④ 叙界对话数据内嵌单引号转义错误、沙盒规则滑杆拼接误用逗号运算符（导致 return 只剩末段）、角色字段名 n/name 不一致，均已修复并复验。未修改 app/、denova-src/、knowledge-base/、V2 及任何生产代码；未读取/输出 API Key。

## 当前状态（最近更新：2026-09-08 11:31 · Doubao）
- 2026-09-08 11:31 · Doubao：**完成 Narraverse 世界系统 Demo（新版·单文件版）**，位于 `design-previews/2026-09-08-narraverse-world-demo-v2/index.html`（约 107KB，纯原生 HTML/CSS/JS 自包含，零依赖零构建，`file://` 双击即开），说明文档 `说明-新版.md`。相对 08:27/08:35 概念 Demo 的升级点：创世大厅明确三入口（文字创建 + 模拟导入 JSON/TXT/MD 世界书 + 混合创建=角色卡+AI 补全世界历史/地图/势力/冲突）；控制台升级为「我的世界首页」（概览条 + 世界状态 + 资料库八入口抽屉 + 500 年纪元时间线 + 三模式入口）；角色档案九分页（基础/背景/性格/能力/技能/装备/关系/成长路线/经历）；写作模式流式续写/生成对白/改写；游戏模式 8 场景分支含战斗检定与双结局；**沙盒模式为重点**——世界时钟（第 N 年·季节·日）自主推进 1 天/7 天/1 季，事件流按类型（势力/城市/冒险/异变/日常）生成并实时更新势力影响力与地区发展/稳定度，三种介入（角色介入创建旅行者入世 / 事件介入按「谁发现→哪些势力响应→世界如何变化」推演并改状态 / 规则调整四滑杆影响事件权重），明确「不是自动生成小说」；AI 团队四 Agent + 协作流水线 + 任务台日志流。地图与势力关系为内联 SVG 自绘，沿用 DESIGN.md 暖黑/铜色/苔绿设计系统。
- 验证：内联 JS（约 70KB）通过 `node --check`；Edge 无头模式对 8 个视图（创世/控制台/角色列表/角色档案/写作/游戏/沙盒/AI 团队）逐一截图确认渲染（`_shots/`）；页面内冒烟自测（`?smoke=1` 真实 DOM 驱动）8 项全过：创世 8 步流程完整、沙盒推进 7 天生成事件、角色介入、事件介入推演面板、规则调整、游戏分支跳转、角色九页签、资料抽屉。未修改叙界/Module3/Module4/Denova 任何生产代码与用户资料。

## 当前状态（最近更新：2026-09-08 08:35 · Doubao）
- 2026-09-08 08:35 · Doubao：**单文件版 Demo 新增「我的世界」列表层**，修正产品流程：世界生成完成后不再直接进入控制台，而是先进入世界列表页（`#/worlds`），用户选择具体世界后才进入该世界的控制台与角色档案。世界列表展示 4 个活跃世界（黑月大陆/永夜群岛/灰烬纪元/青云天阙）+ 2 个已归档世界，每个世界卡片含类型、状态、历史/角色/地点/势力统计、当前时代与重大事件。
- 创世时若输入新世界名称，会自动创建世界对象并加入列表顶部，标记「新世界」角标；导航栏新增「我的世界」入口；控制台与角色中心顶部均加「切换世界」按钮，非完整世界进入时显示「设定尚在 AI 生成中」提示并以黑月大陆资料作参考。JS 语法通过，世界列表页截图验证渲染正常。

## 当前状态（最近更新：2026-09-08 08:27 · Doubao）
- 2026-09-08 08:27 · Doubao：**完成 Narraverse 概念 Demo（单文件版）**，位于 `design-previews/2026-09-08-narraverse-concept-demo/index.html`。纯原生 HTML/CSS/JS 单文件自包含，零依赖零构建，双击即可在浏览器打开（`file://` 协议可用）。覆盖任务书全流程：创世大厅（文字描述 + 4 题材预设 + 模拟导入 JSON/TXT/MD 角色卡）→ 世界生成（宇宙球体动画 + 8 步 AI 日志逐条播报 + 进度条）→ 世界控制台（黑月大陆概览 128 角色/56 地点/12 势力/41 事件线 + 当前世界状态 + 资料库六入口抽屉 + 500 年横向时间线）→ 角色中心（4 名立绘角色卡 + 完整档案页含基础/外貌/性格/背景/技能/装备/成长路线/关系）→ 三模式（写作大纲+流式续写+剧情建议、文字冒险 10 场景分支脚本含状态/物品/线索变化、自由沙盒推进 1 天/7 天 NPC 动态+世界影响条）→ AI 团队（四 Agent 卡片+协作流水线+实时任务台日志流）。
- 视觉素材：原创生成 6 张图（黑月大陆主视觉、林夜/艾琳/黑龙王/院长 4 张角色肖像、世界地图），存于 `assets/`。势力关系为手绘 SVG 图谱，地图带区域热点标注。
- 验证：内联 JS 通过 `node --check` 语法校验；Edge 无头模式对 8 个 hash 视图（首页/生成/控制台/角色列表/角色档案/写作/游戏/沙盒/Agent）逐一截图确认渲染正常，截图存 `_shots/`。未修改生产代码、用户资料或 API 配置。
- **两版共存说明**：本会话先于 08:16 完成单文件版 `index.html`；随后并行会话 WorkBuddy 进入同目录产出多文件版，为避免覆盖将其入口命名为 `index-multifile.html`。两版入口不同、互不覆盖，用户可对比取舍。单文件版优势是零服务器依赖、双击即开、便于分享；多文件版优势是代码分模块、支持 `?quick=1` 直达参数。

## 当前状态（最近更新：2026-09-08 08:25 · WorkBuddy）
- 2026-09-08 08:25 · WorkBuddy：**完成 Narraverse 概念 Demo（多文件版）**，位于 `design-previews/2026-09-08-narraverse-concept-demo/`。纯原生 HTML/CSS/JS 零依赖零构建，入口 `index-multifile.html`（支持 `?quick=1` 跳过创世直达控制台）。覆盖任务书全流程：创世大厅（文字描述/预设模板/模拟导入 JSON 角色卡·TXT·MD）→ 世界生成动画（世界之种凝聚 + 四 Agent 日志逐条播报）→ 世界控制台（黑月大陆概览/世界状态/张力/资料库六入口）→ 角色中心（4 名立绘角色档案 + SVG 关系网络）→ 世界资料（规则/时间线/可点击热点地图/势力/生物/物品）→ 三模式（写作大纲流式生成、文字冒险 8 场景分支脚本含状态与物品变化、沙盒时间推进事件流）→ Agent 星系网络页。数据全部为 `js/data.js` 内置模拟。已用 Node `--check` 全量语法校验 + Edge 无头模式对 9 个页面截图验证渲染（`_shots/quick_*.png`、`multi_home.png`）。
- **并行冲突记录**：检测到另一并行 AI 会话于 08:16-08:17 在同目录产出单文件版 Demo 并覆盖了 `index.html`（附 `_check.js`、`_shots/index_*.png` 等）。为避免互相覆盖，多文件版入口命名为 `index-multifile.html`，两版共存；说明文档为 `说明-多文件版.md`。

## 当前状态（最近更新：2026-09-07 12:56 · Codex）
- 2026-09-07 12:56 · Codex：**修复本地正式入口在 `localhost` / `127.0.0.1` 间切换导致浏览器资料看似丢失的问题**。Denova 启动提示与启动器实际入口统一使用 `http://127.0.0.1:<实际端口>`；Web 前端检测到 `localhost` 时保留路径、查询参数和片段并自动跳转到同端口的 `127.0.0.1`，避免继续创建第二套 localStorage/IndexedDB 来源。未删除、迁移或覆盖旧浏览器存储、`.denova` 文件和冒险数据。
- 验证：前端 `tsc --noEmit` 与 Vite 生产构建通过；`cmd/denova` Go 定向测试通过；`internal/api` 仅剩仓库已知的 Windows 符号链接权限测试 `TestWorkspaceSwitchCanonicalizesSymlinkIdentity` 失败，与本次入口修复无关。正式 `setup_narraverse2.ps1 -NoOpen` 完成资料生成、资源同步、锁定依赖安装、前端/Go 构建并启动 `denova-src/output/denova.exe`（PID 44708）。`127.0.0.1:8080` 根页、`/api/status`、`/api/books`、`/api/library/assets?limit=100` 均返回 HTTP 200；重启后书籍 6、总资料库资产 6。浏览器实际确认 `localhost:8080/?mode=library` 刷新后跳转到 `127.0.0.1:8080/?mode=library`，正式页面仍显示四模块入口与总资料库内容。构建仅有既有大 chunk 体积提示和 pnpm 更新提示，未读取或输出 API Key、Authorization 或完整用户存档。

## 当前状态（最近更新：2026-09-07 10:48 · Codex）
- 2026-09-07 10:48 · Codex：**为总资料库全部角色设定卡增加资产级“停止 / 删除”操作**。停止会将该角色卡仍在处理的本地翻译目标标记为 `cancelled`，并尽力取消 8097 队列中的对应任务；删除经过确认后只移除 Master 活跃索引，将原始导入资料、翻译资料、已有冒险实例和来源文件转入可追溯的归档索引并写入 tombstone，不删除实际资料文件或冒险数据。若队列不可用，仍可安全移除活跃索引并返回警告；删除后不影响已有冒险实例。
- 前端所有 `character_template` 卡片均显示“停止 / 删除”；没有待处理任务时停止按钮禁用，处理中的卡可停止；删除提供二次确认。取消后的失败/取消字段可重新处理。未新增依赖、未新建模型配置，未读取或输出 API Key、Authorization 或完整用户存档。
- 验证：Go `internal/book` 通过；前端定向 Vitest 3 个文件共 41 个测试通过；TypeScript 通过；Vite 生产构建通过；正式 `setup_narraverse2.ps1 -NoOpen` 完成前端嵌入和 Go/Denova 构建并启动。Denova executable 为 `denova-src/output/denova.exe`（PID 33872），根页、`/api/status`、总资料库接口和 8097 bridge 均返回 HTTP 200；页面实际确认角色卡操作入口与按钮状态，浏览器启动日志无 error/warning。归档保留逻辑由隔离测试验证，未对真实用户角色卡执行删除。

## 当前状态（最近更新：2026-09-07 09:58 · Codex）
- 2026-09-07 09:58 · Codex：**修复“总资料库 → 加入当前冒险”角色卡空指针异常**。`buildMasterRuntimeOperations` 在合法缺少 `character_book` 的角色卡上直接访问 `card.CharacterBook.Entries`，导致阶段“生成冒险资料操作”返回 `invalid memory address or nil pointer dereference`；现仅在 `card.CharacterBook != nil` 时建立嵌套条目映射，保留无角色书角色卡的单角色资料生成逻辑。新增无 `character_book` 角色卡实例化回归测试。
- 修复前回归测试按预期复现失败，修复后 Go `internal/book` 全量通过；生产 `setup_narraverse2.ps1 -NoOpen` 已重新完成前端/Go/Denova 构建并启动。使用当前 Master 资料库的隔离副本对 4 张角色卡逐一调用真实加入接口，均返回 HTTP 200，未写入真实冒险；真实 8080 根页、`/api/status` 与 8097 bridge 健康检查均返回 200。未修改模型配置、API Key、原始角色卡或用户存档。

## 当前状态（最近更新：2026-09-06 23:35 · Codex）
- 2026-09-06 23:35 · Codex：**按用户选定的 D「角色编辑台」方向完成角色卡整体 UI 实施**。角色详情头部现在集中显示本地头像位、名称、来源和标签；缺失的常见档案字段显示“未知”；左侧新增角色条目目录，支持新增自定义条目和编辑已有角色书条目。新增条目通过既有 Master 人工编辑/CAS 版本保护接口持久化，未新增独立模型配置、未改变模块边界。
- 头像仅从资料库中已归档的本地 PNG 提供 `/api/library/assets/:id/avatar`，不抓取远程头像地址；没有把远程地址或用户存档写入日志。未重构 Module3/Module4，未修改模型网关或 API Key。
- 正式 `setup_narraverse2.ps1 -NoOpen` 已完成资料生成、资源同步、前端构建和 Go/Denova 构建；启动脚本因本机 Windows 将 `8097` 置于排除端口范围（`WinError 10013`）未能启动 bridge，未改动系统端口保留。已直接启动当前构建的 `denova.exe`，正式页面 `http://127.0.0.1:8080/?mode=library` 与 `/api/status` 均返回 200。
- 验证：TypeScript 通过；LibraryView 36/36 通过；i18n 3532 keys 对齐；Go `internal/book` 通过；Vite 生产构建通过；`git diff --check` 通过。Go API 全量回归的唯一已知失败仍是 Windows 未授予创建符号链接权限的环境测试，非本轮角色卡逻辑失败。

## 当前状态（最近更新：2026-09-06 18:45 · Codex）
- 2026-09-06 18:45 · Codex：**角色卡整体 UI 方案与四方向样机**。确认当前 `CharacterReader` 的人物档案头部与角色概览重复显示 `character.description`，列表与详情头像为固定 `UserRound`；新增独立 `design-previews/2026-09-06-character-card/index.html`，呈现轻量资料页、角色阅读工作台（推荐）、人物书页、角色编辑台四方向，以及推荐方向的分区阅读、示例草稿、主题切换和本地 PNG 预览交互。当前任务为设计提案，尚未修改生产页面或实施头像资源 API。
- 按用户追加范围只读检查 `knowledge-base/角色卡` 与 `世界观设定`：角色主目录 39 张 JSON、23 张同名 PNG（19 竖图、3 方图、1 横图），介绍最长 14,262 字符、标签最多 45 个；37 个 avatar 为远程地址、2 个为空值标记；世界观目录 50 份 JSON。方案据此增加长文、可展开标签、可选字段、叙述者/RPG 类型和本地头像优先处理；未输出角色正文、远程地址或完整运行存档，未修改原始资料与用户已有改动。
- 使用已安装 Node 与 qiaomu-design 自带预览脚本，绑定本机 `127.0.0.1:3881`；未安装新增依赖。浏览器确认四方向与推荐交互可见、选择按钮恰好 4 个、示例草稿在切换分区后保留、取消与主题切换正常；桌面及实际约 520 CSS px 窄布局未发现横向溢出。请求 390 px 时浏览器返回约 520 px，故不宣称已完成 390 px 验收。PNG 资源接入、正式保存和正式平台构建尚未实施；用户方向选择接口当前为空，未代替用户提交方向。

## 当前状态（最近更新：2026-09-06 18:06 · Codex）
- 2026-09-06 18:06 · Codex：**调整总资料库角色标签与人物档案展示**。`MasterAssetSummary` 新增角色 `tags` 摘要，列表中将标签放在资产名称正下方并显示“标签：”；复用 Master 当前标签，常见英文标签在显示层转为中文并去重。角色详情的人物档案摘要移除固定高度和溢出裁剪，保留完整换行文本；设定书简介入口与独立大条目简介逻辑未改变。
- 验证：Go `internal/book` 与 `internal/api/handlers` 定向测试通过；前端 TypeScript 检查通过，LibraryView 34/34 通过，Vite 3614 modules 生产构建通过。正式 `denova.exe` 已重建并启动，`/api/status` 返回 200；浏览器实际确认总资料库名称下显示标签、人物档案完整文本可见，以及“碧蓝航线”仍显示“设定书介绍 / 编辑介绍”。未修改用户资料内容，未 commit/push。

## 当前状态（最近更新：2026-09-06 17:52 · Codex）
- 2026-09-06 17:52 · Codex：**按最终确认收口角色卡资料页**。角色卡不再提供独立“角色卡简介”区域，仅保留角色档案中的“标签”区域；常见英文标签在当前内容视图中显示为中文，并支持人工编辑、逗号/换行分隔后保存到 Master 的 `character.tags` 字段。原始角色卡字段不被覆盖，未知标签可直接在编辑框中改成中文；“设定书介绍”功能保持在 `lorebook_template` 资产详情头部并继续可编辑。
- 验证：LibraryView Vitest 33/33 通过；Go `internal/book` 与 `internal/api/handlers` 定向测试通过；Vite 3614 modules 生产构建通过；资源已同步并重建正式 `denova.exe`，`/api/status` 返回 200。浏览器实际确认角色卡显示中文标签、编辑框和保存/取消控件，未保存真实用户内容；确认“碧蓝航线”仍显示“设定书介绍 / 编辑介绍”。未 commit/push，未读取或输出密钥/完整用户存档。

## 当前状态（最近更新：2026-09-06 17:37 · Codex）
- 2026-09-06 17:37 · Codex：**补齐角色卡独立简介栏**。此前“编辑介绍”只允许 `lorebook_template`，角色卡没有对应入口；现扩展既有 Master 简介接口，角色卡简介保存到独立的 `master.description` 元数据，不覆盖 `character.description` 等运行字段。角色卡详情头部不再重复显示原始卡片长文本，改为“角色卡简介”可编辑面板；清空简介会移除该元数据。
- 验证：新增 Go 角色卡简介隔离/保存/清空测试；Go `internal/book` 与 `internal/api/handlers` 通过，前端 TypeScript 通过，LibraryView 33/33 通过，Vite 3614 modules 构建通过；正式 `denova.exe` 已重建并启动，浏览器实际确认“角色卡简介 / 编辑介绍 / 填写这张角色卡的简介”可用并取消退出。未保存真实用户内容，未读取或输出密钥/完整存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 17:10 · Codex）
- 2026-09-06 17:10 · Codex：**补齐总资料库角色档案第一步**。在既有角色详情内容页内增加人物档案头部、原文名称与标签展示，并增加“当前内容 / 原文 / 中英对照”只读切换；角色概览、性格与背景、场景与开场、示例对白、内部设定和高级指令继续复用已有 Master 字段、编辑入口与技术信息页，未新增数据结构、模型配置或依赖。
- 验证：LibraryView Vitest 32/32 通过，TypeScript 类型检查通过，Vite 3614 modules 生产构建通过；资源已同步并重建正式 `denova.exe`，正式页面与 `/api/status` 均返回 200；浏览器实际确认角色档案、原文切换和中英对照均可用。Go `internal/book` 与 `internal/api/handlers` 定向测试通过。仅停止并替换同路径 Denova 进程，保留旧 executable 备份；未读取或输出密钥/完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 16:44 · Codex）
- 2026-09-06 16:44 · Codex：**将总资料库设定书的大条目简介独立到资产详情头部并保持可编辑**。简介只读取 `lorebook.description`/顶层 `description`，为空时显示“暂无简介”；移除前端摘要和后端摘要将第一个小条目正文提升为简介的兜底逻辑，小条目正文仅在条目内容区域显示。复用既有 `PATCH /api/library/assets/:id/description`，未新增配置或依赖。
- 验证：Go `internal/book` 与 `internal/api/handlers` 定向测试通过；前端 `LibraryView` 31/31 通过，TypeScript 类型检查通过，Vite 生产构建通过（3614 modules）；正式 `denova.exe` 已重建并启动，`http://127.0.0.1:8080/?mode=library` 与 `/api/status` 返回 200。浏览器实际确认“设定书介绍 / 编辑介绍 / 暂无简介”位于大条目头部，并确认编辑器可打开后无修改退出；未写入用户资料，未读取或输出密钥/完整存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 15:46 · Codex）
- 2026-09-06 15:46 · Codex：**完成 Module4 时间推进、世界记录折叠和角色导入可见化**。V1.5 普通自由叙事行动现在复用现有短行动语义（1 tick / 5 精力），查看类操作仍为零时间；模型失败继续保持时间、精力和存档不变，晚间最后行动通过既有 Settlement 可靠进入次日早晨。旧版 legacy 世界行为不变。
- 主世界记录只呈现当前游戏日，最近 6 轮默认展开，今天更早内容放入原生折叠区；跨天后旧日正文退出主叙事流并使用已有每日摘要归档，不删除原始行动数据。NPC Runtime 卡新增来源角色卡绑定状态、来源版本以及 12 项实际导入字段明细，复用已有 `sourceBindings.snapshot`，未增加角色同步或版本管理系统。
- 验证：新增回归先在旧实现失败，修复后 16 个 Module4 测试全部通过；正式初始化完成资料库生成、资源同步、前端 3614 模块构建、Go/Denova 构建与启动。`verify_narraverse2.ps1` 全部通过：页面 smoke、根页、`/api/status`、Narraverse、8097 bridge、30 个脚本资源及四模块共享模型真实检查均成功。正式 executable PID 72112，地址 `http://127.0.0.1:8080/?mode=narraverse`；浏览器确认世界记录已按“轮”计数，正式页面加载 `module4-v19`。未读取完整用户存档或凭据，未修改实际世界内容；两份用户新增角色卡模板文件保持未跟踪、未改动。

## 当前状态（最近更新：2026-09-06 13:16 · Codex）
- 2026-09-06 13:16 · Codex：完成 GitHub 提交与发布收口。综合改动已形成交付提交 `1ad0ee5`，并与远端领先的 2 个文档提交正常合并后推送到 `origin/main`；源码无冲突，协作日志同时保留本地完整开发记录和远端 AI 协作原则记录。新增忽略临时构建副本 `denova-src/web/dist-verify-entry/`，未纳入构建产物、运行数据或密钥；脱敏扫描仅命中测试占位值、README 示例和模板标记。
- 提交前验证：前端全量 164 个测试文件、950 个测试通过；Go 全量普通包通过，剩余 8 个失败均为当前 Windows 未授予创建符号链接权限导致的安全边界用例，未发现真实逻辑回归。GitHub `main` 已包含本轮交付源码、测试、部署入口、文档和架构图产物。
- 2026-09-06 12:48 · Codex：完成等待反馈修复后的正式重建与验收。16 个 Module4 脚本测试、页面 smoke、根页、`/api/status`、Narraverse、8097 bridge 和 30 个脚本资源通过；正式 `denova.exe` PID 64444，8080 可访问。浏览器确认实际载入 `module4-v18`，等待状态节点存在；真实失败请求会恢复按钮并保留原输入。当前四模块共享供应商调用统一返回脱敏 HTTP 401，属于 Denova Settings 凭据状态，与本轮前端等待反馈无关。
- 2026-09-06 12:45 · Codex：**补齐 Module4 行动请求等待反馈**。提交自由行动或推荐行动后，行动区立即进入 `aria-busy` 状态，禁用输入和行动按钮以防重复提交，显示转圈与“世界正在生成下一段，请稍候”；超过 8 秒自动提示“仍在生成，请不要重复提交”，成功或失败后恢复。缓存版本升级至 `module4-v18`，确保正式页面刷新取得最新 JS/CSS。
- 回归测试先验证旧实现缺失等待区而失败，再在实现后通过；覆盖等待态显示、控件禁用、慢响应提示和结束恢复。待执行正式重建、完整 Module4 验收和浏览器真实等待态检查；未修改 API、模型配置、世界规则或存档结构，未读取或输出凭据。
- 2026-09-06 12:16 · Codex：全新浏览器页重新进入开放沙盒，确认新版“场景记录 / 世界记录 / 继续写下去”界面在桌面宽度下正常显示；世界档案按钮交互可用。全新页错误/警告日志为空；此前旧页记录的动态模块加载错误属于重建前缓存，未在全新页复现。
- 2026-09-06 12:14 · Codex：浏览器回归时发现旧图标清理脚本存在 `MutationObserver.observe` 空节点竞态，做最小 `document.body`/`MutationObserver` 守卫，避免加载时序产生控制台启动错误；未改变 Module4 或业务逻辑。待随正式入口重建后复验。
- 2026-09-06 12:13 · Codex：完成 UI D 改版后的正式重建与浏览器验收。`setup_narraverse2.ps1 -NoOpen` 成功完成资料库生成、资源同步、锁定依赖确认、前端生产构建和 Go/Denova 构建；正式 executable PID 70804，HTTP 8080 正常。`verify_narraverse2.ps1` 全部通过（Module4 测试、页面 smoke、根页/API/Narraverse/8097 bridge、30 个脚本资源、四模块共享模型网关检查）。刷新后的 Module4 已实际显示叙事卷轴界面，世界档案抽屉可打开/关闭，未触碰用户存档或模型配置。
- 2026-09-06 12:10 · Codex：**完成 Module4 UI D 视觉改版**。在不改变 API、模型网关、世界规则和存档结构的前提下，将游玩页从“世界档案面板”调整为“叙事卷轴 / 场景书页”：当前场景正文优先、地点示意图降为边缘上下文、行动记录改为带时间标记的连续记录流、自由行动区改为低干扰行动栏，世界管理改为按需打开的档案抽屉。同步更新 `DESIGN.md` 记录本轮设计方向。
- 本轮已完成 `play-view.js`、`shell.js` 语法检查和 Module4 PlayView fallback diagnostic test；未修改模型配置，未读取或输出 API Key、Authorization 或完整用户存档，未 commit/push。
- 2026-09-06 11:52 · Codex：**完成 DeepSeek 真实网关验收并修复推理模型探测缺陷**。用户提供的 DeepSeek Key 仅通过用户级设置写入服务端配置，未写入代码、日志或界面回显；直连 /models 与 /chat/completions 均成功。首次网关 401 在正式进程重载用户配置后消失；随后发现 deepseek-v4-pro 的推理模型在 max_tokens=16 时只返回 reasoning_content，原连通性探测误判为空响应，现将探测预算提高到 256。
- 重建正式 executable PID 70328 后，writing、game、narraverse、module4 四模块真实模型检查全部通过；页面 smoke、Module4 测试、HTTP 根页/API/Narraverse/8097 bridge、30 个脚本资源也全部通过。未读取或输出 API Key、Authorization 或完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 11:26 · Codex）
- 2026-09-06 11:26 · Codex：完成本轮全平台回归收尾。正式入口已重建，HTTP 根页、API、Narraverse、8097 bridge 与 30 个脚本资源通过；页面 smoke 通过；全新浏览器验收确认空白叙界仅显示“尚未开始冒险”和创建入口，不再显示 0/0 状态栏，控制台无 error/warning。
- 前端全量 164 个测试文件、950 个测试通过；Go 定向回归通过。Go 全量剩余仅为 Windows 主机未授予创建符号链接权限的安全边界测试，未发现新的普通文件、恢复或接口失败；模型真实调用未强行触发，以避免使用或暴露现有供应商凭据。未读取或输出 API Key、Authorization 或完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 11:24 · Codex）
- 2026-09-06 11:24 · Codex：**完成空状态修复后的正式重建**。叙界资源同步、前端 3614 模块构建和 Go/Denova 构建成功，正式 ` denova.exe ` PID 60908 已启动；构建仅有既有大 chunk 警告。
- 待做最终浏览器回归：无冒险状态栏隐藏、进入冒险后状态栏恢复、四模块入口和异常日志。未读取或输出 API Key、Authorization 或完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 11:22 · Codex）
- 2026-09-06 11:22 · Codex：**修复叙界无冒险时的误导性状态栏**。空状态不再展示 `HP 0/0`、`MP 0/0`、场景预设和上下文编辑入口；进入具体冒险时恢复右侧状态栏，删除当前冒险后重新隐藏；初始化和删除共用同一重置路径。页面 smoke 增加空状态右栏隐藏断言。
- 真实浏览器只读复验确认更新检查不再泄露 GitHub 403 原始 JSON；手动检查可正常返回 Release 信息。待同步资源、重建正式 executable 并完成最终回归。未读取或输出 API Key、Authorization 或完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 11:19 · Codex）
- 2026-09-06 11:19 · Codex：**将首批修复重建并接管正式 executable**。完成叙界资源同步、前端 3614 模块生产构建、Go/Denova 构建，正式进程为 ` D:\Narraverse2.0\denova-src\output\denova.exe `（PID 56340）；仅停止同路径旧进程，未触碰其它进程或用户运行数据。
- 构建仅有既有大 chunk 警告；待继续执行正式 HTTP/页面 smoke、更新错误脱敏和浏览器复验。未读取或输出 API Key、Authorization 或完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 11:16 · Codex）
- 2026-09-06 11:16 · Codex：**收敛前端全量回归的并发超时噪声**。`AutomationsView.test.tsx` 中包含跨语言切换、后台 reload、冲突归档和本地编辑恢复的竞态回归；单文件约 1 秒通过，但全套 164 个文件并发时超过默认 5 秒。仅为该用例设置 15 秒上限，未放宽全局测试超时，也未改变生产逻辑。
- 当前全量前端回归待重跑；Go 全量剩余失败均为 Windows 主机未授予创建符号链接权限的安全边界测试，普通文件/恢复/原子写入定向回归已通过。

## 当前状态（最近更新：2026-09-06 11:12 · Codex）
- 2026-09-06 11:12 · Codex：**补齐 Windows 版本恢复的目录同步抽象**。`book/versions` 恢复写入原先直接对 `os.Root` 打开的目录句柄调用 ` Sync `，Windows 会返回 Access denied；现复用独立的 ` syncDirectory ` 平台实现，非 Windows 保留目录耐久化，Windows 按系统限制跳过不支持的目录同步。文件权限位断言也不再把 Windows 的无效 POSIX mode 映射当成失败。
- 定向验证待本轮继续完成；未读取或输出 API Key、Authorization 或完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 11:09 · Codex）
- 2026-09-06 11:09 · Codex：**开始全平台回归排查并修复首批可复现问题**。正式页面 smoke 改为等待 `Module4.open` 真实就绪，消除固定 500ms 带来的首轮加载竞态；Windows 读文件测试改用 JSON 序列化绝对路径，绝对路径验证保留用户输入显示；版本恢复改用项目已有的 Windows 目录同步抽象，避免把目录句柄直接 Sync 导致恢复失败；更新检查不再把 GitHub 403/429 原始响应正文回传到界面；文件替换与冲突目录权限断言按 Windows 文件系统语义校正。
- 定向验证：页面 smoke、TypeScript、i18n、正式入口 SkipTests 验收均已通过；待完成本批改动后的 Go/前端回归及正式 executable 重建复验。未读取或输出 API Key、Authorization 或完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-06 09:17 · Codex）
- 2026-09-06 09:17 · Codex：**修复配置包全量测试的真实基线缺口**。`TestConfigTemplatePreseedsWritingSubAgentsAsEditableConfig` 原先读取被 `.gitignore` 排除且干净仓库不存在的 `denova-src/config.toml`，导致全量 `go test ./config` 无条件失败；现改用仓库内可追踪的 `config/testdata/writing-subagents.toml` fixture，保留六个写作 SubAgent 的完整断言，不改变运行时配置加载或用户配置。
- 验证：`go test ./config -count=1` 全量通过。fixture 仅用于测试，未复制到正式 output、未读取或修改用户 `.denova` 数据，未 commit/push。

## 当前状态（最近更新：2026-09-06 09:15 · Codex）
- 2026-09-06 09:15 · Codex：**把四模块真实模型检查纳入正式验收脚本**。`tools\verify_narraverse2.ps1` 现在逐一调用 writing/game/narraverse/module4 的状态与真实测试端点；本地路由不可用或返回模块错配会直接失败，供应商 401/403、404、未配置和其它上游异常则按独立状态输出，不再把“页面通过”误读成“模型调用通过”。
- Windows PowerShell 5.1 初次执行发现新加入的中文异常字符串在无 BOM 脚本中解析不稳定，已改为英文内部错误字符串并验证脚本可执行；不影响用户界面文案。最终输出：Denova/页面/资源/Module4 测试通过，四模块 `ModelGateway=provider-auth-required`，均为上游 HTTP 401。未输出密钥、Authorization 或供应商正文，未 commit/push。

## 当前状态（最近更新：2026-09-06 09:11 · Codex）
- 2026-09-06 09:11 · Codex：**完成密钥脱敏提示后的第二次正式重建与最终复验**。设置页在 API Key 输入框旁显示“已保存（不回显）”，避免用户把脱敏误认为配置丢失；不改变凭据传输和保存边界。正式初始化入口重新完成资源同步、锁定依赖确认、Vite 3614 模块构建、Go/Denova 构建并启动，当前 executable PID 45656。
- `verify_narraverse2.ps1` 再次通过：Module4 16 项、页面 smoke、根页、`/api/status`、Narraverse、8097 bridge 和 30 个脚本资源均正常。运行时 `/api/settings` HTTP 200，所有层凭据字段均为空，仅保留 2 个已配置提示；四模块统一网关状态完整，但真实供应商测试仍全部上游 HTTP 401。
- 本轮新增 UI 提示后 TypeScript 与 i18n 3506 键检查通过；未 commit/push，未输出任何密钥值或 Authorization。401 仍需用户在 Denova Settings 更换有效 Key 后复测。

## 当前状态（最近更新：2026-09-06 09:06 · Codex）
- 2026-09-06 09:06 · Codex：**完成凭据脱敏修复后的正式重建与运行复验**。再次执行 `tools\setup_narraverse2.ps1 -NoOpen`，完成资料库生成、叙界资源同步、锁定依赖确认、Vite 3614 模块构建、Go/Denova executable 构建并启动；正式 executable 为 `D:\Narraverse2.0\denova-src\output\denova.exe`，当前 PID 67880，入口仍为 `http://127.0.0.1:8080/?mode=narraverse`。
- `verify_narraverse2.ps1` 通过：Module4 16 项、页面 smoke、根页、`/api/status`、Narraverse、8097 bridge 全部正常，30 个脚本资源无失败；运行时 `/api/settings` HTTP 200，所有层的模型/图像密钥字段均无非空值，仅保留已配置提示。四模块统一模型状态均显示配置完整，但真实测试仍一致为上游 HTTP 401，未将其误报为成功。
- 本轮定向 Go、设置页 Vitest 7/7、TypeScript 检查、网关测试和设置处理器编译检查通过；配置包全量测试仍被既有 `TestConfigTemplatePreseedsWritingSubAgentsAsEditableConfig` 基线失败拦住，与本轮脱敏改动无关。未 commit/push，未输出任何密钥值或 Authorization。

## 当前状态（最近更新：2026-09-06 09:02 · Codex）
- 2026-09-06 09:02 · Codex：**修复设置接口的凭据回传与保存闭环**。`/api/settings` 及用户/工作区更新响应现在只返回 API Key 已配置布尔提示，不返回密钥正文；前端类型与默认配置投影同步支持该提示；提交脱敏设置草稿时保留既有模型/图像 Key，填写新 Key 时允许替换，删除的配置仍不会被隐式恢复。新增配置层脱敏、保留和新 Key 替换回归测试，并确保脱敏不会修改服务端内存快照。
- 定向验证：Go 配置测试通过，设置页相关 Vitest 7/7 通过，TypeScript 检查通过。审计期间曾发现一个内部诊断命令表达式错误，导致密钥正文进入工具输出；未写入项目文件、日志或 Git，日志不记录任何密钥值；建议后续更换该凭据。待正式重建后复验接口响应与运行页面；未 commit/push。

## 当前状态（最近更新：2026-09-06 08:51 · Codex）
- 2026-09-06 08:51 · Codex：**完成最终正式部署与全新浏览器回归**。初始化脚本自动停止同路径旧 executable 后，重新生成资源、同步 iframe、锁定依赖安装、Vite 3614 模块构建和 Go/Denova 构建；正式 `D:\Narraverse2.0\denova-src\output\denova.exe` 当前运行 PID 69184。
- 正式验收通过：根页面、`/api/status`、Narraverse、8097 bridge HTTP 200；30 个脚本资源无失败；Module4 16 项定向测试、页面 smoke、世界创建/进入/移动/等待/持久化回载通过；全新浏览器四模块入口均可进入且无前端崩溃，设置页可打开，Module4 方向 C 的世界档案/当前位置/叙事正文/行动区可见。四模块共享 API 测试均真实执行并一致返回脱敏 HTTP 401（只剩当前 Denova Settings 凭据需用户更新）。
- 已 fetch GitHub `origin/main`；远端最新为 `02154d39ac2098a5d7e7d30e917caae1bf2ed635`，本地基线 `85b9640decd364132ff5c20a2063842c79adefc6` 落后 2 个文档提交，远端相对本地无源码目录变化；因工作树含用户/本轮未提交改动，未 reset、clean、merge 或 push。未读取或输出 API Key、Authorization 或完整用户存档。

## 当前状态（最近更新：2026-09-06 08:48 · Codex）
- 2026-09-06 08:48 · Codex：**把旧 executable 的安全接管纳入正式初始化脚本**。`tools/setup_narraverse2.ps1` 在构建前仅按完整路径匹配并停止旧 `denova-src\output\denova.exe`，等待其退出后再清理/构建/启动；不会结束其它 Denova、Vite、Node 或用户进程，也不删除运行数据。
- 该修复针对 Windows 文件锁和“构建成功但实际仍运行旧 executable”的真实返工点；待重新执行正式初始化验证自动接管流程。未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 08:46 · Codex）
- 2026-09-06 08:46 · Codex：补齐共享模型状态回归测试断言；前端定向 Vitest **4 个文件 / 17 个测试通过**。本次测试修改仅调整测试断言，不改变运行逻辑；控制组件的 401 失败优先显示逻辑已覆盖；未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 08:45 · Codex）
- 2026-09-06 08:45 · Codex：**修正共享模型状态显示语义**。四模块顶部状态现在将“配置字段完整”与“真实 API 测试成功”分开；一旦测试得到 401/404/超时等失败，失败结果优先显示，图标不再误报绿色健康，并在宽屏状态条展示脱敏错误提示。
- 新增前端回归覆盖“配置完整但上游 401 不得显示为成功”；未修改模型路由、API Key、用户存档或模块边界；未读取或输出 API Key、Authorization 或完整上游响应；未 commit/push。

## 当前状态（最近更新：2026-09-06 08:42 · Codex）
- 2026-09-06 08:42 · Codex：**修复正式页面重建后的静态资源错配问题**。浏览器进入 Denova 设置时发现旧页面请求已被删除的 `SettingsView` chunk，静态回退错误地返回 `index.html`（HTTP 200），导致动态模块加载失败；现对缺失的 JS/CSS/字体/图片等带扩展名资源返回真实 HTTP 404，对 `/api/*` 未知路径保持 404，并让 SPA 入口 HTML 使用 `Cache-Control: no-store`，避免重建后继续缓存旧 chunk 清单。
- 新增静态回退判定单测；`go test ./internal/api -run TestShouldServeSPAFallback -count=1` 通过。尚未重新打包和重启正式 executable；未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 08:29 · Codex）
- 2026-09-06 08:32 · Codex：**完成方向 C 的正式构建与浏览器回归**。正式初始化脚本重新生成资源、同步 Module4 UI、完成 Vite 3614 模块构建和 Go/Denova 构建；脚本首次保留同路径旧 executable，已只按精确路径停止 `D:\Narraverse2.0\denova-src\output\denova.exe` 并重新复制/启动当前构建，未触碰其它进程或旧运行数据。
- `verify_narraverse2.ps1` 通过：正式 executable PID 34556，根页面、`/api/status`、Narraverse、8097 bridge 均 HTTP 200，30 个脚本资源无失败，Module4 16 项测试与页面 smoke 全部通过。浏览器新页实际看到“开放沙盒 · 世界档案”、纸质绿灰场景、当前位置地图、叙事正文与行动区；未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 08:29 · Codex）
- 2026-09-06 08:29 · Codex：**落地 Module4 UI 方向 C「世界档案」**。保留现有行动规则、世界管理、叙事时间线和共享模型网关，仅重做 Module4 的视觉层与场景信息布局：纸质绿灰画布、炭绿正文、锈红行动信号、等宽状态字段、已知地点示意图 + 当前场景正文双栏；移动端收缩为单列。补充项目级 `DESIGN.md` 的方向确认与响应式约束。
- 地图只从当前世界已有地点生成，不虚构路线或状态；新增焦点可见、轻量 hover、细线层级、滚动条和 reduced-motion 规则，未引入外部字体、依赖或新的模块边界。待重新执行正式初始化、启动并用浏览器进行视觉回归；未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 08:22 · Codex）
- 2026-09-06 08:22 · Codex：**完成统一网关的正式重建与运行验收**。正式初始化入口已重新生成资料库资源、同步 iframe 资源、安装锁定依赖、构建前端和 Go/Denova executable；发现初始化脚本保留了旧的同路径进程后，仅停止并重启了 `D:\Narraverse2.0\denova-src\output\denova.exe`，未触碰其它进程或旧运行数据。
- 真实运行检查：根页面、`/api/status`、叙界页面和 8097 bridge 均 HTTP 200；验收脚本确认正式 executable PID 68148、30 个脚本资源全部可取、Module4 16 项定向测试和页面 smoke 全部通过。工作台已逐一切换 Module1–4，四模块状态刷新均显示共享配置存在，四模块真实 API 测试均返回脱敏 HTTP 401（配置/模型路由正常，供应商鉴权仍需在 Denova Settings 修正）。
- 额外验证：统一状态接口四模块均返回配置完整；空消息 `/api/model/chat` 稳定返回 HTTP 400 `invalid_request`；`git diff --check` 除已修复的测试文件末尾空行外无问题。前端 Vitest 5 文件/22 测试、Vite 3614 模块构建、后端网关定向测试通过；构建仅有既有大 chunk 警告。未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 08:14 · Codex）
- 2026-09-06 08:14 · Codex：**统一四个模块的共享模型调用并补充可见验收入口**。新增 `/api/model/status`、`/api/model/test`、`/api/model/chat`，由 Denova 服务端按 Module1 写作、Module2 游戏、Module3 叙界、Module4 开放沙盒映射现有共享 Settings；浏览器 iframe 不再直接携带 API Key 请求供应商。Base URL 会去除重复的 `/chat/completions` 后缀，401/404/限流/超时等只返回脱敏分类消息。
- 工作台顶部新增按当前模块切换的“刷新模型状态 / 测试 API”控制；状态只显示是否配置、模型连接结果和延迟，不显示 API Key、Authorization、完整供应商正文或用户存档。新增前后端定向回归覆盖：模块映射、URL 脱敏、模拟上游成功、401 信息不泄露、前端按钮行为。
- 验证：后端网关定向 Go 测试通过；前端 Vitest **5 个文件 / 22 个测试通过**；Vite/TypeScript 生产构建成功（3614 个模块），仅有既有大 chunk 警告。完整 Go 测试仍有此前已知的 Windows 符号链接权限、临时目录访问和路径转义失败，未将其误报为本次功能通过。正式 executable 尚待本轮重新打包后验收；UI 视觉方向预览保留 A/B/C/D 四个选项，未擅自替用户定稿。未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 07:25 · Codex）
- 2026-09-06 07:25 · Codex：**将 Module4 行动反馈统一为“每一步生成一段正文”**。移动、等待、睡眠、探索、观察、训练、NPC 互动和无法被本地规则归类的非空自由输入，都会先由现有规则计算候选结果，再通过 Denova 共享模型生成通常 150–300 字的连续叙事；查看状态、世界和日志仍即时执行。自由文本不再显示“无法确定要执行什么”，电话/消息等输入也作为开放行动交给叙事层，不再被静态拒绝。
- 状态提交保持原子性：模型只能叙述规则已经算出的时间、精力、地点、判定与物品结果，不能自行改数值；模型失败时丢弃候选世界，时间、精力、地点、日志均不改变，原输入继续保留。自定义行动正文下不再显示“已记录自定义行动”机械回执，旧存档里已有的同类历史回执也在展示层隐藏。上下文继续使用脱敏边界，仅提供当前场景、在场人物、可见物件、近期行动与可见事实。
- 修改范围：`app/module4/core/action.js`、`ai/interaction.js`、`ui/play-view.js`；补充 Action/Interaction/PlayView 与正式页面 smoke 回归；缓存标记从 v16 升为 `20260906-module4-v17` 并同步 Denova iframe 测试。未新增依赖、未改变存档结构、模块边界或共享 Settings。
- 验证：Module4 **16/16**、NarraverseWorkspace/ModeRouter **13/13** 通过；正式初始化脚本完成资源同步、Vite **3612 个模块**、Go/Denova executable 重建并启动。正式验收脚本确认移动/等待/睡眠触发 3 次模拟正文并持久化，根页面、`/api/status`、Narraverse、8097 bridge 和 29 个脚本资源全部正常。浏览器确认 v17、四模块入口、Module4 世界和输入区可见，旧机械回执已隐藏。正式进程为 `D:\Narraverse2.0\denova-src\output\denova.exe`（PID 68932），地址 `http://127.0.0.1:8080/?mode=narraverse`。构建仅有既有大 chunk 警告；真实共享模型调用仍受当前 HTTP 404 配置问题影响，本轮未把模拟响应当成真实 API 通过，未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 07:20 · Codex）
- 2026-09-06 07:20 · Codex：**修复总资料库“手动添加条目后不显示”**。原流程只调用冒险资料库的 `/api/lore/items`，却没有写入当前总资料库资产的 `nested_entries`，因此添加成功后页面列表仍从旧的 Master 投影读取。现新增 `POST /api/library/assets/:id/entries`，先把人工条目持久化到当前设定书资产，再写入当前冒险；详情刷新后即可显示。
- 新增条目使用独立人工来源身份和字段版本，缺失字段直接建立；同一名称、正文、关键词的客户端重试会复用已有人工条目，不会重复生成。保留页面版本 CAS 保护，避免旧页面覆盖新内容。
- 修改范围：`denova-src/internal/book/master_human_edit.go`、对应测试；`internal/api/routes.go`、`handlers/handler_library.go`；`web/src/lib/api-client/master-library.ts`、`features/library/LibraryView.tsx` 及测试。
- 验证：Go `internal/book`、`internal/api/handlers` 通过；前端 TypeScript 通过；LibraryView **30/30** 通过；Vite **3612 个模块**构建成功；同步 `output/web` 与 `internal/webfs/dist` 后重建并重启正式 `output/denova.exe`。正式总资料库页面、设定书详情和“手动添加条目”入口可访问，HTTP 200；未执行真实新增、删除或修改用户资料操作，未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-06 06:49 · Codex）
- 2026-09-06 06:49 · Codex：**修复 Module4 更新在浏览器中不可见的缓存失效点**。源码和打包资源此前仍使用 `v=20260905-module4-v15`，导致浏览器可能继续复用旧 iframe/脚本；已将 `app/index.html` 与 Denova Narraverse iframe 版本标记统一递增为 `v=20260906-module4-v16`，并同步更新相关测试断言。
- 验证：NarraverseWorkspace/ModeRouter 定向测试 **13/13** 通过；正式初始化入口重新完成资源同步、Vite **3612 个模块**构建和 Denova executable 构建。运行中的正式页面返回 Narraverse HTTP 200，页面只含 v16、不含旧 v15，`/api/status` 返回 200；项目完整验收脚本 **16 项 Module4 测试、页面 smoke、29 个资源检查全部通过**。未改变模块边界、存档结构、模型 Settings 或 API 配置；未 commit/push。
- 当前正式进程为 `D:\Narraverse2.0\denova-src\output\denova.exe`（PID 34564），入口 `http://127.0.0.1:8080/?mode=narraverse`。本轮仅修改缓存版本标记及对应测试、日志；未读取或输出 API Key、Authorization 或完整用户存档。
- 2026-09-05 23:41 · Codex：**修复 Module4 对话失败后文本框不更新/输入丢失**。自由输入提交时显示“处理中…”，成功行动才清空输入；模型请求失败或普通行动失败时保留原文并支持直接重试。公共模型错误改为脱敏分类提示（未配置、HTTP 失败、超时、未知失败），不回显 API Key、Authorization 或供应商原始响应。
- 修改范围：`app/module4/ai/interaction.js`、`app/module4/ui/play-view.js`、`app/module4/ui/shell.js`；`tools/test_module4_action.js`、`tools/test_module4_interaction.js`、`tools/test_module4_play_view.js` 增加失败保留、错误脱敏和恢复输入断言。未引入新依赖，未改变存档结构或模型 Settings。
- 验证：三项定向 Node 测试、三份模块语法检查和 `git diff --check` 通过；正式初始化入口重新完成前端 3612 模块构建、Denova executable 重建。完整验收脚本 16 项 Module4 测试、页面 smoke、根页面、`/api/status`、Narraverse、8097 bridge 和 29 个资源检查全部通过。浏览器端实测文本框可更新，成功查看状态后清空，失败行动后原文保留。
- 踩坑：首次重启后 smoke 访问到 `denova-src\\web` 源码页而非 `output\\web` 打包页，已停止精确 executable 进程并显式使用 `output\\web` 重启；当前正式进程 PID 58072，地址 `http://127.0.0.1:8080/?mode=narraverse`。未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-05 23:29 · Codex）
- 2026-09-05 23:29 · Codex：**完成翻译人工编辑放宽与正式运行验收**。总资料库条目/角色卡编辑改走独立人工直写接口，不再经过模型候选、变量保护或严格翻译校验；仅保留防止旧页面覆盖新版本的 CAS 版本保护。缺失的常用字段可直接创建，角色卡开场白统一使用 `character.openings[n]` 路径；条目保存采用一次性字段更新，避免保存后待处理项重复生成。
- 修改范围：新增 `denova-src/internal/book/master_human_edit.go`、`master_human_edit_test.go`；更新 Library API 路由/处理器、Master Library API client、`LibraryView.tsx` 及其测试、中英文资料库文案；更新 `tools/start_narraverse.ps1`，正式 executable 从 `denova-src` 使用真实运行数据，并明确使用 `output/web` 打包前端，避免生成第二份空运行目录或误载入源码开发页。
- 验证：Go `internal/book`、`internal/api/handlers` 测试通过；`go build ./...` 通过；TypeScript 检查通过；LibraryView **29/29** 通过；正式根页面 HTTP 200、`/api/status` 返回 `has_state=true`、生产页使用打包资源且不引用 `/src/main.tsx`；人工编辑页面已在浏览器只读确认，显示“直接保存、不经过模型审核、缺失字段自动创建”。PATCH 人工编辑路由已注册（不存在资产的安全探测返回结构化 HTTP 400）。
- 本轮未执行保存、删除、加入冒险或修改用户资料；未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-05 18:52 · Codex）
- 2026-09-05 18:52 · Codex：**完成 Module4 体验改造 1、2、3 项**。进入/移动后自动生成当前场景叙述；主游玩区改为“现场叙述 → 行动时间线 → 下一步输入”的连续流程；地点选择、人物状态、每日预览、世界历史和调试入口收纳到“世界管理”，关闭管理区时不再把次要表单放进主游玩区。未改变模块边界、存档结构、模型 Settings 或 API 配置。
- 修改范围：`app/module4/ui/play-view.js`、`app/module4/module4.css`、`tools/test_module4_play_view.js`。为场景叙述、行动/回应时间线和管理区补充定向回归断言；本轮未引入新依赖、未修改旧用户运行数据。
- 验证：Module4 PlayView/Action 回归、Node 语法检查、定向结构断言通过；正式初始化入口第二次运行成功，Vite **3612 个模块**构建成功，Denova executable 已重建并启动。正式页面、`/api/status`、Narraverse、8097 bridge 均正常，Module4 **16 项定向测试**与页面 smoke 全部通过，29 个脚本资源无加载失败。
- 踩坑：首次正式构建因当前项目旧 `denova.exe` 占用 `output/log/2026-09-05.log` 失败；已按精确 executable 路径停止该项目进程后重试成功。当前正式进程为 `D:\Narraverse2.0\denova-src\output\denova.exe`，运行地址 `http://127.0.0.1:8080/?mode=narraverse`。未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。

## 当前状态（最近更新：2026-09-05 18:20 · Codex）
- 2026-09-05 18:20 · Codex：**修复“加入当前冒险 HTTP 500”不可定位，并增加设定书介绍的独立人工编辑**。总库资产加入流程现在按准备事务、读取原件/条目、解析、读取冒险资料、生成操作、写入、登记实例等阶段包装错误，并在 API 日志中只记录资产 ID、阶段和错误摘要；异常不再只显示泛化的 HTTP 500。设定书解析会保留顶层 `description`，总库内容页新增“设定书介绍”编辑器，使用独立 PATCH 元数据接口，可修改、清空介绍，不改写归档原文件，也不把它误当成条目编辑。
- 修改范围：`denova-src/internal/book/material_import.go`、`character_card.go`、`master_library.go`、`material_import_test.go`；`internal/api/routes.go`、`handlers/handler_library.go`；`web/src/features/library/LibraryView.tsx`、`LibraryView.test.tsx`、Master Library API client、中英文资料库文案。
- 验证：Go `internal/book`、`internal/api/handlers` 通过；新增设定书介绍保存/清空与加入失败阶段化错误回归通过；LibraryView **27/27** 通过；TypeScript、i18n 3504 键对齐、Vite 3612 模块生产构建通过。安全同步 `output/web` 与 `internal/webfs/dist` 后编译 embedweb executable；旧文件保留为 `output/denova-pre-fix-20260905-1816.exe`，正式 `output/denova.exe` 已重启。正式页面 HTTP 200，`/api/status` 正常；缺失资产诊断的加入接口返回 HTTP 400 且显示“准备实例事务”阶段，设定书介绍接口路由已注册。
- `internal/api` 仍有 3 个既有 Windows 符号链接/临时目录权限测试失败（privilege/access denied），失败测试与本轮功能无关；未读取或输出 API Key、Authorization 或完整用户存档，未触碰 `.denova` 用户数据、既有临时文件，未 commit/push。

## 当前状态（最近更新：2026-09-05 17:45 · Codex）
- 2026-09-05 17:45 · Codex：**修复翻译候选保护标记拦截、人工保存覆盖和手动新增表单误预填**。严格路径现在会把缺失/多出的变量、链接、数字标记按类别和数量返回，不回显原文、链接或用户内容；自动模型候选仍必须完整保留保护标记。总资料库条目编辑、Denova 处理结果人工修改、单条/批量“批准并完成”均接入显式人工覆盖路径，并保留 CAS 版本冲突保护；覆盖决定写入 Proposal 审计字段。手动添加条目改为空白新建表单，不再把当前条目内容当作待修改内容。
- 修改范围：`denova-src/internal/book/master_agent.go`、`master_agent_test.go`；`internal/api/handlers/handler_library.go`；`web/src/features/library/LibraryView.tsx`、`LibraryView.test.tsx`；Master Proposal API client 与中英文资料库文案。未改变模块边界、模型 Settings 或用户数据结构。
- 验证：Go `internal/book`、`internal/api/handlers` 通过；新增保护标记详细错误与人工覆盖回归通过；TypeScript 通过；前端全量 Vitest **162 个文件 / 942 个测试通过**；Vite 生产构建 3612 模块通过，仅保留既有大 chunk 警告。同步 `output/web` 与 `internal/webfs/dist` 后重建 embedweb `D:\Narraverse2.0\denova-src\output\denova.exe`，备份为 `denova.exe.pre-marker-fix-20260905.bak`；正式进程由启动脚本运行，`http://127.0.0.1:8080`、`/api/status`、`/narraverse/index.html?embedded=denova` 均返回 200。浏览器只读确认手动新增表单为空白；未点击真实应用、批准、删除、重翻或新增操作。
- 未读取/输出 API Key、Authorization 或完整用户存档；`.denova`、既有 `output/` 临时文件未触碰；未 commit/push。

## 当前状态（最近更新：2026-09-05 16:55 · Codex）
- 2026-09-05 16:55 · Codex：**修复总资料库条目“冲突但不说明原因”并增加手动添加兜底**。前端不再吞掉 `instantiateMasterAsset` 的后端错误，加入当前冒险失败时直接显示名称重复、ID 重复、版本冲突等真实原因；设定书嵌套条目旁新增可编辑的“手动添加条目”，预填标题、正文和关键词，复用现有 `/api/lore/items` 创建接口，重复名称仍显示后端具体原因。补齐原始 `key` 字段到手动添加关键词的兼容读取，并修复正文标签显示内部 i18n key 的问题。直接素材导入新增结构化冲突详情（原目标、冲突副本、原因、建议），在导入结果中展示，未输出条目正文。
- 验证：仓库自带 Go 工具完成 `gofmt`、`go test ./internal/book`、`go test ./internal/api/handlers`、`go build ./...`；前端 TypeScript、i18n 3497 键对齐、LibraryView 26/26、TranslationQueueControl 9/9、全量 Vitest 942/942、Vite 3612 模块构建均通过。正式 embedweb executable 已重建，`output/web` 与 `internal/webfs/dist` 同步，旧 executable 保留为 `denova.exe.pre-conflict-details-20260905.bak` 与 `denova.exe.pre-translation-label-20260905.bak`；正式进程 PID 33676 运行于 `127.0.0.1:8080`，`/api/status` 返回 200。初次全量测试的默认 5 秒 Automations 并发用例超时，单独提高该用例超时及全量使用 15 秒后均通过，属于测试时序限制而非本轮改动失败；Vite 仅保留既有大 chunk 警告。
- 本轮只读打开并核对目标设定书，确认“手动添加条目”按钮和可编辑表单、正文标签均已显示；未提交手动添加、加入当前冒险、应用、删除或重翻操作，未读取或输出 API Key、Authorization、完整用户存档，未触碰 `.denova` 用户数据；未 commit/push。

## 当前状态（最近更新：2026-09-05 15:55 · Codex）
- 2026-09-05 15:55 · Codex：**补齐翻译冲突的人工“批准并完成”闭环**。Denova 处理结果中的冲突 Proposal 现在只有在用户明确点击“批准并完成”时才允许强制重绑定到当前 Master 字段版本，然后写入当前候选/人工修改、激活正式译文、推进导入事务并结束可用翻译流程；后端仍校验非空及变量/链接/数字标记，普通后台路径不绕过 CAS。单条和批量入口均支持，高风险仍需二次确认。原有 Proposal/队列删除保持软删除：候选进入 `rejected`、队列任务进入 `deleted`，不物理删除原始资料、正式译文或用户存档。
- 验证：Go `internal/book` 新增单条/批量 conflict force 回归并通过，`go test ./internal/book -count=1` 与 `go test ./internal/api/handlers -count=1` 通过；前端 TypeScript 通过，LibraryView + TranslationQueueControl **33/33** 通过；Vite 生产构建 3612 模块成功；新 embedweb executable 已构建并替换正式 `denova.exe`，备份为 `denova.exe.pre-force-approval-20260905.bak`。正式进程运行于 `127.0.0.1:8080`，当前工作区保持注册的 `output\\.denova\\projects\\总`；根页面、`/api/status`、总资料库、Narraverse、Module4 均返回 200，内嵌前端资源含新的 force/批准文案。
- 本轮未点击真实应用、批准或删除按钮，未重译旧译文，未读取或输出 API Key、Authorization 或完整用户存档；未 commit/push。构建警告仅为既有大 chunk 提示。

## 当前状态（最近更新：2026-09-05 13:14 · Codex）
- 2026-09-05 13:14 · Codex：**核对 GitHub 最新 main 并同步文档基线**。GitHub 连接器确认远端 `main` 为 `02154d39ac2098a5d7e7d30e917caae1bf2ed635`，其父提交为本地 `85b9640decd364132ff5c20a2063842c79adefc6`；两提交之间只有 `README.md`、`docs/交付/AI协作决策原则.md`、交付清单、交接指南和本协作日志的文档变化，没有源码、测试或构建文件变化。已将远端新增的 AI 协作原则、渐进读取条款和交接入口同步到本地，未执行 reset/clean/覆盖 dirty 工作树。普通 `git fetch origin main` 及 Schannel 重试均因当前网络 TLS 握手失败，故本地 Git ref 仍实际为 `85b9640`，不伪造为 `02154d3`；正式 executable 的行为源码与最新 main 的代码部分一致，最终报告同时列出两者。

## 当前状态（最近更新：2026-09-05 13:06 · Codex）
- 2026-09-05 13:06 · Codex：**完成上一轮真实验收发现的两个缺口及部署缓存修复，并完成最终启动验收**。新建 V1.5 世界现在会在保存回调后自动准备首日 Preview；Store 解析失败时保留原始主存档并建立本机恢复副本，禁止恢复期间覆盖写入，并在 Module4 显示恢复提示与“尝试恢复保留副本”入口。为避免第一次部署的浏览器缓存继续加载旧脚本，Denova iframe、Module4 JavaScript 和样式统一加入 `20260905-module4-v15` 缓存版本标记。
- 使用正式入口 `powershell -ExecutionPolicy Bypass -File .\tools\setup_narraverse2.ps1 -NoOpen` 重新生成资料库、同步资源、确认 pnpm 依赖、构建 3612 个前端模块、编译 Go/Denova executable 并启动。最终 executable 为 `D:\Narraverse2.0\denova-src\output\denova.exe`，PID 5224；正式根 URL 为 `http://127.0.0.1:8080/?mode=narraverse`。全量验证入口通过：16 个 Module4 Node 测试、正式页面 smoke（含新建世界首日 Preview、行动、跨日、刷新持久化）、Denova/Narraverse 相关 Vitest 13/13、根页面/API/Narraverse/8097 bridge 均 200、29 个脚本资源 HTTP 错误 0。
- 通过 OpenCLI 1.8.6 连接现有 Edge 扩展（v1.0.24）完成最终真实浏览器验收：四个顶层入口逐一可切换；`20260905-module4-v15` iframe 正常加载；合成世界可创建、从列表重新进入、首日 Preview 自动生成；自由输入移动、等待、睡眠至次日并恢复精力；管理视图关闭后内部面板不进入普通游玩 DOM；页面可滚动；Ctrl+R 后世界、时间、地点和精力保持。验收合成世界保留，未读取或输出完整用户存档、API Key、Authorization 或模型响应，未调用模型。
- 本轮仅修改实现所需源码、缓存版本、测试、方案和日志；未安装 Playwright 或其它新增依赖，OpenCLI 原有升级提示未执行；未 commit/push。真实共享模型链路仍未配置/未验收，因此不把 fallback 当作真实模型通过；使用时仍通过 Module4 现有共享 Settings 配置模型即可。

## 当前状态（最近更新：2026-09-05 12:32 · Codex）
- 2026-09-05 12:32 · Codex：按用户建议复用 OpenCLI 1.8.6；doctor 初始提示扩展未连接，重启其 daemon 后现有 Browser Bridge 1.0.24 成功连接，无新增安装。通过 OpenCLI 在真实 Edge 打开正式 `http://127.0.0.1:8080/?mode=narraverse`，逐一点击写作模式、游戏模式、叙界、开放沙盒；核对顶层选中状态与 Module4 iframe 实际显示。
- 使用新建合成世界“OpenCLI 验收世界 20260905”完成表单创建、列表重新进入、移动到图书馆（精力 95）、等待到晚上、睡眠到第 2 天早晨（精力 100）；Ctrl+R 整页刷新后重新进入 Module4，时间和地点保持。展开管理内容后验证 iframe BODY 实际可滚动（scrollHeight 2089、clientHeight 663、scrollTop 100）。验收结束收起管理并回到顶部；合成验收世界保留。未读取完整用户存档、模型设置或密钥，未调用模型。
- 正式 `/api/status` 返回 200 且为 JSON 对象；浏览器 Performance 中 33 个同源资源 HTTP 错误 0、未知响应状态 0。此前真实浏览器验收工具阻塞已解除，但不据此宣称 V1.5 全方案完成：新建世界首次移动仍显示“今天的 NPC 日程还没有生成”；源码显示自动准备只挂在 Module4.open。另本轮只读审查确认 Store.load 解析失败会退为空状态、下一次保存没有损坏数据保护；这些实现缺口须继续修复，历史“只剩浏览器验收”结论不再适用。本轮仅更新验收记录和方案状态，未修改业务源码或重建，未 commit/push。

## 当前状态（最近更新：2026-09-05 12:26 · Codex）
- 2026-09-05 12:26 · Codex：补充页面 smoke test 的世界列表回归：创建合成世界后从列表重新打开，再关闭管理视图，随后执行移动、等待、睡眠并从存储重新加载；验收脚本完整运行成功，结果仍为 Module4 16/16、页面路径通过、资源 29/29。

## 当前状态（最近更新：2026-09-05 12:25 · Codex）
- 2026-09-05 12:25 · Codex：**新增可重复执行的正式验收入口**。新增 `tools\verify_narraverse2.ps1`，只读检查项目 executable 是否按精确路径运行、Denova 根页面、`/api/status`、Narraverse 页面、8097 bridge 和全部脚本资源，并调用 Module4 定向测试及真实页面脚本 smoke test；新增 `denova-src\web\scripts\test-narraverse-page-smoke.mjs` 使用项目已有 jsdom，不引入新依赖、不读取真实存档。
- 首次运行发现 PowerShell 正则字符串和哈希表内联 `if` 写法不兼容 Windows PowerShell 解析器，已改为双引号正则、独立测试状态变量和 ASCII 错误消息。完整命令 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\verify_narraverse2.ps1` 已成功：16 个 Module4 测试通过、页面 smoke 通过、根页面／API／Narraverse／bridge 全部 200、29 个脚本资源 0 失败。
- 验收保持脱敏：仅使用新建 jsdom origin 和合成世界；未读取、输出或写入 API Key、Authorization、真实用户存档，未安装 Playwright 或其它工具，未 commit/push。

## 当前状态（最近更新：2026-09-05 12:02 · Codex）
- 2026-09-05 12:02 · Codex：**完成正式页面运行级 smoke test，并修正游玩区残留的预演提示文字**。页面级检查使用项目现有 `denova-src/web` 中已锁定的 `jsdom`，加载正式 executable 提供的真实 `/narraverse/index.html?embedded=denova`，执行 Module4 打开、合成世界创建、关闭管理区后的 DOM 隔离、自由输入移动、等待、睡眠和 `Store.load()` 持久化恢复；结果全部通过，页面脚本无 `jsdomError`。唯一 console 错误是 jsdom 环境没有 IndexedDB 时既有的 localStorage 回退提示，不代表正式浏览器故障。
- 将游玩区末尾“从生成今日预演开始”的旧提示改为“新的一天会自动准备新的安排”，避免与 S5 自动准备行为矛盾；改动后 Module4 16 个定向测试、语法检查和正式前端／Go／Denova 重建均通过。当前 executable PID 12412，根页面、`/api/status`、Narraverse 页面、8097 bridge 和 29 个脚本资源均返回 200。
- 该 smoke test 是真实页面脚本的 JSDOM 运行验证，不等同于真实 Edge 鼠标点击；Computer Use 仍无法安全确认 Windows 当前 URL，Module1–4 的人工点击路径仍待工具恢复。未安装新工具、未读取／输出 API Key 或用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-05 11:56 · Codex）
- 2026-09-05 11:56 · Codex：**补齐游玩区与管理／调试视图的 DOM 边界，并完成改动后的正式重建**。`PlayView.render` 现在只有在用户显式打开“世界管理”时才生成 NPC 日程、今日预演、世界轨迹和试玩诊断；管理关闭时这些内部数据不进入普通游玩区 DOM，切换管理状态会重新渲染。同步更新脱敏 UI 回归测试。
- 踩坑与处理：首次重建因仍在运行的本项目 `D:\Narraverse2.0\denova-src\output\denova.exe` 锁定 `output\log` 而失败；按 executable 精确路径确认后只停止该进程，重新运行正式初始化脚本成功。未结束来源不明进程，未删除旧用户数据。
- 验证：Module4 16 个 Node 定向测试与脚本语法检查通过；Denova Workbench／Narraverse 相关 4 个 Vitest 文件共 21/21 通过；正式前端 3612 模块构建、Go/Denova 构建成功并启动新 executable。正式页面、`/api/status`、`/narraverse/index.html?embedded=denova`、8097 bridge 均返回 200；页面引用的 29 个脚本资源全部返回 200。构建保留既存的大 chunk 警告，无失败。
- 浏览器 Computer Use 仍因无法安全确认 Windows 当前 URL 自动停止，故点击级 Module1–4 与世界创建／进入／刷新验收仍未宣称通过；未安装 Playwright 或其它新工具，未读取／输出 API Key、Authorization 或完整用户存档，未 commit/push。

## 当前状态（最近更新：2026-09-05 11:41 · Codex）
- 2026-09-05 11:41 · Codex：**按已确认的 Module4 V1.5 可执行方案完成 S0–S7 的最小闭环实现**。保留现有 Module4 核心结构，新增规则版本/时间 tick/存档 revision、自由文本行动解析、等待与睡眠、场景物件与受限交互、玩家/NPC/预演上下文边界、单 NPC 对话、候选事件与有限跨天推进、轻量日预演，以及以游玩为主的界面收纳。新增或修改范围集中在 `app/module4/`，并补充 6 个脱敏 Node 定向测试 fixture；更新 `代码指南.md` 记录 V1.5 边界。
- 解决的问题：V1.5 同地移动不再扣费耗时；等待/睡眠统一推进并在次日重置 tick；旧版世界继续保留兼容行为；受限事实不会进入玩家日结；模型交互失败不消耗行动、不写入日志；预演地点非法时回退到稳定本地数据；候选事件只有满足触发条件后才进入事实，且有活动数、转移次数和应用去重上限。未读取或输出 API Key、Authorization、完整用户存档或真实用户存储。
- 验证：`app/module4/**/*.js` Node 语法检查通过；16 个 Module4 Node 定向测试全部通过（含 7 天无模型连续性 fixture、上下文隔离、交互失败无成本、预演回退、事件去重）；正式 `tools/setup_narraverse2.ps1 -NoOpen` 完成资料库生成、资源同步、pnpm 依赖确认、Vite 构建、Go/Denova 构建和启动。正式 `D:\Narraverse2.0\denova-src\output\denova.exe` 正在运行；根页面、`/api/status`、`/narraverse/` 及 Module4 静态资源均返回 200。构建仅有既存的大 chunk 警告，无失败。
- 浏览器级点击验收未完成：复用现有 Edge 时，Computer Use 因无法在 Windows 上足够确定当前浏览器 URL 而按安全策略停止，因此没有把 Module1–4 点击进入、世界创建/进入和刷新等 UI 路径虚报为已通过。未安装 Playwright、未安装其它新工具、未 commit/push；现有翻译及其它用户改动保持不变。

## 当前状态（最近更新：2026-09-05 07:02 · Codex）
- 2026-09-05 07:02 · Codex：**完成 Module4 V1.5 改造思路与需求总概的源码对照评审**。按用户补充将引用对话“读取项目模块”的最新回复视为需求总概，结合 `D:\Narraverse2.0\Narraverse2.0_模块四现有版本改造总结.md`、当前 `app/module4/`、代码指南和历史设计边界，新增 `D:\Narraverse2.0\docs\module4\2026-09-05-Module4-V1.5-可执行改造方案.md`。建议保留规则内核，按 S0–S7 补齐场景界面、行动与时间、存档提交、认知过滤、真实对话、轻量预演和跨天事件；列出具体文件、依赖、调用预算、旧世界升级副本策略及 18 项验收场景。现有 Encounter 与 Judgment 已在 Action 内执行，`ai/interaction.js` 尚为占位，未将后者误记为现成功能。
- 验证：现有 Module4 Node 定向测试 10/10 通过；Node VM 合成探针确认“我去酒吧找林雨／等待／睡眠”尚未形成完整行为语义、同地移动仍扣费耗时，以及当前日结会直接将当天 events 转 Facts；因此方案明确新事件候选须确认触发后再入事实。未读取真实 localStorage、API Key 或用户存档，未调用真实模型、安装工具、重建或替换 executable；只新增方案并更新本日志，未修改业务源码、用户提供的原总结、原有暂存状态或翻译工作成果，未 commit/push。

## 当前状态（最近更新：2026-09-05 00:13 · Codex）
- 2026-09-05 00:13 · Codex：**完成翻译功能的后续质量与人工收口优化，明确只作用于新任务和用户主动重试**。本轮未重译、迁移、清理或覆盖既有译文、缓存、任务、总资料库内容和用户存档。Python 翻译器将选择器与 Go Master 字段判定统一为“过滤 URL/模板/HTML/代码/免翻术语/短缩写，但保留 Harbor、dock、Age 等真实短英文标签”；质量契约升级为 cache 12 / prompt 9 / validator 9 / contract 2，增加中文缺失、短原文回显、英文残留、占位符数量和 Markdown 结构检查，失败或待审结果最多自动纠错重试一次，坏缓存不复用且不把丢失占位符追加到译文末尾。长文本按段落/列表组分块并带上下文，避免一次请求过大导致无效译文。
- Denova 处理结果工作台改为默认收起原文/候选，保留展开查看；已有可用 Proposal 可直接“应用”，人工改动才走“保存并应用”；刷新后移除已应用/已删除行，零数量批量按钮不显示，保留逐项/批量软删除。后端对同字段同候选保存幂等去重，应用一个候选后将同源其它候选软拒绝并保留审计，避免条目重复出现。
- 验证：Python 翻译质量测试 19/19；Go 1.26.5 `go test ./internal/book -count=1` 通过；TypeScript 检查通过；LibraryView 与 TranslationQueueControl 测试 31/31；Vite 生产构建 3612 模块通过；embedweb executable 构建成功。正式 `D:\Narraverse2.0\denova-src\output\denova.exe` 已启动于 `http://127.0.0.1:8080`，显式恢复到既有 `output\.denova\projects\narraverse-master-library`；`/api/status`、根页面、总资料库、Narraverse、Module4 端点均返回 200。内置浏览器只读确认总资料库可见目标资产“系统、状态窗口、状态、统计、信息、分析、菜单”（13 条目）；未点击应用、删除、重译。
- 仍未做真实模型批量调用，这是有意保留的范围边界；新任务若模型仍返回异常，会进入 Denova 集中人工处理，而不是自动写入。未执行 commit/push；原有 29 项精确暂存和 D 组排除状态保留。正式旧 executable 备份为 `denova.exe.pre-translation-20260905.bak`，`output/` 中用户此前选择保留的临时文件未触碰。

## 当前状态（最近更新：2026-09-04 22:50 · Codex）
- 2026-09-04 22:44 · Codex：**复核当前 Denova 处理结果并补回候选删除入口**。只读检查用户当前打开的设定书“系统、状态窗口、状态、统计、信息、分析、菜单”：共 40 个字段、13 条 Proposal，其中 10 条已应用、2 条同字段的 `proposed` 候选（属于互斥备选）、1 条因 `input_revision` 过期而处于 `conflict`；未发现空译文或原文回显。确认旧候选的 CAS 基线不能自动放宽，否则可能覆盖新译文；冲突项应通过重新翻译或人工编辑后重新生成当前 Proposal。新增 Denova 处理结果单条“删除候选”和批量“删除所选”，后端采用 `rejected` 软删除保留审计记录，运行队列候选沿用既有软删除接口；原文、已应用译文和用户存档不受影响。删除功能尚未在正式页面点击，仅以测试和只读页面验收。
- 验证：前端 Vite 生产构建 3612 个模块、TypeScript 检查、`LibraryView.test.tsx` 22/22、Go `internal/book` 与 `internal/api/handlers` 测试通过；正式 `denova.exe` 已重新构建并启动，`/api/status` 返回 200。只读页面确认“删除候选”和“删除所选（0）”可见、布局未遮挡；临时构建目录已移出仓库，未暂存 D 组或构建产物，未执行真实删除/应用。

## 当前状态（最近更新：2026-09-04 22:15 · WorkBuddy）
- 2026-09-04 22:15 · WorkBuddy：**应要求回退「Denova 处理结果删除按钮」功能（服务 + 代码均回退到 21:25 之前版本）**。用户决定不要该功能，保持改动前状态；今日其余翻译修复（批量/单条应用互斥、批量确认、proposed 纳入候选、队列索引恢复等）全部保留。
- 代码回退（外科手术式，仅移除删除按钮相关改动）：`internal/book/master_agent.go` 删除 `RejectMasterProposal`；`master_agent_test.go` 删除 2 条 reject 用例；`internal/api/routes.go` 删除 `/library/proposals/:id/reject` 路由；`internal/api/handlers/handler_library.go` 删除 `HandleLibraryProposalReject`；`web/src/lib/api-client/master-library.ts` 删除 `rejectMasterProposal`；`LibraryView.tsx` 删除每行「删除」按钮、`removeResidual` 及相关 import；两 locale 删除 `library.review.delete/deleteFailed/deleted` 3 key；`LibraryView.test.tsx` 删除 2 条删除回归与相关 mock。
- 验证：gofmt 干净、`go test ./internal/book -count=1`、`go test ./internal/api/handlers -count=1`、`go build ./internal/...` 全部通过；前端 `tsc --noEmit` 通过、`LibraryView.test.tsx` 19/19（回到删除按钮前基线）、i18n 3473 键对齐；全库 grep 无 `RejectMasterProposal/rejectMasterProposal/removeResidual` 残留。
- 运行服务：已停止新版（含删除按钮），换回 20:16 旧版 `denova.exe`（`denova.exe~`，88.5MB，md5 c5cf4d19…），并将旧 exe 内嵌前端解出镜像到 `output/web` 与 `internal/webfs/dist`（恢复无删除按钮界面）；旧版 exe 已另存备份。仍未 commit/push，暂存待用户指示。

## 当前状态（最近更新：2026-09-04 20:18 · Codex）
- 2026-09-04 20:18 · Codex：**补充单条与批量应用的跨操作互斥**。除同一字段的同步 Ref 锁外，批量应用进行时禁止单条应用，单条应用进行时禁止批量应用，避免两个入口同时为同一运行候选创建 recovery Proposal。新增快速双击回归断言，确认只创建一次 Proposal；19/19、类型检查和最新正式构建复验通过。此次仍未点击真实“保存并应用”，不改写用户翻译数据。

## 当前状态（最近更新：2026-09-04 20:11 · Codex）
- 2026-09-04 20:11 · Codex：**修复“保存并应用后待处理条目变成两个”**。确认问题来自同一字段可能同时存在旧的 active Proposal/运行候选，以及快速重复点击可能重复提交；未删除历史 Proposal 或用户数据。总库处理结果现在按字段屏蔽已应用时间点之前的旧候选，并在界面内折叠完全相同的候选；单条和批量应用增加同步进行中的 Ref 锁，避免同一字段/批次重复创建或应用。应用成功后继续解决关联队列任务。新增回归场景覆盖“应用后接口同时返回 applied 与旧 proposed 候选”并通过；未点击正式页面应用按钮。
- 验证：`LibraryView.test.tsx` 19/19、`tsc --noEmit`、Vite production build（3612 modules）、`go test ./internal/book -count=1`、embedweb Go 构建均通过；正式 `denova.exe` 已重新嵌入最新前端并运行于 `127.0.0.1:8080`，`/api/status` 返回 200。只读页面检查确认“Denova 处理结果”正常加载，单条“保存并应用”按钮可见且未禁用；未改写现有翻译内容。临时构建目录已移出工作树，`output/` 中用户选择保留的 4 个临时文件未触碰。

## 当前状态（最近更新：2026-09-04 19:46 · Codex）
- 2026-09-04 19:46 · Codex：**修正无 Proposal 候选的批量应用遗漏**。前一版已支持单条运行候选直接保存并应用，但批量准备阶段仍只为 dirty 行创建 Proposal；现统一改为“有候选译文且没有 Proposal 时也先创建/验证”，并在 dirty 单条、批量成功后都尝试解决关联队列任务。回归测试增至 `LibraryView.test.tsx` 19/19；正式前端和 embedweb executable 已重新构建、启动并完成 `/api/status` 200 验证。未点击正式页面的应用按钮，未改写用户翻译数据。

## 当前状态（最近更新：2026-09-04 19:42 · Codex）
- 2026-09-04 19:42 · Codex：**补齐单条候选直接应用与应用后收口**。总库“Denova 处理结果”现在允许带有运行队列候选译文但尚未手工修改的行直接执行“保存并应用”，内部自动创建并验证 recovery Proposal 后再应用；应用成功后解决关联队列任务，并将已应用 Proposal 对应字段从旧的运行队列待处理投影中排除，避免条目继续显示。新增前端回归用例覆盖“未编辑直接应用”和“应用后消失”。未在正式页面点击应用，用户翻译数据未改写。
- 验证：`LibraryView.test.tsx` 18/18、TypeScript、`go test ./internal/book -count=1`、Vite 生产构建 3612 modules、embedweb Go 构建均通过；正式 `denova.exe` 已更新并运行于 `127.0.0.1:8080`。页面只读复核仍能看到现有待确认候选的单条按钮，未执行任何应用操作。

## 当前状态（最近更新：2026-09-04 19:24 · Codex）
- 2026-09-04 19:24 · Codex：**修复总资料库单条候选“保存并应用”按钮被视觉挤压/遮挡的问题**。在不改变翻译数据和应用逻辑的前提下，为处理结果行增加响应式换行与长路径截断，状态/风险/操作按钮保持不可收缩并置于操作层；正式前端重新同步到 `output/web`，重新构建并启动 `denova.exe`。页面只读验收确认首条“保存并应用”按钮完整可见、未禁用、命中元素为按钮本身；未点击任何单条或批量应用按钮，未改写候选、翻译版本或用户数据。Vite 3612 modules、TypeScript、`go test ./internal/book -count=1` 和 embedweb Go 构建通过；`/api/status` 返回 200。临时验证目录已移出工作树，其他功能按本轮范围未检查。

## 当前状态（最近更新：2026-09-04 19:00 · Codex）
- 2026-09-04 19:00 · Codex：**收口翻译人工审核与批量确认链路**。复核当前总资料库后确认 2,326 条翻译版本仍在磁盘、桥接队列 `order=[]` 且接口为 0 条任务；`天际城` 资产的 Denova 处理结果仍可加载 5,617 条展示项，其中 508 个候选处于 `proposed`。原实现把 `proposed` 排除在批量确认之外，造成选择后按钮仍为 0；后端批量接口也会直接拒绝该状态。最小修复为：总库工作台把 `proposed` 纳入可确认候选；前端按 100 条接口上限自动拆批；后端批量确认逐条先做 CAS/候选校验再应用；单条候选和人工修改的“保存并应用”先验证再应用，避免只保存不应用或状态不一致。未点击“应用”，没有改写现有候选、翻译版本或用户存档。
- 验证：`LibraryView.test.tsx` 17/17；`go test ./internal/book -count=1` 通过；`tsc --noEmit`、Vite 生产构建（3612 modules）和 `go build -tags embedweb` 通过。正式 `denova.exe` 已更新并运行于 `127.0.0.1:8080`，`/api/status` 返回 200；翻译结果页面实测选择全部后显示并启用“批量确认应用（508）”。前端临时构建目录已移出工作树到用户临时目录；`output/` 下用户选择保留的旧临时文本未触碰。其他功能按本轮范围未检查。

## 当前状态（最近更新：2026-09-04 17:03 · Codex）
- 2026-09-04 17:03 · Codex：**恢复翻译队列索引**。发现页面显示 0 条并非总资料库或任务文件被删除：Master manifest 仍有 1,417 条翻译记录，8097 任务文件仍有 2,447 个；真正异常是 `denova-src/.denova/narraverse-translation-jobs/queue.json` 的 `order` 被写成空数组，导致接口和页面不可见。先保留原 `queue.json` 备份，再从现有任务文件按创建时间重建非终态索引，排除已 `applied`/`deleted` 任务；恢复期间仅停止已确认的旧 `denova_bridge.py` 进程（无 `running` 任务），启动加载最新代码的新桥。验证时接口从 671 条持续下降到 633 条，状态为 `running=1`、`queued=528`、`pending_review=17`、`failed=86`、`completed=1`，`paused=false`，磁盘 `order=633`，说明任务已恢复处理。未修改翻译正文、Master 数据、源码或用户存档；本次临时恢复脚本已移除。

## 当前状态（最近更新：2026-09-04 15:48 · Codex）
- 2026-09-04 15:48 · Codex：**接续完成翻译五项修复的阶段 5–7 与三处同步**。复核既有阶段 1–6 后确认精修已使用“启动前 Proposal ID 快照 + 新增同字段 polish Proposal”轮询，成功/拒绝/冲突/120 秒超时及资产切换/卸载均清理字段级 poller；本地队列已增加手动刷新和 failed/conflict/cancelled 逐项容错清理。阶段 7 额外发现并修复三个同源边界：① `master_review` 质量通过后原先被标为 `completed`，但前端不会自动写回该策略，现恢复为 `pending_review` 并统一进入总库显式确认；② Master 待审任务虽已隐藏行内采用/编辑，但仍可能进入顶部“全部采纳”，现将批量采纳严格限定为非 Master 旧 Lore 任务；③“前往总资料库处理”原先仅为文字提示，现改为可点击按钮并切换到 `library` 模式。未新增依赖、服务或第二套审核系统。
- 验证：Python 翻译质量测试 15/15；前端 `LibraryView`、`TranslationQueueControl` 与 3 个相关 api-client 文件共 31/31，队列边界补丁后单文件复验 9/9；Vite production build 两次使用全新 outDir 成功，均转换 3612 个模块，仅保留既有 >500 kB chunk 警告；Go 1.26.5 下 `go test ./internal/book -count=1`、`go test ./internal/api/handlers -count=1`、`go build ./...` 通过。`go test ./internal/api/...` 的子包通过，但顶层 `internal/api` 有 3 个与本次功能无关的 Windows 环境失败：`TestWorkspaceSwitchCanonicalizesSymlinkIdentity` 缺符号链接权限，`TestWorkspaceDeleteCreatesRestorableVersion` 与 `TestVersionPathRestoreAPI` 在测试临时 `chapters` 目录同步时 Access denied；未为此扩大业务改动。
- 构建清理：默认 Vite 旧 `dist/assets` 清理由当前 safe-delete 批量守卫拦截，改用全新 outDir 验证。三个验证目录各 139 项，直接删除同样被守卫阻止，已整体移出工作树至 `C:\Users\11\AppData\Local\Temp\Narraverse2-build-verify-20260904-1545`，正式 `dist`、executable、源码和用户数据未删除或覆盖。同步更新 `代码指南.md`、最终方案实施状态和本日志；本轮未替换正式构建产物，也未执行 executable 页面烟测。

## 当前状态（最近更新：2026-09-04 14:47 · WorkBuddy）
- 2026-09-04 14:47 · WorkBuddy：**按《翻译功能五项问题修复方案 最终版》实施阶段 1–4**。按用户要求「完成阶段4后暂停」，阶段 5（精修 Proposal 轮询）、阶段 6（队列收束）、阶段 7（全量构建验证与三处同步）尚未开始，`代码指南.md` 待阶段 7 一并更新。
- 阶段1（翻译质量契约）：`tools/denova_full_export.py` 引入三态契约 pass/needs_review/failed 与 7 个 quality_codes（empty_output/source_echo/mixed_language/missing_cjk/placeholder_mismatch/structure_mismatch/bridge_or_model_error）、单次纠错重试、仅 pass 写成功缓存、缓存按当前契约回验；版本 `TRANSLATION_CACHE_VERSION 10→11`、`PROMPT 6→8`、`VALIDATOR 6→8`、新增 `QUALITY_CONTRACT=1`；`translation_job_completion_status` 改为仅 `review_content` 进 pending_review，master_auto/master_review 合格即 completed。`tools/test_denova_full_export.py` 重写，15/15 通过。
- 阶段2（质量字段贯通）：`internal/book/master_runtime.go` 增加 CandidateTranslation/QualityStatus/QualityCodes/QualityReason；`ReviewRequired` 收窄为 `job.Status == "pending_review"`（去掉 ApplyPolicy 判断，避免合格的 master_review 全被标成待确认）；`ApplyMasterProposals` 增加可变参数 `confirmedHighRisk`，高风险未确认返回 `requires_high_risk_confirmation`；handler 增加 `confirmed_high_risk` 入参；同步更新 `master-library-runtime.ts`、`local-translation.ts`、`master-library.ts`。
- 阶段3（人工判断集中入口）：`web/src/features/library/LibraryView.tsx` 用新的 `ReviewWorkbench`（Denova 处理结果工作台）替换 `ProposalList`，支持全选/仅待修改/仅失败、批量重译、批量保存人工修改为 recovery proposal、批量确认应用、高风险合并二次确认、逐字段原文/候选对照编辑与 dirty 标记；移除详情页的 applyProposal/applyProposals。i18n zh-CN/en-US 新增约 45 个 `library.review.*` 键。`LibraryView.test.tsx` 12/12 通过。
- 阶段4（单资产重投影）：新增 `internal/book/master_sync.go`，实现 `ReprojectMasterAsset`（按 masterItemID 粒度，非 importID）、`MasterAssetAdventureUsage`、`FindAssetInstance`、`MarkInstanceSynced`；目标完整性全检（缺失即整体失败，绝不 continue 后仍更新 LoadedRevision）、名称分配排除本资产自身 target、保留冒险侧 Enabled/Type/TypeSource/LoadMode/Image/Provenance、ApplyOperations 失败即快照回滚、仅成功后写 LoadedRevision。新增路由 `GET /api/library/assets/:id/adventure-usage` 与 `POST /api/library/assets/:id/sync-adventure`；前端 `master-library.ts` 增加 `fetchMasterAssetAdventureUsage` / `syncMasterAssetToAdventure`，详情页「加入当前冒险」旁新增「同步到当前冒险」按钮，启用条件完全由后端 usage 决定（不依赖 usages.length）。
- 验证：`go build ./internal/...` 通过；`go test ./internal/book/` 全量通过（新增 `master_sync_test.go` 5 个用例：usage 判定、只更新自身目标、保留冒险本地字段、目标缺失原子失败、未加入资产拒绝同步）；`tsc --noEmit` 通过；`node scripts/check-i18n-keys.mjs` 3465 键对齐；LibraryView 测试 12/12。
- 踩坑：① `MaterialImportResult` 只有在 `InstantiateMasterAsset` 返回时才有 ItemIDs，测试 fixture 必须回写；② `LoreItemImage` 字段是 `ImagePath` 而非 `Path`；③ `LoreStore.ApplyOperations` 会按 Type 归一化覆写 `TypeSource`，测试只能断言「未被改写」；④ `LibraryDetail` 取数 effect 里的 `setActionMessage(null)` 会抹掉操作成功提示，已改为仅在切换 `masterItemID` 时清空。

## 当前状态（最近更新：2026-09-04 13:12 · Codex）
- 2026-09-04 13:12 · Codex：**完成翻译功能五项问题修复方案最终定稿**。综合初版方案、GPT2.0 复审、当前 Python/Go/React 代码事实和用户确认，新增 `docs/2026-09-04-翻译功能五项问题修复方案 最终版.md`。最终方案将可疑/失败翻译的人工判断集中到“叙界总资料库 → Denova 处理结果”，本地翻译队列收束为技术监控与清理入口；批量选择、批量重译、批量保存人工修改、批量确认应用、逐项容错及高风险合并确认列为本期必做。同时收紧统一质量契约、缓存/任务版本、单资产冒险重投影、LoadedRevision 原子性和精修 Proposal 轮询边界。保留两份原评审文档，未修改业务源码、运行配置或用户数据；本次仅为设计文档整理，未执行构建或运行验收。

## 当前状态（最近更新：2026-09-04 11:03 · Codex）
- 2026-09-04 11:03 · Codex：**恢复本机 Ollama Qwen2.5 管家模型接入**。确认本机 Ollama 在线且已有 `qwen2.5:7b`，未重复下载；备份当前运行配置后，在 `denova-src/output/.denova/config.toml` 仅新增 `qwen25-local` 本地 profile（回环 OpenAI 兼容端点）并将 `agent_models.tool_agent` 绑定到该 profile，保留现有默认云端模型和其它运行数据。重启路径明确属于当前工作树的 Denova executable 后，`/api/status`、`/api/settings` 配置加载验证通过；本地 Ollama `/v1/chat/completions` 真实请求返回 HTTP 200 和非空响应。未修改业务源码、Module1–4、API 配置架构或用户作品数据；API Key 未写入本条日志或输出。验证期间 `go test ./config ./internal/providercompat` 中 `providercompat` 通过，`config` 仍有与本次接入无关的既有模板测试失败（缺少写作子 Agent 模板）。

## 当前状态（最近更新：2026-09-04 10:18 · Codex）
- 2026-09-04 10:18 · Codex：**接入项目内 Go 工具链**。从 `E:\Narraverse\.workbuddy\tools\go` 复制完整 Go 1.26.5 windows/amd64 工具链到 `D:\Narraverse2.0\tools\go`，源目录保留不动；复制后源/目标均为 15,026 个文件、235,812,629 字节，目标 `bin\go.exe` 版本核验通过。更新 `tools/setup_narraverse2.ps1`，以后优先调用项目内 `tools\go\bin\go.exe`，只有项目内工具链不存在时才回退到 `NARRAVERSE_GO` 或系统 PATH；更新 `.gitignore` 忽略该本地工具链，避免二进制依赖误提交。未修改业务源码、API 配置或用户运行数据。

## 当前状态（最近更新：2026-09-04 10:08 · Codex）
- 2026-09-04 10:08 · Codex：**完成 Narraverse2.0 首次接手的轻量工具与验证基线**。按当前 GitHub `main` 事实读取 `README.md`、`AI协作指南.md`、`docs/交付/Narraverse2.0交接指南.md` 和 `docs/交付/Narraverse2.0交付清单.md`；检查 Git、Git Bash、GitHub 权限、Node、pnpm、Python、PowerShell、Go 及现有 Module4 测试能力。仅按已批准来源安装用户级 Archify `tt-a1i/archify`，未安装 Playwright、系统依赖或新服务。生成总体架构、Module4 状态、Module4 创建/进入流程和发布/验收流程四份 Archify JSON/HTML 产物；四份 showcase 校验均通过，现有 Codex 内置浏览器加载四份 HTML 并检查标题、内容和桌面视口无横向溢出。现有 Module4 定向测试 10/10、关键 JS 语法检查通过；Archify `visual-check` 因当前命令环境无 Chrome/Chromium 未执行成功，不影响已生成产物的 showcase 校验和内置浏览器检查。Go 未加入当前 PATH，但已确认本机存在可复用的临时 Go 1.26.8 工具链；未修改业务源码、API 配置或用户运行数据，临时 Python 静态服务器已停止。

## 当前状态（最近更新：2026-09-04 09:22 · Codex）
- 2026-09-04 09:22 · Codex：**收紧 AI 文档读取成本**。用户指出“每个任务都读完整交接资料”会无谓消耗 token；将 AI 协作原则改为渐进读取：只在新 AI 首次接手、新会话缺少可靠上下文、切换仓库或明确要求完整审查时进行完整定位，同一会话的后续任务只检查 Git 状态并读取任务相关源码、测试和局部规则。普通问答、只读排查、单文件文案和已有 PR 小修不重读全套日志/设计/历史。未修改业务代码、API、存储、构建流程或 Module1–4 功能。

## 当前状态（最近更新：2026-09-04 09:17 · Codex）
- 2026-09-04 09:17 · Codex：**新增 AI 协作决策原则**。新增 `docs/交付/AI协作决策原则.md`，明确唯一真源、worktree/branch/PR 决策、何时说明或等待用户、低收益检查止损、用户手动更高效的场景、反过度防御原则、Module4/生成资源边界和最小交付汇报格式。交接指南、README 与交付清单均已链接此文件，后续管理 AI 应先读它再决定是否创建新 worktree、分支或 PR。未修改业务代码、API、存储、构建流程或 Module1–4 功能。

## 当前状态（最近更新：2026-09-03 · Codex）
- 2026-09-03 · Codex：**新增新手 PowerShell 入口** `Start-Narraverse2.ps1`。脚本复用既有 `tools/setup_narraverse2.ps1` 与 `tools/start_narraverse.ps1`：首次运行或使用 `-Setup` 时执行正式初始化，后续运行直接启动当前 `denova-src/output/denova.exe`；支持 `-NoOpen`，未改动 Module1–4 业务逻辑、API 配置或旧部署数据。

## 当前状态（最近更新：2026-09-03 15:38 · Codex）
- 2026-09-03 15:38 · Codex：**修正启动器启动前的旧端口探测顺序**。即使目标 executable 尚未运行，启动器现在也不会先扫描并采纳其他工作树的 5174/旧端口页面；只有确认当前 executable 已在运行时才复用其地址，否则先启动当前 executable，再按进程路径验收实际端口。验证目标：旧 5174 仍存在时，启动器从停止状态启动当前 `denova.exe` 并使用 8080；旧目录与运行数据未删除或覆盖。

## 当前状态（最近更新：2026-09-03 15:35 · Codex）
- 2026-09-03 15:35 · Codex：**补齐 Windows PowerShell 启动无扩展名 Go 产物的兼容**。Git Bash 现有构建脚本输出 `denova-src/output/denova`，Windows PowerShell 的 `Start-Process` 无法直接启动无扩展名 PE 文件；`tools/start_narraverse.ps1` 现在仅在当前 output 内生成/复用同版本 `denova.exe` 启动副本，并用该副本路径识别进程，保留原有构建产物和端口选择逻辑。验证：旧 5174 Vite 页面仍在时，启动器可跳过它并启动当前 executable；API、正式页面、Module4 smoke 继续通过。

## 当前状态（最近更新：2026-09-03 15:12 · Codex）
- 2026-09-03 15:12 · Codex：**修复正式启动器对旧部署/静态端口的误识别**。第二次干净部署实测发现，`tools/start_narraverse.ps1` 仅按页面内容探测时，会把旧 `E:\Narraverse\denova\denova.exe` 或其他工作树的 Vite `5174` 页面当成当前 Denova，从而跳过当前 executable 启动。启动器现在要求监听端口的进程路径与传入的 `$DenovaExe` 一致，并按目标 executable 判断是否已有进程；未修改端口协议、Module1–4、API 配置或旧目录数据。验证：当前 GitHub `main` 干净克隆重新运行正式初始化脚本，前端构建与 Go 构建成功，随后以当前 executable 启动并完成正式页面、API、Module4 smoke 验收。
## 当前状态（最近更新：2026-09-03 13:57 · Codex）
- 2026-09-03 13:57 · Codex：**补充跨电脑部署与端口交接说明**。在当前交接指南中明确 5174 静态检查服务、5173 Denova Vite 开发前端、8080 Denova Go/正式 executable 默认入口，以及 8097/8098 可选本地桥接服务的职责边界；补充开发模式命令、正式模式访问地址、端口自动顺延、端口占用查询、停止方式、浏览器 origin 隔离和另一台 Windows 电脑的首次部署清单。README 增加端口速查和交接指南链接。未修改业务逻辑、API transport、存储结构或 Module1–4 功能。

## 当前状态（最近更新：2026-09-03 13:51 · Codex）
- 2026-09-03 13:51 · Codex：**补齐 Narraverse2.0 跨电脑交接材料**。新增 `docs/交付/Narraverse2.0交接指南.md`，明确当前目标仓库/提交、AI 接手顺序、5174 静态检查模式、正式 Denova 构建链、Module1–4 入口、Module4 最小玩法路径、共享 API 设置、生成文件边界、常见问题和验收证据。README 增补 5174 启动说明并指向当前交接指南；交付清单记录目标 `main` 与快照提交并勾选已验证项目；旧 Module4 阶段交接/状态文档标记为历史归档，避免新 AI 把旧仓库与旧路径当成当前状态。未修改 Module1–4 玩法、API transport、Denova Go 业务或存储结构。

## 当前状态（最近更新：2026-09-03 13:23 · Codex）
- 2026-09-03 13:23 · Codex：**完成 Narraverse2.0 staging 交付链验证**。使用 Go 官方 `go1.26.8.windows-amd64` 临时工具链完成 `go build ./...`；现有 `build.sh` 在 Windows Git Bash 下因 CRLF 和 `pnpm.cmd` 命令名暴露两个兼容点，仅在交付快照中规范 `build.sh` 行尾并增加 `pnpm.cmd` 选择，不改构建步骤。随后 `build.sh` 完整生成 `denova-src/output/denova` 与 updater；初始化脚本成功完成生成、同步、依赖安装、构建和启动。正式 executable 隔离端口 smoke 的根页面、`/api/status`、Narraverse 和 Module4 资源均返回 200。旧工作树和运行数据未纳入快照；待完成目标仓库推送与第二个干净克隆复验。

## 当前状态（最近更新：2026-09-03 12:56 · Codex）
- 2026-09-03 12:56 · Codex：**建立 Narraverse2.0 干净快照交付**。以当前 `feature/module-four` 的 `3c629ed8eafa017c1c63617b9f883d65934bf8d1` 为导入基线，在独立 staging 生成源码快照，新增可移植 Windows 初始化入口 `tools/setup_narraverse2.ps1`、根目录 `Launch-Narraverse.cmd` 适配、README 和交付清单。初始化入口会按顺序检查工具链、生成本地库、同步 Narraverse 静态资源、安装前端依赖、调用既有 Denova `build.sh` 并启动生成的 executable；未引入 Module4 专属 API 配置或新后端。将少数旧工具中的本机绝对路径改为项目相对路径/环境变量；没有导入 node_modules、构建产物、运行数据、旧 executable 或 API Key。后续在目标私有仓库 main 推送并用第二个临时目录复验。

## 当前状态（最近更新：2026-09-03 11:45 · Codex）
- 2026-09-03 11:45 · Codex：**校正模块层级并补齐 Denova 顶层模块四入口**。产品模块定义统一为：模块一写作、模块二游戏、模块三叙界、模块四开放沙盒；没有把叙界内部的冒险/酒馆/离线游戏误当成顶层模块。Denova 顶栏新增与前三者同级的“开放沙盒”入口，仍复用现有叙界同源 iframe：宿主通过 `module4-open` 控制 `app/module4/` 显示，Module4 关闭后通过 `module4-closed` 返回叙界顶层状态。未新增 `WorkspaceMode`、API 配置、后端路由或存储；保留 `app/module4/` 独立边界，并准备同步静态资源与运行测试。

## 当前状态（最近更新：2026-09-02 21:32 · Codex）
- 2026-09-02 21:32 · Codex：**完成宽松 Daily Preview 的正式包运行验证并对齐 UI 状态**。在当前工作树重建并启动正式 `denova` executable 后，创建独立 Module4 验证世界，使用现有共享模型 transport 完成一次真实 Daily Preview；模型返回合法 JSON，但省略了日程内容，现按空对象补齐并保存为非 fallback 预演。随后执行推荐移动 Action，状态由第 1 天早、图书馆、100 精力变为第 1 天中、教室、95 精力；重启 executable 后重新进入同一世界，位置、时段、精力和已生成 Preview 均正确恢复。发现 UI 仍沿用旧的“地点/行动内容完整”显示规则，已将 `play-view.js` 的可用状态判断对齐 `Preview.isCurrentDayValid()`，稀疏但结构有效的 Preview 显示为“已生成”；更新 PlayView 回归。Module4 10/10 回归、语法检查、静态同步和正式 `denova-src/scripts/build.sh` 均通过。未修改 callLLM、transport、API 配置、Store、Module3、Denova Go 业务逻辑；API Key 未写入日志或输出。

## 当前状态（最近更新：2026-09-02 20:45 · Codex）
- 2026-09-02 20:45 · Codex：**继续放宽 Daily Preview 对稀疏日程的兼容**。模型省略整个时段对象或 `schedule` 时，Preview 现在按空对象补齐 morning / afternoon / evening，不再因此显示“预演不完整”；JSON、当前 NPC 覆盖、NPC id 和三段对象结构仍保留。新增稀疏 schedule 回归，Module4 10 个测试、语法检查和 `git diff --check` 通过。尚未再次调用真实模型；需重新同步静态资源并重建 executable 后再做一次受控验证。

## 当前状态（最近更新：2026-09-02 20:26 · Codex）
- 2026-09-02 20:26 · Codex：**放宽 Module4 Daily Preview 的 AI 日程内容校验**。真实模型返回合法 JSON、正确 NPC 和三段 schedule 对象时，不再因单个时段缺少地点或行动内容而整次 fallback；仍保留 JSON 根结构、NPC 对应关系、`mood/goal`、三段时段对象等运行安全边界。`isCurrentDayValid()` 同步只校验时段对象结构，缺地点时 Encounter 自然返回未遇见，缺行动内容时允许空日程保存并保持同日幂等。更新 `tools/test_module4_preview.js` 覆盖空内容 Preview 的 completed 与持久化有效性；Module4 10 个定向测试、全部 Module4 JS 语法检查和 `git diff --check` 通过。未修改 callLLM、API 配置、retry、fallback、Store、Module3、Denova Go 后端或真实模型 transport；本次没有输出或记录 API Key。

## 当前状态（最近更新：2026-09-02 18:16 · Codex）
- 2026-09-02 18:16 · Codex：**完成收束分支推送与归档 SHA 校正**。`feature/module-four` 已将 `origin/main` 合入以解决收束 PR 的唯一日志冲突，随后推送至 `origin/feature/module-four`；当前远端 HEAD 为 `5175aba`，PR #14 已更新，仍保持 open、未合并。第 4 份续接归档已同步记录当前 HEAD；原对话的 3 份归档、PR #13 发布 hotfix 和本工作树新增体验补丁均保留。当前只等待收束 PR 审查，不开始 P1，不修改 `main`。

## 当前状态（最近更新：2026-09-02 18:14 · Codex）
- 2026-09-02 18:14 · Codex：**完成分支收束准备并解决收束 PR 的日志冲突**。将 `origin/main` 的 PR #13 发布 hotfix 合入 `feature/module-four`，冲突仅发生在 `项目协作日志.md` 顶部时间线，已保留双方完整记录；未发生 Module4 源码冲突。收束合并提交为 `fb9252d`，待推送后创建的 PR #14 目标为 `main`，本阶段不直接合并、不开始 P1。

## 当前状态（最近更新：2026-09-02 18:05 · Codex）
- 2026-09-02 18:05 · Codex：**完成原对话归档与当前工作树续接归档，准备进入分支收束**。保留原有 `01_模块四P0阶段总结.md`、`02_AI项目交接.md`、`03_当前状态与下一步.md`，新增 `04_本工作树续接与收束总结.md`，明确记录原归档之后的工作树改动、提交、测试与远端状态。当前 `feature/module-four` / `origin/feature/module-four` 均为 `b0869b4efe64b62ff02a2c354757ab976d5bf3fd`；`origin/main` 为 `ddb97e3ac576326856e97ca3f34d643161e9e55b`。Task 分支历史已收束到 Module4 集成分支；本次新增归档和体验补丁已发布到 `origin/feature/module-four`，下一步仅创建 `feature/module-four → main` 收束 PR，不直接合并、不开始 P1。未记录或输出任何 API Key。

## 当前状态（最近更新：2026-09-02 17:41 · Codex）
- 2026-09-02 17:41 · Codex：**审查并独立保存一组 Module4 体验/可观测性补丁**。当前改动新增 `actionLogs` 世界级持久化，用于记录主要行动前后状态；旧世界通过默认空数组兼容，查看类 Action 不写入行动日志，未知旧字段继续保留。Preview 增加日程行动内容与地点完整性检查，fallback 生成可展示的三时段骨架；PlayView 将 Preview 有效性、缺失日程和当前世界地点统一展示，并增加折叠式“试玩检查”区域，显示当前状态、NPC 日程和最近行动，不改变 Module3、Denova 后端、共享 API 配置或模型传输。相关 Module4 10/10 回归测试、全部模块四 JS 语法检查、`app/app.js` 检查和 `git diff --check` 通过；未发现密钥进入改动内容。该补丁已在 commit `b0869b4` 保存并推送到 `origin/feature/module-four`。

## 当前状态（最近更新：2026-09-02 11:58 · Codex）
- 2026-09-02 11:58 · Codex：**修复 Module4 Daily Preview 空日程的可玩性与试玩可观察性**。Preview 现在要求每个 NPC 的 morning / afternoon / evening 都包含行动内容；有地点的世界还必须包含有效地点，空对象不再被视为完整预演。模型结果不完整时沿用已有 fallback，并从 Runtime 目标和世界地点生成确定性的三段短日程；`Preview.isCurrentDayValid()` 同步按内容完整性判断，避免“已生成 / 未设置”矛盾。Play View 将地点 id 显示为地点名，缺失项明确显示“安排缺失”，普通玩家文案移除 source snapshot 等实现细节，并新增折叠式“试玩检查”区，显示每日 NPC 三段安排、Preview 状态、玩家状态和行动描述。统一 Action 管线新增当前世界 `actionLogs`（最多保留最近 100 条），记录行动前后 Day / 时段 / 地点 / 精力与结果描述；查看状态、查看世界、查看日志不写入记录。新增/更新 Preview、Action、Play View 定向断言；Module4 10 个测试、全部 Module4 JS `node --check`、`app/app.js` `node --check` 和 `git diff --check` 均通过。本次未新增 API、模型请求、P1 玩法或 Module3 / Denova 后端改动。

## 当前状态（最近更新：2026-09-02 08:42 · Codex）
- 2026-09-02 08:42 · Codex：**完成 Module4 P0 阶段归档，并校正当前发布基线**。新增归档文档 `docs/阶段归档/模块四P0发布/01_模块四P0阶段总结.md`、`02_AI项目交接.md`、`03_当前状态与下一步.md`，内容依据当前源码、Git 历史、远端分支和 GitHub PR 实际状态整理；本次仅更新文档，未修改业务源码、未提交、未推送。当前 `feature/module-four` 与 `origin/feature/module-four` 均为 `b4eacd502b5b1038d82ec1841051cbd57a36f6b8`，是 Module4 P0 集成基线；正式 `origin/main` 为 `ddb97e3ac576326856e97ca3f34d643161e9e55b`，包含 PR #12 发布合并和 PR #13 Denova 发布入口/静态资源兼容 hotfix，PR #1–#13 均已合并，当前无开放 PR。PR #13 的范围为根目录 `.gitignore` 规则修正、恢复 `denova-src/cmd/denova/`、17 个头像文件名由全角括号改为 ASCII 括号、生成器兼容性修正；未改 Module4、Module3、API 配置、存储结构或 Go 业务逻辑。生产发布工作树 `<project-root>-release-module4-p0` 基于 `origin/main` 构建并完成既有 Module4/Denova 验收证据；原始 `<project-root>` 工作树保持用户原有 dirty 状态，归档过程未对其执行 reset、clean、stash、restore 或覆盖。已记录的验证包括 Module4 10/10、Denova host 12/12、静态资源哈希一致、前端/Go 构建与正式 executable smoke；用户随后确认正式运行态可创建世界、生成真实 Preview、执行 Action、完成跨日结算并恢复精力。当前仍未创建 `module4-p0` tag。已知非阻塞限制：Windows 目录锁可能使静态同步 atomic rename 报 `EPERM`，可按既有 staging snapshot 方式安全恢复；Denova frontend 保留 8 个既有 baseline failures；Module4 仍使用 localStorage，长期 Facts/dailyLogs 容量治理留待后续；用户已反馈整体表达仍较混乱，属于后续 UX 工作，不纳入 P0 基线。

## 当前状态（最近更新：2026-09-02 00:26 · Codex）
- 2026-09-02 00:26 · Codex：**Release Hotfix 已打通正式 Denova 构建与 executable smoke，待创建 PR**。分支继续基于 `origin/main@9712153`：根 `.gitignore` 将运行目录规则锚定为 `/denova/`，恢复追踪正式 `cmd/denova` 的 `main.go`、`logging.go`、`main_test.go`；另将 17 个叙界头像文件名中的全角括号改为 ASCII，并在 `tools/gen_local_library_v2.py` 的现有头像查找处使用同一规范化规则。17 个头像改名前后 SHA-256 全部一致且无大小写不敏感命名冲突；重新生成的 39 张角色卡含 23 个可解析头像引用，其中 17 个改名头像均恰好引用一次、无旧全角文件名。Narraverse 源、Denova public 与 dist 的 23 个头像及 21 个 Module4 文件哈希一致，旧头像残留为 0。Go 1.26.5 下定向入口测试、`go build ./cmd/denova/`、`go build ./...`、前端 `pnpm build` 与正式 `scripts/build.sh` 全通过；正式产物 `denova-src/output/denova` 从输出目录启动后，根页面、`/api/status`、Narraverse 与 Module4 资源均 HTTP 200，并由 Ctrl+C 触发 Hertz graceful shutdown。未修改 Module4/Module3 业务、API、依赖、存储或 `go.mod` / `go.sum`；原始用户工作树全程只读。
- 2026-09-01 18:53 · Codex：**合并 PR #11 并确认 `feature/module-four` 为 Module4 P0 集成基线**。PR #11 在 base `feature/module-four`、head `codex/module4-task10`、HEAD `5ce347bb`、状态 `OPEN` 且 mergeable=true 时按普通 merge 合并；GitHub merge commit 为 `f2c543d07e160757d9adf34fac49e22b231d5770`，本地集成分支已 fast-forward 到该提交，任务分支保留。以合并后的集成分支重新生成 `app/local_library.js` 并执行现有静态同步；Windows 原子改名再次遇到 `EPERM`，按既有安全恢复方式保留完整快照，`app/module4/` 与 Denova 静态 `module4/` 共 21 个文件 SHA-256 一致。Task 1–10 Module4 回归、全部模块四 JS / `app.js` / 同步脚本语法检查、`git diff --check` 和 Denova 两个宿主定向测试（12/12）通过。5173 合并分支运行态可进入 Module4，世界列表/创建/打开、Preview、Action 均可用；iframe 实测 `overflow-y:auto` 从顶部滚到 `scrollTop=1477.33 / max=1478`，底部 UI 可访问；共享 endpoint/model 可读取，运行时 error 日志为空。无需再次消耗真实模型 API；Task 10 已有真实模型闭环证据继续有效。非阻塞限制：Windows 目录锁可能使同步脚本最终 atomic rename 报 `EPERM`，但完整临时快照可安全恢复；Denova 前端既有 8 个 baseline failures 未在本轮重做。当前不启动 P1，`feature/module-four` 可进入合并到 `main` 前的最终发布评审。

## 当前状态（最近更新：2026-09-01 18:32 · Codex）
- 2026-09-01 18:32 · Codex：**完成 Task 10 P0 真实共享模型闭环，结论改为 PASS**。在 `localhost:5173` 的 Denova embedded 当前 PR 源构建中，沿用现有共享 DeepSeek/OpenAI-compatible HTTPS endpoint 与模型 `deepseek-v4-flash`，未新增 Module4 API 配置。先将第 4 天既有 fallback 的 401 证据保留，再经统一 Action 管线推进到第 7 天早晨；在补强 Daily Preview Prompt（commit `b75e8b1`）后只点击一次“生成今日预演”，页面显示“已生成”并返回完整 morning / afternoon / evening NPC 日程，无“本地备用”或错误诊断。随后执行推荐移动 Action，状态从第 7 天早、教室、100 精力变为第 7 天中、图书馆、95 精力；刷新并重新进入模块四后，Preview 仍为已生成，位置、时段和精力均正确恢复。共享调用路径为 `PlayView.bindPreview()` → `Module4.AI.Preview.startDay()` → `window.callLLM()` → 现有 `state.apiConfig`；API Key 未写入日志、PR 或输出。定向 Preview 测试、全部模块四 JS 语法检查、`app/app.js` 语法检查、`git diff --check` 和 5173 运行时 Prompt 标记核对通过。Task 10 / P0 验收可标记 PASS；未新增 P1 功能，未修改 transport、validator、fallback、retry、Store、Module3 或 Denova Go 后端。

## 当前状态（最近更新：2026-09-01 18:19 · Codex）
- 2026-09-01 18:19 · Codex：**补强 Task 10 Daily Preview Prompt 并完成一次真实模型验证，结果仍为 PARTIAL**。仅修改 `app/module4/ai/prompts.js`，明确要求每个输入 NPC 恰好对应一项、非空 `mood` / `goal`、完整 `morning` / `afternoon` / `evening` 对象、严格复用原始 `npcId`、`worldEvents: []` 及严格 JSON 输出；同步更新 `tools/test_module4_preview.js` 的 Prompt 契约断言，未修改 `callLLM`、transport、API 配置、Preview validator、fallback、retry、Store、Module3 或 Denova 后端。定向 Preview 测试、全部模块四 `node --check`、`app/app.js` `node --check` 与 `git diff --check` 通过。同步脚本仍因 Windows 运行态锁在原子改名处报 `EPERM`，已核对临时快照完整后恢复正式目录；源文件、静态快照和 `http://localhost:5173/narraverse/module4/ai/prompts.js` 标记一致。使用已保存的共享 DeepSeek 配置点击一次生成（内部既有最多 2 次尝试），transport 已返回模型内容，但两次均未通过 schema 校验，最终 `previewStatus=fallback`，`previewError=NPC npc_11419062 缺少 mood 或 goal`；未执行后续 Action/刷新恢复，未将 Task 10 提升为 PASS。API Key 未写入日志、PR 或输出。

## 当前状态（最近更新：2026-09-01 17:07 · Codex）
- 2026-09-01 17:07 · Codex：**修复 Task 10 的 Denova 静态资源版本同步阻塞，未调用真实 API**。核对发现 `localhost:5173/narraverse/module4/ui/play-view.js` 缺少 `sanitizePreviewError` 与“预演失败原因：”，而当前 `app/module4/ui/play-view.js` 已包含二者，说明运行时仍在使用旧快照。现有同步脚本要求 `app/local_library.js`，但该文件是被忽略的生成产物；`tools/gen_local_library_v2.py` 又硬编码 `<project-root>`，无法在当前 Git worktree 生成。生成器现改为从脚本定位项目根并允许环境变量覆盖，重建本地库（50 本设定、39 张角色卡）后重新执行既有同步。Windows 在原子替换时锁住目录，完整临时快照已核对后恢复为正式 `public/narraverse`。最终 `app/module4/` 与 `denova-src/web/public/narraverse/module4/` 21 个文件 SHA-256 全部一致，5173 运行时资源已确认包含两个诊断标记；无需重启开发服务器。`py_compile`、相关 JS 语法检查、`test_module4_play_view.js` 与 `git diff --check` 通过。未修改模块四业务逻辑、API 配置或 Denova Go 后端，真实 Preview smoke 继续暂停。

## 当前状态（最近更新：2026-09-01 16:02 · Codex）
- 2026-09-01 16:02 · Codex：**完成 PR #11 的最小 Preview 失败诊断补丁，未再次调用真实 API**。`app/module4/ui/play-view.js` 仅在 `currentDay.previewStatus === 'fallback'` 且已有 `previewError` 时，在“今日预演”区域显示“预演失败原因”；继续保留“本地备用”状态，不写入新存储，不改变 transport、API 配置、重试次数、Preview 解析/ fallback 行为、Store schema、Module3 或 Denova Go 后端。错误文本先做 Bearer、`api_key` / `apiKey` / `Authorization`、URL query token/key 的最小脱敏，再复用现有 `escape()` 输出。新增 `tools/test_module4_play_view.js`，覆盖 completed 不显示、fallback 显示、HTML 转义、敏感值脱敏和空错误不渲染；Task 4 Preview 回归、该 UI 定向测试、全部模块四 JS 语法检查、`app/app.js` 语法检查与 `git diff --check` 均通过。按用户要求本轮完成后暂停，下一次真实模型调用需重新授权。

## 当前状态（最近更新：2026-09-01 15:31 · Codex）
- 2026-09-01 15:31 · Codex：**模块四 Task 10 真实共享模型复验仍为 PARTIAL，未新增业务代码**。在 PR #11 当前源构建 `localhost:5173` 的 Denova embedded 运行态，使用既有共享 `state.apiConfig` 的 DeepSeek/OpenAI-compatible HTTPS endpoint 与模型 `deepseek-v4-flash`；API Key 仅在本地设置页用于认证，未写入日志、PR、截图或测试输出。对已有“滚动验收世界”点击一次“生成今日预演”后，页面保持“生成中…”约 55 秒，最终显示“本地备用 / 预演解析失败，已使用本地 fallback”。该路径由 `PlayView.bindPreview()` 调用 `Module4.AI.Preview.startDay()`，后者只复用 `window.callLLM()`；当前实现内部尝试上限为 2，未执行任何额外手动重试。浏览器控制台仅见与本次请求无关的 `MutationObserver.observe` TypeError；现有 UI 及受限浏览器检查环境均不暴露存储的 `previewError`、HTTP 状态或响应体，故不能诚实断言 HTTP/API 层错误类型、状态码，亦不能把 fallback 当真实 Preview。未执行后续 Action/刷新恢复，不将 Task 10 提升为 PASS；下一步应先在不改玩法的前提下提供可观测的请求错误证据，再决定是否修复 P0 集成阻塞。

## 当前状态（最近更新：2026-09-01 14:40 · Codex）
- 2026-09-01 14:40 · Codex：**修复模块四 Task 10 的 P0 页面滚动验收缺口，未新增玩法或业务规则**。根因是全局 `app/style.css` 将 `body` 固定为 `overflow: hidden; height: 100vh`，而模块四激活态没有覆盖纵向溢出；仅在 `app/module4/module4.css` 增加 `body.module4-active { overflow-y: auto; overflow-x: hidden; }`，保留既有 `height: 100vh`，不改全局 body、旧叙界布局、Denova `NarraverseWorkspace` 或 Go 后端。当前 PR 源构建的 standalone 与 Denova embedded 均实际用滚轮事件验证模块四内部可纵向滚动，页面可到达底部的 Preview、Facts / dailyLogs、Action 和自由输入区域；嵌入式确认滚动发生在 iframe 内 `body`（scrollHeight 1925px / clientHeight 656px），宿主仍 `overflow: hidden`。返回旧叙界后 `module4-active=false`、`.app` 恢复 `flex`、body 仍为 `overflow: hidden`。Task 1–9 回归、全部模块四 JS 与 `app/app.js` 语法检查、Denova 12 个嵌入宿主定向测试均通过；真实共享模型 Preview 继续作为 Task 10 的唯一剩余 P0 验收项，整体结论仍为 PARTIAL。

## 当前状态（最近更新：2026-09-01 12:46 · Codex）
- 2026-09-01 12:46 · Codex：**模块四 Task 10 真实共享模型复验仍为 PARTIAL，未新增业务代码**。在 PR #11 当前源构建 `20260901-module4-p0` 的 Denova embedded 运行态中，沿用 Narraverse 现有共享 API 设置，配置为 DeepSeek/OpenAI-compatible HTTPS endpoint、模型 `deepseek-v4-flash` 且 API Key 已填写保存（日志不记录密钥）；未新增模块四 endpoint/key/model/settings。固定世界 `Task 10 真实模型验收` 先通过统一 Action 管线完成 Day 1 晚间行动并结算到 Day 2 morning，确认精力恢复为 100/100、旧 Day 1 fallback 不会冒充 Day 2 Preview；随后点击“生成今日预演”，页面进入“生成中…”约 30 秒，最终保存为 `previewGenerated=true` 但 `previewStatus=fallback`，UI 明确显示“本地备用”和“预演解析失败，已使用本地 fallback”。因此没有得到可验收的真实 Preview，不能继续声称完成“真实 Preview 下后续 Action + 刷新恢复”，Task 10 不得提升为 PASS。当前 UI 未暴露底层 `previewError` 或 HTTP 状态，浏览器错误日志也为空；随后源构建标签页崩溃且浏览器安全策略拒绝重新加载该地址，故本轮只能准确记录可观察错误，不能伪造模型/HTTP 成功结论或绕过限制取证。除本协作日志与 PR 验证说明外不修改业务代码；PR #11 继续保持打开。

## 当前状态（最近更新：2026-09-01 09:15 · Codex）
- 2026-09-01 09:15 · Codex：**模块四 Task 10 P0 结论修正为 PARTIAL，真实共享模型 smoke 尚未完成**。在当前 `codex/module4-task10` 源构建的 Denova embedded 运行态创建固定验收世界、加入 Android 18，并实际触发 Daily Preview；调用路径确认是 `PlayView.bindPreview()` → `Module4.AI.Preview.startDay()` → 现有 `window.callLLM()` → Narraverse 共享 `state.apiConfig`，模块四未新增 endpoint/key/model/settings。当前源构建运行域与既有 `127.0.0.1:8081` 运行域的共享配置均为空（endpoint/model 为空、API Key 不存在），`callLLM()` 在网络请求前准确失败为“API 未配置，请先在设置中填写 API 信息”；Preview 按既有规则重试后将 `currentDay` 保存为 `fallback`，页面显示“本地备用”，因此不能把 fallback 后的 Action 作为真实模型闭环证据。当前已安装的 8081 iframe 仍是旧缓存版本 `20260830-denova-dark`，本轮使用 PR #11 当前源构建版本 `20260901-module4-p0` 验证模块四入口；未修改业务代码、API 配置、Denova Go 后端或 P1 玩法。PR #11 继续保持打开，等待环境提供现有平台共享模型配置后补做真实 Preview + 后续 Action。

## 当前状态（最近更新：2026-09-01 08:46 · Codex）
- 2026-09-01 08:46 · Codex：**完成模块四 Task 10 P0 Integration Acceptance 验收**。当前分支已通过同步白名单语法检查、Task 1–9 全部模块四回归（9/9）、20 个模块四 JS 语法检查、`app/app.js` 语法检查、Denova Web `pnpm build`、嵌入宿主定向测试（12/12）和 `git diff --check`。浏览器实际验证覆盖独立版创建固定世界、加入 3 名 NPC、预演、自由输入移动、连续行动结算至 Day 4、刷新恢复；Denova Web embedded 覆盖同样的创建/加入/预演/移动/跨日/刷新恢复。全套 Denova 前端测试为 909/917 通过，剩余 8 项为既有 `main`、`AutomationsView`、`SettingPanel` 测试超时/断言失败，与本次 Task 10 文件无关，未扩大范围修复。Task 10 本轮仅修复 `module4/` 静态目录漏同步、iframe 资源版本缓存和对应宿主测试基线，不新增 P1 玩法、不修改模块四业务规则、Denova Go 后端或 API 配置。

## 当前状态（最近更新：2026-09-01 08:22 · Codex）
- 2026-09-01 08:22 · Codex：**开始模块四 Task 10 P0 Integration Acceptance，并修复 Denova embedded 资源同步阻塞**。真实 Git 状态已确认 PR #10 merge commit `0c04c139` 已进入 `origin/feature/module-four`，Task 9 head `eedfb2cf` 为当前 HEAD 祖先；旧 `codex/module4-task10` 已删除并从最新集成分支重建。独立版闭环已通过当前模块四 UI 创建固定“魔法大学”世界、持久化与入口检查；代码回归中的 Task 1–9、模块四/`app.js` 语法检查和 `git diff --check` 均通过。验收发现 `denova-src/scripts/sync-narraverse-assets.mjs` 白名单漏掉 `app/module4/`，会使嵌入版无法加载模块四；本轮仅将 `module4` 纳入现有同步白名单、更新 embedded iframe 资源版本并补充 Denova 变更记录，随后继续执行 standalone 与 Denova Web embedded 实际闭环烟测。未新增 P1 玩法，未修改模块四业务规则、Denova Go 后端或 API 配置。

## 当前状态（最近更新：2026-08-31 23:55 · Codex）
- 2026-09-01 00:15 · Codex：**修复模块四 Task 9 PR #10 的 targeted `chat / help` 在场约束**。Action Executor 将“必须在场”与“需要 Judgment”拆为独立类型集合：明确指向 NPC 的 `chat / help` 现在复用 `Encounter.checkNatural()` 校验当前地点与时段；目标不在场时在 Clock、Judgment、Facts 之前返回 `target-not-present`，不扣精力、不推进时间、不写 `interactedNpcIds` 或 Facts；在场的 `chat / help` 仍是无需 Judgment 的普通互动，`invite / persuade / request_help` 保持原有在场与判定路径。新增 Action 与 Settlement 回归，覆盖在场互动、异地拒绝、状态不变和未互动 NPC 日结记录。未修改 World/Store/Migration schema；真实 Denova embedded 烟测仍留待 Task 10。
- 2026-08-31 23:55 · Codex：**完成模块四 Task 9：Facts + Day Settlement**。新增独立 `core/facts.js` 与 `core/settlement.js`：Facts 只记录实际生效的 Judgment 后果、成功日程改写和重要 world event，`dailyLogs` 仅保存玩家可读的短日结摘要；未互动 NPC 依当前有效晚间日程/目标写入简短日志，不凭空改关系。Clock 晚间主要行动不再自行跨日，而是由统一 `Action.execute()` 立即调用唯一的 Settlement 入口，清理 player/NPC `temporaryState`，保留 relation、mood、source binding、Facts 和 rewrite 历史，随后进入次日早晨、恢复精力并切换为未预演的 `currentDay`。Daily Preview Prompt 现在读取最近关键 Facts；UI 增加最近 Facts / 每日记录的紧凑展示。旧存档经现有 World 归一化补齐 `facts`、`dailyLogs` 与当天互动标记，不触碰旧 Adventure。新增 `tools/test_module4_settlement.js`，覆盖三日连续运行、事实持久化与 Prompt 读取、日程改写跨日失效、临时状态清理、关系保留、日结日志、刷新恢复与次日预演；Task 1–9 全部回归、所有模块四 JS 与 `app/app.js` 语法检查均通过。未实现 Task 10、长期记忆/向量库、AI 总结、战斗或 Denova 后端；真实 Denova embedded 烟测仍留待 Task 10。

## 当前状态（最近更新：2026-08-31 23:24 · Codex）
- 2026-08-31 23:24 · Codex：**完成模块四 Task 8：Schedule Rewrite**。世界新增独立、可追溯的 `scheduleRewrites`，每条记录保留 NPC、当前日与目标时段、原始 Preview/Runtime slot、replacement slot、原因、来源 Action、Judgment outcome 与创建时间。`Schedule.getEffectiveSlot()` 现统一执行“原始日程 + 当前世界最新 rewrite”的读取规则；Encounter 与 Judgment 无需复制地点/日程判断，因此下午改到公园后会在公园相遇、图书馆不再遇见，原始 Preview 仍保留。Action 仅在明确改期的 `invite` / `persuade` 得到 `success` 后写入后续时段 rewrite；`costly_success`、`failure`、`critical_failure` 结构化返回但不改写。自由输入支持最小的“邀请艾琳下午去公园”解析。新增 `tools/test_module4_schedule_rewrite.js`，覆盖成功、说服、失败、costly、追溯字段、有效日程、相遇、重载、世界隔离和原始日程不覆盖；因 World/Store schema 扩展额外回归 Store 与 NPC Runtime。未实现 Facts、日结算、长期记忆、AI 长篇叙事或新地图系统；真实 Denova embedded 烟测仍留作 Task 10 最终验收。

## 当前状态（最近更新：2026-08-31 23:01 · Codex）
- 2026-08-31 23:01 · Codex：**修复模块四 Task 7 PR #8 的人际 Judgment 在场约束**。`Action.execute()` 现要求 `invite / persuade / request_help` 先解析到唯一且显式的目标 NPC，再复用 `Encounter.checkNatural()` 确认目标处于当前地点与时段的已遇见集合；无目标返回 `target-required`，目标不在场返回 `target-not-present`，二者都不进入 Judgment、不改 Runtime、不扣精力且不推进时间。探索、高强度和风险自由输入仍可在无 NPC 目标时正常判定。新增在场、异地、无目标和无目标风险行动回归断言。按新的风险分层约定，本轮仅运行 `test_module4_location`、`test_module4_action`、`test_module4_judgment`、全部模块四与 `app/app.js` 语法检查、`git diff --check`，均通过；Task 9 后执行 Task 1–9 全回归，Task 10 执行最终全量回归与 Denova embedded 烟测。

## 当前状态（最近更新：2026-08-31 22:37 · Codex）
- 2026-08-31 22:37 · Codex：**完成模块四 Task 7：Judgment**。统一 `Action.execute()` 在 Clock / Encounter 前识别需要不确定性判定的邀请、说服、请求帮助、探索、高风险训练及明确风险的自由输入；`core/judgment.js` 使用可复现的综合评分，返回 `success / costly_success / failure / critical_failure`、分数、因素和结构化后果。关系、心情、当前有效日程、目标、精力与已有玩家属性只参与当前世界 Runtime 判定；后果仅小幅更新 `relation`、`mood`、`temporaryState`，失败与严重失败仍可继续游戏，且绝不改写后续日程。新增 `tools/test_module4_judgment.js`，覆盖四档结果、同输入确定性、无判定行动、风险自由输入、状态持久化和邀请成功不改日程。Task 1–7 回归、模块四与 `app/app.js` 语法检查、`git diff --check` 已通过；未实现 Task 8 日程改写、Facts、日结算、长期记忆、战斗或长篇 AI 互动。真实 Denova embedded 运行态烟测仍保留为后续回归事项。

## 当前状态（最近更新：2026-08-31 22:06 · Codex）
- 2026-08-31 22:06 · Codex：**修复模块四 Task 6 PR #7 的自由输入 Parser 接线阻塞**。`PlayView.bindFreeInput()` 现在直接把原始字符串传给统一 `Action.execute()`，由唯一的 `parseFreeText()` / `normalize()` 路径识别意图；未在 UI 复制关键词规则。Task 6 定向测试新增真实表单提交替身，验证“去图书馆”执行移动并扣除精力、推进时段，“我帮她整理桌上的资料”执行帮忙，未知文本保持不耗时的 `custom`，且 `source` 保持 `free_input`。Task 1–6 回归、全部模块四 JS 语法检查、`app/app.js` 检查和 `git diff --check` 已通过；未实现 Judgment、日程改写、Facts 或日结算。

## 当前状态（最近更新：2026-08-31 21:54 · Codex）
- 2026-08-31 21:54 · Codex：**完成模块四 Task 6：统一 Action 管线**。在 `app/module4/core/action.js` 集中定义规范 Action、推荐行动、保守自由输入解析和统一 Executor；推荐按钮、移动、主动寻找与自由输入均经过同一 Normalizer/Executor，Clock 负责时段与精力，Location 继续只负责原子移动，Encounter 继续只读当前有效日程。移动正式消耗现有 `move` 费用并推进一个时段，免费查看不推进时间；未知自由输入保留为 `custom`，不提前调用 Judgment、Schedule Rewrite、Facts 或 AI 互动。新增 Task 6 headless 测试，Task 1–6 回归、模块四语法检查、`app.js`/`bridge.js` 语法检查和 `git diff --check` 已通过；未修改旧 Adventure、Denova Go 后端或 API 配置。

## 当前状态（最近更新：2026-08-31 21:26 · Codex）
- 2026-08-31 21:26 · Codex：**修复模块四 Task 5 PR #6 的地点名称引用阻塞**。`Location.get()` 现在统一支持地点 `id` 与 `name`，返回规范地点对象；Encounter 继续只通过该入口取得规范 `location.id` 后比较，确保 NPC schedule 使用 `{ location: "图书馆" }` 与玩家 `library` 能自然相遇和主动找到，不同地点名称不会误匹配。新增对应正例/反例回归测试；未处理移动耗时/精力，保持 Task 5 的 location-only 约束。Task 1–5 回归、全部模块四语法检查和 `git diff --check` 已通过。

## 当前状态（最近更新：2026-08-31 21:17 · Codex）
- 2026-08-31 21:17 · Codex：**完成模块四 Task 5：Location + Encounter**。新增世界级地点模型和 `player.location` 稳定 ID，支持创建世界时按行录入地点、地点移动和刷新恢复；新增 `Schedule.getEffectiveSlot()` 作为当前有效日程统一入口，并以当前日 Preview/Runtime 日程执行自然相遇与主动寻找。相遇严格匹配当前时段和玩家位置，未遇见返回正常结果，不强制刷 NPC；迁移器保留未知字段，并从旧存档已有位置/日程引用建立最小地点记录。未实现 Action Parser、推荐行动、Judgment、Schedule Rewrite、Facts、日结算或 AI 互动叙事；未修改旧 Adventure、`app.js` 或 Denova Go 后端。Task 1–5 定向测试、语法检查和 `git diff --check` 已通过。

## 当前状态（最近更新：2026-08-31 20:59 · Codex）
- 2026-08-31 20:59 · Codex：**修复模块四 Task 4 PR #5 的两项阻塞审查意见**。统一使用 `previewGenerated === true && currentDay.day === world.clock.day` 判断当前日 Preview 有效；跨日后 UI 不再展示旧日 schedules，并重新显示生成入口，`startDay()` 在新日重新生成且同日保持幂等。Daily Preview Prompt 增加紧凑 `world.title/description`，已有 `world.locations` 时仅附带只读摘要并复用现有地点引用校验。新增 Day 1 → Day 2 回归和世界上下文断言；Task 1/2/3/4 全部回归、语法检查和 `git diff --check` 已通过。未实现地点系统及其他后续模块。

## 当前状态（最近更新：2026-08-31 20:43 · Codex）
- 2026-08-31 20:43 · Codex：**完成模块四 Task 4：NPC Source Snapshot / Version Binding + Daily Preview**。在 `world.sourceBindings[sourceRef]` 保存加入世界时的角色资料快照和来源版本标识，快照与 NPC Runtime 分离；源角色卡后续修改或删除不再影响已有世界，旧 Task 3 世界迁移为明确标记的 legacy snapshot。新增独立 Daily Preview Prompt 与结构化解析，复用公共 `callLLM`，校验当前 NPC、三时段日程和可用地点引用；当天只生成一次，失败最多重试两次后使用本地 fallback，并持久化到 `currentDay.schedules/events`。新增最小 UI 入口与 `tools/test_module4_preview.js`，未实现地点相遇、Action、判定、日程改写、Facts、长对白或完整剧情；未修改 `app.js`、Denova Go 后端、Adventure 数据和 API 配置。Task 1/2/3 回归、Task 4 定向测试、Node 语法检查和 `git diff --check` 已通过；真实 Denova embedded 运行态烟测仍保留为后续回归事项。

## 当前状态（最近更新：2026-08-31 20:12 · Codex）
- 2026-08-31 20:12 · Codex：**完成模块四 Task 3：NPC Runtime + 现有资料接入**。复用现有冒险 `characterCards` 与运行时可用的 `LOCAL_LIBRARY.cards` 作为只读来源，优先使用角色卡已有 `source_ref.source_id`，旧卡则用稳定字段指纹生成 `sourceRef`；每个模块四世界独立保存 `world.npcs`，Runtime 仅保存名称、心情、关系、目标、临时状态和早/中/晚日程骨架，不写回角色卡原件。迁移器改为保留 `player` 及 Runtime 嵌套未知字段后再覆盖规范字段；新增三角色接入、原始资料不变、多世界隔离、刷新恢复和旧 Task 1/2 存档迁移测试。严格未实现 Daily Preview、地点相遇、Action、判定、日程自动生成、Facts 和 AI 调用；未修改 `app.js`、Denova Go 后端或 API 配置。

## 当前状态（最近更新：2026-08-31 19:38 · Codex）
- 2026-08-31 19:38 · Codex：**完成模块四 Task 2：Clock + Energy**。在 Task 1 Store 基础上将 Day、morning/afternoon/evening、主要行动推进、集中式精力消耗表、免费信息操作不推进、晚间跨日和次日精力恢复收口到 `app/module4/core/clock.js`；迁移层为旧 Task 1 世界补齐 `clock` 与 `player.energy/maxEnergy`，Store 增加世界级自动保存。新增 `tools/test_module4_clock.js`，并更新 Store 回归夹具加载模块四时钟依赖。严格未实现 NPC Runtime、Daily Preview、地点相遇、Action Parser、判定、日程改写、Facts 和 AI 调用。localStorage 仍仅承载模块四早期小型状态；进入长日志/Facts 阶段前仍需重新评估 IndexedDB 或拆分持久化。真实 Denova embedded 运行态烟测继续作为后续回归事项。

## 当前状态（最近更新：2026-08-31 19:17 · Codex）
- 2026-08-31 19:17 · Codex：**完成模块四 Task 1：Store + World CRUD**。在 Task 0 骨架上新增模块四专用 `localStorage` 命名空间 `narraverse:module4:state`，实现世界创建、列表、打开、删除、自动保存和刷新恢复；新增世界管理 UI 与 `tools/test_module4_store.js` 定向 headless 测试。写入失败时回滚内存变更；未读取或修改旧 `adventureAI_state`、Adventure、角色/世界原件。严格未实现时间、精力、NPC、Daily Preview、Action、判定、日程改写和 Facts。

## 当前状态（最近更新：2026-08-31 18:45 · Codex）
- 2026-08-31 18:45 · Codex：**完成模块四 Task 0 骨架**。从 `origin/feature/module-four` 创建 `codex/module4-task0`，在 `app/module4/` 建立 core / ai / state / ui 职责目录、`window.Module4` 命名空间、独立空工作区和返回旧叙界入口；仅对 `app/index.html` 做最小接入并复用现有主题变量。未实现 Task 1+ 的存储、时间、NPC、日程预演、行动、判定或日结算，未修改 `app/app.js`、Denova Go 后端、Adventure 数据和旧引擎。`node --check`、模块四冒烟测试与 `git diff --check` 已通过；仓库未提供现有 Narraverse headless/regression harness。

## 当前状态（最近更新：2026-08-30 22:39 · Codex）
- 2026-08-30 22:39 · Codex：**准备私有 GitHub 源码仓库**。已创建根目录 `.gitignore`，排除本地运行状态、构建产物、备份、归档内容、机器配置和密钥文件；将 `app/app.js` 内嵌的 API、语音和图片服务默认密钥清空，保留用户通过设置页在本机配置的能力。GitHub 认证已恢复，待完成暂存安全复核后创建私有仓库并首次推送。

## 当前状态（最近更新：2026-08-30 22:18 · Codex）
- 2026-08-30 22:18 · Codex：**将叙界色调同步为 Denova 暗色工作台**。保留既有写作/冒险布局与冒险列表优化，仅将画布、面板、输入区、弹窗、列表缩略图、按钮和选中态统一到 Denova 的午夜蓝黑色值（`#0e131d` / `#151922` / `#1b2230` / `#303b50` / `#edf2ff` / `#b6c7ff`），保留绿色成功与红色危险语义色。已更新 iframe 缓存版本为 `20260830-denova-dark`，同步到 `denova-src/web/public/narraverse`，`pnpm build` 通过并部署至 `denova/web`；8081 运行态确认主题色值生效，390px 移动端无横向溢出，控制台无错误，源文件与部署文件哈希一致。

## 当前状态（最近更新：2026-08-30 22:20 · Codex）
- 2026-08-30 22:20 · Codex：**修正导演台处理边界**。根据用户确认，将“导演台前台删除”改为“导演台前台停用”：生产游玩页不挂载、不提供切换入口、不展示导演过程，但 `DirectorPanel`、`DirectorConsole`、`DirectorBackstage`、后台导演服务、数据结构、API、i18n 和测试均不删除，保留后续恢复能力。已同步修正规格与实施计划；本轮未修改生产代码。

## 当前状态（最近更新：2026-08-30 22:12 · Codex）
- 2026-08-30 22:12 · Codex：**完成三模式游玩体验实施计划**。用户已确认规格，按“冒险标签数据契约 → 删除游玩前台导演台 → 沉浸对话稳定输出 → 规则信息按情境披露 → 后台导演解耦 → 定向测试与部署”的顺序拆分实施；计划写入 `docs/superpowers/plans/2026-08-30-三模式游玩体验实施计划.md`。当前仅修改设计/计划文档，尚未修改生产代码，等待用户确认实施计划。

## 当前状态（最近更新：2026-08-30 22:06 · Codex）
- 2026-08-30 22:06 · Codex：**补充冒险标签设计**。确认导演台前台删除后，冒险仍可配置 3～5 个剧情倾向标签并设置一个主要标签；标签分为气质、节奏、重点三类，持久化到故事数据，同时作用于沉浸对话与规则冒险，仅作为低优先级剧情引导，不覆盖玩家当前行动、世界设定、角色卡和事实状态。已同步补充至 `docs/superpowers/specs/2026-08-30-三模式游玩体验设计.md`；本轮未修改生产代码。

## 当前状态（最近更新：2026-08-30 22:03 · Codex）
- 2026-08-30 22:03 · Codex：**确认三模式游玩体验设计**。用户确认采用“沉浸对话 / 规则冒险 / 创作管理”三模式，规则冒险保留；前台删除叙界导演台、常驻状态栏和技术过程，后台导演服务与数据暂时保留并改为不阻塞玩家的异步能力；正文改为稳定缓冲后批量呈现，状态按战斗/检定/升级等情境披露。已将架构、数据流、实施阶段与验收标准写入 `docs/superpowers/specs/2026-08-30-三模式游玩体验设计.md`；本轮未修改生产代码，等待用户复核规格后再制定实施计划。

## 当前状态（最近更新：2026-08-30 22:00 · Codex）
- 2026-08-30 22:00 · Codex：**将叙界视觉统一到写作/冒险工作台，并优化冒险列表**。在 `app/index.html` 增加“我的冒险 / 故事档案 / 数量”层级；在 `app/app.js` 将列表缩略图改为本地 Lucide SVG 模式图标，补充模式、职业、等级、状态、回合数和最近更新时间，保留 AI 剧情简介为两行辅助摘要；在 `app/redesign.css` 用暖黑纸面、铜色左侧状态线、低干扰操作按钮和空列表提示重做列表层级，不改存档结构与引擎逻辑。为避免静态资源缓存，iframe、叙界 CSS 和 `app.js` 统一使用 `20260830-story-index` 版本号；已同步到 `denova-src/web/public/narraverse`，`pnpm build` 通过并部署至 `denova/web`。8081 运行态确认新 iframe 版本、SVG 列表结构和空状态；390px 移动端无横向溢出，控制台无错误。

## 当前状态（最近更新：2026-08-30 19:43 · Codex）
- 2026-08-30 19:43 · Codex：**将 CodexBridge 微信入口连接到 Narraverse**。核对其他任务与 CodexBridge 接入项目后，确认微信桥接此前默认工作目录为 `E:\`；仅修改用户目录下的桥接启动包装器 `<user-profile>\codexbridge-service.mjs`，将 CodexBridge `--cwd` 改为 `<project-root>`。保留原有微信登录态、私聊白名单、群聊关闭、`workspace-write + on-request` 审批和本机 Codex app-server。重启并清理旧监督进程后，日志确认新实例 `default_cwd: <project-root>`，当前锁实例正常。已有微信会话可能保留旧 cwd，需在微信发送 `/new <project-root>` 建立项目会话。

## 当前状态（最近更新：2026-08-30 19:11 · Codex）
- 2026-08-30 19:11 · Codex：**将嵌套在 Denova 内的叙界 UI emoji 替换为本地 SVG 图标**。通过 GitHub `lucide-icons/lucide` 仓库检索并核对 LICENSE 与图标源文件，选择线性、`currentColor` 风格；在 `app/index.html` 增加带许可说明的 Lucide inline sprite 与 UI emoji → SVG 归一化层，保留叙事正文 emoji，避免新增运行时依赖；在 `app/redesign.css` 增加图标尺寸与对齐规则。已同步到 `denova-src/web/public/narraverse`，构建通过并部署到 `denova/web`；嵌入态扫描确认 UI emoji 残留为 0、SVG 图标 186 个、无横向溢出，`node --check` 通过。同步命令仍需显式传入 `<project-root>\app`。

## 当前状态（最近更新：2026-08-30 18:51 · Codex）
- 2026-08-30 18:51 · Codex：**将叙界从“AI 控制台”视觉改为“夜间创作室”**。新增项目级视觉锚点 `DESIGN.md`；更新 `app/index.html`，移除外部 Inter 字体加载并为重设计样式增加版本号；在 `app/redesign.css` 追加最终覆写层，改用暖炭黑 / 铜色信号 / 苔灰辅助色，去除紫蓝渐变、霓虹辉光、玻璃模糊、过度胶囊和左侧强调条，强化内容优先与编辑工作区层级。已用显式参数将叙界资源同步到 `denova-src/web/public/narraverse`，`pnpm build` 通过并部署至 `denova/web`；已在桌面端、390px 移动端和“创建新冒险”流程运行态验证，未修改业务逻辑或 API 行为。叙界资源同步命令必须显式传入 `<project-root>\app`，否则会因缺少源目录参数失败。

## 当前状态（最近更新：2026-08-30 18:38 · Codex）
- 2026-08-30 18:38 · Codex：**完成 Denova C「深夜档案馆」页面美化**。更新 `denova-src/web/src/index.css` 的深色工作台变量为午夜蓝黑 / 靛色信号体系，补充总资料库专属工具栏、单列资产阅读流、悬停与焦点状态；更新 `LibraryView.tsx` 添加稳定样式钩子并将资产卡片改为单列阅读布局。已通过 `pnpm build`，并同步构建产物至 `denova/web`；已在 1280px 与 390px 运行态验证列表、详情页、筛选布局和卡片点击流。首次同步命令因缺少 Narraverse app 目录参数失败，后确认本项目运行时应直接同步 `dist` 到 `denova/web`，未影响最终结果。

## 当前状态（最近更新：2026-08-30 18:24 · Codex）
- 2026-08-30 18:24 · Codex：**建立 Denova 页面美化视觉预览**。基于当前运行态与源码，制作 `design-previews/2026-08-30-denova-page-polish/index.html`，提供“暖黑编辑室”“冷白索引”“深夜档案馆”“纸张工作台”四个方向，范围限定为工作台壳层与总资料库；未修改生产 UI、Master 数据、8097 或 Agent。等待用户确认方向后再实施。

## 当前状态（最近更新：2026-08-30 12:15 · Codex）
- 2026-08-30 12:15 · Codex：**补充活动文档改名兼容入口**。因项目外工作台档案仍引用旧的 `COLLABORATION_LOG.md` 与 `CODE_GUIDE.md`，在项目根目录为它们建立指向中文正式文件的兼容硬链接；旧名与中文名保持同一文件内容，避免日志或代码指南出现两份状态。`AGENTS.md` 继续保留原名以确保工具自动发现。未修改生产代码、Master 数据、8097、Agent 或 UI。

## 当前状态（最近更新：2026-08-30 12:12 · Codex）
- 2026-08-30 12:12 · Codex：**统一主要活动文档的中文文件名并修正活动引用**。三份 1.0 文档、代码指南、项目协作日志和 AI 协作指南已改为中文名称；`AGENTS.md` 保留原名以确保工具自动发现。已同步根目录规则、当前 1.0 文档、代码指南、AI 协作指南、平台模块总览和项目协作 Skill 的活动路径；历史日志、历史归档和工作缓存中的旧路径保留为历史记录，未修改生产代码、Master 数据、8097、Agent 或 UI。

## 当前状态（最近更新：2026-08-30 11:59 · Codex）
- 2026-08-30 11:59 · Codex：**将过程文档归档目录改为中文名称**。目录从 `<project-root>\docs\_history\2026-08-30-process-docs\` 重命名为 `<project-root>\docs\_history\2026-08-30-历史过程文档\`，其中 19 份文件内容保持不变。

## 当前状态（最近更新：2026-08-30 11:46 · Codex）
- 2026-08-30 11:46 · Codex：**归档第一批过程性文档**。将 19 份已被 1.0 总览、Implementation Facts、主文档或当前生产代码取代的历史分析/设计/实施文档移动到 `<project-root>\docs\_history\2026-08-30-process-docs\`，未物理删除，保留原始文件内容和可恢复路径。未修改生产代码、Master 数据、8097、Agent 或 UI。

## 当前状态（最近更新：2026-08-30 约11:00 · AI 1 定稿）
- 2026-08-30 约11:00 · AI 1（定稿）：**正式基线确认**。用户确认 `<user-profile>\Videos\黑袍\ai知识库\Narraverse - Denova 1.0 (1).md` 为 Narraverse / Denova 1.0 正式主文档基线，职责分工：主文档负责"理解系统"，Implementation Facts 负责"核验当前代码事实"。该版已落实 AI 1 上轮全部审阅意见（16 章重组、隔离理由前置、双 Agent 区分、terminal-failure PARTIAL/NOT IMPLEMENTED 消歧、Retry/Recovery 三概念区分、存储位置、代码修改起点、附录化）。已复制为 `<project-root>\Narraverse-Denova-1.0-主文档.md`（41KB）与 `Narraverse-Denova-1.0-Implementation-Facts.md`（32KB）并列存放。旧的 `Narraverse-Denova-1.0-项目总览-初稿.md` 已被本版取代，未删除，保留待用户处置。本轮未修改任何生产代码。

## 当前状态（最近更新：2026-08-30 约10:55 · AI 1 审阅）
- 2026-08-30 约10:55 · AI 1（审阅）：以「项目文档总编辑」身份审阅《Narraverse / Denova 1.0 项目总览》初稿（位于 `<user-profile>\Videos\黑袍\ai知识库\Narraverse - Denova 1.0.md`，约 60 个一级章节）与 `<project-root>\Narraverse-Denova-1.0-Implementation-Facts.md`。审阅结论：①60+ 一级章节碎片化，七节点/Recovery/隔离/粒度等概念多处重复；②事实冲突：主文档 #54 与 #56 对「8097 正式 terminal-failure 协议」双重标注（PARTIAL 与 NOT IMPLEMENTED 同列），#52 将 Polish Candidate 列入已完成但 Facts 标 CURRENT/PARTIAL，#41「未来系统仍然知道」实为 CURRENT 事实；③缺失：Config Manager Agent（双 Agent 结构）、Master 磁盘存储位置（相邻 narraverse-master-library 工程 + manifest 路径）、旧 worldbook_entry 实例兼容保证、代码修改起点；④#60「AI 接手任务时的推荐方式」为空章节；⑤已给出推荐新目录约 16 章。本轮仅审阅，未修改任何源码/正文；等待用户确认后由 AI 1 或用户改稿。事实冲突清单已在对话中输出。

## 当前状态（最近更新：2026-08-30 09:40 · Codex）
- 2026-08-30 09:40 · Codex：**生成《Narraverse / Denova 1.0 Implementation Facts》事实底稿**。按当前生产源码定点核对 Denova、Master Library、素材导入、Translation/8097、Master Agent、Recovery、Adventure Instance、API、资料库 UI、配置、测试验证和技术债；为能力标注 CURRENT/PARTIAL/LEGACY/NOT IMPLEMENTED，并增加 `Documentation Drift`，指出 `CODE_GUIDE.md` 仍把资料库页面描述为只读而当前 UI 已有导入、受控编辑、Proposal 和 Agent 入口。本轮只新增 `Narraverse-Denova-1.0-Implementation-Facts.md`，未修改生产代码；当前环境 `go/gofmt` 不可直接调用，文档中的 Go 验证状态沿用最近已有执行记录并明确标注未在本轮重跑。

## 当前状态（最近更新：2026-08-30 08:56 · Codex）
- 2026-08-30 08:56 · Codex：**执行 P2 第一小步：资料库编辑预览与字段级 Agent 上下文**。现有角色卡/Lorebook 编辑表单增加“预览修改”，用修改前/修改后对照确认后才保存；现有保存继续逐字段调用 Master Proposal → Validate → CAS → Apply，未新增写入接口，不改原始文件或 Adventure。内容页新增“让 Agent 处理”，打开右侧配置管理 Agent 时传入 `master_item_id + field_path + field_label`，让 Agent 从当前字段开始理解和处理；未新增 Agent 类型或放宽权限。资料库定向 Vitest 6 项通过，i18n 3419 键对齐，Vite 构建通过，web dist 已部署到 `denova/web`。

## 当前状态（最近更新：2026-08-30 08:52 · Codex）
- 2026-08-30 08:52 · Codex：**为角色卡增加直接编辑**。角色卡内容页现在可编辑人物名称、介绍、性格、创作者备注、场景、开场白、其他开场白和示例对白；系统指令、对话后指令仍保持只读并交给配置管理 Agent。保存逐字段复用 Master Proposal → Validate → CAS → Apply，生成新总库版本，不修改原始文件或 Adventure。资料库定向 Vitest 5 项、TypeScript/Vite 构建通过，web dist 已部署到 `denova/web`。

## 当前状态（最近更新：2026-08-30 08:44 · Codex）
- 2026-08-30 08:44 · Codex：**为总资料库 Lorebook 条目增加直接编辑**。阅读器新增“编辑条目”表单，可修改标题、正文、关键词、副关键词；保存按字段顺序复用现有 Master Proposal → Validate → CAS → Apply，生成新总库版本，不修改原始文件，也不直接写 Adventure。前端新增中英文文案；资料库定向 Vitest 5 项、TypeScript/Vite 构建通过，web dist 已更新到 `denova/web`。当前未增加新的后端写入接口或数据模型。

## 当前状态（最近更新：2026-08-30 08:30 · Codex）
- 2026-08-30 08:30 · Codex：**完成 P1.5 最小批量 Proposal 应用**。新增 `/api/library/assets/:id/proposals/batch-apply` 与前端“批量应用安全修改”入口；默认选择低风险、已验证/候选就绪的 Proposal，高风险不进入批量。后端逐条调用既有 Apply/CAS 流程，前一条应用造成父 Master revision 变化时，仅对目标字段未变化的后续 Proposal 做最小 rebase；目标字段发生变化、字段不属于当前资产或其他安全校验失败，会逐条返回冲突，不放宽 CAS。保留单条“应用修改”不变。前端资料库 Vitest 5 项、i18n 3400 键、Vite build、Go `gofmt`、`go test ./internal/book ./internal/api/handlers`、`go build ./...` 均通过。运行版已更新并重启，备份为 `denova/backup/library-batch-20260830-082853`。

## 当前状态（最近更新：2026-08-30 07:46 · Codex）
- 2026-08-30 07:46 · Codex：**修复 Lorebook 条目关键词未使用翻译结果的问题**。阅读器现在优先读取总库中 `keys / secondary_keys` 的活动中文翻译，尚未完成的字段回退原关键词；搜索同时保留原始英文关键词作为召回兜底。针对《苏斯魔法书》确认当前有 25 个翻译目标、21 个已完成，4 个仍排队（3 个关键词字段 + 1 个条目标题），因此“内容栏部分中文、关键词不全面”由未完成任务和前端读取路径共同造成。没有修改配置管理 Agent、模型配置、翻译队列或数据。资料库定向 Vitest 5 项、i18n 3395 键对齐、Vite 构建通过；web dist 已重新部署到 `denova/web`。

## 当前状态（最近更新：2026-08-30 07:10 · Codex）
- 2026-08-30 07:10 · Codex：**继续补全资料库剩余 P1 前端体验**。版本页改为用户化版本卡片：显示机器初译/润色候选/当前使用/历史版本、字段的人类可读位置、译文内容和更新时间，不在普通版本页暴露 translation version ID 或模型名；技术信息页继续保留底层诊断。Lorebook 条目目录增加窄屏抽屉：桌面端仍为左侧目录，移动端点击“打开目录”后从左侧展开，选择条目或点击遮罩即可关闭。未修改后台 API、导入流程、翻译流程或 Agent 权限。资料库定向 Vitest 5 项通过，`pnpm build` 通过；web dist 已更新到运行版 `denova/web`。 

## 当前状态（最近更新：2026-08-30 06:53 · Codex）
- 2026-08-30 06:53 · Codex：**完成总资料库 P0 + 核心 P1 阅读型前端改版并部署运行版**。总库列表从 revision/SHA 表格改为整资产卡片，展示中文名称、角色卡/设定书类型、一句话简介、内部条目数、用户三态与 Adventure 使用数；现有 `MasterAssetSummary` 只读投影最小增加 `description/nested_entry_count`，没有新增持久状态或数据模型。详情顶部只回答“是什么、能否使用、是否需处理、能否加入冒险”，默认进入“内容”，并分成“内容 / 处理进度 / 版本 / 已加入冒险 / 技术信息”五页；Asset ID、SHA、归档路径、内部版本与原始 JSON 已全部后置到技术信息。角色卡按角色概览、性格与背景、场景与开场、示例对白、内部设定、高级指令排版；Lorebook 使用稳定 `entry_id` 的两栏目录/正文阅读器并支持标题、关键词和正文搜索；无意义的 `comment/评论` 标题回退首关键词。七节点改为简洁处理摘要，完整节点默认折叠；配置管理 Agent 顶部入口保留，处理进度也可打开同一右侧栏。主状态统一为“可以使用 / 正在处理 / 需要你处理”，`usable + optional pending` 显示“核心完成，辅助内容后台完善且不影响使用”。Go 定向测试、`go build ./...`、资料库 Vitest 4 项、i18n 3380 键与 Vite 构建通过；默认 Go 缓存一度被 Windows 拒绝访问，改用项目临时缓存后通过。生产备份为 `denova/backup/library-ui-p0-20260830-0646`，新二进制和 web 已部署，8080 运行正常。真实 9 资产浏览器验收通过：艾可默认显示人物档案；《苏斯魔法书》显示 8 条目录、可搜索正文与“辅助内容不影响使用”；技术 ID 默认不可见；处理详情默认关闭；未发送 Agent 消息、未加入冒险、未应用 Proposal。浏览器已保留在总资料库页面供用户查看。
- 2026-08-29 23:36 · Codex：**完成配置管理 Agent 的总资料库读取、原文件理解与受控修改能力，并部署到运行版**。总资料库资产详情复用现有 `ConfigManagerChat`，点击“配置管理 Agent”后在右侧打开可调宽度的同一 Agent，会话按 `master-library + master_item_id` 固定到当前资产；Agent 可列出总库资产、读取规范化整卡、嵌套 Entry、精确字段及通过 `LoadSourceRevision()` 获取的不可变原文件。新增内置 `master-library` Skill，明确“总库概览 → 必要时原件 → 精确字段 → Proposal → Validate”的读取/修改流程。Config Manager 只能创建并校验 Master Proposal，不能直接 Apply，也不能借普通 `write_lore_items`/filesystem write 绕过边界；前端展示“修改前/修改后”，由用户点击“应用修改”，高风险字段额外确认，最终沿用既有 CAS、字段范围和风险校验。Go 定向测试、前端资料库 3 项测试、i18n 3323 键、Vite 构建与 `go build ./...` 通过；较宽 Go 包测试仅保留既有 Windows 符号链接/权限/路径格式失败。生产备份为 `denova/backup/master-library-agent-20260829-2333`，新二进制、web 和 `denova/skills/master-library/SKILL.md` 已部署并重启，8080 与 `/api/skills` 验证通过；浏览器实测“艾可”详情右侧 Agent 面板和 Proposal 差异/应用入口可见，未发送真实 Agent 修改请求，也未应用历史 Proposal。
- 2026-08-29 22:43 · Codex：**完成《总资料库前端展示与改版评审说明》**。面向其他 AI 整理总资料库的产品定位、用户任务、当前列表/详情/完整内容/七节点/加入冒险界面、现有真实资产、主要 UI 痛点和改版验收标准；重点说明 ID、SHA、版本串和归档路径造成的“乱码感”，建议用户信息优先、技术信息折叠、大设定书增加目录/搜索/分组，并给出希望其他 AI 输出的信息架构、文字线框图、中文文案和 P0/P1/P2 改版清单。文档只讨论 UI 展示与交互，不包含代码、接口或后台实现方案，路径为 `docs/总资料库前端展示与改版评审说明.md`。
- 2026-08-29 21:51 · Codex：**完成 Master 整卡化运行验收、总库导入隔离修正与总库用户态中文化**。修正 Master-only transaction 在活动中文名称写回后误进入 Adventure 投影的问题：总库导入现在只完成 Source/Master/Translation/Pipeline，只有明确的 `/api/library/assets/:id/instances` 才生成 Adventure Lore；新增回归覆盖。总库列表/筛选/详情/七节点/任务与内容版本状态已将 `usable/staging/record_kind/semantic_type/hy_mt_active/translation_active` 等用户可见枚举翻译为中文，生产页面实测显示“可用、待处理、角色设定卡、设定书、机器初译（当前）、当前译文”。8097 增加受限的稳定嵌套路径校验，支持 `character_book.entries/<entry_id>/...` 与 `lorebook.entries/<entry_id>/...`；四本迁移设定书的中文顶层名称已生效，当前 9 个 active Master Asset 全部为 `usable`。真实临时 Adventure 验收中，《苏斯魔法书》首次实例化展开 8 个 Lore、产生 8 个 `nested_entry_lore_ids`，重复加入新增 0/跳过 8，随后恢复生产 Master manifest 并将测试事务/工程归档到 `denova/backup/library-instance-acceptance-20260829-1930`，未写入用户真实 Adventure。运行投影补充 optional nested comment 为空时按 keywords/secondary keys/entry ID 回退命名，避免单个可选名称阻止整本实例化。四本设定书共有 432 个翻译目标，354 个已激活；重启后重新投递余下 78 个可选字段，HY-MT 对 `Maine` 等专名未产生可用中文，保留为可选警告/诊断，不影响资产可用和加入冒险。Go 定向测试/构建、Python 定向测试、资料库 Vitest 2 项和 Vite 构建通过，最新 web 已部署；部署前备份为 `denova/backup/library-localization-20260829-1906`。
- 2026-08-29 18:05 · Codex：**完成 Master 整卡化、嵌套 Entry 稳定化与导入隔离**。角色卡/独立 Lorebook 现在各自只生成一个 active 顶层 Master Asset；内部 Worldbook/Lorebook Entry 保留 `entry-<hash>` 稳定身份并写入父资产 `nested_entries` 与 `character_book.entries/<entry_id>/...`、`lorebook.entries/<entry_id>/...` 字段路径。顶层 `character.name` / `lorebook.name` 进入必需 `name_zh` 翻译目标，列表优先使用活动中文名称；嵌套字段沿用现有 Translation/CAS/Recovery/实例展开链路，内部条目在资产详情完整内容中单独展示。新增 `master-migrate` 一次性归档工具和 legacy 索引：生产总库执行前备份为 `denova/backup/master-granularity-20260829-173900`，已将 21 个旧 `worldbook_entry` 移出 active manifest，生成/更新 2 个父资产，保留 8 个旧 Adventure 实例内容和全部旧文件/译文，写入 `.narraverse/master/legacy-mapping.json`、`legacy_*` 索引与 tombstone；active 资产为 5 个角色卡 + 1 本 Lorebook，无 active dangling item refs。新增整卡、稳定 ID、嵌套实例映射和旧资产迁移测试；Go `gofmt`、`go test ./internal/book`、`go build ./...`、前端 `tsc`/Vite 构建通过；全量 Go 测试仍有既有 Windows 权限/符号链接/绝对路径边缘失败，未扩大修复。生产已部署新 `denova.exe` 与 web dist 并重启 8080，API 已复核 active 资产与嵌套内容；当前未开发自动同步、Entry 选择器、批量实例化或新 Agent。

## 当前状态（最近更新：2026-08-29 约 · Codex）
- 2026-08-29 14:38 · Codex：**完成总资料库入口、整卡内容查看和冒险实例化的最小功能补充**。总资料库列表新增“导入到总资料库”，调用 Master-only 导入接口：原文件进入总库并进入现有翻译队列，但不会自动加入当前冒险；资产详情新增折叠式“完整内容”，可查看原件字段、来源语义、运行语义及规范化字段/当前内容；详情页新增“加入当前冒险”。现有冒险“加载资料”对话框新增“总资料库”页签，可筛选并多选 `usable` 资产加入当前冒险；实例化复用既有 `FinalizeMasterImport`，同一资产重复加入同一冒险保持幂等。新增 `/api/library/import-material` 与 `/api/library/assets/:id/instances`，未改导入主流程、HY-MT、8097、Agent 或 Master→Adventure 同步。Go 定向测试、`go build ./...`、前端 3 个定向 Vitest 文件共 7 项、`pnpm build` 均通过；已部署生产 `denova/denova.exe` 与 `denova/web`，备份为 `denova/backup/library-assets-20260829-1432`。生产 API 复核成功，当前总库 2 个资产；浏览器已验收总库入口、导入弹窗、资产详情完整内容和加入冒险按钮，未用真实资产执行写入型点击。
- 2026-08-29 12:55 · Codex：**第六阶段运行版部署与验收完成，未进入第七阶段**。使用 Go 1.26.7 临时工具链完成 gofmt、Master Runtime/Agent 定向测试和 `go build ./cmd/denova`；此前完整 `go build ./...` 已通过。生产 `<project-root>\denova\denova.exe` 与 `web` 已更新并启动，HTTP/API 200；用户配置保持 `agent_models.tool_agent.profile_id = qwen25-local`，对应 Ollama `qwen2.5:7b`、`http://127.0.0.1:11434/v1`。浏览器验收通过：总资料库列表、Aiko 详情、Source/SHA/revision、七节点、翻译 7/7、Recovery 字段“已完成”、Pipeline `usable` 均可见；Runtime 显示 `recovered`/失败 0，8097 历史失败原因仍保留于诊断。前六阶段真实链路已完成记录：qwen25-local Recovery Agent 启动 1 次，Proposal/Validate/CAS/Apply/新 Translation Version/active/recovered 均成功；needs_user 回归通过且未越过高风险边界。期间仅为验收修正 Runtime 只读聚合对 `failed/retrying` 历史任务的 recovered 映射，未修改 8097、导入/HY-MT 主流程、Proposal/CAS/模型解析或用户配置；保留备份于 `denova\backup\runtime-acceptance-20260829-112352`。后续已知技术债：8097 仍用 `failed + 5 秒` 临时终态判断，完整 `go test ./...` 未作为本机全套验收要求执行。停止开发，等待下一阶段明确授权。
- 2026-08-29 12:53 · Codex：最终浏览器验收继续发现 Runtime 聚合顺序问题：`retrying` 字段在 `final_failure=false` 分支提前返回，无法读取已存在的 `recovered` claim。将 claim 读取/已恢复版本收口放到最终失败判断之前；仍保留 8097 历史任务与技术诊断，不修改队列协议。待重新 gofmt、测试、构建部署并复验生产详情页。
- 2026-08-29 约 12:50 · Codex：运行版复验发现上一版聚合收口未覆盖 8097 历史任务的 `retrying` 状态；虽然 Master 当前 Translation Version 已完成 Recovery，详情字段仍显示“翻译未完成”。按本阶段允许的最小 Runtime/UI 修复，将 recovered claim 查询范围扩展到 `failed/retrying`，不改 8097、不删除队列历史；已完成 gofmt、资料库回归测试并重新构建生产版，待浏览器最终确认。
- 2026-08-29 约 12:47 · Codex：运行版验收发现真实可见问题：Aiko 已 Recovery 成功且 Master translation pipeline 为 `completed`，但 8097 历史 failed 仍使详情字段显示“翻译未完成”。按本阶段范围新增只读聚合收口：当持久化 Recovery claim 为 `recovered` 且当前活动 Translation Version 已不同于 Recovery 基线时，将用户态 `task_status` 推导为 `completed` 并重算 `failed_fields`；保留原 queue job、`failure_reason`、attempts 等诊断，不删除或回写 8097。新增 Go 回归测试覆盖 recovered 隐藏历史失败与 needs_user 保持 failed 两条路径。待重新构建部署并做浏览器验收。
- 2026-08-29 约 04:12 · Codex：已停止临时 `denova-model-fix.exe` 验证实例；Aiko/Kate 测试 8097 任务按审计保留后标记删除，Recovery claim 移入 `denova-src/output/recovery-test-claims`，日常 `qwen25-local` 配置未改动。
- 2026-08-29 04:10 · Codex：**完成 Master capability 修复后的真实 Recovery 验证**。`ManifestForTool` 不再把 `create_master_proposal/validate_master_patch/apply_master_patch` 误归类为普通 `file_write`；`file_write=false` 时三个受控工具均可调用，`create_file/write_file` 仍被拦截。首次用错误冒险工作区测试时，Apply 正确拒绝了不属于当前冒险的导入事务；切换到导入事务所属的 `KT` 后，`qwen25-local → qwen2.5:7b` 只启动 1 次，成功生成合法 Recovery Proposal，Validate/CAS/Apply 成功，新 Translation Version `1ef1b9b5-807c-53b0-af06-da3f6999ec49` 激活，旧版本保留，Master translation pipeline 为 `completed`，Recovery claim 为 `recovered`。8097 失败任务仍作为运行队列历史记录存在，Runtime 聚合仍可显示该历史 failed，但不影响 Master Pipeline 完成；本阶段未改 8097 或聚合规则。Kate 的高风险 `character.system_prompt` 回归中，Qwen 未生成 Proposal，claim 以“未生成唯一可验证建议”进入 `needs_user`，没有执行 Apply；因此确认未越过高风险边界，但尚未形成“生成候选后被风险规则拦截”的完整高风险样例。未做 cloud default 对照，因为 Qwen 已能生成结构合法 Proposal 并完成低风险恢复闭环。测试后配置仍为 `qwen25-local`；测试任务/claim 保留审计后清理，临时验证进程待停止。
- 2026-08-29 约 03:52 · Codex：定向测试已确认三个 Master 工具实际绕过了 `file_write` 拦截；测试失败仅因既有中间件会在成功正文后追加工具结果元数据，已将断言改为检查成功正文，不改变生产逻辑。
- 2026-08-29 约 03:48 · Codex：修正 Master capability 定向测试中的包名引用，将测试使用的 `AgentKindToolAgent` 改为现有配置包中的 `config.AgentKindToolAgent`；不涉及生产逻辑。准备重新执行 gofmt、Master capability 定向测试及 Go 编译验证。
- 2026-08-29 03:44 · Codex：**开始按批准范围修复 Master 专用工具 capability 误分类**。`ManifestForTool` 增加 Master 受控工具名单 `create_master_proposal/validate_master_patch/apply_master_patch` 的优先分类，使其不再继承 `create_*`/`write_*` 到普通 `file_write`；普通文件写工具仍保留原拦截规则。新增 middleware 定向测试覆盖三个 Master 工具在 `file_write=false` 时可进入受控逻辑，以及 `create_file/write_file` 仍被阻断。待完成 gofmt、定向测试并用现有 `qwen25-local` 重跑真实 Recovery E2E。
- 2026-08-29 03:26 · Codex：**第六阶段模型配置定点验证完成，但成功 Recovery E2E 被真实发现的既有 Master 工具能力误判阻塞**。用户配置已通过现有 `agent_models.tool_agent.profile_id` 指向 `qwen25-local`；重启临时 Denova 后 `/api/settings` 有效配置与 Agent 日志均确认 `tool_agent / http://127.0.0.1:11434/v1 / qwen2.5:7b`，未再访问远程模型。真实 8097 失败任务成功触发唯一 Recovery claim，Agent 只启动 1 次并读取了流水线/字段/翻译状态；但 `create_master_proposal` 因通用 `create_*` 名称规则被误判为 `file_write` 工具，在 Master 已关闭 `file_write` 的安全配置下被拦截，未生成 Proposal，因此未进入 CAS/Apply/新 Translation Version/recovered。按用户边界未放宽权限、未改 Proposal/CAS/Runner/8097；测试 claim 已归档到 `denova-src/output/recovery-test-claims`，测试队列任务保留审计后删除，生产 8080 未修改。新增 Tool Agent 选中 profile 与默认回退解析测试通过；`gofmt`、完整 `go build ./...`、配置定向测试和 Master/高风险资料定向测试通过。该工具能力映射问题需用户批准后另行定点修复。
- 2026-08-29 03:14 · Codex：**开始第六阶段定点修复：Master Agent 模型配置选择**。只读追踪确认 Master Agent 复用 `AgentKindToolAgent → ResolveAgentModel`，当前用户配置存在 `qwen25-local` 但未设置 `agent_models.tool_agent.profile_id`，因此按既有规则回退到显式远程 `default` profile。按用户批准的最小方案，新增 Tool Agent 显式 profile 与默认回退的解析回归测试，并仅在 `<project-root>\denova\.denova\config.toml` 增加 `[agent_models.tool_agent] profile_id = 'qwen25-local'`；未修改 Resolver、Agent Runner、Ollama 接入、Recovery 架构或 8097 协议。待完成 gofmt、定向测试、重启后的实际模型确认及 Recovery/needs_user 回归。
- 2026-08-29 02:00 · Codex：**完成第五阶段 Master 自动恢复调度器**。新增 `internal/app/master_recovery.go` 独立后台 Orchestrator，不由 Runtime/Pipeline GET 触发；读取 8097 现有 `status/attempts/updated_at`，以“已实际尝试、稳定 failed、队列未暂停且超过 5 秒窗口”作为当前协议下的最小最终失败判断。新增 `internal/book/master_recovery.go` 持久化 recovery claim，key 为 `master_item_id + field_path + input_revision + source_sha256` 的 SHA-256，同一失败版本只允许一次 Agent Recovery，重启后仍去重。闭环为 claim → 受限 Master Agent → 唯一 Recovery Proposal → Validate/CAS → 低风险自动 Apply → Master Pipeline 复核；高风险、歧义、CAS 冲突、无 Proposal 或复核失败进入 `needs_user`。Runtime 增加 `attempts/updated_at/final_failure/recovery_status`，前端只显示“Denova 正在处理/已完成/需要确认”，底层诊断仍保留。未修改 8097 队列核心、Master Agent 安全边界、Workspace Change、导入/HY-MT 主流程或其他节点。`pnpm check:i18n`、`pnpm build`、资料库定向 Vitest（2 文件 3 项）通过；Go/gofmt 仍不可用，新增 Go 代码与测试未实际编译执行，待工具链补齐。
- 2026-08-29 01:39 · Codex：**完成总资料库与节点工作台第四阶段 Master 专用安全适配层**。后端新增 `master_runtime.go`（Master + 8097 Runtime Queue 只读聚合）、`master_agent.go`（字段级 Proposal/Patch、CAS、候选/采用版本生命周期）、`agent/master_agent_tools.go`（8 个单资产单字段受限工具）和 `app/master_agent.go`（复用既有 Agent Runner/Task/SSE）；新增 Runtime、Proposal 列表/创建/验证/应用和 Master Agent 任务 API。`ApplyMasterProposal` 强制校验 `master_item_id/field_path/source_sha256/input_revision/base_translation_version`，候选不覆盖活动译文，确认采用追加新 Translation Version；前端详情页增加 Denova 精修入口、候选结果与采用按钮，默认不展示技术错误，详细诊断仍折叠保留。未修改 Workspace Change、导入主流程、HY-MT 核心、Master→Adventure 同步、stale、Warning 持久化或历史数据。前端 `pnpm check:i18n`、`pnpm build`、资料库 Runtime/Library 定向 Vitest 通过；新增 Master 候选生命周期 Go 测试但本机无 `go/gofmt`，未能实际编译/执行，待工具链补齐后验证。当前 Agent recovery 由受限任务入口触发，8097 既有重试仍保持后台优先；自动队列 watcher 尚未接入，避免让只读查询 API 产生副作用。
- 2026-08-29 00:58 · Codex：**完成总资料库节点工作台第三阶段：字段级翻译聚合与基础处理**。新增 `master-library-runtime.ts`，按 `master_item_id + field_path` 只读合并 Master Translation Version 与 8097 Runtime Queue，保留 `task_status/content_version_status/translation_version/failure_reason/review_required/input_revision` 等诊断字段，不写回 Master。资料库详情页增加节点点击查看、字段级翻译状态、进度、折叠诊断和失败任务重试；普通 UI 仅显示“处理中/已完成/需要确认/翻译未完成/实时状态暂不可用”，技术错误不默认暴露。解析/规范化仍只展示现有 inferred 证据，检查与 usable 继续使用后端 Pipeline Status，stale 规则未扩展；未接 Agent、Proposal/Patch、自动修复、导入/HY-MT 主流程或 Master→Adventure 同步。新增聚合与重试回归测试；Vite/TypeScript、i18n、3 项定向 Vitest 通过。完整 Vitest 仍有既有 `src/main.test.tsx` 卡住失败/CSS 解析警告；Go/gofmt 仍不可用，待工具链补齐。
- 2026-08-29 00:36 · Codex：**完成总资料库与节点工作台第二阶段只读界面**。新增 `features/library/LibraryView.tsx`：资料库独立页面支持名称/来源搜索、`record_kind`、`semantic_type`、`availability` 筛选、分页和 usage 数量；资产详情页只读展示基本信息、Source/Revision/SHA、翻译版本、问题、多个 Adventure usages，以及七节点状态概览。新增总资料库共享入口并接入现有 `FeaturePageShell`、`WorkbenchShell`、`ModeRouter` 和 WorkspaceMode；详情页复用第一阶段四个查询 API，未修改导入/HY-MT/Agent/Patch/自动重跑/实例同步，也未迁移历史数据。新增中英文 i18n 与页面回归测试；`pnpm build`、`pnpm check:i18n`、资料库定向 Vitest 通过。完整 Vitest 仍在既有 `src/main.test.tsx` 卡住并失败，伴随 CSS stylesheet 解析警告；Go/gofmt 当前不可用，Go 验证待工具链补齐。本阶段发现第一阶段 API 数据可支持只读页面，但节点 parse/normalize/check 的 `inferred/reason` 仍需在 UI 中明示，未扩大数据层。
- 2026-08-29 约 · Codex：**完成总资料库只读查询层与 Pipeline Status 第一阶段**。新增 `MasterLibraryStore.ListAssets/GetAsset/GetAssetPipeline` 只读投影和 `/api/library/assets`、详情、pipeline、translations、usages 接口；列表支持查询、record_kind、semantic_type、availability、分页。状态由现有 Master source/item/translation/import/instance 数据推导，区分 `staging/usable`，来源 revision 不一致时标记下游 `stale` 并保留失效原因；加入 `content_version_kind` 视图（HY-MT/候选/已确认润色推导），未新增重复状态真源或修改导入主流程。前端新增 `master-library.ts` 客户端类型与测试；TypeScript/Vite 生产构建通过。Vitest 定向运行在本机启动后长时间无输出并已停止；本机未安装 Go 工具链，Go 单测待工具链可用时执行。未迁移总库数据。
- 2026-08-28 13:16 · Codex：修复用户导入角色卡时的 Master 翻译队列拒绝，并补齐总库的书架显示名。根因是 8097 `denova-bridge` 自 00:34 驻留，未重载本轮 12:50 才加入的 Master 字段路径/写回策略校验；仅重启该桥接服务后，已保留的 Aiko 导入事务（原件、规范条目、7 个待译字段）成功重新入队，未重导或删除数据。当前队列因 `game` 自动暂停而保留 7 个 queued 任务，离开游戏后继续。`MasterLibraryStore` 初始化新增 `book.json`，现有运行总库也已补写，`/api/books` 已验证显示“叙界总资料库”。验证：8097 health 通过、创建 7/跳过 0、队列身份字段完整、书架 API 可见；本机未安装 Go，新增 Go 单测待具备 Go 工具链时执行。
- 2026-08-28 12:54 · Codex：补充 Master 默认导入的 API 客户端回归，确认请求固定携带 `management_mode=master_managed` 且字段级翻译目标可归一化；相关前端定向验收现在为 10 项全通过（替代上一条记录中的 9 项计数）。
- 2026-08-28 12:50 · Codex：**叙界总资料库 Master-first 导入/翻译流水线已实施并部署**。普通“加载资料”现在默认 `master_managed`：完整 PNG/JSON 原件先不可变归档到相邻 `narraverse-master-library`，角色卡规范化为结构化 CharacterTemplate、世界书逐 entry 保存 `source_semantics/runtime_semantics`，安全字段生成/激活独立 `translation_version_id` 后才创建当前冒险独立 Lore 实例；高级复选框可明确选择 `unmanaged_direct` 临时直导。新增四级 UUID 契约（source/master item/translation/import-instance）、按 SHA 去重且不按文件名误合并、`id=0` 与无 ID 指纹身份保护、字段哈希回写校验、幂等事务/finalize，以及高风险 system prompt 的人工确认通道。8097 队列现支持带 Master 身份的受限 field path 和 `master_auto/master_review`；前端完成后回写总库，非当前 Lore。验证：`go test ./internal/book`、Python `LoreTranslationTests` 22 项、相关 Vitest 9 项、TypeScript/Vite 生产构建、i18n 3164 键、Denova HTTP 200、8097 健康、105 个前端静态文件 SHA 一致均通过。已部署 `denova/denova.exe` 和 `denova/web`，完整可恢复备份为 `.workbuddy/backups/master-pipeline-20260828-1247/`，同目录保留 `denova.exe.previous-20260828-1247`/`web.previous-20260828-1247`。全仓 Go/前端套件的失败仍为既有 Windows fsync/symlink/inode 与并行 DOM 5 秒超时，未归因于本次功能。
- 2026-08-28 07:35 · Codex：**完成叙界总资料库与导入翻译流水线批准规格修订（仅设计，未实施）**。用户确认普通冒险默认“先入总库、翻译校验、再生成冒险实例”，高级模式才允许 `unmanaged_direct` 临时直导。现行规格冻结原文件/清洗翻译/总库/冒险实例四层架构，新增 `translation_version_id` 形成四级 ID，拆分稳定 `record_kind` 与可变 `semantic_type`，将角色卡规范形态改为结构化 CharacterTemplate，完整保留 World Info 原始语义，并明确 HY-MT 全安全字段初译、Agent 选择性润色、旧冒险不批量改写及 MVP/二阶段边界。旧 V2 已标记为历史基线；2026-08-26 已部署规格新增 CURRENT/PLANNED 衔接说明。本轮未修改业务代码、未创建或迁移总库数据，未运行代码测试；当前目录不是 Git 仓库，无法提交 commit。
- 2026-08-27 19:05 · Codex：**接入本地 Qwen2.5-7B 管家 profile**。先备份到 `.workbuddy/backups/local-butler-20260827-184351/`，确认本机 RTX 4060 Laptop 8GB 显存不适合 23GB 的 qwen3.6；通过 Ollama 下载并校验 `qwen2.5:7b`（4.7GB，Q4_K_M）。用户级 Denova 配置新增 `qwen25-local`，使用 `http://127.0.0.1:11434/v1`、`sk-local-free`，互动故事显式绑定该 profile；保留云端默认和旧 qianwen profile。Ollama OpenAI 兼容接口实测返回“本地模型连接成功”，Denova 重启后 `/api/settings` 实测生效 profile 与绑定正确；前端生产构建通过。当前仍未新增独立 provider 代码，因为 Denova 已有统一 model_profiles/Agent 选择链，避免重复实现。
- 2026-08-27 04:32 · Codex：**修复 HY-MT 空回复/拒绝误报并让失败任务可直接修改**。复核推翻了 03:50 的“模型真实拒答”判断：HY-MT 是专用翻译模型，长提示模板会被当作正文；正文中的正常“抱歉”也被旧校验器误判为拒绝；只含受保护代码块的分片则会因无可译文本返回空。现统一为约 180 字符有序分片、已有中文行/中文片段原样保护、只注入当前片段命中的术语、极简 HY-MT 短指令、无英文的保护块直接透传，并将拒绝检测收窄到真实拒绝句。队列中的 Kate 正文（8611 字符）实测完成为 `pending_review`，译文 3884 字符/2203 个汉字，保留 `{{user}}` 30 个与 `{{char}}` 6 个，未自动覆盖正文。失败、冲突、待确认任务均显示带文字的“修改”按钮；前端构建通过并部署，Python 语法检查通过，8097 桥已确认更换 PID 后加载新代码。
- 2026-08-27 04:11 · Codex：**翻译队列新增删除与冲突对照编辑**。后端新增删除接口：非运行任务从活动队列移除并标记 `deleted`，任务 JSON 保留审计；运行中任务必须先取消。冲突任务新增原文/译文双栏编辑弹窗，采用时读取当前 LoreItem 并执行版本保护后写回。前端已部署到 `denova/web`；TypeScript/Vite 构建通过。定向 Vitest 命令受当前 pnpm 脚本环境限制未成功运行，未宣称测试全绿。
- 2026-08-27 04:03 · Codex：**将翻译分片与保护修复固化为通用能力**。`LocalTranslator` 的正文翻译统一使用约 320 字符有序分片（名称/简介保持原策略），每片独立缓存后拼接；中文主体可原文透传；模型回显提示模板会剥离；受保护变量/URL/代码/正则按计数补回；缓存、提示词和校验版本递增。该逻辑同时覆盖资料库单条翻译、批量队列和全量导出，不针对单个角色特判。Python 语法检查通过，桥接已重启加载新版本。
- 2026-08-27 03:50 · Codex：**修复/诊断翻译失败类型**。玩家角色简介重试后已采用；玩家角色正文因任务基线早于当前条目修改而触发真实冲突，禁止覆盖；Kate 正文先修正过宽的 `/.../` 正则保护规则并重试，最终返回“空内容或拒绝话术”，确认是 HY-MT 对该长篇露骨乱伦内容拒答，不是 Ollama 离线或校验器误报。翻译器版本/缓存版本已递增，桥接重启加载新规则；保留英文原文，未强行写入失败译文。
- 2026-08-27 03:40 · Codex：**清理已删除 `ggg` 工程的翻译队列**。确认队列曾因 `ggg` 目录删除仍保留 1182 条排队/运行任务，并会消耗 HY-MT；先重新设为 `manual` 暂停，再通过现有取消接口将这 1182 条任务标记为 `cancelled`，未删除任务文件。复核结果：队列暂停、运行任务 0、`ggg` 排队 0；`666` 的 3 条任务也未被删除（仅保留原有状态）。
- 2026-08-27 03:30 · Codex：**修复“加载角色卡与设定书”`Cannot read properties of null (reading 'map')`**。根因是 Go 空切片在 JSON 中序列化为 `null`，前端直接渲染 `warnings.map`，同时导入结果数组也存在同类风险。API 边界现将素材列表、预览警告和导入结果数组统一归一化为空数组；界面增加防御式渲染；后端预览/结果也保证空集合输出为 `[]`。TypeScript/Vite 构建通过并同步到 `denova/web`，Denova HTTP 200。Go 单元测试因当前环境未安装 `go` 命令未能运行。
- 2026-08-27 03:17 · Codex：**完成《叙界总资料库设计（第二版正式规格）》**。在第一版基础上冻结三层 ID（`source_id` / `master_item_id` / `instance_id`）、`schema_version`/`data_version`/`revision`、manifest 双向索引、字段级 Base/Local/Remote 三方合并、active/deprecated/archived 生命周期、结构化自动触发条件、追加式审计日志与幂等/引用完整性验收。第一版保留为历史基线；尚未开始备份、迁移或代码实现。规格见 `docs/superpowers/specs/2026-08-27-narraverse-master-library-design-v2.md`。
- 2026-08-27 02:58 · Codex：**完成《叙界总资料库设计》正式规格与自审，尚未开始数据迁移**。用户锁定：总库使用独立 Denova 工程 `narraverse-master-library`（界面名“叙界总资料库”）；先做不可变全量备份；哈希相同自动去重、同名异内容保留版本；三份代表素材先试译；HY-MT 初译通过结构校验后直接成为中文工作版，在线强模型润色为可选升级并由 `source_id` 清单精确关联原文/初译/输出；其他冒险从总库选择性复制受管快照；中途加载角色默认只“加入世界”；所有冒险的角色记忆、关系和成长完全独立。自审修正了翻译状态歧义，并明确缺少必需译文的条目不得启用或被冒险加载。规格见 `docs/superpowers/specs/2026-08-27-narraverse-master-library-design.md`。当前项目无 Git 历史，无法提交规格 commit。
- 2026-08-27 01:49 · Codex：批量翻译减负修复已部署到 `<project-root>\denova\web`，8097 Bridge 已重启加载后端清理逻辑；部署前快照为 `<project-root>\.workbuddy\backups\translation-queue-20260827-014808`。运行验收：Denova HTTP 200、Bridge v5 正常、105 个静态源文件哈希差异 0；全局活动队列 178 项中 `applied=0`、待确认 9 项，证明旧已采用索引已从轮询结果清除，审计 job 文件未删除。
- 2026-08-27 01:45 · Codex：**解决批量翻译待确认堆积与任务抽屉卡顿**。按用户确认，今后“批量翻译”产生的正文与元数据均在原文/revision 未变化时自动写回；单条正文仍保持人工确认。现有积压任务可点“采用全部待确认”串行安全写回，变化或缺失条目转冲突而不覆盖。任务抽屉只渲染最近 100 条可操作任务，已采用历史不再展示；后端在任务采用后从活动 `queue.json` 移除 ID，旧 applied 记录会在状态查询时自动清理，独立 job 文件继续保留审计，从根源减少 1.2 秒轮询的读盘、传输和 React 渲染压力。验证：Python 26 项、翻译定向前端 8 项、i18n 3151 键及生产构建通过；完整前端并行套件 898/906，8 个无关 5 秒时序失败拆分复跑 55/55 通过，判定为并行负载波动而非本次回归。
- 2026-08-26 20:40 · Codex：**Denova 后台翻译队列与资料加载库已实施、验证并部署**。资料库现支持单条/当前工程批量加入 HY-MT 持久串行队列，名称走中文化模式，简介可安全自动写回，正文只生成待确认译文；顶栏任务抽屉支持暂停、继续、取消、重试，进入游戏会在当前字段结束后以独立 `game` 原因暂停，离开游戏不会覆盖手动暂停。新增“加载资料”统一浏览叙界知识库与多选上传 PNG/JSON；角色卡复用 Tavern 解析，设定书逐 entry 导入且无 200/500 上限，保存完整原件、来源清单、稳定 ID、managed hash，并在检测到 Denova 手改时保留原条目和创建冲突副本。新导入只自动排队名称与简介。HY-MT 实测 `Alice → 爱丽丝`，NSFW 文本、`{{char}}/{{user}}` 与 URL 均保持完整。验证：Python 27 项、相关 Go 包、Denova 前端全套 903 项、i18n 3148 键及生产构建通过；实际知识库 112 个素材可列出，二进制下载字节数/SHA 一致，真实 153-entry 设定书与含内嵌 lore 的英文角色卡预览正确。已部署到 `denova/denova.exe` 与 `denova/web`，可恢复备份为 `.workbuddy/backups/deploy-20260826-203412`。全量 Go 套件仍受既有 Windows 目录 fsync、符号链接权限和 POSIX mode 断言影响，未误报全绿；Go embed 还会拒绝一个既有含中文全角括号的资源名，因此本次使用 Denova 已支持的外置 `web` 目录部署。
- 2026-08-26 15:44 · Codex：**完成并落盘 Denova 后台翻译队列与资料加载库设计**。用户逐项批准：单条＋当前工程批量翻译；名称强制中文化；新导入素材只自动翻译名称与简介并在修订匹配时自动保存；正文只生成待确认译文；翻译与游戏严格分开，进入游戏暂停、离开恢复；资料加载同时支持浏览叙界知识库和多文件上传，角色卡复用现有解析器、设定书逐 entry 导入且无 200/500 条上限。设计采用 8097 持久串行队列＋Denova 全局协调器，补入缓存/提示词/校验器版本、缓存重校验、失败可见、请求竞态保护、来源清单和手改冲突保护。规格见 `docs/superpowers/specs/2026-08-26-denova-background-translation-and-material-library-design.md`；本轮只修改设计与协作日志，尚未修改业务代码或运行实现测试。当前目录不是 Git 仓库，无法提交设计 commit。
- 2026-08-26 15:06 · MainAgent：**修复翻译结果为英文/提示词回显问题**。用户反馈翻译出来还是英文。排查发现：①旧翻译缓存（120个文件）中保存了模型回显提示词结构的错误结果（含"上下文：""术语：""来源："标签和英文术语表），重试时命中旧缓存；②原提示词使用英文 "Context:/Terminology:/SOURCE:" 标签，模型将这些标签和术语表也翻译输出，导致译文被提示词回显污染。修复：①重写提示词为中文指令，明确要求"只输出译文本身，不要输出任何标签、说明、术语表或原文"，使用"参考上下文：""参考术语表：""需要翻译的文本："标签；②增强回显检测，新增中文标签检测（参考上下文/参考术语表/需要翻译的文本/上下文/术语表/来源）；③清除全部 120 个旧翻译缓存；④更新 Python 测试以匹配新提示词格式，22 项全通过；⑤重启 8097 桥接服务。验证：用含 character/personality/scenario/consent/aftercare/dirty talk 的英文文本测试，返回纯中文译文"爱丽丝是一位勇敢的英雄，她致力于拯救世界。她性格坚强，始终注重同意和事后照护的问题。在这种情境下，使用淫语是被允许的。"，无提示词回显，NSFW 术语正确翻译。
- 2026-08-26 14:30 · MainAgent：**修复术语表校验导致翻译失败问题**。用户反馈翻译时出现"角色=>角色场景=>场景性格..."术语链报错。原因：全局术语表包含 character/scenario/personality/consent/dominant 等常见英文词，校验逻辑强制要求原文包含这些词时译文必须出现对应中文，模型用同义词即触发"译文未遵守术语表"错误。修复：①精简术语表从 11 个减到 3 个，只保留 NSFW 专业术语（aftercare/breeding kink/dirty talk）；②术语表校验从强制报错改为仅参考不报错；③重启 8097 桥接。
- 2026-08-26 12:58 · MainAgent：**Denova 资料库翻译功能优化：容错 + 一键翻译**。①后端 `translate_lore_fields()` 从"任一字段失败整体失败"改为"部分成功"模式：单个字段翻译/校验失败时保留原文并记录到 `failed_fields`，其他字段仍正常翻译。②前端改为一键翻译：点击「译为中文」按钮直接翻译正文并自动采用，移除弹窗流程。（注：一键翻译前端改动已于 13:18 撤销，恢复弹窗模式；后端部分成功模式保留。）
- 2026-08-26 11:47 · MainAgent：**Denova 资料库单条本地翻译功能已完成实施与部署**。在资料库条目编辑器正文上方新增「译为中文」按钮，弹窗支持字段选择（正文默认勾选，名称/简介可选）→ 本地 HY-MT 串行翻译 → 原文/译文双栏预览 → 采用译文。后端 `ExportJobManager.translate_lore_fields()` 复用现有 `LocalTranslator`（缓存/术语表/占位符保护/校验），独立 `_translation_lock` 串行化，任一字段失败整体失败；桥接新增 `POST /api/denova/translator/translate`（仅回环地址，2 MiB 上限，8 种错误码）。前端新增 `local-translation.ts`（支持 AbortSignal）和 `LoreTranslationDialog.tsx`（快照防覆盖、stale 检测、切换条目自动 abort）。预览阶段零写入，点击「采用译文」才修改 draft 并进入现有 `useLoreItemAutosave` 自动保存。不翻译标签/关键词/ID/类型/加载策略/来源信息，不调用付费 API，不增加 Denova Agent 工具。测试：Python `LoreTranslationTests` 15 项全通过，前端 `local-translation.test.ts` 5 项 + `LoreTranslationDialog.test.tsx` 8 项全通过，TypeScript 检查通过，i18n 3082 键对齐，Vite 生产构建成功并同步到 `denova/web`（备份 `backup/denova-web-20260826-114419`）。
- 2026-08-26 10:40 · Codex：补充翻译验收保护：检测 HY-MT 回显 `Terminology/Context/SOURCE` 提示模板的异常结果，字段改为保留英文原文并记录翻译失败，不再把提示回显标成 `translated=true`。新增回归覆盖回显拒绝，Python 7 项测试通过；8097 v5 已重启加载。
- 2026-08-26 10:12 · Codex：读取验收暴露本次导出的真实缺陷：嵌入角色卡含截断标记，7 本设定书共 350 entries 却被写成 7 个整本 JSON lore。已修正 `denova_full_export.py`：无 `source_ref` 时唯一知识库候选优先回源；嵌入 lorebook 的 `content` JSON 包装先解包；嵌入副本若含截断则标记 `incomplete/truncated`；manifest/report 不再硬编码截断为 0。新增回归覆盖唯一源回源与逐条 entry，Python 7 项测试通过；8097 v5 已重启加载修复。当前 Denova 旧工程需从叙界重新导出，不能把旧 9 条 lore 当作修复结果。
- 2026-08-26 09:32 · Codex：按用户确认已实际下载并注册 HY-MT。安装器遇到 Hugging Face siblings 接口省略 LFS 字段时，使用此前官方文件页核验的大小/哈希并继续校验；最终下载 1,133,080,512 bytes，SHA-256 `4383ac0c3c8e476de98ff979c2a3f069f8c4fb385e7860cf2d28da896cc477c7`，Ollama 注册为 `hy-mt1.5:1.8b-q4_k_m`。短文本实测翻译成功，`{{char}}` 与 NSFW 术语均保留；Qwen/Llama 未改。
- 2026-08-26 09:12 · Codex：叙界→Denova 全量保真导出已完成构建与部署。Denova 前端生产构建成功并同步到 `denova/web`，部署前备份为 `backup/denova-web-20260826-090857`；运行态 8080 主入口/叙界入口均 HTTP 200，8097 桥已从驻留 v4 重启为 v5，健康接口确认 `fullExport=true`，翻译状态确认 Ollama 在线、HY-MT 尚未安装且原有 `qwen3.6:latest`/Llama 均保留。最终通过 Python 6 项、Node 导出/档案/未来同步/加载库/三步向导等回归、Denova Vitest 18 项、TypeScript+Vite 构建及静态文件哈希核对；本机无 Go 工具链，且本轮未修改 Go 源码，因此 Go 定向测试明确未执行。
- 2026-08-26 09:00 · Codex：全量导出第二阶段已完成：官方 HY-MT 安装器、逐字段翻译/缓存/术语覆盖/失败保留英文、叙界导演退役、Denova 导入工程手动导演默认与后台折叠、永久档案驱动的无截断小说分卷均已落地；重新生成的 `app/local_library.js` 已为 50 本设定书和 39 张角色卡写入 `source_ref v1`。后端 6 项测试、前端 v4 无头测试、对话档案测试、Denova 组件 18 项测试及旧未来同步测试通过；仍需做完整构建、静态同步、部署与最终回归。
- 2026-08-26 04:05 · Codex：已开始实施获批的叙界→Denova 全量保真方案。当前完成 `source_ref v1` 生成、IndexedDB `conversation-archive v1` 追加式档案及发送/编辑/回溯/分支钩子；新增独立 `export-bundle v4` 任务后端和创建/分块上传/预检/启动/暂停/继续/取消/查询接口；手动导出前端已改为直接上传永久档案与完整素材，取消剧情摘要/小说/设定 AI 生成依赖，并提供恢复等级、模型、冲突、翻译失败和截断验收报告。原有 `/api/denova/sync` 未改。JS/Python 语法检查已通过；模型安装、Denova 导演默认值、小说分卷与完整回归仍在进行中。
- 2026-08-26 03:36 · Codex：完成《叙界导出 Denova 全量保真与低成本翻译设计》初稿及自审。经用户逐项确认，方案锁定为：完整原件回源、对话实录作为真源、小说化为可选衍生、本地 HY-MT1.5-1.8B 英中翻译、结构化资料库映射、NSFW 术语表、导入清单与冲突保护，以及“退役叙界重复导演、Denova 导入故事默认仅手动导演”。自审补清了隐藏思考不导出、系统摘要不混入对话、全部存储不等于每轮全部注入、叙界导演数据只保留兼容以及导入工程的导演默认值边界。当前仅写设计文档，待用户最终复核，未修改业务代码或运行测试。
- 2026-08-25 17:39 · AI 1：已将「云书斋小说全本抓取」工作流固化为共享 skill `yunshuzhai-novel-fetcher`，分享进 `<project-root>\.workbuddy\skills\yunshuzhai-novel-fetcher\`（含 `SKILL.md` 工作流文档 6628字 + `scripts/fetch_novel.py` 通用参数化脚本模板 7063字）。该 skill 可从 yunshuzhai.com 抓取任意小说全本打包 TXT——WebFetch 提取目录页 chapter_map → Python+requests 并发(max_workers=5) → regex 4模式提取正文 → 清理HTML实体 → 合并 TXT。已验证两本书：仙子的修行(BOOK_ID=25365,115章,134万字)、反派：我的母亲是大帝(BOOK_ID=14862,85章,149万字)。下次抓取新小说只需修改脚本 CONFIG 区块(BOOK_ID/TITLE/AUTHOR/STATUS/chapter_map)。触发词：抓取小说、打包小说、下载全本、同样的打包。allowed-tools: Read,Write,Bash,WebFetch。
- 2026-08-25 已新增项目交接文档：结合源码、Denova 运行数据、部署快照和本协作日志，整理三条玩法线、真实数据真源、启动排障、最近修正、遗留问题和新 AI 首日接手顺序。交接文档见 `<project-root>\docs\2026-08-25-项目交接文档.md`；本轮只做文档与文件状态核对，未运行测试或构建。
- 2026-08-22 手机端文字排版已修复：消息气泡补齐 flex 收缩边界，AI/玩家文本采用正常中文断行和更舒展行高，手机弹窗减小内边距并放宽辅助说明行距；Denova iframe 与主样式同时加入 `v=20260822`，避免旧缓存继续显示拥挤排版。
- 2026-08-22 Denova 工作台书籍标题真源已修正：顶部书籍切换器、展开栏按钮和导入弹窗统一优先读取当前 `workspace` 对应的书架记录名，不再让 `summary.title` 这类派生摘要标题反向覆盖导航标题；书籍切换后“标题还是上一册、内容已是下一册”的错位已从真源上切断。
- 2026-08-16 手机端适配已修正：Denova 宿主的 390px iframe 会自动启用 `mobile-mode`，无需再手工添加 `?mobile=1`；顶部保留 40px 汉堡按钮，冒险列表/新建/API 设置改为左侧抽屉，选中冒险、打开新建或设置后自动收起；右侧状态面板继续按既定简洁方案隐藏。
- 2026-08-15 已完成《现有游戏项目分析报告》：确认当前产品是 Denova 统一宿主下并存“叙界在线 AI 冒险 / Denova 结构化游戏 / 叙界离线小说游戏”三套运行逻辑，入口已统一但游戏状态真源尚未统一；同时确认嵌入态场景预设消息误切 Denova 模式、Profile/预设未进入主存档 JSON、在线战斗仍由 AI 裁定、离线游戏无持久存档等现状。报告见 `<project-root>\docs\2026-08-15-现有游戏项目分析报告.md`。
- `Launch-Narraverse.cmd` 现为唯一一键入口：自动启动 8097 Denova 同步桥、可选 8098 Pixiv 桥、Denova，并打开 `/?mode=narraverse`；仅此后新建的冒险自动生成 `syncMeta`，本地保存后推送设定/摘要/素材/章节，返回叙界时增量拉回 Denova 的设定、章节与 lore，旧冒险不迁移。2026-08-15 15:45 已实测入口返回 8080、桥接 v4 与嵌入资源均 HTTP 200。
- 叙界、Denova 运行版与 Denova 源码已统一到英文根目录 `<project-root>`：`app/`、`denova/`、`denova-src/`、`knowledge-base/`；核心维护文件已改英文名。Denova 中“写作 / 游戏 / 叙界”三模式、同源持久 iframe、主题/语言协议和首次存档迁移提示均已在迁移后安装版验证。
- 项目是一个**纯静态单页文字冒险平台**，含两条玩法线：
  ① **LLM API 驱动的在线文字冒险**（以 `app/app.js` 为主，支持上下文压缩、前情摘要查看/编辑、角色状态追踪、横向时间轴事件记录、基于压缩上下文的 AI 冒险简介）；
  ② **离线小说游戏**（`app/game_engine.js` + `app/packs/limitless.js`，`GameEngine.open()` 入口，不依赖任何 API，离线可玩）。
- 最近（2026-08-08，AI 1）修复了一个**成长系统严重 bug**：提示词 `EXP + | 数值` 与解析器正则对不上，导致 EXP / 技能点 / 属性点永远解析不到、角色无法升级；并补齐了上下文摘要编辑、精简状态开关、时间轴横向化与事件化、AI 冒险简介等能力。同日又修复了**「同名剧情节点」重复堆积**问题，并把无头回归测试台**固化成可复用技能 `headless-frontend-regression`**。
- AI 2 已完成对 AI 1 借鉴 SillyTavern 代码的审查与修复（性能缓存 / 压缩后设定失效 / 关键词误报 / 单人场景精简等），详见功能记录 2026-08-08。
- 设计产物（Ardot）：平台早期在 Ardot 设计平台绘制了页面/功能设计示意图，后续明确页面设计图不纳入交付物并已删除该节点（`2:15`），其余设计图保留为视觉参考（非代码，不随 `app/` 部署）。文件 ID 待补。
- AI 1 另完成（较早，约 2026-08-08 14:30–14:45）：场景配图切换为 Qwen-Image-3.0-Pro（设置新增图片 API 分组）；弹窗改为仅叉叉关闭 + 全站 Emoji/样式打磨。详见功能记录（2026-08-08 约14:30 / 约14:45）。
- 校验方式：`node --check app/app.js`；本地服务 `cd <project-root>\app; python -m http.server 8099` → http://localhost:8099/

## 修改先后顺序说明
- AI 3（最早）→ AI 2（中期，基于 3）→ AI 1（最近，当前主力）

## 各 AI 负责时段
- AI 3：初期搭建（最早改动与坑）
- AI 2：中期修改（在 3 基础上的改动）
- AI 1：最新改动与当前状态（主导后续开发）
- 注：AI 2 / AI 3 请在「功能与修改记录 / 踩坑记录」中**补写自己负责时段**的内容；本文档由 AI 1 初始化骨架，各 AI 只写自己实际处理并确认过的部分，不确定项标注「待确认」。

## 铁律（每条都必须遵守）
1. 开工前：必读 `项目协作日志.md`（铁律 + 功能/踩坑记录 + 进行中任务）；**涉及代码改动时，先想到 `代码指南.md` —— 改结构/提示词/状态/渲染/交互前先翻指南定位函数与速查表，纯文案或非代码任务可不读**；若存在「进行中」任务，优先接续，不重复开工。
2. 开工中：每完成一件事（修复/新增/排查/生成），立刻追加一条日志，不要等收工才写。
3. 踩坑必记：遇到问题并解决后，无论大小都写进「踩坑记录」。
4. 格式：日期统一 YYYY-MM-DD，并附精确时间 HH:MM（如 `2026-08-08 17:51`；不确定时标 `约`，无法确定标 `（时间待补）`）；
   **功能与修改记录 / 踩坑记录一律按时间倒序，最新修改放最上方**；涉及文件写完整路径；每条记录带 AI 编号。
5. 只写自己实际处理并确认过的内容，不确定项标注「待确认」。
6. **代码经验沉淀（铁律）**：任何涉及代码**结构性改动**（新增/重命名函数、修改 LLM 协议或数据模型、改全局状态字段、修通一个隐藏 bug）后，把可复用的经验**立刻（不必等收工）同步更新进 `代码指南.md`** 对应章节——文件地图、数据模型、提示词管线、渲染管线、速查表等；纯文案/无结构变化的改动可不更新指南，但仍记协作日志。目标：让后续 AI 免重读全部源码。
7. **各 AI 行为潜质登记（铁律）**：每个 AI 在下方「各 AI 行为潜质登记」区**登记自己在文字冒险平台「总是稳定执行的某种行为 / 拥有的某种潜质」**；新增或变化及时更新；写他人部分只能依据公开记录推断并标注「（据协作日志推断，待本人确认）」，不编造不实内容。**Skill 不再逐个登记**——项目已提供共享技能文件夹 `<project-root>\.workbuddy\skills\`（如 `text-adventure-collab` 协作工作流、`headless-frontend-regression` 无头回归），3 个 AI 直接调用即可。
8. **先用先贡献（铁律）**：调用项目共享技能文件夹 `<project-root>\.workbuddy\skills\` 里的任何 skill **之前**，必须先把**自己**在本项目沉淀出的、对其他人也有价值的可复用 skill 分享进该文件夹（贡献先于使用）——固化为 `SKILL.md` 放进 `<project-root>\.workbuddy\skills\<你的skill名>\`，内容须含「何时调用 / 关键函数与锚点 / 避坑」。未贡献过的 AI 先补一份再调用他人；已贡献的（如 AI 1 的 `text-adventure-prompt-protocol`、AI 3 同步的 `headless-frontend-regression`）可直接调用。目的：避免「只取不存」导致文件夹枯竭、各 AI 重复造轮子。

## Skill 库位置与调用说明（2026-08-11 · AI 2 归档）
- **权威副本**：`<shared-skill-library>\`（含 `README.md` 索引；与其它 AI 工具统一收纳在 <external-tools-root>\）
- **运行时镜像**：`<project-root>\.workbuddy\skills\`（workbuddy/Claude 自动发现用，内容与权威副本保持一致，改 skill 两处同步）
- **现有 5 个 skill**：
  1. `text-adventure-collab` — 项目协作规范与代码改动工作流（开工/改代码/记日志前）
  2. `text-adventure-prompt-protocol` — 在线冒险线 LLM 协议与提示词工程（AI 1 贡献，改 app.js 提示词前）
  3. `headless-frontend-regression` — 无头回归测试（Node vm 沙箱真实加载 app.js，改核心流程后回归）
  4. `knowledge-base-governance` — 知识库治理（chub 下载/改名/元数据/分类/重生成）
  5. `stickman-tuning` — 火柴人动作/形象/节奏调参（AI 2 贡献）
- **调用方式**：Codex 读 `<shared-skill-library>\<名称>\SKILL.md` 按「何时调用/关键锚点/避坑」执行；Claude/workbuddy 直接走 `.workbuddy\skills\`。
- **新增/修改流程（铁律 8 延续）**：写 SKILL.md（含何时调用/关键锚点/避坑）→ 同步两处 → 更新 Skill库 README 索引 → 记日志。


## 各 AI 行为潜质登记（铁律 7 专用区 · 固定三段）
> 用途：让接手者一眼看出「这个活谁最合适 / 谁手上有现成 skill」。各 AI 自己维护自己的那段；非本人填写须标注待确认。
> **铁律 8 贡献状态**（调用共享 skill 前先贡献自己的 skill）：AI 1 ✅ `text-adventure-prompt-protocol`（在线线协议+提示词工程）；AI 3 ✅ `headless-frontend-regression`（无头回归，由用户级同步进项目文件夹）；AI 2 ⏳ 待贡献（火柴人调参 / 知识库治理流程，建议固化，待本人操作）。

### AI 1（当前主力 · 我）
- **总是执行的行为**：①开工前必读 `项目协作日志.md`，涉及代码改动时先翻 `代码指南.md` 定位函数；②每完成一件事立刻追加倒序日志（功能/踩坑都带 `YYYY-MM-DD HH:MM` + AI 编号）；③改动完必跑 `node --check` + 无头回归台 `.workbuddy/tmp/adv_harness.js`；④结构性变化同步更新 `代码指南.md`。
- **潜质**：提示词工程（SillyTavern 体系借鉴：World Info / 角色卡 V3 / 宏 / 群像扩展）、协议健壮性修复（成长系统 / 同名节点 / 标签泄漏）、把临时测试台**固化为可复用技能**。
- **说明**：Skill 不再逐个登记，统一走项目共享技能文件夹 `<project-root>\.workbuddy\skills\`；AI 1 已依铁律 8 贡献 `text-adventure-prompt-protocol`（在线线协议+提示词工程，见 `\.workbuddy\skills\text-adventure-prompt-protocol\SKILL.md`），另可直接调用 `text-adventure-collab` / `headless-frontend-regression`。

### AI 2（中期 · 本人确认）
- **总是执行的行为**：①开工前必读 `项目协作日志.md`，涉及代码改动先翻 `代码指南.md` 定位函数；②每完成一件事立即追加带精确时间的倒序日志、踩坑必记；③改动后必跑 `node --check`，核心逻辑写/跑无头行为测试（真实加载 app.js 验证，如 `test_a1_review` / `test_mandala` / `test_stickman`）；④离线小说线（引擎/设定包/火柴人）从零搭建与持续修复；⑤知识库治理（chub 下载、卡片改名、元数据、分类/NSFW 开关）；⑥审查他人代码后修 bug（如 SillyTavern 借鉴审查）。
- **潜质**：动画 / 人形化调参（用视觉模型循环评审从「不像人」提到 8/10）、离线游戏设计（事件图/检定/金手指/多结局）、知识库与数据治理、行为测试编写、性能/缓存优化。
- **说明**：Skill 统一走项目共享技能文件夹 `<project-root>\.workbuddy\skills\`，无需在此登记。

### AI 3（最早 · 据协作日志记录推断，待本人确认）
- **总是执行的行为**：项目早期架构奠基、结构化 LLM 响应协议设计、角色与属性系统奠基、上下文持久化与压缩机制奠基、多主题 + 职业系统、早期稳定性三连修复。
- **潜质**：架构设计、协议 / 数据模型奠基、平台从 0 到 1。
- **说明**：Skill 统一走项目共享技能文件夹，无需在此登记。


## 功能与修改记录（新→旧 · 按时间倒序，附精确时间）

| 日期/时间 | AI编号 | 做了什么 | 涉及文件 | 备注 |
|------|--------|----------|----------|------|
| 2026-08-29 22:43 | Codex | **生成总资料库 UI 改版外部评审文档**：完整介绍当前前端、用户流程、信息层级问题、建议方向、固定产品规则、其他 AI 的评审任务与验收标准 | `<project-root>\docs\总资料库前端展示与改版评审说明.md`、`<project-root>\COLLABORATION_LOG.md` | 仅 UI/UX 与中文文案，不包含代码、接口或后台技术方案；已扫描无 TODO/TBD/技术实现占位 |
| 2026-08-29 21:51 | Codex | **完成整卡 Master 的运行闭环和中文界面**：隔离 Master-only translation finalize 与 Adventure instance；支持稳定 nested field path 进入 8097；补 optional entry 运行时命名回退；翻译用户可见的总库状态、资产/内容类型、任务状态和内容版本；真实临时冒险验证 8 Lore 展开及重复加入幂等 | `<project-root>\denova-src\internal\book\master_library.go`、`material_import.go` 及测试，`internal\api\handlers\handler_material.go`，`web\src\features\library\LibraryView.tsx` 及测试，`web\src\i18n\locales\{zh-CN,en-US}\library.ts`，`<project-root>\tools\denova_full_export.py` 及测试，`<project-root>\CODE_GUIDE.md` | 9 个 active Master 全部可用；4 本迁移设定书中文名称生效；354/432 字段译文 active，78 个可选专名字段由 HY-MT 返回无可用中文，保留诊断但不阻塞 usable；定向 Go/Python/Vitest 与 Vite/Go build 通过；生产 web 已部署 |
| 2026-08-28 07:35 | Codex | **修订总资料库/导入翻译流水线架构并形成现行批准规格**：默认导入改为 Master-first；建立原件、翻译版本、总库条目、冒险实例四层；冻结 `source_id` / `master_item_id` / `translation_version_id` / `instance_id`；角色卡使用结构化 CharacterTemplate，世界书保留 source/runtime 双重语义；HY-MT 覆盖安全自然语言字段，Agent 只生成可选润色候选；旧冒险和总库更新均禁止静默覆盖 | `<project-root>\docs\superpowers\specs\2026-08-28-叙界总资料库与导入翻译流水线设计.md`、`<project-root>\docs\superpowers\specs\2026-08-27-narraverse-master-library-design-v2.md`、`<project-root>\docs\superpowers\specs\2026-08-26-denova-background-translation-and-material-library-design.md`、`<project-root>\COLLABORATION_LOG.md` | 仅文档设计；完成静态一致性自审后交付；未改业务代码、未运行代码测试；目录非 Git 仓库，无法提交 |
| 2026-08-27 02:58 | Codex | **叙界总资料库正式规格完成**：确定独立总库位置、备份与清点、去重/版本、三份试译、HY-MT 初译直入库、在线润色文件映射、中文/英文关键词、总库受管快照、冒险中途加载、角色模板/实例与跨冒险状态隔离、恢复和验收标准 | `<project-root>\docs\superpowers\specs\2026-08-27-narraverse-master-library-design.md` | 已做占位符、内部一致性、范围和歧义自审；修正翻译状态与失败条目启用规则；待用户复核规格，未实施迁移 |
| 2026-08-27 01:49 | Codex | **完成批量翻译减负部署与运行验收**：同步生产构建到 Denova 外置 web，重启 8097 Bridge，触发旧 applied 索引兼容清理并核对运行端 | `<project-root>\denova\web`、`<project-root>\tools\denova_full_export.py`、`<project-root>\.workbuddy\backups\translation-queue-20260827-014808` | HTTP 200；Bridge v5；105 个静态文件 hash mismatch=0；活动队列 applied=0、待确认=9；审计文件保留 |
| 2026-08-27 01:45 | Codex | **批量翻译自动确认＋积压任务一键采用＋队列减负**：批量正文改为安全自动写回，单条正文继续待确认；增加“采用全部待确认”并逐项做原文/revision 校验；UI 仅渲染最近 100 条可操作任务；后端从活动队列移除 applied ID、保留独立审计文件并兼容清理旧记录 | `<project-root>\denova-src\web\src\features\interactive\components\setting-panel\LoreBatchTranslationDialog.tsx`、`<project-root>\denova-src\web\src\features\translation\TranslationQueueControl.tsx`、`<project-root>\tools\denova_full_export.py`、中英 i18n 与相关测试 | Python 26/26、定向前端 8/8、i18n 3151 键、生产构建通过；全套并行超时项单独复跑 55/55 通过 |

| 2026-08-26 20:40 | Codex | **后台翻译队列＋资料加载库正式落地**：新增 8097 持久 HY-MT 串行任务、名称中文化、元数据安全自动写回、正文待确认、独立 manual/game 暂停；Denova 全局任务抽屉、单条/批量翻译；叙界知识库浏览与多文件 PNG/JSON 导入；设定书逐 entry、角色卡与内嵌世界书拆分、原件归档、稳定 ID、重复导入更新和手改冲突保护；完成构建、外置 web 同步和运行版部署 | `tools/denova_full_export.py`、`tools/denova_bridge.py`、`denova-src/internal/book/material_import.go`、`denova-src/internal/api/handlers/handler_material.go`、`denova-src/web/src/features/translation/TranslationQueueControl.tsx`、`denova-src/web/src/features/interactive/components/setting-panel/{LoreTranslationDialog,LoreBatchTranslationDialog,MaterialImportDialog}.tsx`、`denova-src/web/src/lib/api-client/{local-translation,material-library}.ts` | Python 27 项、相关 Go 包、前端 903 项、i18n 3148 键、生产构建和真实素材冒烟通过；全量 Go 套件存在 Windows 环境既有失败，详见当前状态 |
> 2026-08-25 交接审查：详见 `<project-root>\docs\2026-08-25-项目交接文档.md`；本轮只做静态审查和文件状态核对，未运行测试、构建或启动服务。
| 日期/时间 | AI编号 | 做了什么 | 涉及文件 | 备注 |
|------|--------|----------|----------|------|
| 2026-08-26 11:47 | MainAgent | **Denova 资料库单条本地翻译功能实施与部署**：①后端 `ExportJobManager` 新增 `translate_lore_fields()`，复用现有 `LocalTranslator`（缓存/术语表/占位符保护/校验），独立 `_translation_lock` 串行化，按 name→brief_description→content 顺序翻译，任一字段失败整体失败；②`denova_bridge.py` 新增 `POST /api/denova/translator/translate` 路由，仅允许回环地址，2 MiB 请求上限，8 种错误码（invalid_request/empty_fields/payload_too_large/translator_offline/model_missing/translation_failed/translation_validation_failed）；③前端新增 `local-translation.ts` API 封装（支持 AbortSignal，解析后端错误码）和 `LoreTranslationDialog.tsx` 弹窗组件（字段选择→翻译→原文/译文双栏预览→采用译文，快照防覆盖、stale 检测、切换条目自动 abort）；④`LoreEditor.tsx` 正文上方新增「译为中文」按钮，点击采用仅修改 draft 并进入现有 `useLoreItemAutosave` 自动保存；⑤中英文 i18n 新增 16 个翻译键。不翻译标签/关键词/ID/类型/加载策略/来源信息，不调用付费 API，不增加 Denova Agent 工具，不改全量导出流程和 Go 后端。 | `tools/denova_full_export.py`、`tools/denova_bridge.py`、`denova-src/web/src/lib/api-client/local-translation.ts`、`denova-src/web/src/lib/api-client/local-translation.test.ts`、`denova-src/web/src/features/interactive/components/setting-panel/LoreTranslationDialog.tsx`、`denova-src/web/src/features/interactive/components/setting-panel/LoreTranslationDialog.test.tsx`、`denova-src/web/src/features/interactive/components/setting-panel/LoreEditor.tsx`、`denova-src/web/src/lib/api-client/index.ts`、`denova-src/web/src/i18n/locales/zh-CN/settingPanel.ts`、`denova-src/web/src/i18n/locales/en-US/settingPanel.ts`、`.workbuddy/tmp/test_denova_full_export.py`、`CODE_GUIDE.md` | Python `LoreTranslationTests` 15 项全通过；前端 `local-translation.test.ts` 5 项 + `LoreTranslationDialog.test.tsx` 8 项全通过；TypeScript `tsc --noEmit` 通过；i18n 3082 键中英文对齐；Vite 生产构建成功（`✓ built in 1.58s`）；构建产物已同步到 `denova/web`，部署前备份 `backup/denova-web-20260826-114419`。8097 桥接服务需重启以加载新路由。人工验收项（普通英文/超长正文/NSFW/占位符/取消/切换条目/Ollama关闭/刷新保存/导出冲突保护）待用户实测。 |
| 2026-08-26 10:40 | Codex | **修正翻译回显误报**：HY-MT 若返回 `Terminology/Context/SOURCE` 提示模板，现判定为失败并保留英文原文；新增测试防止 `translated=true` 伪成功。 | `<project-root>\tools\denova_full_export.py`、`.workbuddy\tmp\test_denova_full_export.py` | Python 7 项测试通过；桥已重启。 |
| 2026-08-26 10:12 | Codex | **根据 Denova 全量读取验收修复导出缺陷**：报告确认 16/16 文件读取成功，但当前工程的原始素材仍包含嵌入截断副本，且 350 条设定书 entries 被压成 9 条资料项。现改为无来源引用时先按唯一文件名/内部字段回源；对 `title + content(JSON) + _loreParsed` 嵌入设定书先解包原始 JSON；嵌入无法回源时严格写入 `incomplete/truncated`；manifest 和验收报告使用实际截断数。 | `<project-root>\tools\denova_full_export.py`、`.workbuddy\tmp\test_denova_full_export.py`、`<project-root>\tools\denova_bridge.py` | Python 7 项测试通过；桥已重启为 v5。旧 Denova 工程未自动覆盖，需重新从叙界导出。 |
| 2026-08-26 09:32 | Codex | **按用户要求完成 HY-MT 实际下载与推理验证**：通过 8097 安装任务下载官方 `HY-MT1.5-1.8B-Q4_K_M.gguf`，文件大小 1,133,080,512 bytes，SHA-256 校验通过；注册独立 Ollama 模型 `hy-mt1.5:1.8b-q4_k_m`。用含 `{{char}}` 和 NSFW 术语的短句完成真实翻译冒烟，返回正常中文且占位符保留。 | `<project-root>\tools\denova_full_export.py`、`<project-root>\denova\.denova\narraverse-hy-mt-installer.json`、Ollama 模型注册 | 初次安装因 HF API 缺少 LFS 字段安全失败；改用已核验官方大小/哈希并继续下载。现有 Qwen/Llama 未修改。 |
| 2026-08-26 09:12 | Codex | **完成全量保真导出的生产构建、运行态部署与最终验收**：修正 Denova 两个历史测试夹具的当前类型字段，更新叙界旧两步向导回归为现行三步流程；完成 TypeScript/Vite 生产构建，将 `dist` 与同步后的叙界静态资源部署到运行版并逐文件哈希核对。发现 8097 仍驻留旧 v4 后，仅重启已确认的 Python 桥，现健康接口为 v5/`fullExport=true`。Ollama 状态确认 `qwen3.6:latest` 23GB 与 Llama 保持原样，HY-MT 未擅自下载，首次需要时仍由 UI 询问。 | `<project-root>\denova-src\web`、`<project-root>\denova\web`、`<project-root>\.workbuddy\tmp\test_adventure_{types,wizard}.js`、`<project-root>\COLLABORATION_LOG.md`、获批设计规格 | 通过 Python 6 项、Node 导出/永久档案/未来同步/加载库/向导/AI 路径/新功能回归、Vitest 18 项、`node --check`、`py_compile`、`pnpm build`、8080/8097 运行态冒烟；备份 `<project-root>\backup\denova-web-20260826-090857`。本机未安装 Go，Go 定向测试未执行；本轮无 Go 源码改动。 |
| 2026-08-26 09:00 | Codex | **完成全量导出的翻译、导演与小说适配并建立回归网**：①模型安装器只在确认后从腾讯官方 HY-MT GGUF 仓库下载 Q4_K_M（约 1.06 GiB），动态读取官方 SHA-256/大小、支持下载续传、校验后注册独立 Ollama 模型，不触碰现有 Qwen/Llama。逐字段翻译保护模板/URL/HTML/Markdown/代码/正则，支持全局及单冒险术语覆盖、NSFW 术语、缓存和失败字段英文回退。②叙界移除导演 Tab/提示注入/预设拉取/`EVENTS-CHECK` 协议，新冒险不再创建导演状态，旧 JSON 字段保留。Denova 读取导入清单：仅叙界导入工程的新故事默认手动导演并首次折叠后台，已有故事/普通工程不改。③小说化改读永久档案，按消息边界和约 12000 字分卷，保存来源消息映射，移除 8000 字硬截断。④重生成加载库，为 50 本设定书、39 张角色卡附 `source_ref v1`。⑤新增/更新 Python、Node、Vitest 测试，覆盖 >500 条、长 V3 卡、稳定 ID/手改冲突、分块/哈希、模板与 NSFW 术语、缓存/拒绝、磁盘不足、暂停续跑、原子回滚、档案事件、导演默认与小说无截断。 | `<project-root>\tools\denova_full_export.py`、`<project-root>\tools\denova_bridge.py`、`<project-root>\app\app.js`、`<project-root>\app\index.html`、`<project-root>\app\presets.js`、`<project-root>\app\local_library.js`、`<project-root>\denova-src\web\src\features\interactive\components\{InteractiveLayout,StoryStage,NewStorySetupPanel}*`、`<project-root>\.workbuddy\tmp\test_*`、`<project-root>\CODE_GUIDE.md` | 通过：Python 6 项、Denova 组件 18 项、v4 前端无头、conversation archive、旧 Python/JS future sync；待完整构建、静态资源同步和部署。 |
| 2026-08-26 04:05 | Codex | **完成全量保真导出的第一阶段真源与任务管线**：①知识库生成器为角色卡/设定书预览附加相对路径、稳定来源 ID 与 SHA-256；用户上传角色卡保留完整原始对象。②新增独立于上下文压缩的 IndexedDB 分块追加式永久对话档案，发送、编辑、回溯、快照、分支切换均写事件，旧冒险给出 `complete/best_effort/incomplete` 恢复等级。③新增 `export-bundle v4` 后端，支持 8 MiB 内幂等分块、超大记录再分片、回源/磁盘/模型预检、任务控制、原始对话 JSONL/Markdown/分卷/分支输出、角色卡确定性分组、世界书逐条导入、稳定 ID、原子提交、备份和手改冲突保护。④手动导出 UI 改读永久档案并上传完整素材，移除摘要模型、小说与预览截断依赖；结束时严格报告恢复、翻译失败、冲突和截断，不把警告显示成全绿成功。 | `<project-root>\app\app.js`、`<project-root>\app\index.html`、`<project-root>\tools\gen_local_library_v2.py`、`<project-root>\tools\denova_bridge.py`、`<project-root>\tools\denova_full_export.py`、`<project-root>\tools\translation_glossary.json`、`<project-root>\CODE_GUIDE.md`、`<project-root>\COLLABORATION_LOG.md` | `node --check app/app.js` 与 Python `py_compile` 通过。原有轻量同步载荷/行为未修改；本地翻译模型安装、Denova 资料库/导演适配、小说分卷和完整回归尚待继续。 |
| 2026-08-26 03:36 | Codex | **完成叙界→Denova 全量保真导出与低成本翻译设计初稿及自审**：基于源码核查确认当前角色卡实际导出截断预览、扩展字段遗漏、摘要仅读最近 8 条 AI 消息、小说生成不覆盖压缩历史、桥接存在每本 200/全局 500 条上限；实测本机 `qwen3.6:latest` 为 36B/23GB，加载约 594 秒且聊天 13 分 35 秒后 500。经用户逐项确认，设计锁定完整原件回源、追加式对话档案、原始实录优先、角色卡语义拆分、世界书逐条导入、本地 HY-MT1.5-1.8B 英中翻译、NSFW 术语表、稳定 ID/冲突保护/原子提交、清单验收、退役叙界轻量导演，以及导入故事默认仅手动 Denova 导演。 | `<project-root>\docs\superpowers\specs\2026-08-26-叙界导出Denova全量保真设计.md`、`<project-root>\COLLABORATION_LOG.md` | 当前仅设计与静态诊断，未修改业务代码、未运行测试或构建；自审已消除待定边界，规格待用户最终复核后再制定实施计划。 |
| 2026-08-23 15:18 | MainAgent | **Denova 启动崩溃排查 + 局域网监听重大修正 + 手机端访问地址更新**：①用户反馈 Launch-Narraverse.cmd 不弹窗口、浏览器报 `Failed to fetch dynamically imported module: SettingPanel-BUFRVwaZ.js`。排查发现 denova.exe 启动后立即崩溃退出（日志仅输出2行即断，8080未监听），导致前端加载到一半服务已断。手动前台运行 denova.exe 后恢复正常（PID 27084），推测为残留进程/端口冲突导致启动失败。②**重大修正**：确认 `allow_lan_access = true` 生效后，denova.exe **直接监听 0.0.0.0:8080**（所有网卡），并非 8月16日 认为的"写死 127.0.0.1"。当时加的 8081→8080 端口转发**非必需**，手机可直接用 8080 端口访问。此前误判原因是 denova 未彻底重启导致 allow_lan_access 配置未生效。③手机端访问地址统一更新为 `http://192.168.1.233:8080/narraverse/index.html?mobile=1`（带 ?mobile=1 启用精简移动端；Denova 宿主 iframe 内由 Codex 19:10 改进为自动检测宽度切换，无需手动加参数）。④手机端现状汇总：Codex 已于 8/16 19:10 完成 iframe 自动宽度检测+抽屉式侧边栏，8/16 20:20 完成 Denova 主界面移动端注入，8/22 14:48 修复手机端文字拥挤。 | `denova/.denova/config.toml`、`denova/log/2026-08-23.log` | 踩坑：denova 启动失败时日志只写2行就断，无 panic 输出；前台运行可恢复；allow_lan_access 需彻底重启 denova 才生效；启动脚本用 -WindowStyle Hidden 所以本来就不弹黑窗，属正常现象。 |
| 2026-08-22 14:48 | Codex | **修复叙界手机端文字拥挤**：为消息气泡补 `min-width: 0`，正文与玩家文本使用正常中文断行、长串兜底换行和更舒展行高；手机弹窗改为 18px 内边距，辅助说明补 1.65 行高；主样式和 Denova iframe 同时增加版本参数，避免页面或 CSS 单边缓存。 | `<project-root>\app\style.css`、`<project-root>\app\index.html`、`<project-root>\denova-src\web\src\features\narraverse\NarraverseWorkspace.tsx`、相关测试期望、部署资源 | 390px 直接入口确认 `style.css?v=20260822`、弹窗 18px 内边距、辅助文字 18.15px 行高和 `overflow-wrap:anywhere` 生效；Vite 构建部署成功。完整 `pnpm build` 被既有 `workbench-utils.test.ts` 的 `WorkspaceSummary` 类型缺字段阻断，与本次修改无关。 |
| 2026-08-22（时间待补） | Codex | **修复 Denova 工作台书籍切换栏与实际书籍偶发错位**：定位到工作台导航标题 `currentBookName` 之前优先取 `summary.title`，而真实切换真源其实是当前 `workspace` 与书架记录；当摘要刷新慢一步或残留旧标题时，会把展开栏/切换器标题覆盖成上一册。现改为统一优先读取 `workspace` 对应的 `books` 记录名，仅在书架缺项时才回退到摘要标题；并补充纯函数回归测试，防止未来再次把派生摘要当成导航真源。 | `<project-root>\denova-src\web\src\App.tsx`、`<project-root>\denova-src\web\src\components\workbench\workbench-utils.ts`、`<project-root>\denova-src\web\src\components\workbench\workbench-utils.test.ts` | 本轮按用户要求直接实施，未运行测试；测试留给 A1。 |
| 2026-08-16 19:10 | Codex | **修复手机端 v2 的宿主入口失效与导航丢失**：①复核发现 Denova 手机 iframe 实际宽度为 390px，但当前代码仅识别 URL `mobile=1`，正常 `/?mode=narraverse` 的 iframe 没有 `mobile-mode`，侧栏+正文+右栏仍挤在同屏。②改为按 iframe 自身 `matchMedia('(max-width: 768px)')` 自动切换，并保留 `mobile=1/0` 显式覆盖及横竖屏变化监听。③不再永久删除左侧栏；复用顶部汉堡按钮，将冒险列表/新建/API 设置改为 320px 抽屉，新增独立关闭按钮，并在切换冒险、打开新建或设置时自动收起。④输入区加入底部安全区。 | `<project-root>\app\index.html`、`<project-root>\app\app.js`、`<project-root>\app\style.css`、`<project-root>\denova-src\web\public\narraverse\`、`<project-root>\denova\web\narraverse\` | 真实浏览器复核：Denova 宿主 390px 自动出现 `mobile-mode`，正文宽 390/无横向溢出，右栏隐藏，汉堡可打开/关闭抽屉；1280px 时 class 为空、侧栏恢复静态布局、关闭按钮隐藏。覆盖前备份：`<project-root>\backup\mobile-auto-20260816-190146`。 |
| 2026-08-16 18:36 | MainAgent | **移动端适配 v2（修复版）**：上一版媒体查询在 Denova iframe 内嵌场景下不生效（iframe 视口宽度≠手机屏幕宽度）。改为 URL 参数 `?mobile=1` 强制启用：index.html head 中加 JS 检测参数并给 html 加 `mobile-mode` class；style.css 中所有移动端规则改用 `.mobile-mode` 前缀；新增隐藏右侧状态面板（.right-panel，含 HP/MP/状态/任务/导演台）；正文区占满宽度，对话框只留输入框+发送按钮。手机直接访问纯 Narraverse 页面 `/narraverse/index.html?mobile=1` 即可绕过 Denova 外层导航栏。 | `denova/web/narraverse/index.html`、`denova/web/narraverse/style.css`、`app/index.html`、`app/style.css` | 访问地址：`http://电脑IP:8080/narraverse/index.html?mobile=1`（2026-08-23 确认 allow_lan_access 生效后直接用 8080，无需 8081 转发；Denova 宿主 iframe 内由 Codex 19:10 改进为自动检测宽度，无需手动加参数）；不带 mobile=1 参数时保持原桌面样式不变。 |
| 2026-08-16 18:25 | MainAgent | **新增移动端样式适配**：在 `style.css` 末尾添加 `@media (max-width: 768px)` 媒体查询，手机访问时自动隐藏侧边栏、顶部工具栏、事件/掷骰/下一步按钮，仅保留正文（narrative-text）、选项框（choice-btn）、对话框（输入框+发送按钮）；调整字体大小、内边距、按钮最小高度（44px触摸友好）、模态框宽度适配手机屏幕。修改前已备份至 `backup/mobile-style-20260816-182530/`。 | `denova/web/narraverse/style.css`、`app/style.css` | 后发现该方案在 Denova iframe 内嵌时媒体查询不触发，已于 18:36 改为 `?mobile=1` 参数强制方案。 |
| 2026-08-16 20:20 | Codex | **Denova 主界面（/）手机适配注入**：确认 8080 实际加载 `denova/web/index.html`（外部 web 目录生效），叙界 /narraverse/ 适配已生效（mobile-mode 已在），但主界面 index.html 未适配（之前 AI 改 `app/` 不影响 8080）。在 `denova/web/index.html` 注入移动端适配：窄屏（≤768px）自动给 html 加 `deno-mobile` class + 触控目标 ≥44px/防 iOS 缩放/防横向溢出/弹窗与抽屉宽度适配/安全区；备份至 `denova/web/backup-mobile-main-20260816/`；8080 验证注入生效 | `<project-root>\denova\web\index.html`、`<project-root>\denova\web\backup-mobile-main-20260816\` | 手机实测（8081 局域网）后按需迭代；改叙界须改 `denova/web/narraverse/` 或改 `app/` 后手动同步 |
| 2026-08-16 约17:40 | MainAgent | **局域网访问方案修正：端口转发 + 防火墙脚本修复**：①排查发现 `denova.exe` 写死监听 `127.0.0.1`，无 `-host`/`-listen` 参数，无法直接改监听地址；改用 Windows 端口转发 `netsh interface portproxy add v4tov4 listenport=8081 listenaddress=0.0.0.0 connectport=8080 connectaddress=127.0.0.1`，将 `0.0.0.0:8081` 转发到 `127.0.0.1:8080`。②修复 `open_firewall_ports.bat` 中文乱码：文件 UTF-8 无 BOM 被 cmd 按 GBK 解码，加 `chcp 65001` 切换代码页；脚本更新为含端口转发配置 + 防火墙规则（TCP 8081/8097/8098，规则名 Narraverse LAN Access）。③手机访问地址改为 `http://电脑IP:8081/?mode=narraverse`（8081→8080 转发）。④另确认 Denova 设置界面自带「允许局域网访问」（allow_lan_access）功能，用户已开启并设置远程访问用户名，配置写入 `denova/.denova/config.toml`。 | `tools/open_firewall_ports.bat`、`denova/.denova/config.toml` | 关键踩坑：当时因 denova 未彻底重启，allow_lan_access 未生效，误判为"写死 127.0.0.1 只能用端口转发"。2026-08-23 确认 allow_lan_access=true 生效后 denova 直接监听 0.0.0.0:8080，端口转发非必需；bat 脚本中文必须加 chcp 65001 否则乱码；电脑局域网 IP 为 192.168.1.233（勿用 192.168.48.1 VMware 虚拟网卡）。 |
| 2026-08-16 17:11 | MainAgent | **新增局域网手机访问支持**：将 denova_bridge.py 和 pixiv_bridge.py 的监听地址从 `127.0.0.1` 改为 `0.0.0.0`；前端 app.js 新增 `DENOVA_BRIDGE_BASE`/`PIXIV_BRIDGE_BASE` 常量，用 `window.location.hostname` 动态适配主机，替换全部 8 处硬编码 `127.0.0.1:8097/8098`；同步源文件与部署文件；生成防火墙端口开放脚本（需管理员运行）。修改前已备份至 `backup/lan-access-20260816-170844/`。 | `tools/denova_bridge.py`、`tools/pixiv_bridge.py`、`app/app.js`、`denova/web/narraverse/app.js`、`tools/open_firewall_ports.bat` | 初版以为 Denova 主服务监听所有网卡，后发现写死 127.0.0.1，已于约17:40 改用 8081 端口转发方案。 |
| 2026-08-15 18:51 | A1/Codex | **统一非代码文档使用中文文件名**：按用户最新命名规则，将现有游戏项目分析报告由英文文件名改为中文文件名，并同步修正协作日志和工作台项目档案中的引用。 | `<project-root>\docs\2026-08-15-现有游戏项目分析报告.md`、`<project-root>\COLLABORATION_LOG.md`、`E:\Workspace\projects\Narraverse\README.md` | 仅调整文档文件名与引用，不涉及代码和测试。 |
| 2026-08-15 18:42 | A1/Codex | **完成现有游戏项目全量现状审查**：从统一入口、三条玩法循环、功能系统、Adventure/Turn/Actor State/离线 State 数据模型、AI 表现层与真实逻辑层、跨模式同步、实际运行数据和规划差异出发形成 12 节分析报告；明确区分已确认、推测、待确认与建议，并按 P0/P1/P2 给出后续优先级。 | `<project-root>\docs\2026-08-15-现有游戏项目分析报告.md`、`<project-root>\COLLABORATION_LOG.md` | 本轮按用户要求只做静态审查，未执行测试；当前运行数据未发现 Denova Interactive 故事目录与 Narraverse 同步索引，浏览器 IndexedDB 内容待确认。 |
| 2026-08-15 15:50 | A1/Codex | **修复双击一键入口在 Windows PowerShell 5.1 下无法解析**：将 `start_narraverse.ps1` 中 6 处中文错误信息/注释改为纯 ASCII，避免无 BOM UTF-8 被 Windows PowerShell 5.1 按系统 ANSI 解码后破坏引号与语句块。 | `<project-root>\tools\start_narraverse.ps1`、`<project-root>\COLLABORATION_LOG.md`、`<project-root>\CODE_GUIDE.md` | 使用与 `Launch-Narraverse.cmd` 相同的 `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe` 解析通过；实际执行 `-NoOpen` 成功返回 `http://127.0.0.1:8080/?mode=narraverse`。 |
| 2026-08-15 15:45 | A1/Codex | **完成 Denova 唯一入口的一键启动与新冒险双向同步**：①启动器自动前置启动 8097 同步桥和已配置的 8098 Pixiv 桥，再启动 Denova 并进入叙界；失败时保留控制台与日志提示。②同步桥升级 v4，新增 health/push/pull，按稳定 sync id 固定工程，覆盖管理文件前自动备份。③仅新冒险写 `syncMeta`，`saveState` 防抖推送标题、四段设定、摘要、角色/设定素材和章节；旧冒险保持原样。④Denova 返回叙界时通过 `visibility-changed` 拉回设定、章节与 lore；不以精简载荷覆盖原始角色卡。⑤同步静态快照、更新宿主协议测试、构建并部署 Denova 前端。 | `<project-root>\Launch-Narraverse.cmd`、`<project-root>\tools\start_narraverse.ps1`、`<project-root>\tools\denova_bridge.py`、`<project-root>\app\app.js`、`<project-root>\app\bridge.js`、`<project-root>\denova-src\web\src\features\narraverse\NarraverseWorkspace.tsx`、`<project-root>\.workbuddy\tmp\test_denova_future_sync.py`、`<project-root>\.workbuddy\tmp\test_narraverse_future_sync.js` | Python/JS 同步回归、既有 Denova 导出回归、宿主组件 8/8、i18n 3068 键和正式构建全部通过；一键启动实测 `http://127.0.0.1:8080/?mode=narraverse`，8097 health v4、页面与嵌入 app.js 均 200。部署备份：`<project-root>\backup\denova-web-20260815-154257`。 |
| 2026-08-15 15:24 | A1/Codex | **完成“Denova 唯一入口 + 新冒险数据互通”设计**：用户确认方案 A；入口脚本自动前置启动 8097 Denova 数据桥接，Pixiv 配置完整时启动 8098，再启动 Denova 并进入叙界；仅新创建冒险生成稳定 `syncMeta.id`，自动双向同步标题、设定、角色卡、设定书、摘要与章节，旧冒险不扫描不迁移；明确桥接 health/push/pull API、Denova 管理文件、按区段最后写入生效、覆盖前备份和失败不阻塞本地游玩。 | `<project-root>\docs\2026-08-15-denova-entry-and-future-sync-design.md`、`<project-root>\COLLABORATION_LOG.md` | 当前仅固化已确认设计，尚未修改业务代码。 |
| 2026-08-15 14:59 | A1/Codex | **完成统一目录英文命名与一致性收尾**：将 Denova 本地说明改为 `LOCAL_README.md` 并更新为统一平台说明；扫描活动代码、工具、技能和配置，确认无旧根路径残留（指南中的禁止示例除外）、pnpm 命令链接无旧源码路径、PowerShell 启动器解析无错误、11 个 Denova 工程路径全部指向新目录；恢复迁移前当前工程 `11`。 | `<project-root>\denova\LOCAL_README.md`、`<project-root>\denova\.denova\books.json`、`<project-root>\CODE_GUIDE.md`、`<project-root>\COLLABORATION_LOG.md` | 验证结束后无 Denova 进程；旧 `<external-denova-installation>*` 均不存在；旧中文项目根为空。 |
| 2026-08-15 14:53 | A1/Codex | **完成叙界与 Denova 英文统一目录迁移**：将叙界、Denova 运行版、Denova 源码统一到 `<project-root>`，核心维护文件与启动入口改英文名，知识库改为 `knowledge-base/`；修复工具、技能、Pixiv、Denova 桥接和 `books.json` 的活动路径；重建 pnpm 链接与知识库产物，同步静态快照并部署新前端；启动器改为相对定位 Denova，且支持发布端口自动回退 `8080..8090`。 | `<project-root>\`、`<project-root>\denova\`、`<project-root>\denova-src\`、`<project-root>\knowledge-base\`、`<project-root>\Launch-Narraverse.cmd`、`<project-root>\tools\start_narraverse.ps1`、`<project-root>\CODE_GUIDE.md`、`<project-root>\COLLABORATION_LOG.md` | 21/21 定向测试、i18n 3068 键、JS/Python 语法、`pnpm build` 全通过；实际运行自动切到 8081，根页面与 `/narraverse/index.html` 均 HTTP 200，`/api/books` 读取 11 个工程。新部署备份：`denova/web.backup-20260815-1447`。旧 `<external-text-adventure-project>` 已无文件，但被当前 Codex 工作区句柄占用，结束并重开任务后可删除空目录。 |
| 2026-08-15 14:34 | A1/Codex | **开始执行英文统一目录迁移**：将启动器默认 Denova 路径改为相对项目根解析，并新增带精确源路径校验、目标冲突保护、运行进程保护、文件计数校验、核心文件英文重命名、活动路径替换和迁移清单输出的一次性迁移脚本。 | `<external-text-adventure-project>\tools\start_narraverse.ps1`、`<external-text-adventure-project>\tools\migrate_to_unified_layout.ps1`、`<external-text-adventure-project>\项目协作日志.md` | 下一步运行迁移脚本，把三套目录原子移动到 `<project-root>`。 |
| 2026-08-15 14:30 | A1/Codex | **完成叙界与 Denova 英文统一目录迁移设计**：用户确认方案 A，目标根目录为 `<project-root>`，包含 `app/`、`denova/`、`denova-src/`、`knowledge-base/`；核心维护文档和启动入口改英文名，角色卡/设定书等用户内容标题保留中文；规格明确路径替换、依赖重建、验证与回滚边界。 | `<external-text-adventure-project>\docs\2026-08-15-unified-english-project-layout-design.md`、`<external-text-adventure-project>\项目协作日志.md` | 当前仅固化已批准设计，尚未移动目录。 |
| 2026-08-15 14:19 | A1/Codex | **校正 Denova 第三模式交付记录**：将 13:52 条目限定为源码实现与构建验证，把实际安装部署、备份和双击入口保留在 14:16 独立记录；同步修正“当前状态”为已部署新前端、exe 本体未替换，避免重复或歧义。 | `<external-text-adventure-project>\项目协作日志.md` | 仅校正文档记录，不改业务代码。 |
| 2026-08-15 14:16 | A1/Codex | **将已验证的新前端部署到实际 Denova 安装并补齐双击入口**：确认旧 `denova.exe` 优先读取同级磁盘 `web/`，备份原前端后部署 Vite 构建；使用原 exe 前台实测 `http://127.0.0.1:8080/?mode=narraverse`，叙界同级模式、iframe 与迁移提示正常；修正启动器同时探测发布版后端同端口和开发版 Vite 端口，新增 `启动叙界.cmd`。 | `<external-denova-installation>\web`、`<external-denova-installation>\web.backup-20260815-1402`、`<external-text-adventure-project>\tools\start_narraverse.ps1`、`<external-text-adventure-project>\启动叙界.cmd`、`<external-text-adventure-project>\代码指南.md`、`<external-text-adventure-project>\项目协作日志.md` | 启动器 `-NoOpen` 实测输出 `http://127.0.0.1:8080/?mode=narraverse`；实际安装版浏览器无 Narraverse/bridge 新增错误；测试结束后 Denova 与预览进程均已停止。exe 本体未替换。 |
| 2026-08-15 13:52 | A1/Codex | **完成叙界作为 Denova 第三顶层模式的 A→D 源码实施**：①Denova 新增 `narraverse` 内容模式、桌面/移动三模式切换、共享页面返回叙界、无书籍深链接、持久 iframe、双语加载/错误态和主题/语言消息；②叙界新增 `embedded=denova` 宿主识别、重复品牌/旧 Denova 套壳去除、同源消息校验、迁移备份提示；③新增白名单静态同步脚本，将叙界完整快照打入 `web/public/narraverse` 与 Vite `dist`；④补齐 21 项定向测试、正式构建、宽屏/390px 浏览器验收及代码指南/CHANGELOG。 | `<external-denova-source>\web\src\stores\workspace-store.ts`、`<external-denova-source>\web\src\App.tsx`、`<external-denova-source>\web\src\components\workbench\ModeRouter.tsx`、`<external-denova-source>\web\src\components\workbench\WorkbenchShell.tsx`、`<external-denova-source>\web\src\features\narraverse\*`、`<external-denova-source>\web\public\narraverse\`、`<external-denova-source>\scripts\sync-narraverse-assets.mjs`、`<external-text-adventure-project>\app\app.js`、`<external-text-adventure-project>\app\bridge.js`、`<external-text-adventure-project>\app\index.html`、`<external-text-adventure-project>\app\redesign.css`、`<external-text-adventure-project>\代码指南.md` | 定向测试 21/21、i18n 3068 键、JS/PowerShell 语法和 `pnpm build` 全通过；全套误触发时 875/881 通过，5 个既有超时与本功能无关，1 个本次 iframe 模拟已修复。实际安装部署另见 14:16 记录。 |
| 2026-08-15 10:56 | Codex | **将 Denova 第三模式实施方案改为 A1 单人串行执行**：删除 AI-A/AI-B/AI-C 并行分工与独占目录表述，统一规定 A1 按“Denova 第三模式 → 叙界嵌入态 → 静态打包/启动器 → 集成验收”顺序推进；补充源文件优先、静态快照不可单独修改、每阶段自检与回写规则。 | `<external-text-adventure-project>\docs\2026-08-15-叙界作为Denova第三模式实施方案.md`、`<external-text-adventure-project>\项目协作日志.md` | 后续该方案全部交由 A1 执行，不再分派其他 AI；仅修改文档，未运行测试。 |
| 2026-08-15 10:54 | Codex | **完成“叙界作为 Denova 第三顶层模式”实施方案**：按用户确认的 A 方案，将 Denova 定为唯一宿主，明确写作/游戏/叙界同级导航、持久同源 iframe、静态资源随 Denova 打包、嵌入态去重、深链接与兼容启动器、旧 IndexedDB 数据迁移提示、双语/主题/响应式要求；拆分 AI-A（Denova 前端）、AI-B（叙界嵌入态）、AI-C（打包/启动器）和集成人四个无交叉工作包，补充消息协议、实施顺序、验证清单与完成标准。 | `<external-text-adventure-project>\docs\2026-08-15-叙界作为Denova第三模式实施方案.md`、`<external-text-adventure-project>\项目协作日志.md` | 仅编制可交付给其他 AI 的具体方案，未修改 Denova/叙界业务源码，未运行测试。 |
| 2026-08-15 00:10 | Codex | **项目品牌正式命名为「叙界 Narraverse」**：统一更新浏览器标题、侧栏 Logo、空状态标题、在线叙事 AI 身份、全量导出应用标识、重设计预览，以及代码指南/AI 协作规范/协作日志标题；保留现有项目目录 `<external-text-adventure-project>`，避免路径引用和工具脚本失效。 | `<external-text-adventure-project>\app\index.html`、`<external-text-adventure-project>\app\app.js`、`<external-text-adventure-project>\app-redesign-preview.html`、`<external-text-adventure-project>\代码指南.md`、`<external-text-adventure-project>\AI协作规范提示词.md`、`<external-text-adventure-project>\项目协作日志.md` | 品牌显示名：`叙界 Narraverse`；导出标识：`Narraverse`。纯文案与标识替换，未执行测试。 |
| 2026-08-14 11:28 | Codex | **在线 AI 冒险三步创建向导实施**：①重排创建弹窗为“选择素材 → 故事方案 → 确认开局”，将玩家信息、角色卡/世界设定卡、已选素材托盘和选填故事偏好集中到首步；②新增创建会话状态、严格 JSON 方案解析与素材限长摘要，调用现有 `callLLM` 生成 3 套侧重点不同的故事背景，展示自动标题、卖点、基调标签、玩家身份、目标、地点和角色定位；③选择方案后自动填入冒险标题、世界背景、玩家身份、起始地点、初始目标、开场钩子及其他说明，确认页可继续编辑并以紧凑列表展示已加载素材；④新增“跳过 AI，手动填写”和生成失败保留输入/重试路径；⑤修复自定义职业回落默认剑士的根因：移除 `sendNextStep()` 中误用未定义 `opts` 的错位代码，以 `getLockedProfession()` 将自定义职业作为提示词、方案归一化和最终创建的硬约束；⑥自动标题写入 `adv.title`，AI 开场钩子优先作为首轮叙事 seed；⑦补齐向导响应式样式与代码指南。 | `<external-text-adventure-project>\app\index.html`、`<external-text-adventure-project>\app\app.js`、`<external-text-adventure-project>\app\redesign.css`、`<external-text-adventure-project>\代码指南.md`、`<external-text-adventure-project>\项目协作日志.md` | **按用户指示未运行任何测试、语法检查或浏览器验证；测试任务交由 A1，当前状态为“实现已写入、待 A1 验证”。** |
| 2026-08-14 11:07 | Codex | **在线 AI 冒险三步向导实施计划完成**：将已确认设计拆为 5 个可独立测试任务——创建会话状态与方案解析器、三步弹窗导航、AI 三方案生成与选择、确认页字段映射/自动标题/开局钩子/自定义职业根因修复、视觉与文档全量回归；每项均写明精确接口、失败测试、最小实现、验证命令和预期结果。计划自检已覆盖规格全部要求，无 TBD/TODO，占位符与函数命名一致。 | `docs/superpowers/plans/2026-08-14-ai-adventure-wizard.md`、`项目协作日志.md` | 项目非 Git 仓库，计划以逐任务测试门槛替代 commit；待选择执行方式后编码。 |
| 2026-08-14 11:02 | Codex | **在线 AI 冒险创建向导体验改造设计完成**：经用户逐项确认，确定三步式流程“选择素材 → AI 生成 3 套故事方案 → 确认开局”；故事方案自动生成可编辑冒险标题，并填入世界背景、玩家身份、起始地点、初始目标、角色定位、基调与开局钩子；自定义职业作为最高优先级全流程锁定；生成前支持选填故事偏好，失败时保留素材与输入。已固化完整数据模型、AI JSON 契约、字段映射、失败处理、样式与验收标准，并完成 TBD/矛盾扫描。 | `docs/superpowers/specs/2026-08-14-ai-adventure-wizard-design.md`、`项目协作日志.md` | 当前仅完成已批准设计文档，待用户复核文档后进入实施计划与编码。 |
| 2026-08-14 09:39 | Codex | **项目审查与验证**：完成静态安全/稳定性审查；发现明文凭据、开放 CORS 的本地写入桥接、设定书提示注入、冒险类型分类契约不一致等风险。运行 7 个 JS 语法检查、4 个 Python 编译检查；9 个现有无头测试中 7 个通过，`test_adventure_types.js`（6 项分类数据缺失）与 `test_extensions_inbox.js`（未重置 TTS 朗读状态的测试夹具）失败。Python 编译检查刷新了 `tools/__pycache__/` 缓存；未修改业务源码或配置。 | `app/`、`tools/`、`.workbuddy/tmp/`、`项目协作日志.md` | 建议优先轮换凭据并收紧本地桥接访问边界；测试失败已定位，待后续修复/校正测试。 |
| 2026-08-13 12:50 | AI 1 / SeniorDeveloper | **审查 AI 2 P1-P4 落地 + 修命令重复**：用户称「其它的 A2 做完了」，核查 AI 2 接 P0 完成的 P1-P4。实地核对：P1（state.denovaBooks@21 / loadDenovaBooks@5592 拉 Denova 工程 lore / gatherLoadItems@5631 合并挂载 / 启动自动拉取@5921）；P2（renderDirectorPanel@7097 接 #directorPanel/#directorStatus / toggleDirector@7064 / switchRightTab director 分支@3207 / buildDirectorPromptBlock@1217 公共块）；P3（adventure.novelPlan@532 / versionLedger@533 / createSnapshot 写账本@3941 / buildNovelSystemPrompt 注入大纲@6941）；P4（theme-neutral.css 落地 + index.html 引用 / getTheme@3300 setTheme@3303 initTheme@3308 / 启动应用@7494 / 主题命令带全分组）。**发现并修复命令重复 bug**：AI 2 在 app.js UI_COMMANDS 末尾硬编码「打开 Denova 写作台/游戏模式」(3294-3295)，与 bridge.js DOMContentLoaded push 的同名命令冲突（命令面板双份）。修正方向：Denova 2 命令留 app.js 静态（vm 测试可见），bridge.js 改为只 push Profile 导出/导入 2 命令（app.js 无此两条，不重复）。**回归验证**：node --check 全过；test_denova_export 全过、new_features 15/0、adventure_wizard 37/0、ai_path 7/0、offline_ai 7/0、pixiv 全过、tts 14/0；adventure_types 6 失败=知识库数据覆盖（6 类冒险「其他」推荐分类无对应设定书，非代码）、extensions_inbox 1 失败=TTS 语音不支持边缘降级——均非本次改动回归。真实浏览器命令面板 22 条无重复。 | app/app.js（恢复 Denova 2 命令静态注册）、app/bridge.js（删 Denova push 只留 Profile push） | P0-P4 全链路落地确认；剩余 P5 远期 + Denova 侧 messageAdapter |
| 2026-08-13 10:40 | AI 2 | 撰写给 Denova 作者的反馈建议《Denova反馈建议-酒馆卡与设定书导入优化.md》：3 个痛点（无 lorebook 导入入口/角色卡解析僵硬（PNG 卡+V3 字段+模板占位符）/导入内容一整条塞满）；3 项建议（新增 lorebook 导入并按条目化映射写入 items.json v2（keywords/type/importance/load_mode/brief_description）、角色卡导入增强（PNG tEXt/V3 字段/多开场白/{{char}} 清理/预览确认）、导入反馈与可逆）；附本地已验证实现（extractCharaFromPNG/normalizeCardObject/parseBookEntries/denova_lore_sync）供参考 | `docs/Denova反馈建议-酒馆卡与设定书导入优化.md` | 可提交 GitHub Issues 或 Discord；如需英文版可补译 |
| 2026-08-13 10:20 | AI 2 | **命令面板「显示全部命令」开关**：用户反馈命令面板缺 导出Denova/生成小说/离线游戏/掷骰/Denova写作台/Denova游戏模式 6 条——根因是 P0 场景预设 cmdFilter 按场景隐藏 writing/game 组；新增 cmdShowAll 开关（复选框+localStorage 记忆）+ 隐藏数量提示，一键查看/使用全部 20 条命令；无头测试 64 项全过 | app/app.js、app/index.html、代码指南.md | 场景过滤（渐进披露）保留，showAll 是可发现性补充 |
| 2026-08-13 09:50 | AI 2 | **统一回溯中心（P-B）完成**：剧情线抽屉升级双视图——🗺 分支图（Denova 风格泳道：每条剧情线一条泳道、节点按时间列排布、SVG 贝塞尔连线、6 色分支板、分支 pill 切换）+ 📋 列表（原时间轴保留）；回溯点叠加：💾快照（createSnapshot 新增 fromIndex 定位）/ 🌿分支记录 / 📍IF线存档，点击即 读档/切分支/切线；「从节点开新分支」= rewindTo(fromIndex,true)（旧路径存分支可切回）；视图切换记忆（adventureAI_timelineView）；无头测试 60 项全过 | app/app.js、app/index.html、代码指南.md | B 项完成；A（导演台状态中心）待做 |
| 2026-08-13 09:10 | AI 2 | 安装 superpowers 技能库（obra/superpowers，★27 万 agentic 开发方法论框架）到 Codex 技能目录 `<user-profile>\.codex\skills\`：用本机 skill-installer 脚本从 main 分支安装 14 个技能——brainstorming（头脑风暴）/dispatching-parallel-agents（并行子代理）/executing-plans（执行计划）/finishing-a-development-branch（收尾开发分支）/receiving-requesting-code-review（代码评审双向）/subagent-driven-development（子代理驱动开发）/systematic-debugging（系统化调试）/test-driven-development（TDD）/using-git-worktrees（git 工作树）/using-superpowers（入口）/verification-before-completion（完成前验证）/writing-plans（写计划）/writing-skills（写技能） | `<user-profile>\.codex\skills\*` | 这是软件开发方法论技能（非创作类），下个 turn 起自动生效；注意与项目共享 Skill 库（<shared-skill-library>）是两套体系 |
| 2026-08-12 16:56 | AI 1 / SeniorDeveloper | **整合规划说明书升级 v4（决策锁定版）**：用户就 v3 附录 B 的 5 个决策点拍板：①统一外壳=iframe 内嵌 Denova Web（非跳转）；②纯本地无云端同步；③视觉取第三套中性「工作室中性」为默认基线（平台暗色游戏风/Denova 安静降为可选主题）；④导演台入口=右栏 Tab（全屏为二级展开）；⑤功能组织=场景预设驱动显隐。据此产出 v4，归档 v3 至 `docs/归档/平台整合规划说明书-v3.md`。v4 新增：§3.1 三通道架构图（iframe 表面 / HTTP API 数据 / 桥接 :8097 批量 + postMessage 跨 iframe 命令）；§4.7 场景化预设（5 内置预设：灵感探索/严肃长篇/互动游戏/快速试玩/世界观构建，随 Work 保存，驱动右栏 Tab/导演台字段/命令面板过滤/视觉密度）；附录 B 决策锁定值表；附录 D 预设矩阵；附录 E postMessage 桥接契约草案（信封/5 cmd/origin 白名单/ver 版本化/Denova 侧 messageAdapter）。路线调整：P2 升为「导演台+场景预设」并纳入首期（P0+P1+P2），P5 改为换机迁移/局域网远程。 | `docs/平台整合规划说明书.md`、`docs/归档/平台整合规划说明书-v3.md` | 下一步：按 P0 拆任务（iframe 嵌入+postMessage 契约+本地 Profile） |
| 2026-08-12 16:20 | AI 1 / SeniorDeveloper | **撰写《统一大平台整合规划说明书》**：用户要求把文字冒险平台与 Denova 整合为统一大平台，输出结构清晰的规划说明书（7 要点：目标背景/用户定位/架构规划/功能整合/体验一致/实施路线/风险备注）。基于已读的结合方案 v1、代码指南、导出链路三件套撰写，落地 `docs/2026-08-12-统一平台整合规划说明书.md`。核心主张「统一外壳+双引擎」（不 fork Denova），对话/写作/游戏三姿态并列 + 共享数据层(M5)/用户系统(M4)/导航(M6)；去重/互通/协同三策略；双协同（写作驱动对话、游戏进度关联对话）；P0-P5 阶段路线（建议首期 P0+P1）；双引擎并存/数据格式/工具杂乱风险应对。附可复用资产清单 + 4 个待评审决策点（外壳内嵌vs跳转/是否云端账号/首期范围/视觉基线）。 | `docs/2026-08-12-统一平台整合规划说明书.md` | 下一步：用户评审 4 决策点后立项排期 |
| 2026-08-12 08:40 | AI 2 | **P4 视觉统一完成**：①theme-neutral.css 第三套中性主题（body[data-theme=neutral] 变量层+表面覆盖，默认仍暗色游戏风）；②getTheme/setTheme/initTheme（localStorage adventureAI_theme，启动应用）；③命令面板新增 主题暗色/主题中性/打开 Denova 写作台/打开 Denova 游戏模式 4 条命令；④右栏 Tab 记忆（adventureAI_rightTab）+ 启动恢复；无头测试 50 项全过；代码指南已更新 | app/theme-neutral.css、app/app.js、app/index.html、代码指南.md | v4 路线 P0-P4 全部完成，剩 P5 远期（换机迁移/远程） |
| 2026-08-12 08:10 | AI 2 | **P3 写作工程化完成**：①大纲/细纲——adventure.novelPlan（outline+groups）、generateNovelOutline（LLM 主线+章节组细纲）、renderNovelPlan/saveNovelPlan（小说弹窗大纲区）、buildNovelSystemPrompt 注入大纲细纲（一键生成按大纲写作）；②版本账本——adventure.versionLedger（ts/type/object/blobId，对齐 Denova ledger 语义）、createSnapshot 写账本、snapshotDiff 对比最近两次（角色字段/对话条数/摘要）、存档弹窗「🔄 对比最近两次」；无头测试 45 项全过；代码指南已更新 | app/app.js、app/index.html、代码指南.md | P4 视觉统一待做（v4 路线下一阶段） |
| 2026-08-12 07:40 | AI 2 | **修复「导演台完全不生效」**：根因——导演指令只注入普通模式 buildSystemPrompt，酒馆模式（tavern，用户主玩）完全没注入，且 tavern 提示词写死「不涉及骰子检定」；修复：抽公共 `buildDirectorPromptBlock(adventure)` 注入 普通模式/酒馆模式/syncSystemPrompt customPrompt 覆盖分支；tavern 检定措辞改为「导演台启用时按 [CHECK] 输出」；导演块补输出示例（CHECK/EVENT_NEW）增强 AI 遵守度；无头测试扩到 39 项全过（含两模式注入/停用不注入断言） | `app/app.js`、`代码指南.md` | 若仍不产出 [EVENTS]/[CHECK]，需进一步强制（每轮必输出或改为对话内提示） |
| 2026-08-12 07:10 | AI 2 | **P2 导演台 + Denova 设定书 AI 入库完成**：①协议扩展 [EVENTS]/[CHECK]（parseGameResponse/applyParsedResult/buildSystemPrompt 导演指令）；②冒险对象 director 字段 + directorInit 兜底；③右栏 🎬 导演台面板（renderDirectorPanel/toggleDirector）：启用时拉取 Denova 18 预设 → DIRECTOR_THEME_MAP 题材映射 → 填目标/事件卡/规则标签；检定引导走平台 /d20；④加载库 Denova 条目「📥 AI 整理入库」→ aiOrganizeDenovaBook（LLM 中文整理 → 桥接 import-kb 写入知识库 denova-<工程名>.json）；⑤桥接新增 director-presets/import-kb GET/POST 接口（实测通过）；无头测试 35 项全过；代码指南已更新 | tools/denova_bridge.py、app/app.js、代码指南.md | 事件卡/检定经协议自动追踪；Denova 侧 messageAdapter 仍待源码配合 |
| 2026-08-12 06:20 | AI 2 | **P1 对话固化完成（接 AI 1 P0）**：①桥接 denova_bridge.py 新增 GET /api/denova/projects（10 工程+lore 数实测）与 /api/denova/lore?book=（老板 101 条实测）；②前端 loadDenovaBooks() 把 Denova 工程 lore 转成可挂载设定书条目（Denova·前缀、【关键词】content、summary 类型统计、category=Denova工程）缓存 state.denovaBooks；③gatherLoadItems 合并（加载库可直接勾选挂载进冒险背景书）；④renderLoadLibrary 首次打开自动拉取；⑤导出完成 postToDenova sync-state/export-done 通知 + 导出弹窗「🔗 在 Denova 中打开」按钮（openDenovaShell）；无头测试扩到 24 项全过；代码指南已更新 | `tools/denova_bridge.py`、`app/app.js`、`app/index.html`、`代码指南.md` | 写作侧成稿→加载库挂载链路已通；Denova 侧 messageAdapter 待源码配合（现 no-op） |
| 2026-08-12 05:42 | UI Designer | **UI 重设计落地真实代码**：确认原型方向后直接落地（字体模仿 Denova=Inter、对话显示思考过程）。新增 `app/redesign.css`（style.css 之后加载，仅覆盖视觉令牌与关键表面，原 2000 行 style.css 零改动）；改 `index.html`（顶栏 14 按钮→3常驻+「✎创作」下拉+视图图标簇+命令面板；右栏 8 段→常驻速览+Tab；加 cmd-overlay）+ `app.js`（命令面板/下拉/Tab JS + 思考过程捕获渲染）。思考过程：readStream 捕获 reasoning_content/thinking→流式实时显+存 msg.thinking→renderStory 折叠显示，仅流式模式。右栏渲染器按原 ID 写 DOM 包裹进 .rp-body，逻辑未改。node --check 通过，结构闭合核对 OK。 | `app/redesign.css`、`index.html`、`app/app.js`、`代码指南.md` §13 | 下一步：浏览器实测/微调 |
| 2026-08-12 05:40 | AI 2 | **整合规划说明书收敛为 v3 权威版**：合并 AI 1《统一平台整合规划说明书》（统一外壳+双引擎骨架）与 AI 2《实地版》（305 函数/30 Go 模块等实地证据与 5 处修正），产出唯一现行版 `docs/平台整合规划说明书.md`（v3.0，含版本沿革 §8 与附录 A 复用资产/B 待评审决策/C 实地证据）；其余 5 份历史文档移入 `docs/归档/` | `docs/平台整合规划说明书.md`、`docs/归档/*` | 待评审决策点 5 项见附录 B（外壳承载方式/云端同步/首期范围/视觉基线/导演台入口） |
| 2026-08-12 05:32 | UI Designer | **UI 重设计 · 可点击原型**：基于确认范围（重设计平台UI·桌面网页·暗色游戏风）产出独立零依赖原型 `app-redesign-preview.html`，复刻三大重设计——①顶栏 14 按钮收口为 3 常驻+「✎创作」下拉+「视图」图标簇+Ctrl/Cmd+K 命令面板；②右栏 8 段长滚改常驻状态速览+Tab；③创建向导分节卡片化。含设计令牌/组件规范/对比度修正(WCAG AA)。用户选「先出可点击原型」。未落真实代码，待确认后并入 app/。 | `app-redesign-preview.html` | 下一步：用户确认后落 Phase1-5 |
| 2026-08-12 05:10 | AI 2 | **基于实地阅读重写整合规划说明书（v2）**：下载 Denova 源码（<external-denova-source>，9.3MB）实地读取 30 个 Go 模块 + web/src 全部功能面 + 18 个导演预设文件 + 10 个本地工程；平台侧提取 305 函数/40 分组/18 弹窗/4 抽屉/5 脚本/知识库规模；产出 docs/平台整合规划说明书-实地版.md（7 章 + 附录 A/B 证据清单）与 docs/平台整合规划说明书-版本比对.md；关键修正：①Denova 剧情工程化比认知强（interactive 100+ 文件），弱的只是轻量入口；②发现平台已有掷骰体系（d20/随机事件），检定应融合而非另起炉灶；③移植范围收敛为 导演台+检定规则 子集；④发现 Denova 原生 卡导入/小说导入 组件；⑤预设实为 18 文件非「七套」 | docs/平台整合规划说明书-实地版.md、docs/平台整合规划说明书-版本比对.md、<external-denova-source>（源码副本） | 建议以 v2 为实施基准 |
| 2026-08-12 04:40 | AI 2 | 撰写《平台整合规划说明书》`<external-text-adventure-project>\docs\平台整合规划说明书.md`：7 大章节（整合目标与背景/用户偏好与定位/平台架构规划/功能整合方案/用户体验一致性/阶段性实施路线/风险与备注）；含重叠域比较与优化空间、双引擎协同闭环（灵感→设定→冒险→章节→大纲→反向驱动）、M0-M5 里程碑（M0 基础互通已完成，M1 导演台 → M2 写作工程化 → M3 版本账本 → M4 导航视觉统一 → M5 深度协同）；风险覆盖 技术/体验/数据/维护 | `docs/平台整合规划说明书.md` | 与《2026-08-11-Denova结合方案.md》配套：说明书定方向里程碑，结合方案定落地细节 |
| 2026-08-12 04:30 | AI 1 | **ai知识库中文简介补全（自读原文、自己写总结；B 示范批次已确认风格后推进）**：据用户要求把 世界观设定/角色卡 的 `设定集元数据.json` 简介（summary）补全——严格基于原文、保留风格/语气/用词（含露骨词不洗白）、不改编不删改；`{{char}}`/`{{user}}` 一律删/替换为主人公/主角；英文原文译地道中文。本轮完成 9 条（示范批次 8 条 + Sexual positions）：①5 本设定书——火影忍者、chub-赛博朋克2077、chub-哈利波特、女尊世界、Sexual positions_world_info（18 条体位，原被错标 nsfw:false/category:其他，本次翻译并修正为 nsfw:true/category:NSFW/tags）；②4 张角色卡——申鹤、喜多川海梦（secretly slutty/身体挑逗）、塞拉菲娜女王（含 NTR/gentle femdom/breeding）、精液珍贵世界·叙述者（旁白驱动、绝不替主人公发言）；用脚本 `update_meta_pilot.py` 原子写入并校验（50 书/39 卡 总数不变）。同步 `说明文档.md`：8 行描述更新（火影/赛博/哈利波特/女尊/申鹤/海梦/塞拉菲娜/精液世界）+ 新增 Sexual positions 行归 NSFW 段 | `<external-text-adventure-project>\ai知识库\设定集元数据.json`、`<external-text-adventure-project>\ai知识库\说明文档.md`、`.workbuddy\tmp\update_meta_pilot.py` | 用户风格确认：「1 就这样，{{char}}/{{user}} 删/替换主人公/主角，露骨放开」；B 批次先确认质量再铺开；此前口误"剩余85/81"更正为 80（89-9=80：45 设定书+35 角色卡） |
| 2026-08-12 04:10 | AI 2 | **导出补「小说全文.txt」**：用户指出「下载全部(.txt)」生成的小说全文没进 Denova → 前端导出时按同款格式拼接全文（第 N 章 · 标题 + 正文，章节间空行）随 `novel.fullText` 传入，桥接写入 Denova 工程根 `小说全文.txt`（兼容顶层 novelFullText）；端到端验证通过 + 无头测试扩到 17 项全过 | `app/app.js`、`tools/denova_bridge.py`、`代码指南.md` | 与分章 chapters/*.md 并存，Denova 可 read_file 读全文 |
| 2026-08-12 03:58 | AI 1 | **Denova 方案 5 条建议全部落地 + 修 Denova 路径（用户澄清"把 1 2做了"=把全部5条写进方案+修books.json）**：①方案文档 `docs/2026-08-11-Denova结合方案.md` 现含全部 5 条——第1/2项 Step0/Step1 优化（前次已写）、第3项 新增 Step 0·b「导出到 Denova 一键衔接」(~0.5天)、第4项 Step3 账本格式对齐 Denova `changes/ledger.jsonl`+`blobs/`、第5项 路径问题记为已知问题并补 §8 环境路径；状态行/§7修订记录同步；②**直接修复 Denova 内部** `<external-denova-installation>\.denova\books.json` 第二条 "11" 工程旧路径 `<legacy-denova-root>\...`→`<external-denova-installation>\.denova\projects\11`（项目实际存在，仅前缀过期，JSON 校验通过）；Denova 安装路径常量统一 `<external-denova-installation>` | `<external-text-adventure-project>\docs\2026-08-11-Denova结合方案.md`、`<external-denova-installation>\.denova\books.json` | 平台代码仍零改动；修订前 03:54 那条"3/4/5未采纳"作废，以此条为准；books.json 改动属 Denova 安装目录内部数据，非平台代码 |
| 2026-08-12 03:54 | AI 1 | **Denova 结合方案 Step0/Step1 描述优化（按源码实测，未改平台代码）**：据 2026-08-12 读 Denova 源码结论，把 `docs/2026-08-11-Denova结合方案.md` 第 1(Step0)、2(Step1) 项建议写入方案文档。①Step0 素材互通——角色卡 Denova 原生支持导入酒馆卡，删除"角色卡→lore"转换方向，只保留 设定书(lorebook)→Denova lore(`.denova/projects/<book>/.denova/lore/items.json` 纯 JSON) 单向转换 + 反向；工期 1-2天→~1天；②Step1 导演台——状态系统/事件卡包直接复用 Denova `story-director-modules/` 下 apocalypse/xiuxian/western-fantasy/infinite-flow/urban/default/trpg 七套题材预设（module_refs 可插拔 + `*_disabled` 开关），按平台 26 类冒险类型映射，不再从零手搓词条库；工期 3-5天→~2-3天；③同步更新文档状态行（原稿 AI2→修订 AI1）、风险节（"lore 格式需先读源码"→已读源码确认）、新增 §7 修订记录；④待定建议（Step3 账本对齐 Denova ledger.jsonl、新增"导出到 Denova"按钮、Denova 的 books.json 过期路径）用户暂未采纳，保留原方案 | `<external-text-adventure-project>\docs\2026-08-11-Denova结合方案.md` | 用户原话「把 1 2做了」=做方案文档第1(Step0)、2(Step1)项优化；纯文档改动，平台代码零改动；第3/4/5项待拍板 |
| 2026-08-12 03:50 | AI 2 | **导出到 Denova 增强（章节+设定文档+进度条）**：①**一键生成章节接入**：`generateNovelChapter` 生成结果持久化到 `adv.novelChapters`（IndexedDB，刷新不丢），弹窗打开时恢复，导出时写入 Denova `chapters/v00001-第一卷/ch0000N-标题.md`（纯文本剥 #/>/- 标记）并更新 progress.md/outline.md/character-states.md；②**一键生成全部设定**：`generateSettingsDoc(adv, silent)`（LLM 生成中文设定文档，按 世界设定/角色/规则/物品 分节，失败降级原文），小说弹窗加「📜 生成设定文档」「⬇ 下载设定txt」按钮，导出时写 `setting/设定汇总.txt`；③**可视化进度条**：新增 `denovaExportModal` 进度弹窗 + `setExportProgress`（5%准备→10%摘要→40%设定→65%写入→100%完成，显示百分比/阶段/明细）；无头测试 16 项全过，端到端验证章节写入/中文汇总/进度更新 | `app/app.js`、`app/index.html`、`tools/denova_bridge.py`、`代码指南.md` | 章节需先在一键生成中完成才随导出带入 |
| 2026-08-12 03:20 | AI 2 | **导出到 Denova v3（按 Denova 文件导入模板重构 + 恢复 NSFW 词条）**：用户要求①不要动 NSFW 词条 → 移除 v2 的 nsfw 标记/标题正则过滤，词条书全量导出（上限放宽每本 200/总量 500，去重保留）；②结合 `<external-denova-installation>\Denova 文件导入模板.txt` 重构导出：生成 CREATOR.md（剧情摘要+核心创作规则模板）/ideas.md（类型=theme、其他要求）/setting/outline.md（主线=goal）/setting/progress.md（回合数+【当前场景/近期事件/当前局势/伏笔】从摘要提取）/setting/character-states.md（逐角色卡生成）；lore 条目对齐模板：importance（character→major 其余 important）、load_mode（character/faction→resident 其余 auto）、brief_description（"类型 名称。前3句"）；前端 payload 加 theme/turns；端到端实测（NSFW 词条保留+文件齐全）+ 无头测试 11 项全过 | `tools/denova_bridge.py`、`app/app.js`、`docs/2026-08-11-Denova结合方案.md`、`代码指南.md` | 模板文件来自用户：`<external-denova-installation>\Denova 文件导入模板.txt` |
| 2026-08-12 02:40 | AI 2 | **导出到 Denova 质量修复（v2）**：用户实测反馈导出太烂，定位 4 个根因：①剧情摘要=AI 回复原文拼接（无总结、NSFW 全文外泄）→ 改为 LLM 生成结构化摘要（contextSummary 优先、callLLM 生成、失败降级）；②lore 全量灌入 NSFW 词库（申鹤工程 75 条全是 BBC/penis 词条）→ 桥接跳过 nsfw 标记书 + 标题命中 NSFW 词库正则（术语/词库/性/催眠等）双保险 + 条目去重 + 每本 40/总量 80 上限 + 类型排序；③设定只有一段 → 解析 adv.setting 四段（世界背景/玩家身份/初始目标/其他）分节写 CREATOR.md 与 setting/；④卡片 {{char}} 占位符残留 → 清理；denova_bridge.py 升级 v2、exportToDenova() 重写，端到端实测 + 无头测试 9 项全过 | tools/denova_bridge.py、app/app.js、docs/2026-08-11-Denova结合方案.md、代码指南.md | 用户旧导出的「申鹤/冒险者」工程内容为旧版产物，重新导出即覆盖 |
| 2026-08-12 02:10 | AI 2 | **Denova 结合方案 Step 0 + Step 0·b 落地**：①`tools/denova_lore_sync.js`（export/import/list）——平台设定书 → Denova lore（items.json v2，entry→item：keywords/content/type 启发式推断），实测导出 火影忍者153条/龙与地下城68条/异世界奇幻RPG28条 并注册 books.json，反向回测 135 条可读；②`tools/denova_bridge.py`（8097，POST /api/denova/export）——前端一键导出当前冒险（设定书+角色卡+剧情摘要）为 Denova 工程（lore/CREATOR.md/setting/），角色卡转 character lore（Denova 原生导入之外的快捷通道）；③顶栏「🚀 导出Denova」按钮 + `exportToDenova()`；④无头测试 `test_denova_export.js` 7 项全过；⑤方案文档/代码指南已更新，测试工程已清理 | `tools/denova_lore_sync.js`、`tools/denova_bridge.py`、`app/app.js`、`app/index.html`、`docs/2026-08-11-Denova结合方案.md`、`代码指南.md` | 下一步 Step 1 剧情导演台（复用 Denova story-director-modules 七套预设） |
| 2026-08-11 23:50 | AI 2 | **Denova × 平台结合方案（调研，未改代码）**：全量比对两边 8 个功能域（写作/剧情/角色设定/Agent 自动化/上下文/版本/图像/体验），读 Denova DESIGN.md 与仓库结构、18 个 Skill、平台代码指南；结论：平台保持轻量入口，按渐进披露移植 Denova 的 剧情导演台（目标/压力/代价/事件卡包/检定/状态系统）→ 大纲/细纲/审稿 → 版本账本；Step 0 先做素材互通（平台知识库 ↔ Denova lore 转换脚本）；方案文档 `<external-text-adventure-project>\docs\2026-08-11-Denova结合方案.md`；待用户拍板方向/优先级 | `<external-text-adventure-project>\docs\2026-08-11-Denova结合方案.md` | 待确认 |
| 2026-08-11 23:05 | AI 2 | **Pixiv P2+P3 完成**：①桥接新增 5 接口：/ugoira（动图拉 zip 解帧缓存 tools/.ugoira_cache/，返回帧序列+delay）、/ugoiraframe（缓存帧直出）、/bookmark 与 /unbookmark（收藏/取消）、/dataurl（≤10MB base64）；详情接口加 is_bookmarked；全部实测通过（真 ugoira 52 帧、收藏加删无残留）；②前端看图器加 播放动图/收藏/换一个/设为冒险场景图（注入当前冒险最后一条 AI 消息 sceneImage + saveState + renderStory）；③加载预览底部加 搜图参考（自动带卡片名/书名）、小说弹窗加 搜封面图（自动带书名）；④无头测试扩到 17 项全过；⑤app.js 统一 LF 行尾 | app/app.js、app/index.html、tools/pixiv_bridge.py | 待用户试用反馈 |
| 2026-08-11 22:15 | AI 2 | **Pixiv 接入 P0+P1 完成**：①装 pixivpy3.7.0；②新增本地桥接 `<external-text-adventure-project>\tools\pixiv_bridge.py`（127.0.0.1:8098，refresh_token 登录成功【用户 Sjr】，代理 7897，0.8s 节流；接口 status/search/ranking/illust/user/img 代理/download 保存到 `ai-images/pixiv/`；token 存 `tools/pixiv_config.json`，不进日志）；③前端重构：pixiv 弹窗改 搜索/排行榜 双页签 + 结果网格 + 详情看图器（多图合集逐页原图/上一张下一张/随机/R18 角标与 NSFW 开关联动/下载原图/原站打开），替换原「仅跳链接」版；④无头冒烟测试 `<external-text-adventure-project>\.workbuddy\tmp\test_pixiv.js` 19 项全过（抓到并修复 `const pages` 赋值 bug）；⑤平台服务 8099 已重启；⑥代码指南新增「Pixiv 搜图（App API 本地桥接）」章节 | `app/app.js`、`app/index.html`、`app/style.css`、`tools/pixiv_bridge.py`、`tools/pixiv_config.json` | P2 看图器细化（动图/收藏）与 P3 创作联动（章节配图/头像搜索）待用户确认 |
| 2026-08-11 21:10 | AI 2 | 调研 Pixiv App API 接入方案（未改代码，待用户确认）：候选①akameco/pixiv-app-api（TS/MIT/★226/2023 停更，Promise 客户端，metaPages 直接给多图原图）；②upbit/pixivpy（Python/★2046/活跃维护，refresh_token 登录+内置国内网络绕过）。推荐 pixivpy 做本地桥接服务（`<external-text-adventure-project>\tools\pixiv_bridge.py`，端口 8098）+ 前端 Pixiv 面板；能力：站内搜索/排行/看图器（多图合集/动图/收藏/随机）/图片本地代理（带 Referer）/保存到 ai-images/章节配图/头像搜索。方案已交用户，待确认 3 问：refresh_token 获取方式、本机代理端口、R18 是否显示 | 无（纯调研） | 待用户确认后实施 P0（装 pixivpy+起桥接）→P1（搜索+排行）→P2 看图器→P3 联动 |
| 2026-08-11 20:30 | AI 2 | 把共享 Skill 库归档进 **<shared-skill-library>\**（与其它 AI 工具统一收纳）：复制 `.workbuddy\skills\` 下 5 个 skill（text-adventure-collab / text-adventure-prompt-protocol / headless-frontend-regression / knowledge-base-governance / stickman-tuning）；新增 `<shared-skill-library>\README.md` 索引（用途/何时调用/权威副本与运行时镜像关系/新增修改流程）；协作日志新增「Skill 库位置与调用说明」章节，向 AI 1/2/3 说明位置与调用方式 | `<shared-skill-library>\*` | 权威副本=AI工具，运行时镜像=.workbuddy\skills，改 skill 两处同步 |
| 2026-08-11 20:10 | AI 2 | 应要求把散在 E 盘根目录的 AI 项目统一收进 **<external-tools-root>\**：①停掉运行中的 denova.exe / AI-Novel 开发服务 / Obsidian 后移动 4 个文件夹（AI-Novel-Writing-Assistant、Denova、Obsidian素材库、Obsidian参考库）；②同步更新所有引用路径：素材库内 使用说明/新手导航/教程摘要、参考库 00-中文说明、Denova 使用说明、`<external-text-adventure-project>\tools\gen_obsidian_vault.js` 与 `gen_obsidian_static.js` 的 VAULT 路径、Obsidian 仓库注册表 `%APPDATA%\obsidian\obsidian.json`（JSON 转义需用 JSON.parse 更新，字符串替换会漏）；③全部重启验证：AI-Novel 5173/3000、Denova 8080、Obsidian 4 进程正常；④pnpm 因项目移动要求重装依赖（58.9s，store 缓存命中，better-sqlite3 未再编译失败）；⑤新增 `<external-tools-root>\说明.md` | `<external-tools-root>\*` | 历史日志中的旧路径为当时事实，保留不改 |
| 2026-08-11 19:06 | AI 1 | **审查 A2 在创建向导的改动 + 修复 2 个 bug**：①`customProfession` 在 `showNewAdventureModal` 未重置 → 重开弹窗时上一冒险的自定义职业预填进新冒险（编辑器 `pvProfCustom` 泄漏）；已加 `customProfession=''` 重置；②`goAdventureStep(1)` 回退挂载页时不重渲染加载库 → 第 2 步「移除设定书」后第 1 步复选框仍显已勾（UI 陈旧）；已加 `if(advStep===1) renderLoadLibrary()` 重渲染；另更新过时的向导测试桩（A2 把单一冒险设定文本框拆成 世界背景/玩家身份/初始目标/其他 结构化字段，旧桩 `pvSetting` 引用崩溃） | `app/app.js`、`代码指南.md`、`.workbuddy/tmp/test_adventure_wizard.js` | 审查结论：A2 的向导增强（引导/角色卡自动拆分/冒险设定结构化/主题折叠/列表模式/AI 补充/IF 线修复/全中文输出）逻辑自洽、与既有数据流转闭环；**但仍发现**：A2 声称的回归测试 `test_wizard_v3.js`/`test_wizard_ifline.js`/`test_ai_fill.js` 实际不在项目 `.workbuddy/tmp`（未落盘），无法复核；`collectRecommendBooks` 因移除自动勾选已成死代码（保留无害）；`startNewAdventure` 无 API key 时直接 `showSettings()`（长存门禁，非本次引入） |
| 2026-08-11 18:05 | AI 2 | 独立安装 Denova v0.3.3（alfredxw/denova，AI 小说创作+互动 RPG 平台，Go 单文件，自带中文界面）到 `<legacy-denova-root>`，**不整合**进文字冒险平台：GitHub Release 下载 windows-x64.zip（22,060,341 字节）并 SHA256 校验通过（e60d100f...823975）；解压拍平目录，`denova.exe` 后台启动，前端+后端合一运行在 **http://localhost:8080**；内置 18 个 Skills；默认模型模板 DeepSeek（key 留空待设置页填）；写 `<legacy-denova-root>\使用说明.md`。注意：5173 仍被 AI-Novel-Writing-Assistant 占用，Denova Release 版不走 5173 | `<legacy-denova-root>`、`<legacy-denova-root>\使用说明.md` | 与平台无关，独立安装 |
| 2026-08-11 17:05 | AI 2 | Obsidian 素材库新手向梳理：①新增 `E:\Obsidian素材库\新手导航-从这里开始.md`（一张地图：素材库=用户只用管/参考库=AI 参考不用看/插件=后台工具不用懂 + 三件必会 + 9 插件一句话中文说明 + 首次使用路径 + 常见疑问）；②`00-欢迎页.md` 顶部加导航入口；③`E:\Obsidian参考库\00-中文说明.md`（每个参考库一行中文说明 + 可删提示）；④尝试 `.obsidian/appearance.json` 加 `language: zh-cn` 切简体中文界面（待用户重启确认，若无效需手动 设置→关于→语言） | `E:\Obsidian素材库\新手导航-从这里开始.md`、`E:\Obsidian素材库\00-欢迎页.md`、`E:\Obsidian参考库\00-中文说明.md`、`E:\Obsidian素材库\.obsidian\appearance.json` | 用户反馈插件/参考库全英文看不懂、方向迷茫，已用纯中文导航梳理 |
| 2026-08-11 16:45 | AI 2 | **Obsidian 素材库接管配置**：①为 `E:Obsidian素材库` 安装 9 个社区插件到 `.obsidian/plugins/`（Dataview 0.5.68 / Templater 2.25.0 / obsidian-git 2.38.6 / Kanban 2.0.51 / Excalidraw 2.26.4 / Longform 2.1.0 / Novel Word Count 5.0.0 / WebNovel Assistant 3.8.1 / Iconize 2.14.7，全部官方 GitHub Release 直链下载并核对 manifest），`community-plugins.json` 已写入启用列表；②写 `E:Obsidian素材库AGENTS.md` 接管契约（生成区 02/03/08/附件 vs 创作区 01/04/05/06/07 划分、新素材入库流程、重生成命令、中文优先）；③`使用说明.md` 追加插件清单/启用方法/Codex 接管章节；④`E:文字冒险平台	oolsgen_obsidian_vault.js` 改为**安全版**（只重建 02/03/08 与附件，保留创作区，可反复跑），已实测重跑通过（50 本 + 39 卡 + 索引） | `E:Obsidian素材库.obsidianplugins*`、`E:Obsidian素材库AGENTS.md`、`E:Obsidian素材库使用说明.md`、`E:文字冒险平台	oolsgen_obsidian_vault.js` | 待用户在 Obsidian 设置里启用插件（安全模式需界面确认） |
| 2026-08-11 16:20 | AI 2 | 搭建 Obsidian「小说/酒馆冒险·设定与角色素材库」（独立于平台，纯素材库建设）：①GitHub 下载 4 个参考库到 `E:\Obsidian参考库`（`obsidian-just-write` 中文小说写作库 / `obsidian-novel-starter-vault` 小说起步库 / `obsidian-worldbuilding-template` 世界观模板 / `ttrpg-campaign-vault` 跑团战役库）；②从 `<external-text-adventure-project>\ai知识库` 导入 50 本世界观设定 + 39 张角色卡，生成 Obsidian 库 `E:\Obsidian素材库`（103 条笔记：frontmatter 属性 + 中文简介 + 条目清单/人设/开场白 + 23 张头像 + 90 个原始 JSON 归档 + 分类索引 + 5 套模板 + `.obsidian` 配置 + 中文使用说明/教程摘要）；③安装 Obsidian 1.13.6（官方 GitHub Release，用户目录安装）并打开该库；④生成脚本存档 `<external-text-adventure-project>\tools\gen_obsidian_vault.js`（导入）与 `gen_obsidian_static.js`（静态文件），知识库更新后可重跑刷新 | `E:\Obsidian素材库`、`E:\Obsidian参考库`、`<external-text-adventure-project>\tools\gen_obsidian_*.js` | 已装 Obsidian 1.13.6 并启动打开素材库 |
| 2026-08-11 15:45 | AI 2 | 独立安装 AI-Novel-Writing-Assistant（github.com/ExplosiveCoderflome/AI-Novel-Writing-Assistant，长篇小说 AI 创作系统）到 `E:\AI-Novel-Writing-Assistant`，仅试玩、**不整合**进平台：pnpm workspace 依赖装齐（better-sqlite3 用 GitHub Release 预编译 node-v137 二进制绕过本地编译失败）；修 `E:\AI-Novel-Writing-Assistant\server\scripts\ensure-dev-prisma.cjs`，首次启动自动创建 `server/dev.db`（Windows 下 Prisma 7 schema engine 不会自动创建 SQLite 文件，`prisma db push` 报空白 Schema engine error）；`server/.env` 关 RAG（RAG_ENABLED=false）。启动：`cd E:\AI-Novel-Writing-Assistant; pnpm dev` → 前端 http://localhost:5173 / 后端 http://localhost:3000（/api/health 返回 200） | `E:\AI-Novel-Writing-Assistant\server\scripts\ensure-dev-prisma.cjs`、`E:\AI-Novel-Writing-Assistant\server\.env` | 与文字冒险平台无关，独立安装 |
| 2026-08-11 05:24 | AI 2 | AI 补充输出改为**全中文**：`AI_EXTRACT_SYSTEM` 与 `aiFillSetting`/`aiFillCard` 提示词新增要求——外文翻译成自然地道中文（避免机翻腔）、英文人名/地名本地化（Konoha→木叶、Serena→塞蕾娜）、不出现英文名；仍保留「严格基于原文、保留风格/语气/用词（含敏感词）、不改编不删改」 | `app/app.js` | 回归 4 项全过 |
| 2026-08-11 05:10 | AI 2 | ①第 1 步勾选区**移除预览按钮与上传表单栏**（只留勾选），编辑统一在第 2 步微调；第 2 步新增「⬆ 上传角色卡」（JSON/TXT/PNG chara，导入即自动拆分）；②第 2 步冒险设定区新增**主设定卡下拉 + ✨ AI 自动补充**（`aiFillSetting`：AI 读设定书原文提取 世界背景/玩家身份/初始目标/其他说明，空字段填充）；③角色卡新增 **🤖 AI 补充**（`aiFillCard`：AI 读备注原文提取 外貌/性格/关系/背景，空字段填充、背景兜底进备注）；统一提示词 `AI_EXTRACT_SYSTEM` 要求**严格基于原文、保留风格/语气/用词（含敏感词）、不改编不删改**（与小说文风保留一致） | `app/app.js`、`app/index.html`、`代码指南.md` | 测试 `test_ai_fill.js` 5/5 + 旧回归 5 项全过 |
| 2026-08-11 04:36 | AI 2 | ①创建冒险加**引导**：打开先问是否添加设定/角色卡（先添加→第1页勾选；不添加→直接第2页编辑），两页共享同一批 state/pending，数据流转见代码指南 §11；②**角色卡字段自动拆分** `autoFillCardFields`：勾选/导入/第2页「✨ 自动拆分」按钮触发，把备注里的 Appearance/Personality/Relationship 等节提取到对应字段（幂等，背景保留在备注）；③**冒险设定结构化**：第2页拆成 世界背景/玩家身份/初始目标/其他 四字段，与第1页文本框双向同步，开始冒险时按前缀合成；④**主题网格优化**：26 类冒险类型折叠为「🎯 冒险类型」分组并按题材归类，内置/自定义主题平铺不再杂乱 | `app/app.js`、`app/index.html`、`app/style.css`、`代码指南.md` | 行为测试 `test_wizard_v3.js` 5/5 + 旧回归 5 项全过 |
| 2026-08-11 04:16 | AI 2 | ①加载设定/角色卡新增**列表模式**（工具栏 卡片/列表 切换，列表类说明文档：名称+中文简介+条数/语言+预览，偏好持久化）；②**自定义职业**：第 1 步新增输入框（优先于职业网格）+ 第 2 步预览可改，任意主题/冒险类型可用；③**IF 线进出修复**：`saveLineResume` 内联保存 history/character/摘要（原只存 snapshotId，快照池 24 条裁剪会把主线存档裁掉导致「进得去出不来」）；新增 `applyLineResume`（内联→快照→旧版回退）；`switchToBranch`/`rewindTo` 分支记录 `lineId` 并恢复所属线路 + `trimPlotNodesPastIndex` 对齐时间轴 | `app/app.js`、`app/index.html`、`app/style.css`、`代码指南.md` | 行为测试 `test_wizard_ifline.js` 4 项全过 + 旧回归 6 项全过 |
| 2026-08-10 21:50 | AI 1 | **细分冒险类型（题材×玩法模式 26 类）+ 推荐知识库自动挂载**：用户拍板「在线冒险主题 + 题材×玩法模式更细（约 24+ 类）」。①新增数据表 `GENRE_PROFESSIONS`（19 题材职业池）/ `GENRE_ITEMS`（各题材初始物品）/ `MODE_FLAVOR`（11 玩法模式风味文案）/ `ADVENTURE_TYPE_DEFS`（26 条 `[名称,题材,模式,起始地点,描述,推荐知识库分类[]]`）→ 经 `ADVENTURE_TYPES` map 生成对象（`name/genre/mode/location/desc/items/professions/recommend`），`getAdventureType(name)` 查询；②接入创建页：`getAllThemes()` 纳入 26 类并打标 `isAdventureType:true`（不可编辑/删除）、`getThemeProfessions` 对冒险类型优先返回其题材职业池、`renderThemeGrid` 对冒险类型隐藏编辑/删除工具、`randomizeAdventureForm` 把冒险类型纳入随机题材池；③`startNewAdventure` 选中冒险类型时以 `opts.themeData`(名称/地点/描述/物品)+`opts.themeKey`+`opts.professionData`+`opts.recommendCategories` 注入，创建后由 `collectRecommendBooks(recommendCategories)` 按分类自动挂载对应设定书（原内联逻辑抽取为独立函数，行为等价）；④适配知识库：`chub-僵尸末日` 由 `末日废土` 改归 `恐怖`（让恐怖分类有真实内容，丧尸类型推荐改为 `末日废土+恐怖`），重跑 `gen_local_library_v2.py`；⑤回归 `.workbuddy\tmp\test_adventure_types.js` 28/28 通过，既有 `test_new_features.js` 15/15 无回归 | `app/app.js`（`GENRE_PROFESSIONS`/`GENRE_ITEMS`/`MODE_FLAVOR`/`ADVENTURE_TYPE_DEFS`/`ADVENTURE_TYPES`/`getAdventureType`/`collectRecommendBooks`/`getAllThemes`/`getThemeProfessions`/`renderThemeGrid`/`randomizeAdventureForm`/`startNewAdventure`）、`ai知识库/设定集元数据.json`、`app/local_library.js`、`.workbuddy\tmp\test_adventure_types.js` | 用户原话「再帮我细分更多冒险类型，先给我拍板决定你再做」→ AskUserQuestion 拍板「在线冒险主题 + 题材×玩法模式更细」。铁律 6 同步代码指南 §主题机制 |
| 2026-08-10 19:16 | AI 1 | **创建新冒险两页面增强（可编辑 + 移除自动勾选推荐书）**：用户增强提示词——第1页（设定与挂载，允许不添加设定书）生成数据 → 第2页（查看并微调）可编辑。①`renderAdventurePreview` 重构为 `renderAdventureEditor`：冒险设定（起始地点/角色名/职业/冒险设定）可微调并写回底层表单 `#characterName`/`#adventureSetting` 与 `state.adventureLocationOverride`/`state.selectedProfession`；②角色信息按 `chara_card_v2` 字段（name/appearance/personality/relationship/notes/first_mes/tags）逐张渲染可编辑卡，每张独立 `💾保存`（`saveCharacterEdit` 深拷贝写回 `pendingLoadCards`，不污染全局知识库对象）；③设定书可在第2页移除（`removePendingBook`）；④**移除「自动勾选推荐书」**：删 `onThemeSelected` 中冒险类型选中即挂载推荐书的逻辑、`startNewAdventure` 的 `opts.recommendCategories` 与过时注释、`state.adventureLocationOverride` 在 `showNewAdventureModal` 重置；明确两页面数据流转（页1选择→页2编辑写回→开始冒险复制进 adv）；⑤`index.html` 第2步标签改「查看并微调」；⑥回归 `test_adventure_wizard.js` 35/35（含「选类型不再自动勾选」「角色卡单独保存写回且深拷贝」「冒险设定微调写回」「允许不添加设定书」），另两套 28/28、15/15 无回归 | `app/app.js`（`renderAdventureEditor`/`rowReadonly`/`profSelectHtml`/`bindAdventureEditor`/`saveCharacterEdit`/`removePendingBook`/`onThemeSelected`/`startNewAdventure`/`showNewAdventureModal`/`goAdventureStep`）、`app/index.html`（步骤标签）、`app/style.css`（`.pv-edit-*`/`.char-edit-*`/`.pv-book-remove`）、`.workbuddy\tmp\test_adventure_wizard.js` | 用户增强提示词：「开发一个两页面的冒险设定生成与编辑工具。第一页面：用户通过上传或设定书和角色卡来生成冒险设定详细信息的展示（允许不添加设定书）。第二页面：用户查看并微调冒险相关详细信息和角色信息。移除原有的自动勾选推荐书功能。明确两页面的数据流转关系，确保第一页面生成的设定可在第二页面进行编辑，角色信息支持单独修改与保存（用之前解读的角色卡格式来看角色信息）」 |
| 2026-08-10 18:25 | AI 1 | **创建新冒险改两步向导 + 加载卡片弹性布局**：用户拍板「两步：①挂载知识库+主题角色同页 → ②确认预览」+「卡片式图鉴」。①`index.html` 弹窗加步骤指示器（①设定与挂载 → ②确认开始），body 拆 `#advStep1`/`#advStep2`，footer 按钮按步骤显隐（下一步/上一步+开始冒险）；②`app.js` 新增 `advStep` 状态 + `goAdventureStep/nextAdventureStep/prevAdventureStep` + `renderAdventurePreview()`（第 2 步汇总预览：冒险设定[主题/模式/起始地点/背景/冒险设定]、玩家角色[名称/职业]、加入的角色图鉴[头像+名称+tags+性格摘要，贴近 SillyTavern 角色卡]、挂载设定书清单[标题+分类+条数]，推荐书标「类型推荐」）；③冒险类型推荐挂载提前到第 1 步——`onThemeSelected` 选中冒险类型即 `collectRecommendBooks` 自动勾选进 `pendingLoadBooks`（可手动取消），`startNewAdventure` 不再覆盖 pendingLoadBooks（尊重用户调整）；④加载卡片改纵向 flex 弹性布局：`renderLoadCard` 新增 `load-card-main`/`load-card-namerow` 结构，名称独占一行（NSFW 徽标紧跟同行换行不再挤压名称），简介/标签/元信息依次排列，修复「卡名称被标签挡住」；⑤回归 `.workbuddy\tmp\test_adventure_wizard.js` 25/25 通过，`test_adventure_types.js` 28/28、`test_new_features.js` 15/15 无回归 | `app/index.html`（newAdventureModal 向导化）、`app/app.js`（`advStep`/`goAdventureStep`/`nextAdventureStep`/`prevAdventureStep`/`renderAdventurePreview`/`onThemeSelected`/`startNewAdventure`/`renderLoadCard`）、`app/style.css`（`.adv-steps`/`.pv-*`/加载卡片 flex）、`.workbuddy\tmp\test_adventure_wizard.js` | 用户原话「创建新冒险后先加入设定书和角色卡，读取后再在下一页显示读取到的信息；角色图鉴尽量符合 ai知识库 格式；加载界面太散乱弄弹性布局，卡名称被标签挡住」。铁律 6 同步代码指南 |
| 2026-08-10 16:49 | AI 1 | **修复「点击创建新冒险没反应」（弹窗打不开）**：根因上一轮「冒险类型细分」接入 `renderThemeGrid` 时，把 `edit`/`del` 工具按钮的 `addEventListener` 误置于 `if (!t.isAdventureType)` 块**外**，而 `edit`/`del` 仅在块内 `const` 声明；遍历到首个冒险类型主题时 `edit`/`del` 未定义 → 抛 `ReferenceError` 致 `renderThemeGrid` 崩溃、`showNewAdventureModal` 中断。修复：两监听器移入块内（冒险类型无编辑/删除工具）。`node`+DOM 桩复现 → 弹窗 `display:flex`；两套回归无回归 | `app/app.js`（`renderThemeGrid`）、`.workbuddy\tmp\repro_modal.js` | 铁律 3 记踩坑记录；根因属变量作用域错位（块内 const 被块外引用） |
| 2026-08-10 16:03 | AI 1 | **知识库迁移到项目内 + 重分类 + 平台适配**：①知识库从 `<user-profile>\Videos\黑袍\ai知识库` 整体迁到 `<external-text-adventure-project>\ai知识库`；②生成脚本迁入 `tools/gen_local_library_v2.py`（KB 常量指向新位置），新增 `tools/rebuild_meta_categories.py` 给 `设定集元数据.json` 每条补 `category` 字段并自动补录/清理条目；③清理根目录孤儿文件——`main_Sexual positions_world_info.json` 归位 `世界观设定/`，备份/小说/下载/快捷方式/散落原始卡移入 `归档/`；④修复坏 JSON `叙述者设定-申鹤反差流.json`（两个 JSON 拼接损坏，提取首完整对象+8 条保留）并删除与 `邪术召唤咒典.json` 完全重复的 `邪秽的力量.json`；⑤平台加载界面 `gatherLoadItems` 优先用库的 `category` 字段、扩展 `LOAD_GROUP_ORDER`（加 校园/恐怖/小说模板、统一「末日废土」）；⑥重生成 `app/local_library.js`（50 书/39 卡/598KB，含 8/10 新增「火影忍者」） | `tools/gen_local_library_v2.py`、`tools/rebuild_meta_categories.py`、`app/local_library.js`、`app/app.js`、`ai知识库/设定集元数据.json`、`ai知识库/说明文档.md`、`.workbuddy/skills/knowledge-base-governance/SKILL.md` | node --check 通过；知识库治理技能路径已同步新位置。铁律 6 同步代码指南 §12.2 |
| 2026-08-10 04:58 | AI 2 | ①Pixiv 看图器升级：搜索/🎲随机/◀▶左右切换/合集多图向下展示（`allorigins` 代理拉 pixiv ajax，图片走 `i.pixiv.re`，合集多图由缩略图推导 `_pN_master1200` 免 pages 接口，失败自动降级为原站链接）；②小说生成保留文风/用词：`buildNovelSystemPrompt` 注入「文风设定」书与自定义规则 + 明确要求不替换/淡化/删改敏感词；③每章自动生成章名（第一行「章名：」，联系上下文、≤8字），显示/复制/下载均带章名 | `app/app.js`、`app/index.html`、`app/style.css`、`代码指南.md` | 行为测试 `test_pixiv_novel2.js` 5 项全过；修复：失败提示被末尾重绘覆盖 |
| 2026-08-10 04:26 | AI 2 | 新增三项：①`ai-images` 成图保存文件夹（场景图加「💾 保存成图」按钮，支持 showSaveFilePicker/降级下载）；②Pixiv 搜图入口（头部 🖼 Pixiv → `pixivModal` 输入关键词 → 跳转 `pixiv.net/search?q=…&s_mode=tag&type=artwork`）；③一键生成小说（头部 📖 小说 → `novelExportModal`，10 回合=1 章，`buildNovelChapters` 按 user 消息分章、`stripGameTags` 剔除游戏化标签、逐章调 `callLLM` 小说化改写，支持单章生成/一键全部/复制/下载 .txt） | `<external-text-adventure-project>\ai-images\`、`app/app.js`、`app/index.html`、`app/style.css`、`代码指南.md` | 行为测试 `test_novel_pixiv.js` 4 项全过（真实 app.js 加载） |
| 2026-08-10 02:59 | AI 2 | 按知识库治理 skill 流程新增《火影忍者》世界观设定集：chub 下载 （`Naruto` id=2061581，153 条/246KB）→ 存 `世界观设定/火影忍者.json` → 补 `设定集元数据.json` （简介/tags 同人火影忍者世界/nsfw false/en）→ 重跑 `gen_local_library_v2.py`（设定书 49 本）→ `node --check` + 入库校验通过 → 更新 `说明文档.md` | `<user-profile>\Videos\黑袍\ai知识库\世界观设定\火影忍者.json`、`设定集元数据.json`、`说明文档.md`、`<external-text-adventure-project>\app\local_library.js` | 给「Hinata and Abu」木叶剧情提供世界设定 |
| 2026-08-09 16:39 | AI 1 | **7 项交互改造（用户定向需求，不在电脑旁由 AI 自决）**：①**前情摘要覆盖全部上文**——`manageContext` 的 `summaryInput` 不再把旧消息截断为 350/800 字片段，改为保留 `oldMsgs` 完整内容（仅对单条 >4000 字做上限保护），摘要真正覆盖「全部上文」；②**人物图鉴「🔄 刷新」**——抽屉头部加刷新键，`refreshMandalaFromHistory()` 重新扫描完整对话历史的每条 AI 回复 `[CHARACTERS]` 重建曼陀罗人物卡；③**新增「📍 地点」面板**——头部加 `📍 地点` 按钮 + `locationDrawer` 抽屉，`collectLocations()` 从当前位置/剧情节点/历史 `[STATE]` 位置收集全部到访地点（当前地点标 📍，含刷新键）；④**任务系统可删除/隐藏**——`deleteQuest(name)` 逐条删除、`toggleQuestsPanel()` 隐藏/恢复整栏（头部加 `📋 任务` 按钮 + 任务栏标题 `🙈`，状态存 `adv.hideQuests`）；⑤**每轮对话后插「第 N 回合」分隔**——`renderStory` 在每个玩家消息前渲染回合分隔行（`roundNo` 计数）；⑥**发出的消息可撤回**——玩家气泡加 `↩ 撤回` 键，`withdrawMessage(i)` 移除该消息及紧跟的 AI 回复；⑦**协作日志功能记录表按时间倒序重排**（脚本 `.workbuddy\tmp\sort_log.js` 重排，修正原错位的若干行）。回归 `.workbuddy\tmp\test_new_features.js` 15/15 通过，既有链路/旧存档/TTS 无回归 | `app/app.js`（`manageContext`/`refreshMandalaFromHistory`/`toggleLocations`/`collectLocations`/`renderLocations`/`refreshLocations`/`deleteQuest`/`toggleQuestsPanel`/`withdrawMessage`/`renderStory`/`renderQuestsPanel`）、`app/index.html`（图鉴刷新键/地点抽屉与按钮/任务隐藏按钮）、`app/style.css`（回合分隔/地点/任务删除隐藏样式）、`.workbuddy\tmp\test_new_features.js`、`.workbuddy\tmp\sort_log.js` | 用户原话「前情摘要要摘要全部上文、人物图鉴和地点都加刷新按钮、任务系统可删除或隐藏、每对话后加一条回合、发出的消息可撤回、日志列表理好顺序」。铁律 6 同步代码指南 §4 / §12.5 |
| 2026-08-09 02:33 | AI 1 | **修复「⚠ API 调用失败：mt.re.test is not a function」(设定书缓存持久化致 RegExp 退化)**：根因——`parseBookEntries` 把解析结果缓存进 `book._loreParsed`，缓存含 RegExp 对象（`re: /.../i`）；冒险经 localStorage/IndexedDB（JSON 序列化）存储再读回时，RegExp 退化为空对象 `{}`（truthy 但无 `.test` 方法），于是 `buildLorebookBlock` 内 `mt.re.test(recent)` 抛 `mt.re.test is not a function`。这是「旧存档」问题的又一变种，叠加在 02:18 的 `character.attributes` 缺字段之上。修复：①读取 `_loreParsed` 缓存时，对所有 `mt.latin && mt.re` 但 `typeof mt.re.test !== 'function'` 的 matcher 从 `mt.low`（原始拉丁词）重建正则，恢复精确匹配与缓存性能；②`buildLorebookBlock` 调用点加类型守卫 `(mt.re && typeof mt.re.test === 'function') ? ... : indexOf`，任何坏值都回退 `indexOf` 不再崩溃。实测：`.workbuddy\tmp\repro_lore.js` 由「复现崩溃」变为 re 重建为 RegExp、buildLorebookBlock 不抛错且命中；`.workbuddy\tmp\repro_send.js`/`repro_old_schema.js`/TTS 14/14 无回归；`node --check` 通过 | `<external-text-adventure-project>\app\app.js`、`.workbuddy\tmp\repro_lore.js`、`<external-text-adventure-project>\代码指南.md` | 经典陷阱：任何会被持久化的对象都不要缓存 RegExp/函数/Date 等不可 JSON 序列化值，或读取时用类型守卫重建。铁律 6 同步代码指南 §12.5 |
| 2026-08-09 02:18 | AI 1 | **修复「冒险对话没有答复」(旧存档缺字段致发送静默失败)**：根因——`buildSystemPrompt` 强读 `character.attributes['力量']` 等，`sendMessage` 在 try 块外的 `updateSystemPrompt(adv)`（构建系统提示词）调用它；用户跨多会话的旧存档冒险若缺 `character.attributes/skills/items` 字段即抛 `Cannot read properties of undefined`，因开局发送是 fire-and-forget（不 await），异常变未捕获 rejection 且 `state.isGenerating` 已置 true → **永久卡死，之后所有发送被静默拦截**。首屏 `renderStory` 只读历史不调它，故页面看着正常、一发送就死。修复：①`buildSystemPrompt` 对缺字段兜底（直接修复旧存档）；②把 `updateSystemPrompt`/`renderStory`/`showTypingIndicator` 移入 `sendMessage` 的 try 块，使任何构造期异常都显示「⚠ API 调用失败」并正确复位 `isGenerating`，不再静默卡死；③`adv.character.hp` 判空。实测：`.workbuddy\tmp\repro_old_schema.js` 由「抛错+卡死」变为正常生成 AI 回复；`.workbuddy\tmp\repro_send.js` 健康链路仍正常；`node --check` 通过 | `<external-text-adventure-project>\app\app.js`、`.workbuddy\tmp\repro_old_schema.js`、`.workbuddy\tmp\repro_send.js`、`<external-text-adventure-project>\代码指南.md` | 复现台用 vm + DOM 桩跑真实 app.js 全链路；旧存档缺字段会在下次成功发送后被持久化修复（buildSystemPrompt 顺带补齐缺失字段）。铁律 6 同步代码指南 §12.5 |
| 2026-08-08 23:29 | AI 1 | **CosyVoice 接入收尾（WorkspaceId 落地 + 本地中继代理）**：①端点默认值写入用户给的 WorkspaceId=llm-23ju9mf3n4t0k5dx → `wss://llm-23ju9mf3n4t0k5dx.cn-beijing.maas.aliyuncs.com/api-ws/v1/inference`；②新增「本地中继（WebSocket 代理）」配置 `apiConfig.cosyvoiceRelay`（设置页 input + load/save 同步），`startCosyVoiceTts` 支持中继模式（连中继则代理代注鉴权头）；③新增 `tools\cosyvoice_proxy.py` + `tools\cosyvoice_proxy.json`（预填端点/Key），监听 `ws://127.0.0.1:8787`，连接 maas 时注入 `Authorization: Bearer` 头、双向透明转发，依赖 `websockets`（已装进托管 venv）；④实测：代理接受客户端→上游握手成功返回 `task-started`，完整协议收到 **53171 字节音频帧**，证明 `sk-ws-` Key 作为 Bearer 被 maas 接受、CosyVoice 经中继全链路可用；⑤铁律 6 同步代码指南 §4.4 | `<external-text-adventure-project>\app\app.js`、`<external-text-adventure-project>\app\index.html`、`<external-text-adventure-project>\tools\cosyvoice_proxy.py`、`<external-text-adventure-project>\tools\cosyvoice_proxy.json`、`<external-text-adventure-project>\代码指南.md` | 浏览器原生 WS 无法设握手头，maas 直连必 401；直连 maas 必须跑本地代理并填中继 `ws://localhost:8787`。代理与 Key 在本地未提交，纯静态主体不受影响 |
| 2026-08-08 21:53 | AI 1 | **三处改动（精简开关迁移 + TTS 可点停 + 接入 CosyVoice）**：①「精简角色状态」开关从全局设置页移到「编辑冒险」弹窗，改为按冒险存储（`adv.compactState`），提示词构建 `app.js` 读 `adventure.compactState`；②修复朗读「停不下来」——`readNarrativeAloud(i)` 改为可切换（再点同一条即停止，按钮变「⏹ 停止」），新增 `stopTts()`；③接入 CosyVoice 语音合成——设置加 TTS 引擎选择（原生/CosyVoice）+ 端点/Key/音色字段（Key 已预填），实现 `startCosyVoiceTts` 走 WebSocket 协议（run-task→continue-task→finish-task，Key 作 `?token=` 附加），binary 音频合并播放，失败回退原生；④无头回归 `.workbuddy\tmp\test_tts_cosyvoice.js` 14/14 通过 | `<external-text-adventure-project>\app\app.js`、`<external-text-adventure-project>\app\index.html`、`<external-text-adventure-project>\代码指南.md`、`.workbuddy\tmp\test_tts_cosyvoice.js` | 待确认→已确认服务方为阿里云百炼 maas；端点模板 `wss://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api-ws/v1/inference`（WorkspaceId 待用户替换）。关键限制：浏览器 WS 无法带 Authorization 头，Key 走 ?token= 直连 maas 可能 401；且用户 Key 为 `sk-ws-` 浏览器端 mTLS 风格，或非标准 DashScope `sk-`，疑似某 CosyVoice 封装服务密钥——若连不通需用户提供对应浏览器兼容端点或加代理 |
| 2026-08-08 21:38 | AI 1 | **扩展生态改造（对话框内特性）**：①删除「翻译 / 联网检索」——纯脚手架占位、无实现无配置无特性，徒增误解；②TTS 朗读改为对话框内特性——设置「🔌 扩展生态 → 🔊 语音朗读」开启后，每条 AI 叙事气泡内出现「🔊 朗读」键，调 `readNarrativeAloud(i)`（浏览器原生 `speechSynthesis`，无需 API Key）；③AI 生图明确为对话框内既有特性（`generateSceneImage(i)` → 渲染进气泡 `msg.sceneImage`），移除冗余死开关 `ext_image`；④删除旧「扩展工具」弹窗与头部「🔌 扩展」入口、清理 `useExtension`/`runExtension` 死代码；⑤新增无头回归 `.workbuddy\tmp\test_extensions_inbox.js`（8/8 通过） | `<external-text-adventure-project>\app\app.js`、`<external-text-adventure-project>\app\index.html`、`<external-text-adventure-project>\代码指南.md`、`<external-text-adventure-project>\.workbuddy\skills\text-adventure-prompt-protocol\SKILL.md`、`.workbuddy\tmp\test_extensions_inbox.js` | 用户要求：TTS 旁白/AI 生图 放入对话框内，翻译/联网检索 无特点则删；铁律 6 同步代码指南 §4.4 与共享 skill |
| 2026-08-08 20:51 | AI 2 | 落实铁律 8（先用先贡献）：把本人沉淀的两个可复用 skill 贡献进共享文件夹 `<external-text-adventure-project>\.workbuddy\skills\` —— `stickman-tuning`（火柴人调参：何时调用/关键锚点/比例与关节经验/避坑）与 `knowledge-base-governance`（知识库治理：chub 导入/改名/元数据/头像/生成库/分类规则/避坑）；并更新 `text-adventure-collab` 的已提供索引 | `<external-text-adventure-project>\.workbuddy\skills\stickman-tuning\SKILL.md`、`...\knowledge-base-governance\SKILL.md`、`...\text-adventure-collab\SKILL.md` | 纯文档动作，未改代码 |
| 2026-08-08 20:40 | AI 1 | **新增铁律 8「先用先贡献」+ 代码指南历史经验补录 + AI 1 贡献共享 skill**：①铁律 8——使用项目共享技能文件夹 `<external-text-adventure-project>\.workbuddy\skills\` 前必须先把自己的可复用 skill 分享进该文件夹（贡献先于使用）；②AI 1 示范贡献 `text-adventure-prompt-protocol`（在线线 LLM 协议+提示词工程）到共享文件夹；③代码指南补录历史代码经验：存储层 IndexedDB（§12.1）、知识库生成管线（§12.2）、chub 接口（§12.3）、离线引擎结构（§11）、历史踩坑提炼（§12.5）；④同步更新协作日志铁律、代码指南第 9 节、本规范提示词。落实用户「之前的代码经验也要进代码指南」「Skill 走共享文件夹、先用先贡献」 | `项目协作日志.md`（铁律 8）、`代码指南.md`（§11/§12 + 第 9 节）、`AI协作规范提示词.md`、`<external-text-adventure-project>\.workbuddy\skills\text-adventure-prompt-protocol\SKILL.md` | 用户要求：之前的代码经验也要沉淀；共享 skill 先用先贡献 |
| 2026-08-08 20:21 | AI 1 | **修正规范文件自洽性**：`AI协作规范提示词.md` 自检清单第 5 项与「细节考量」第 5 节仍残留旧名「各 AI 行为潜质与 Skill 登记」，与铁律 7 改名后的「各 AI 行为潜质登记」不一致；已统一改为新名并去掉「或 skill」字样（Skill 不再登记） | `<external-text-adventure-project>\AI协作规范提示词.md` | 用户粘贴规范后自查发现；铁律 6 要求文档口径一致
| 2026-08-08 20:20 | AI 2 | 按铁律 7 确认并更新自己的「行为潜质登记」（原为 AI 1 推断、标注待确认）：确认本人稳定行为与潜质（无头行为测试、离线游戏/动画调参、知识库治理等）；本次为纯文档协作动作，未改代码，无需更新代码指南 | `项目协作日志.md` | 已先读协作日志与代码指南 |
| 2026-08-08 20:12 | AI 3 | **按铁律 7 同步共享技能**：`headless-frontend-regression`（无头回归）此前仅存于用户级 `~/.workbuddy/skills\`，未在项目共享文件夹；依铁律 7「skill 统一放共享文件夹、3 个 AI 直接调用」非破坏性复制至 `<external-text-adventure-project>\.workbuddy\skills\headless-frontend-regression\`（用户级副本保留） | `<external-text-adventure-project>\.workbuddy\skills\headless-frontend-regression\` | 对齐铁律 7；功能记录行 83 的创建位置 `~/.workbuddy/skills\` 为历史记录，未改 |
| 2026-08-08 20:01 | AI 1 | **调整需求：Skill 不再逐个登记，改为项目级共享技能文件夹**：①铁律 7 由「行为潜质与 Skill 登记」改为「行为潜质登记」，删去 Skill 部分；②协作日志「各 AI 行为潜质与 Skill 登记」区改名「各 AI 行为潜质登记」，AI 1/2/3 的 skill 行改为指向共享技能文件夹；③新建项目级技能 `<external-text-adventure-project>\.workbuddy\skills\text-adventure-collab\SKILL.md`（协作规范 + 代码改动工作流，供 3 个 AI 直接调用）；④同步更新 `代码指南.md` 第 9 节与 `AI协作规范提示词.md` | `项目协作日志.md`（铁律 7 + 登记区）、`代码指南.md`（第 9 节）、`<external-text-adventure-project>\.workbuddy\skills\text-adventure-collab\SKILL.md`、`<external-text-adventure-project>\AI协作规范提示词.md` | 用户要求：Skill 不用登记，直接生成 skill 文件夹供 3 个 ai 调用
| 2026-08-08 19:45 | AI 1 | **新增两条协作铁律 + 各 AI 潜质/Skill 登记区**：①铁律 6「代码经验沉淀」——结构性代码改动后立刻同步 `代码指南.md` 对应章节；②铁律 7「各 AI 潜质/Skill 登记」——每个 AI 在新增「各 AI 行为潜质与 Skill 登记」区登记自己总执行的行为/潜质或拥有的 skill；并先填好 AI 1 为样板，AI 2/AI 3 据协作日志记录推断、标注待确认。同步产出可粘贴给任意 AI 的规范提示词文件 `AI协作规范提示词.md` | `项目协作日志.md`（铁律 6/7 + 登记区）、`代码指南.md`（铁律摘要同步）、`<external-text-adventure-project>\AI协作规范提示词.md` | 用户要求：让 AI 1/2/3 都把代码经验写进代码指南，并各自登记潜质/skill 到协作日志 |
| 2026-08-08 19:30 | AI 1 | **离线小游戏 AI 增强 + 火柴人动作扩展**（帮 A2 复查优化）：①离线引擎 `game_engine.js` 新增可选「✨ AI 增强」开关（默认关，界面按钮 `GameEngine.toggleAI`）；开启且 `state.apiConfig` 已配 endpoint/key/model 时，复用全局 `callLLM` 仅**扩写文本层**——事件叙事、选项结果、战斗结局、副本序章，并解析回复末尾 `<action:xxx>` 驱动火柴人；判定/战斗/数值/结局逻辑始终离线，AI 失败自动回退；事件级 AI 文本按 `pack.id+事件id` 缓存到 localStorage 避免重复调用；`headless` 测试路径不触发 AI。②火柴人 `stickman.js` 的 ACTIONS 新增 `search`/`talk` 两个关键帧动作，`game_engine.js` 的 ACTION_RULES 加 `搜索/搜查/翻找/检视/调查`→search、`交谈/对话/询问/交涉/留下线索`→talk 映射，缓解「火柴人长期只 idle」。③UI：`index.html` 离线游戏 `.og-tools` 加 AI 增强按钮+状态提示，`style.css` 加 `.og-ai-busy`(生成中脉冲) / `.og-ai-status` 样式 | `app/game_engine.js`、`app/stickman.js`、`app/index.html`、`app/style.css`、`代码指南.md` | 用户反馈离线小游戏太僵硬，要求接 API；两个无头测试 `test_offline_ai.js`(非AI路径 7/7) / `test_ai_path.js`(AI接线 7/7) 全过；修一处真实 bug：主事件无 `id` 字段致事件级缓存未写入，改用 evId 入参 |
| 2026-08-08 19:11 | AI 1 | **软化铁律第 1 条**：原「开工前并先读 `代码指南.md`」改为条件式——涉及代码改动（结构/提示词/状态/渲染/交互）才先翻 `代码指南.md` 定位函数，纯文案或非代码任务可不读；同步把 `代码指南.md` 开头「先读本文」改为「改代码前先想到本文」。保留「开工前必读 `项目协作日志.md`」 | `项目协作日志.md`（铁律第 1 条）、`代码指南.md`（开头用途） | 用户反馈原指令太僵硬，需加判断 |
| 2026-08-08 19:02 | AI 2 | 修复曼陀罗人物卡：①重复建档——按名字归一化（去空格/大小写）+ 去括号注释的基础名匹配同一角色并合并字段、升级更完整名字，历史重复自动清理；②更新不及时——`renderAll` 每轮调用 `refreshMandalaUI`（抽屉开着即重绘），头部「人物」按钮新增数量角标，解析后立即刷新 | `app/app.js`、`app/index.html`、`app/style.css` | 行为测试 `test_mandala.js` 5 项全过 |
| 2026-08-08 18:55 | AI 1 | **新建 `代码指南.md`（项目根目录）**：结构化梳理代码，供后续 AI 免重读全部源码。含文件地图、全局 `state` 结构、Adventure/CharacterCard/Theme 数据模型、提示词构建管线（注入顺序 + World Info + 宏 + 扩展脚手架）、冒险生命周期、渲染管线、UI 交互速查、校验与无头回归、协作铁律摘要、高频改动「从哪下手」速查表。同步把「开工前先读本文档」写入铁律第 1 条 | `代码指南.md`、`项目协作日志.md`（铁律第 1 条） | 与 `项目协作日志.md` 分工：本文档讲代码结构，日志讲「做了什么」；代码结构性变化时由改动者同步更新 |
| 2026-08-08 18:27 | AI 1 | **5 项 UI 功能（用户定向需求）**：①主题按钮增删改：创建新冒险页主题区改为动态渲染（`renderThemeGrid`），悬停主题按钮可 ✎编辑/✕删除；新增「➕ 新建主题」「⚙ 管理主题」弹窗（`themeEditorModal`/`themeManagerModal`）；主题数据存 `state.customThemes`/`state.deletedThemes`，自定义主题可带职业表（`parseProfessionsText` 解析 `名称|属性|技能`，`getThemeProfessions` 接入职业网格）；`startNewAdventure` 支持直接选中已保存自定义/覆盖主题作为 `customTheme`。②左侧边栏可隐藏：顶栏 `☰` 切换 `toggleSidebar()`，`state.ui.sidebarCollapsed` 持久化，`.app.sidebar-collapsed` 折叠。③冒险列表 HTML5 拖拽重排：`reorderAdventures()` 重排 `state.adventures` 并保存。④角色卡管理弹窗支持「选择文件导入」到当前冒险：`parseFileToCards()`（PNG/TXT/MD/JSON）+ `importCardToAdventure()`，无额外条件限制。⑤聊天界面设置背景：「🎨 背景」弹窗选颜色或上传图片，按冒险存 `adv.chatBackground`，`applyChatBackground()` 在 `renderAll` 应用 | `app/app.js`（`renderThemeGrid`/`onThemeSelected`/`getAllThemes`/`getThemeProfessions`/`openThemeEditor`/`saveThemeFromEditor`/`deleteTheme`/`openThemeManager`/`parseProfessionsText`/`toggleSidebar`/`applySidebarState`/`attachAdventureDrag`/`reorderAdventures`/`parseFileToCards`/`importCardToAdventure`/`applyChatBackground`/`showChatBackgroundModal`/`applyChatBackgroundFromModal`/`clearChatBackground`）、`app/index.html`、`app/style.css` | 无头回归第 13 节全断言通过：职业解析 / 主题新增 / 删除 / 隐藏内置 / 同名恢复 / 列表重排 / 颜色背景 / 图片背景 / 清除背景 |
| 2026-08-08 18:02 | AI 2 | 新增「铁律」节：记录必须带精确时间 HH:MM（不确定标 约/待补），功能与修改记录 / 踩坑记录一律按时间倒序（最新在最上）；按此重建功能记录表并补时间；AGENTS.md 同步该规矩 | `项目协作日志.md`、`AGENTS.md` | 示范新格式 |
| 2026-08-08 17:51 | AI 2 | 审查并修复 AI 1 借鉴 SillyTavern 的代码：①`parseBookEntries` 每轮全量正则解析所有设定书 → 加内容缓存；②设定扫描窗口排除全部 system 消息导致压缩后设定失效 → 保留【前情摘要】参与扫描，且从尾部收集不缩水；③拉丁关键词 substring 误报（run 命中 running）→ 词边界正则 + 长度≥2 过滤；④单 NPC 场景误注入群像规则 → 单人精简；⑤`setNpcTalkativeness` 不刷新面板 → 补 render。行为测试 8 项全过（真实 app.js 加载验证） | `app/app.js` | 测试 `test_a1_review.js`；另确认多开局/宏/扩展/压缩计费无问题 |
| 2026-08-08 13:30 | AI 1 | **借鉴 SillyTavern·进阶（P2）：分组 NPC 同场 + 扩展生态脚手架**。(1) 分组同场：角色卡新增 `present`（默认在场）与 `talkativeness`（0-100，默认50）；`normalizeCardObject`/`addCharacterCard` 捕获；`buildActiveSceneBlock` 仅列出在场角色并标注话痨度 + 群像互动指令，注入两个系统提示词构建器；`setNpcPresent`/`setNpcTalkativeness` 切换在场/调话痨度并刷新提示词；角色卡管理 UI 增加「在场」开关与「话痨度」滑块。(2) 扩展生态脚手架：`EXTENSIONS` 注册表 + `registerExtension`/`setExtensionEnabled`/`runExtension`（以 `apiConfig.extensions` 为开关，未启用/无 API/抛错均优雅返回 null）；默认注册 TTS/生图/翻译/联网四扩展（run 为优雅降级桩，接真实服务只需替换 run）；设置面板四个开关 + 「扩展工具」弹窗四按钮调用 `useExtension` 对当前叙事运行 | `app/app.js`（`buildActiveSceneBlock`/`setNpcPresent`/`setNpcTalkativeness`/`EXTENSIONS`/`registerExtension`/`setExtensionEnabled`/`runExtension`/`useExtension` + 注入两构建器 + `addCharacterCard` + `showSettings`/`saveSettings`）、`app/index.html`（卡片话痨度输入 + 扩展开关 + 扩展工具弹窗 + 头部「扩展」按钮）、`app/style.css` | 无头回归第 12 节全断言通过：字段捕获 / 同场区块仅在场角色+话痨度+群像指令 / 切换在场 / 调话痨度 / 未启用返回 null / 启用无API优雅null / 自定义run返回 / 抛错捕获 / useExtension分支 |
| 2026-08-08 10:50 | AI 1 | **借鉴 SillyTavern·Prompt Manager（P1-2）**：提示词模板化 + 宏变量。新增 `resolvePromptMacros(text, adventure)`（支持 `{{user}}`/`{{location}}`/`{{chapter}}`/`{{level}}`/`{{profession}}`/`{{theme}}`/`{{hp}}`/`{{mp}}`/`{{mood}}`/`{{date}}`/`{{time}}`/`{{random}}`，未知宏原样保留）；在 `buildSystemPrompt`/`buildTavernSystemPrompt` 末尾对整段提示词做宏替换，并在 `syncSystemPrompt` 的 customPrompt override 路径先解析用户宏；新增设置开关 `macroEnabled`（默认开）与 `PROMPT_MACRO_LIST` 参考表。用户可在「自定义规则 / 角色卡 system_prompt / post_history_instructions / character_note」书写宏，每轮随剧情状态刷新。**说明**：未把核心回复格式/规则改为「用户可全量编辑模板」，因那段格式是解析器契约（LLM 必须输出固定 `[NARRATIVE]…[PLOT]` 区块），改为全量可编辑风险高；「模板化」以「整段提示词作为宏模板 + 用户扩展段支持宏」落实 | `app/app.js`（`resolvePromptMacros`/`PROMPT_MACRO_LIST` + 三处 return 接入 + `syncSystemPrompt` + `apiConfig.macroEnabled` + `showSettings`/`saveSettings`）、`app/index.html`（macroToggle + 宏说明） | 无头回归第 11 节全断言通过：基础宏解析 / 未知宏保留 / random 每次不同 / 开关关闭 / 自定义规则经 syncSystemPrompt 解析+自动区块拼接 / 角色卡 system_prompt 宏解析 |
| 2026-08-08 09:20 | AI 1 | **借鉴 SillyTavern·Character V3（P0-2）**：角色卡对齐 V3 字段。`normalizeCardObject` 捕获 `alternate_greetings`/`character_note`/`system_prompt`/`post_history_instructions`/`note_depth`；`buildCharacterCardsBlock` 透出核心约束与多开局数量；新增 `buildCardSystemDirectives`（角色专属 system_prompt，注入系统提示词中段）+ `buildPostHistoryTail`（post_history_instructions 置尾 + character_note @depth 长对话重注入）。多开局：`collectAdventureOpenings` 跨卡收集 first_mes+备选并去重 → 弹窗 `showOpeningChoiceModal` 选择（含随机）→ `beginAdventureOpening` 把 seed 注入 initMsg。`addCharacterCard` 表单与新模态已在 `index.html`/`style.css` 落地 | `app/app.js`（`normalizeCardObject`/`buildCharacterCardsBlock`/`buildCardSystemDirectives`/`buildPostHistoryTail`/`collectAdventureOpenings`/`showOpeningChoiceModal`/`confirmOpeningChoice`/`beginAdventureOpening`/`addCharacterCard`）、`app/index.html`、`app/style.css` | 无头回归第 10 节全断言通过：字段捕获 / 角色卡块 / system_prompt 注入 / 结尾指令 / @depth 重注入 / 跨卡收集去重 / 开局种子注入 |
| 2026-08-08 07:50 | AI 1 | **借鉴 SillyTavern·World Info（P0-1）**：设定书由整段背景一次性装入，改为按条目（`【关键词】描述`）在 `buildSystemPrompt` 时扫描近期剧情消息，命中才注入；新增「设定注入预算（占 maxTokens 百分比）+ 扫描深度」两参数（设置面板可调）。`parseBookEntries` 解析 `【…】` 分段；`buildLorebookBlock` 概览常驻 + 命中条目按需注入 + token 预算熔断。**修复**：扫描窗口原会包含系统提示词本身，导致主题描述中的「巨龙/精灵」被误判为剧情命中而常驻注入；改为只扫描非 system 消息 | `app/app.js`（`parseBookEntries`/`buildLorebookBlock`/`buildSystemPrompt`/`buildTavernSystemPrompt`/`showSettings`/`saveSettings`/`initMsg`）、`app/index.html`（loreScanDepth/loreBudgetPct 表单） | 无头回归 `.workbuddy/tmp/adv_harness.js` 第 9 节全断言通过：概览常驻 / 矮人注入 / 龙·精灵不注入 / 扫描深度边界 / 重新命中 |
| 2026-08-07 | AI 2 | 导入 6 张新角色卡（chub 下载）；新增「讲述者」分类；说明文档补「道具/魔法类设定卡」段（小道具+魔法清单） | `角色卡/`、`设定集元数据.json`、`说明文档.md` | 2 张 PNG 卡已生成头像 |
| 2026-08-07 | AI 2 | 下载并加入申鹤（原神）角色卡：JSON + PNG + 缩略图 | `角色卡/申鹤-shenhe.*`、`app/avatars/` | |
| 2026-08-07 | AI 2 | 加载界面大改：单列小滚动框 → 双列卡片网格；全部设定书/角色卡配中文简介（`设定集元数据.json`）；新增搜索、预览弹窗、分类按钮、角色卡头像 | `app/app.js`、`app/index.html`、`app/style.css`、`app/local_library.js` | |
| 2026-08-06 | AI 2 | 存档持久化迁移：localStorage → IndexedDB（数据库 `adventureAI_db`），localStorage 仅作小数据镜像（>4MB 不写）；「存储体检」显示真实用量与浏览器配额 | `app/app.js` | 解决「存储空间不足，进度可能未保存」 |
| 2026-08-06 | AI 2 | 打通 chub.ai 公开接口（搜索+下载），下载 18 本设定书；其中 2 本与本地已有内容重复已删除 | `世界观设定/chub-*.json` | 见踩坑记录 |
| 2026-08-06 | AI 2 | 本地知识库接入：编写 `local_library.js` 生成器，26 本设定书 + 32 张角色卡合并进「创建冒险」加载列表 | `app/local_library.js`、`gen_local_library_v2.py` | |
| 2026-08-05 | AI 3 | 早期 bug 修复三连：①选项需先点左侧冒险才显示——`sendMessage` 里 `isGenerating` 未在 `renderStory` 前置 false，导致首轮渲染被跳过；②`[CHANGES]/[CHOICES]/[CHARACTERS]` 等结构标签泄漏进对话——LLM 未完美遵守格式时标签被原样显示，重写 `stripSectionTags`（遇到首个非 NARRATIVE 标签即截断）+ 新增 `sanitizeNarrative` 兜底清洗；③`max_tokens` 写死 2000 严重限制输出长度 → 改为可配置 `apiConfig.maxOutputTokens`（默认 4096）并暴露到设置 UI | `app/app.js` | 均为平台早期稳定性问题 |
| 2026-08-05 | AI 3 | 上下文持久化与压缩机制奠基：localStorage 全量序列化（对话历史/角色/分支/快照）；上下文用量达 80% 触发 AI 摘要压缩（`manageContext`），摘要提示词要求保留技能/属性/物品/等级变化，限 400 字 | `app/app.js` | 后由 AI 2 迁移至 IndexedDB（见踩坑记录） |
| 2026-08-05 | AI 3 | 角色与属性系统奠基：HP/MP 条、五属性（力量/敏捷/智力/魅力/幸运）、技能（含等级）+ 可用点数加点、等级/经验、情绪/职业/位置/章节；右侧「实时批注列」显示每轮属性变化卡片 | `app/app.js`、`app/index.html`、`app/style.css` | 成长系统后由 AI 1 修过解析 bug |
| 2026-08-05 | AI 3 | 多主题 + 职业系统：奇幻/科幻/恐怖/末日/武侠/悬疑 + 自定义主题（可保存复用）；每主题 5 种职业含属性与初始技能；随机生成按钮 | `app/app.js` | 主题/职业数据驱动 |
| 2026-08-05 | AI 3 | 结构化 LLM 响应协议设计（平台核心）：定义 `[NARRATIVE][STATE][CHANGES][CHOICES]` 区块，前端解析分离「叙事」与「游戏状态」，流式输出时隐藏结构标签、完成后统一重解析。后续 `QUESTS`/`COMBAT`/`CHARACTERS`/`PLOT` 均在此协议上扩展 | `app/app.js`（`parseGameResponse`/`buildSystemPrompt`/`applyParsedResult`） | 全平台依赖此协议，任何改动需保持区块兼容 |
| 约2026-08-05 | AI 3 | Ardot 页面设计稿绘制：在 Ardot 设计平台为平台绘制页面/功能设计示意图（多张），作为 UI 视觉参考 | Ardot 设计文件（文件 ID 待补） | 非代码交付物，仅作视觉参考 |
| 2026-08-08 约14:45 | AI 1 | 弹窗交互与样式打磨：禁用点击遮罩（窗口外）关闭弹窗，仅叉叉可关；全站补充 Emoji 标识（设置/面板/标题）与按钮辉光、入场动画、关闭按钮旋转等样式 | `app/app.js`、`app/index.html`、`app/style.css` | 用户反馈「点窗口外不关闭」|
| 2026-08-08 约14:30 | AI 1 | 场景配图 AI 由 Pollinations 切换为 **Qwen-Image-3.0-Pro**（DashScope）：`generateSceneImage` 改为 fetch 调用 Qwen-Image 接口、返回图转 dataURL 持久化；设置 UI 新增「图片 API」分组（imageApiEndpoint/imageApiKey/imageModel） | `app/app.js`、`app/index.html`、`app/style.css` | key 已实测可用；出图约 80s，CORS 支持 |
| 2026-08-08 约14:11 | AI 2 | 火柴人「人形化」：用视觉模型循环评审调参——修正肩关节点位（原误在髋部上方，导致上半身是直棍/手垂到膝）、脚踩实地、统一肢体配色、加宽肩髋、对称站姿；评审从「不像人」提升到 8/10 | `app/stickman.js` | 渲染走 `smtest/render_sm.js` + vision 复检 |
| 2026-08-08 约14:00 | AI 3 | 将「页面设计图」从交付物中剔除：在 Ardot 设计文件中删除页面设计图节点 `2:15`（batch_edit 的 `D(\"2:15\")`），明确页面设计图不纳入产物 | Ardot 设计文件（文件 ID 待补） | 其余设计图保留为参考；设计稿非代码，不随 `app/` 部署 |
| 2026-08-08 约13:59 | AI 3 | 知识库卡片改名（重跑生成脚本前的准备）：解读直译/表述不明的卡并改名，同步更新 `设定集元数据.json` 的 books 键与简介、以及 `说明文档.md` 表格行。①4 本设定书：`chub-DND城主设定集`→`chub-龙与地下城城主设定集`、`chub-Re零露格尼卡王国`→`chub-Re从零开始的异世界生活·露格尼卡王国`、`chub-天空分裂科幻`→`chub-分裂天空科幻世界`、`邪秽的力量`→`邪术召唤咒典`（并补建元数据条目）。②8 张角色卡去掉冗余罗马音后缀并补说明：`异世界叙事者/文字RPG游戏/现代生活RPG/精液珍贵世界（叙述者）/老婆墙（场景叙述者）/里番主角模拟器/跟踪狂（怪物女）` 及 `比安卡`→`比安卡（丰腴人妻）`；同步重命名对应 `.png` 与 `app/avatars/*.jpg` | `<user-profile>\Videos\黑袍\ai知识库\`（角色卡/、世界观设定/、设定集元数据.json、说明文档.md）、`<external-text-adventure-project>\app\avatars\` | 改名非删除、可还原；改完须重跑 `gen_local_library_v2.py`（新 PNG 卡先跑 `gen_avatars.py`）刷新平台加载界面（角色卡改名文件 mtime 01:08–01:20；设定书「邪秽的力量→邪术召唤咒典」重命名 mtime 13:59） |
| 2026-08-08 约13:58 | AI 2 | 修复火柴人手脚重叠：手臂长度改短（手只垂到腿中部）、近/远侧肢体横向错位、待机双脚分开、远侧也画手脚点；新增 limbEnds 回归测试（手高于脚、双脚分开） | `app/stickman.js` |
| 2026-08-08 约13:50 | AI 3 | 修复知识库坏 JSON：`世界观设定/邪秽的力量.json` 首行多了一行 markdown 标题 `### 邪秽的力量 - 副本`，导致整体非法、被 `gen_local_library_v2.py` 跳过。提取首 `{` 到末 `}` 的纯 JSON 本体（8 条目，解析有效）写回格式化 JSON，原坏文件备份为 `.bak` | `<user-profile>\Videos\黑袍\ai知识库\世界观设定\邪秽的力量.json` | 修复后须重跑 `gen_local_library_v2.py` 让平台加载界面收录此设定；文件名「邪秽的力量」与内容 name「The Sus Spellbook」不一致，待确认是否需改名（原坏文件 .bak mtime 为 2026-08-06 03:45，本次为 08-08 复核/重确认） |
| 2026-08-08 约13:45 | AI 2 | 火柴人 v3：按 davepagurek/Axis 的父子关节思路重做（Axis 为 Paper.js+jQuery 编辑器、无许可证，不能直接搬）；肘/膝单向折叠不再穿模，实心头+手脚点+刀光+施法光点，比例与节奏调优；新增画布边界测试防「看不见」 | `app/stickman.js` | 测试覆盖渲染期异常与出界 |
| 2026-08-08 约13:20 | AI 2 | 火柴人 v2：多角色舞台（颜色/体型/朝向/景深），10 个动作重做，战斗改为双人对峙编排；设定包新增 9 个角色形象与事件关联 | `app/stickman.js`、`app/game_engine.js`、`app/packs/limitless.js` | 剧情/战斗/结局均按角色播放 |
| 2026-08-08 约13:14 | AI 2 | 修复离线游戏「继续 ▶」按钮卡死：结果/升级/战斗结算页的继续按钮误走 onChoice（反复显示"你选择了：继续"），改为走 `GameEngine.onContinue()`；新增 `simulate(...,{uiMode:true})` 全点击路径测试 | `app/game_engine.js`、测试脚本 `test_game.js` | 已修复并复测 |
| 2026-08-08 约12:55 | AI 2 | 新增离线小说游戏：通用引擎 + 第一套设定包（无限流·诸天轮回），平台内新增入口按钮与全屏界面；不依赖任何 API，离线可玩 | `app/game_engine.js`、`app/packs/limitless.js`、`app/index.html`、`app/style.css` | 4 种结局；单局制随机体验 |
| 2026-08-08 约07:10 | AI 1 | 固化无头回归测试台为**可复用技能** `headless-frontend-regression`：Node `vm` 沙箱加载真实前端源码 + DOM/localStorage/IndexedDB 桩 + 确定性模拟 LLM，可无浏览器/无 API key 跑通完整流程并断言。项目内保留实例 `.workbuddy/tmp/adv_harness.js` | `~/.workbuddy/skills/headless-frontend-regression/`（SKILL.md + references/harness-technique.md + scripts/headless_regression_template.js） | 满足待办「固化成可复用技能/一键脚本」 |
| 2026-08-08 约07:05 | AI 1 | 修复「同名剧情节点」重复堆积：`applyPlotUpdates` 的 `NODE_NEW` 分支对每个同名标题都 `makePlotNode` 新建，LLM 重复输出同一节点时产生大量同名 `plotNodes`。改为先查当前剧情线同名节点，命中则更新既有节点而非新建 | `app/app.js`（`applyPlotUpdates` 约 2920 行） | 无头回归验证：9 回合循环 3 个不同标题 → 去重后恰为 3 个节点（修复前为 9 个） |
| 2026-08-08 约07:00 | AI 1 | 修复成长系统严重 bug：提示词 `EXP + \| 数值` 与 `parseGameResponse` 正则要求数值紧贴 `+` 对不上，导致 EXP / 技能点 / 属性点永远解析不到、角色无法升级。改为解析器容错（允许可选竖线）+ 提示词对齐成 `EXP + 数值 \| 原因` | `app/app.js`（解析正则约 1016/1022/1025 行；`buildSystemPrompt` 约 758/760/761 行） | P0 已修复；附无头回归测试台 `.workbuddy/tmp/adv_harness.js` |
| 2026-08-08 约06:55 | AI 1 | 新增「查看/编辑前情摘要」：压缩后剧情跑偏可人工修正，保存时同步进对话历史的 `【前情摘要】` 系统消息 | `app/app.js`（`showContextSummaryModal`/`saveContextSummary`/`updateSummaryCounter`）、`app/index.html`、`app/style.css` | 上下文面板新增按钮 `editSummaryBtn` → 弹窗 `contextSummaryModal` |
| 2026-08-08 约06:50 | AI 1 | 新增「精简角色状态」开关 `apiConfig.compactState`，降低每轮 token 消耗 | `app/app.js`（`buildSystemPrompt` compact 分支、`saveSettings`）、`app/index.html` | 开启后只保留 HP/MP/位置/章节/属性/技能名/背包名/进行中任务名 |
| 2026-08-08 约06:45 | AI 1 | 修复右侧栏数值更新慢：原 `renderAll` 先跑耗时的 `renderStory`（每轮全量重渲染）再刷侧边栏；改为先 `renderCharacterPanel` 后 `renderStory`，且解析完立即刷新右侧栏 | `app/app.js`（`renderAll`/`sendMessage`） | 用户反馈「更新速度太慢」的根因修复 |
| 2026-08-08 约06:40 | AI 1 | 时间轴改为横向布局 + 事件节点化；压缩/升级/死亡均写入 `adv.eventNodes`；AI 冒险简介融入左侧目录（基于压缩上下文生成，未压缩不生成） | `app/app.js`、`app/index.html`、`app/style.css` | 废弃独立「冒险图鉴」左滑抽屉，合并进左侧目录内联展示 |
| 2026-08-08 约06:35 | AI 1 | 编写无头回归测试台（Node `vm` 沙箱加载真实 `app.js` + DOM/IndexedDB/localStorage 桩 + 确定性模拟 LLM），自动跑通完整流程并验证修复 | `.workbuddy/tmp/adv_harness.js` | 复跑：`C:/Users/11/.workbuddy/binaries/node/versions/22.22.2/node.exe .workbuddy/tmp/adv_harness.js` |
| 2026-08-08 约05:31 | AI 2 | 新增 4 套小说设定集：《我独自升级》lorebook（chub 下载）+ 原创 玄幻修仙·凡人问道 / 末世·废土纪元 / 无限流·诸天轮回（均含大纲+金手指+多结局+随机机制） | `世界观设定/小说设定-*.json`、`设定集元数据.json` | |
| 2026-08-08 约05:25 | AI 2 | 加载界面新增 NSFW 隐藏开关（默认隐藏，localStorage 记忆 `adventureAI_showNsfw`）；修复分类（赛博朋克并入「科幻」、卡片名改两行显示） | `app/app.js`、`app/index.html`、`app/style.css`、`设定集元数据.json` | |

## 踩坑记录（新→旧）

| 2026-08-29 21:51 | 整卡 nested field 初次投递被 8097 报“翻译任务字段、模式或写回策略无效”；实例化含空 comment 的可选 Entry 又报名称为空 | 旧校验器只接受点号/数组路径，不认识稳定的 `entries/<entry_id>/...`；Adventure Lore 投影又错误地把 optional comment 当成必需运行时名称 | 8097 只放行两类明确的 nested stable path 与安全字段；运行时名称依次回退 comment、keys、secondary keys、稳定 entry ID。保留严格路径/脚本字段限制，不放宽 Proposal/CAS 安全边界 | `<project-root>\tools\denova_full_export.py`、`<project-root>\denova-src\internal\book\material_import.go` |
| 2026-08-27 01:44 | Denova 完整 Vitest 并行运行出现 8 个无关 5 秒超时，首次结果 898/906 | 全套 159 个测试文件并行时本机 CPU/DOM 模拟负载过高；失败集中在启动、自动化和 TRPG 时序用例，本次翻译定向测试均通过 | 将 3 个失败文件独立复跑，55/55 全通过；按资源波动记录，不误判为翻译回归，也不通过盲目放宽全局 timeout 掩盖问题 | `<project-root>\denova-src\web` Vitest 回归流程 |
| 日期 | 坑 | 原因 | 解决办法 | 涉及 |
|------|----|------|----------|------|
| 2026-08-26 10:40 | HY-MT 偶尔返回带 `Terminology/SOURCE` 的提示模板，若只比较输出与原文会误判为已翻译 | 小模型在长术语表和复杂 NSFW 文本下可能回显指令框架 | 将提示模板标签视为异常输出，保留英文原文并进入失败报告；真实短译仍需单独冒烟验证 | `<project-root>\tools\denova_full_export.py` |
| 2026-08-26 10:12 | Denova 成功读取全部文件，但验收发现 350 条源 entries 只有 9 条资料项，角色卡仍有截断 | 无 `source_ref` 的嵌入素材被优先当作原件；嵌入 lorebook 是 `content` 内嵌 JSON 字符串，映射器未解包；manifest 还把截断硬编码为 0 | 唯一知识库候选优先回源；解包嵌入 lorebook 后逐 entry 映射；无法回源时显式记录 `incomplete/truncated`；重导出后再验收，不能复用旧工程结果 | `<project-root>\tools\denova_full_export.py` |
| 2026-08-26 09:32 | HY-MT 首次安装在“核对官方文件与磁盘空间”阶段失败 | Hugging Face 公开 siblings 接口偶尔不返回 GGUF LFS 的 `sha256/size` 字段 | 不盲信缺失元数据：使用此前官方文件页核验的固定大小和 SHA-256；若接口有值则强制与固定值一致，下载后仍逐字节复核，再注册 Ollama | `<project-root>\tools\denova_full_export.py` |
| 2026-08-26 09:12 | 磁盘源码与运行版都已更新，但 8097 健康接口仍显示 v4；数个旧 Node VM 回归又因缺少 `location`/`URLSearchParams`/`dataset` 或仍断言两步向导而失败 | Python 桥进程从前一天起常驻，不会随文件覆盖热重载；无头沙箱和测试期望没有跟随宿主检测及三步向导演进 | 部署验收必须同时检查“磁盘哈希”和“运行接口版本”；确认端口所有者、运行目录后只重启桥并验证 v5。旧回归补齐浏览器桩，并按现行三步流程改断言，不为迁就旧测试回退产品 | `<project-root>\tools\denova_bridge.py`、`.workbuddy\tmp\test_adventure_{types,wizard,new_features}.js` |
| 2026-08-26 09:00 | Windows 临时目录并发测试中，原子保存 `job.json` 偶发 `[WinError 5] Access denied`；第一次 Denova 定向测试命令又因 `pnpm test -- files` 把 `--` 传给 Vitest 而误跑全套 | Windows 读取/安全扫描可能短暂占用即将被 `os.replace` 替换的目标；当前 pnpm 脚本参数转发不需要额外 `--` | `atomic_bytes` 只对 `PermissionError` 做 8 次短暂递增重试，其他错误仍立即暴露；定向测试使用 `pnpm test file1 file2`，发现误跑后立即中止，避免把既有全套超时误判为本次回归 | `<project-root>\tools\denova_full_export.py`、Denova Vitest 工作流 |
| 2026-08-22 | 工作台导航标题偶发显示旧书名，展开/切换栏与实际打开书籍对不上 | 顶部导航标题把 `summary.title` 当成第一真源；但摘要属于派生数据，刷新可晚于 `workspace` 真正切换，导致旧摘要标题回写到当前书籍 UI | 导航标题统一优先读取当前 `workspace` 对应的书架记录名，摘要标题只做缺省兜底；凡是“切换入口 / 当前对象标题 / aria 文案”都应绑定同一真源 | `<project-root>\denova-src\web\src\App.tsx`、`<project-root>\denova-src\web\src\components\workbench\workbench-utils.ts` |
| 2026-08-16 19:10 | 手机端 CSS 已写但正常 Denova 入口不生效；首次新增的抽屉关闭键也不可见 | `mobile-mode` 只由 URL 参数设置，而宿主 iframe 固定为 `?embedded=denova`；关闭键最初放在 `.logo` 内，但嵌入态为品牌去重会隐藏整块 `.logo` | 以 iframe 自身 `matchMedia` 自动加 class，URL 参数只作覆盖；关闭键改为 `.sidebar` 直属元素并绝对定位。手机 UI 验收必须检查“宿主 iframe 实际 DOM”和直接页，不能只看磁盘 CSS | `<project-root>\app\index.html`、`<project-root>\app\style.css`、Denova iframe |
| 2026-08-15 15:50 | `pwsh` 解析正常，但用户双击 `Launch-Narraverse.cmd` 后出现中文乱码、字符串缺终止符和缺少右花括号 | 启动脚本是无 BOM UTF-8；PowerShell 7 默认按 UTF-8 读取，而 Windows PowerShell 5.1 会按系统 ANSI 读取，UTF-8 中文字节被误解码后可能吞并 ASCII 引号 | Windows 双击入口调用的 `.ps1` 保持纯 ASCII；启动脚本改动后必须额外用 `powershell.exe -NoProfile -File ... -NoOpen` 实测，不能只用 `pwsh` Parser | `<project-root>\tools\start_narraverse.ps1`、`<project-root>\Launch-Narraverse.cmd` |
| 2026-08-15 15:45 | 首次静态同步把 `--` 当成源目录；首次部署复制又找不到 `dist\*` | pnpm 11 会把 `pnpm run script -- arg` 的 `--` 继续传给 Node；PowerShell `Copy-Item -LiteralPath` 不展开通配符 | 同步统一用 `pnpm run sync:narraverse <project-root>\app`；部署统一用 `Get-ChildItem -LiteralPath $source | Copy-Item -Destination $stage -Recurse`，先复制到暂存目录、再备份旧 `web` 并换入，避免半部署状态 | `<project-root>\denova-src\scripts\sync-narraverse-assets.mjs`、Denova 前端部署流程 |
| 2026-08-15 14:59 | 迁移验证结束后 `books.json.current` 从工程 `11` 回落为列表首项 | Denova 验证启动期间发生了当前工程回写，具体触发分支未进一步修改，避免扩大本次目录迁移范围 | 复核迁移前记录和工程目录存在性后，将 `current` 恢复为新路径下的 `projects\11`；其余 11 个工程注册保持不变 | `<project-root>\denova\.denova\books.json` |
| 2026-08-15 14:53 | Denova 已启动但启动器一直报告前端不可访问 | 8080 被其他程序占用后 Denova 自动回退到 8081；启动器发布版只探测 8080，且每轮还扫描开发端口，导致失败反馈很慢 | 发布端候选改为 `8080..8090`；前台日志确认 8081 后，`-NoOpen` 正确返回 `http://127.0.0.1:8081/?mode=narraverse` | `<project-root>\tools\start_narraverse.ps1` |
| 2026-08-15 14:53 | 迁移脚本无法直接移动/删除旧项目根目录；首次静态同步把 `--` 当成目录名 | 当前 Codex 工作区持有旧根目录句柄；pnpm 11 会把 `pnpm run script -- arg` 中的 `--` 继续传给脚本 | 改为逐项移动旧根目录内容，再移动 Denova 两目录；同步命令改为 `pnpm run sync:narraverse <project-root>\app`。旧根仅剩被占用的空目录 | `<project-root>`、`<project-root>\denova-src\scripts\sync-narraverse-assets.mjs` |
| 2026-08-15 13:52 | `?mode=narraverse` 顶栏已点亮叙界，但主区仍显示书籍管理 | `App.tsx` 原有“无工作区时强制进入 books”逻辑晚于 workspace store 深链接初始化执行，覆盖了自包含的叙界内容路由 | 将无书籍兜底限定为写作/游戏等依赖工作区的模式，明确豁免 `narraverse`；重新构建后用真实浏览器确认无书籍也直接渲染叙界 iframe | `<external-denova-source>\web\src\App.tsx` |
| 2026-08-14 11:28 | 旧两步向导经历多轮增强，实际代码比实施计划摘录多出角色卡编辑、AI 补充等分支，首次尝试整块替换时补丁锚点不匹配 | 计划中的上下文片段不是当前完整源码，直接用大块精确替换对历史复杂区段过于脆弱 | 改为小步补丁：先独立修根因与创建写回，再在旧实现之后落三步向导覆盖层；最后将旧同名入口改为 `legacy*`，避免函数重名和后续维护误判。经验：复杂历史前端应按稳定函数边界增量替换，不依赖摘要中的整块文本 | `<external-text-adventure-project>\app\app.js` |
| 2026-08-12 02:40 | ①导出"剧情摘要"直接拼 AI 回复原文（床戏全文进 Denova）；②挂载设定书把 NSFW 术语书全量导成 lore（75/123 条词条）——根因：摘要无总结逻辑、导出无筛选 | 功能设计缺陷 | 摘要走 LLM 生成；lore 按 nsfw 标记+标题正则+上限+排序过滤；教训：跨工具导出必须做"内容治理"而非原样搬运 | 导出 v2 |
| 2026-08-12 02:10 | ①Python 误写 JS 风格正则字面量（split(/[、,，]/)）→ 用 re.split(r"…")；②Denova 工程注册必须写 books.json（含 current/books/sort_mode），否则 Denova 不显示；③测试工程导出后要清理（projects 目录 + books.json 注册项） | 语言语法 / Denova 工程机制 | 见左 | Step 0 落地 |
| 2026-08-11 23:05 | ①app.js 原文件 CRLF 与 AI 插入的 LF 混合，锚点匹配失败 → 打补丁前先统一换行；②无头测试坑：stub 返回新对象导致断言对象与注入对象不一致 → 固定引用；getElementById 目标需先创建再取 | 行尾混合 / 测试设计 | 见左 | Pixiv P2+P3 |
| 2026-08-11 22:15 | ①pixivpy3 `search_illust` 不支持 `nsfw` 参数（报 unexpected keyword）→ 用 `req_auth=True` 即可含 R18；②`pixiv_config.json` 被 PowerShell 写成 UTF-8 BOM，json.load 报 Unexpected UTF-8 BOM → 改 `utf-8-sig` 读取；③`pixivOpenDetail` 用 `const pages` 后重赋值报 Assignment to constant variable（无头测试抓到）→ 改 `let`；④PowerShell 写含反引号的 JS 模板字符串会吞转义（onerror 单引号丢失）→ 整块替换后必须 node --check + 无头测试 | 库 API 变化 / 编码 / JS 语法 / 转义 | 见左 | Pixiv 接入 |
| 2026-08-11 20:10 | ①pnpm 检测项目移动后要求删除重装 node_modules；②obsidian.json 是 JSON 转义格式（路径含 \\），直接字符串替换旧路径匹配不上 | 项目路径变化 / JSON 转义 | ①确认后重装（store 缓存 58.9s 完成）；②用 JSON.parse→改值→stringify 写回 | 项目迁移 |
| 2026-08-11 18:05 | Denova 下载期间 github.com 多次连接重置（curl 重试 5 次最终完整拿到 22MB 且与官方大小一致），checksums.txt 首次拉取失败重试后成功；SHA256 逐字节比对通过 | 本机访问 GitHub 不稳定 | 大文件用 curl --retry 5 --retry-all-errors；下载后必须对 checksums.txt 校验 | Denova 安装 |
| 2026-08-11 16:45 | ①GitHub API 连续请求被 403 限流；②novel-word-count-plugin 仓库名不存在（正确 `isaaclyman/novel-word-count-obsidian`，插件 id=novel-word-count）；③插件目录名必须与 manifest 的 id 一致（iconize→obsidian-icon-folder、obsidian-webnovel-assistant→web-novel-assistant）否则 Obsidian 不加载 | API 限流/仓库改名/Obsidian 加载规则 | ①绕开 API 用 `github.com/<repo>/releases/latest/download/<file>` 直链；②搜索确认正确仓库；③按 manifest id 重命名目录并校验 | Obsidian 插件安装 |
| 2026-08-11 16:20 | Obsidian 安装包（331MB）Invoke-WebRequest 下载到 246MB 断流 | 本机访问 GitHub 大文件连接被重置 | 用 `curl.exe -L -C -` 断点续传补完剩余 80MB，校验总字节与 GitHub Release 资产一致（331007240） | `<user-profile>\AppData\Local\Temp\obsidian-dl\Obsidian-1.13.6.exe` |
| 2026-08-11 15:45 | 安装 AI-Novel-Writing-Assistant 时三个坑：① better-sqlite3 本地编译失败（无 VS C++ 工作负载、prebuild-install 拉 GitHub 被重置）→ 从 GitHub Releases 直链下载 `better-sqlite3-v12.6.2-node-v137-win32-x64.tar.gz` 解压进 `.pnpm\better-sqlite3@12.6.2\node_modules\better-sqlite3\` 即用；② Prisma 7 `db push` 对 SQLite 报空白 `Schema engine error:`（引擎不会自动创建 db 文件，`validate` 正常）→ 先建空 `server/dev.db` 再 push 即成功，已写进 `ensure-dev-prisma.cjs` 自动兜底；③ PowerShell 5.1 给原生 exe 传含双引号的参数会吞引号（调试 schema-engine 时）→ 改用 Node `spawnSync` 传参 | 涉及 `E:\AI-Novel-Writing-Assistant`（与平台无关，纯经验沉淀） |
| 2026-08-10 16:49 | 点击「创建新冒险」没反应（弹窗不弹出） | `renderThemeGrid` 中 `edit`/`del` 两个工具按钮的 `addEventListener` 误写在 `if (!t.isAdventureType)` 块**外面**，而 `edit`/`del` 仅在块内 `const` 声明；遍历到第一个冒险类型主题（无编辑/删除工具）时 `edit`/`del` 未定义 → 抛 `ReferenceError: edit is not defined`，整个 `renderThemeGrid` 崩溃，`showNewAdventureModal` 中断、弹窗打不开。此 bug 系上一轮「冒险类型细分」接入时引入 | 把 `edit.addEventListener`/`del.addEventListener` 移入 `if (!t.isAdventureType)` 块内（仅内置/自定义主题有编辑/删除工具，冒险类型无）；用 `node` + DOM 桩复现 → 弹窗 `display:flex` 通过；两套回归无回归 | `app/app.js`（`renderThemeGrid`） |
| 2026-08-10 | 知识库坏 JSON：`叙述者设定-申鹤反差流.json` 是两个 JSON 拼接损坏（首对象 1–115 行合法，117 行起又拼了一段残缺 `entries` 碎片），生成脚本整本跳过 | 文件拼接/导出异常导致尾部多出非法内容 | 提取首个完整 JSON 对象（保留 8 条）+ 尝试合并尾部碎片（碎片无法解析已丢弃）；删除与 `邪术召唤咒典.json` 完全重复的 `邪秽的力量.json`；改知识库后务必先跑 `tools/rebuild_meta_categories.py` 再跑 `tools/gen_local_library_v2.py` | `ai知识库/世界观设定/叙述者设定-申鹤反差流.json`、`ai知识库/世界观设定/邪秽的力量.json` |
| 2026-08-08 | 知识库坏 JSON 已修复：`世界观设定/邪秽的力量.json` 之前因首行多了一行 `### 邪秽的力量 - 副本` 而整体非法，库生成时被跳过 | 文件开头混入了非 JSON 文本行（疑似副本残留标题） | 提取首 `{` 到末 `}` 的纯 JSON 本体写回格式化 JSON；修改知识库后必须重跑 `gen_local_library_v2.py` 才能被平台收录 | `<user-profile>\Videos\黑袍\ai知识库\世界观设定\邪秽的力量.json` |
| 2026-08-08 | 成长系统失效：角色永远无法获得 EXP / 技能点 / 属性点，无法升级 | 系统提示词 `[CHANGES]` 指令写 `EXP + \| 数值 \| 原因`，但解析正则要求数值紧贴 `+`（`/^EXP\s*\+\s*(\d+)/`），按提示词格式输出永远匹配不到 | ①解析器三条正则加 `\|?` 容错；②提示词对齐为 `EXP + 数值 \| 原因`（与 HP/MP 风格一致） | `app/app.js` |
| 2026-08-08 | 火柴人整段不显示，舞台空白 | `drawFigure` 里 `hy` 赋值丢了 `var` 声明，严格模式下直接抛 ReferenceError，动画循环中断 | 补 `var hy`；测试改为真实调用 `_test.draw()` （此前测试只算插值没跑绘制，漏掉渲染期异常） | `app/stickman.js` |
| 2026-08-08 | 离线游戏点「继续」后显示"你选择了：继续"并卡死 | 结果页继续按钮被当普通选项处理（onChoice），反复渲染结果页 | 继续按钮直接调 `GameEngine.onContinue()`；新增 `simulate(...,{uiMode:true})` 全点击路径测试 | `app/game_engine.js` |
| 2026-08-07 | 角色卡分类乱 / 卡片名字被截断看不见 | 分类只按 nsfw 二分、名单行截断 | 分类并入 tags（讲述者/NSFW 等），卡片名改两行显示，分类顺序调整 | `app/app.js`、`style.css` |
| 约2026-08-06 | 用户反馈 `<user-profile>\Videos\黑袍\ai知识库\` 内容莫名全部消失 | 不明（已排除本端智能体操作） | 排查回收站 / 卷影副本 / 系统事件日志均无删除痕迹，确认非平台或 Agent 操作所致；目录仅余 `设定/` 与 `oldswf存档.sav`，未能恢复；与同期「邪秽的力量坏 JSON」无因果关系 | `<user-profile>\Videos\黑袍\ai知识库\` |
| 2026-08-06 | localStorage 约 5MB，存档+快照+分支膨胀报「存储空间不足」 | 全量状态塞 localStorage，备份键再翻倍 | 迁移 IndexedDB 主存；<1MB 写主键+备份、1~4MB 只写主键、>4MB 不写 localStorage | `app/app.js` |
| 2026-08-06 | 改知识库后平台加载界面不更新 | `local_library.js` 是生成产物 | 每次改 `世界观设定/` 或 `角色卡/` 后重跑 `gen_local_library_v2.py`；新 PNG 卡先跑 `gen_avatars.py` | 生成脚本 |
| 2026-08-05 | 结构标签泄漏进对话（`[CHANGES]`/`[CHOICES]`/`[CHARACTERS]` 等被原样显示） | LLM 未完美遵守区块格式，标签随叙事文本一起进入气泡 | 重写 `stripSectionTags`（遇到首个非 NARRATIVE 标签即截断）+ 新增 `sanitizeNarrative` 作二次兜底，渲染前清洗 | `app/app.js` |
| 2026-08-05 | 首轮选项不显示，需先点左侧冒险才出现 | `sendMessage` 中 `isGenerating` 置 false 的时机在 `renderStory` 之后，首轮渲染被跳过 | 将 `isGenerating=false` 提到 `renderAll`/`renderStory` 之前 | `app/app.js` |
| 2026-08-05 | 单次回复被截断、选项偶发缺失 | `max_tokens` 写死 2000，长叙事+结构标签易被砍 | 改为可配置 `apiConfig.maxOutputTokens`（默认 4096），暴露到设置 | `app/app.js` |
| （已知） | localStorage 单站约 5MB，存档+快照+分支膨胀会报「存储空间不足」 | 大数据写入 localStorage | 已迁移 IndexedDB 主存，`>4MB` 状态不再写 localStorage；不要在 localStorage 里放大数据 | 存储层 |
| （已知） | PowerShell 里中文路径经 stdin/heredoc 会乱码 | 编码问题 | 一律先写 UTF-8 的 `.py`/`.js` 脚本文件再执行，不要把中文路径直接打进命令行参数 | 通用 |
| （已知） | Codex 的 apply_patch 工具只能操作当前工作目录内文件，`<external-text-adventure-project>` 在 CWD 外 | 工具限制 | 用 Python 脚本做文件整块替换；替换加载库代码块/样式时必须是「整块替换」，否则会重复插入旧内容 | 文件替换 |
| （已知） | PNG 角色卡内嵌 chara 数据（PNG tEXt 块，key 为 `chara`，值是 base64 的 JSON） | 数据藏在 PNG 元数据 | 必须用 PIL 读 `img.info` 提取，不能当文本读；旧卡曾有双 chara 块问题（原始备份在 `修复前备份\`） | 角色卡 |
| （已知） | chub.ai 接口使用细节 | 接口约定 | 搜索用 `api.chub.ai/search?search=关键词&namespace=lorebooks`（设定书）或 `namespace=characters`（角色卡）；下载用 `api.chub.ai/api/v4/projects/{数字ID}/repository/files/raw%252Fsillytavern_raw.json/raw`（设定书）或 `raw%252Ftavern_raw.json/raw`（角色卡）；必须带完整浏览器 UA；搜索不支持中文关键词 | chub.ai |
| （已知） | 新增/修改角色卡或世界观设定后平台加载界面不更新 | 加载库/头像未重新生成 | 必须重跑 `gen_local_library_v2.py`（新 PNG 卡还要先跑 `gen_avatars.py`） | 生成脚本 |
| （已知） | 离线游戏引擎「继续 ▶」按钮卡死 | 走了 `onChoice` 而非 `onContinue` | 结果/升级/战斗结算页的「继续 ▶」必须走 `GameEngine.onContinue()`，绝不能走 `onChoice`；改引擎后用 `simulate(...,{uiMode:true})` 做全点击路径测试 | `app/game_engine.js` |
| （已知） | 角色卡「讲述者」分类错误 / NSFW 内容误显 | 标记来源 | 讲述者靠 tags 中的「讲述者」标签；NSFW 默认隐藏，开关状态存 localStorage 的 `adventureAI_showNsfw` | 角色卡/加载库 |
| （已知） | `世界观设定/邪秽的力量.json` 曾是坏 JSON，生成库时被跳过 | 文件首行混入了 `### 邪秽的力量 - 副本` 非 JSON 文本行 | **已于 2026-08-08 修复**（见上方踩坑记录）：提取纯 JSON 本体写回，原坏文件备份为 `.bak`；修改知识库后重跑 `gen_local_library_v2.py` | `<user-profile>\Videos\黑袍\ai知识库\世界观设定\邪秽的力量.json` |
| （已知） | 服务停止后页面连不上 | 静态服务未运行 | 启动命令 `cd <external-text-adventure-project>\app; python -m http.server 8099`；新文件（如 `packs/`）要确认能被 HTTP 访问 | 本地服务 |
| （已知） | chub 下载的卡落盘路径/命名混乱 | 默认命名 | chub 下载的卡会以 `main_*_spec_v2` 形式落在知识库根目录，按 `说明文档.md` 的规则归入 `角色卡\` 并起中文名 | 知识库 |

## 待办 / 进行中
- [x] 项目交接文档（负责人：Codex，状态：已完成 · 2026-08-25）：已梳理当前源码、运行数据、部署快照、项目对话遗留问题和新 AI 接手顺序；交接文档见 `<project-root>\docs\2026-08-25-项目交接文档.md`。
- [x] Denova 唯一入口一键启动 + 仅新冒险自动双向同步（负责人：A1/Codex，状态：已完成 · 2026-08-15 15:45）
- [x] 把无头回归测试台（`.workbuddy/tmp/adv_harness.js`）固化成可复用技能 / 一键脚本（负责人：AI 1，状态：已完成 · 2026-08-08）
- [x] 处理「同名剧情节点」小问题：LLM 重复输出相同标题 `NODE_NEW` 会产生多个同名 `plotNodes`（负责人：AI 1，状态：已完成 · 2026-08-08）
- [x] AI 2 补写中期修改记录（负责人：AI 2，状态：已完成）
- [x] AI 3 补写初期搭建记录（负责人：AI 3，状态：已完成 · 2026-08-08）
- [ ] 微信接入评估（负责人：待定，状态：未实施）：曾评估「微信小程序接入」方案（用户先选方案 3 后取消），当前 `app/` 为纯前端架构、无接入层；如后续重启需新建后端接入层，与现有 SPA 解耦。

## 借鉴清单（SillyTavern 分析 · 2026-08-08 · AI 1）
- [x] 【借鉴 SillyTavern·World Info】设定集关键词按需注入：把"设定书"由整段背景一次性装入，改为按条目(Key 关键词 + Content + Order + Position)在 `buildSystemPrompt` 时扫描近期叙事/玩家输入，命中才注入，并加"设定注入预算(占比)"与扫描深度（负责人：AI 1，状态：已完成 · 2026-08-08）
- [x] 【借鉴 SillyTavern·Character V3】角色卡对齐 V3 字段：多开局 `alternate_greetings`、固定深度 `character_note`(@depth 注入核心约束)、`system_prompt` 覆盖 / `post_history_instructions` 结尾指令（负责人：AI 1，状态：已完成 · 2026-08-08）
- [x] 【借鉴 SillyTavern·Prompt Manager】提示词模板化 + 宏变量（`{{char}}/{{user}}/{{location}}/{{chapter}}/{{level}}`），拆分 `buildSystemPrompt` 单体区块为可配置模板（负责人：AI 1，状态：已完成 · 2026-08-08）
- [x] 【借鉴 SillyTavern·进阶】分组 NPC 同场（group chat + talkativeness 自然激活）、扩展生态（TTS 旁白 / AI 生图 / 翻译 / 联网检索）（负责人： AI 1，状态：已完成 · 2026-08-08）
# 2026-08-29 02:52 · Codex：**完成第六阶段 Master 流水线集成验证与稳定化核验**。按项目既定启动器恢复真实 8097，并使用官方 Go 1.26.7 临时工具链完成本阶段源码 `gofmt`、`go build ./...` 和 Master 相关定向单测；期间修复 `internal/book/master_library_query_test.go` 一处新增测试语法错误，补充 `master_agent_test.go` 覆盖 input_revision/source_sha256/base_translation_version 三类 CAS 冲突。全量 `go test ./...` 仍被若干既有 Windows 权限/符号链接/路径/原子替换测试阻断，Master 查询、Runtime、Recovery claim、Proposal CAS、翻译版本定向测试通过。真实 8097 + Master Aiko 资产 + 旁路源码 Denova Recovery watcher 已跑通失败分流：8097 failed(attempts=1) → 持久化 claim → 实际启动 Master Agent Runner → 模型不可用时进入 `needs_user`；重启旁路 Denova 后同一 claim、同一 task_id 保留且未重复启动。随后 Ollama 恢复在线并确认 HY-MT/qwen 模型存在，但当前 Agent 运行配置仍解析到远程 DeepSeek，未完成“Agent 生成 Proposal → CAS Apply → 新 Translation Version”的成功闭环，因此不宣称成功。检查确认 8097 当前只有 attempts/status/updated_at，没有 terminal_failure/max_retries/next_retry；`failed + 5秒` 是临时兼容判断，5 秒窗口无法可靠识别队列即将 retry，协议缺口记录但未重写 8097。按本地自用原则不再扩大防御性代码或修复无关 Windows 测试；旁路测试进程已停止，真实 8080 部署未覆盖，待后续出现真实问题再定点修复。
# 2026-08-29 02:54 · Codex：**按用户本地自用原则收敛验证策略**。确认后续不为 Windows 权限、符号链接、路径断言等非核心边缘测试继续增加防御性代码；保留直接关系数据安全的 CAS、Recovery claim 幂等和关键日志，其余问题先记录，出现真实影响再定点修复。Ollama 已重新在线并确认 HY-MT 与本地 Qwen 模型存在；本次 Recovery 失败样例的两个 8097 活动任务已通过官方 delete 操作移出活动队列，审计文件保留；Master 原件、活动 Translation Version 和正式实例未修改。旁路 `denova-sixth.exe` 已停止，未覆盖 8080 运行版。
# 2026-08-29 02:56 · Codex：**隔离第六阶段测试残留**。发现两条本次 Recovery E2E 产生的 `needs_user` claim 若留在总库活动目录，可能影响未来同字段同身份的真实 Recovery；已将其从总库 claim 目录移动到 `denova-src/output/recovery-test-claims/` 旁路归档，未删除 JSON、未修改 Master 资产和正式版本。活动 8097 队列保持为空。

# Laya 新版 Demo 重构 · 进度快照与交互逻辑现状

> **生成时间**：2026-09-24 16:10；2026-09-24 18:40 更新：分析/提交拆层收口，正式协议另开任务设计。文档最后修改 commit 以 `git log -1 -- 本文件路径` 为准。
> **分支**：`laya-p2-safe`。**本快照最后核对基线 commit**：`38dd466`；接手时以 `git rev-parse HEAD` 与 `git ls-remote --heads origin laya-p2-safe` 为准，不把移动分支的“当前 HEAD”写死为永久事实。
> **用途**：**交接文档**——供其它 AI 在零上下文情况下接手，继续新版交互逻辑的施工。§1–§9 是现状与结论，§10–§14 是运行手册、铁律、命令速查、API 施工草案与提交规则。
> **数据来源**：`tests/capability_profiles.json`（2026-09-24 14:22 生成）、`tests/runs/eras.json`、`Laya接入报告.md` §18–§20、用户 2026-09-24 下达的《Laya 新版 Demo 重构规划：结构化判断引擎》。

---

## 1. 定位重述（新规划的北极星）

| | 旧定位 | 新定位 |
|---|---|---|
| Laya 是什么 | 小 NPC Agent（又判又写） | **Narraverse 的轻量级结构化判断层** |
| 谁做创作 | Laya 生成行为/台词 | 创作归 Claude 级 LLM |
| 铁律 | — | **有限判断 → Laya，开放创造 → LLM** |
| demo 验证什么 | "Laya 是不是聪明 NPC" | "Laya 能否**稳定**完成有限范围的判断任务"（稳定性 / 格式可靠性） |
| 终局一句话 | — | **Laya 不是另一个 Claude。Laya 是 Narraverse 的状态判断引擎。** |

---

## 2. 进度时间线（已完成的部分）

| 阶段 | 内容 | 提交/产物 |
|---|---|---|
| P0/P1 | 桥主程序、narra_config、演示页、问题集写法铁律 | 仓库初版 |
| P2 | 能力档案：grade→status→可写性判定 | `capability` / `p2_acceptance`(54 断言) |
| P2.5 | trust_shift 上下文敏感性：state 轴可用 / history 轴不可用 | `p25_acceptance`（14 PASS/2 FAIL 即结论） |
| P3 第一阶段 | trust_shift 最小闭环（State Transition + Commit） | `15a1660` |
| P3 第二阶段 | 中性死区 + fondness 多维度 + per-turn clamp | `9c17f43` 内含 |
| P3P2（本轮） | ① 目录分工（cases/experience）② 翻译 fail-closed ③ 修复 `out["engine"]` 路由崩溃 | `9c17f43` + `250316d`，已推送 |
| 2026-09-24 下午 | 用户下达新版重构 spec；完成三类判断 × 能力映射分析（本文档 §4/§5） | 本文档 |

**P3P2 三个关键修复**（细节见报告 §20）：
1. `translate_to_en` 失败从「静默回落中文」改为 **fail-closed**（抛 `TranslationFailure` → 整轮 `status="invalid"`，不调 Laya / 不 proposal / 不 commit）。
2. 空 content 真因 = reasoning 吃光 `max_tokens`（600→1600 + 提示词「照译不解释」）。
3. `/decide` `/turn` 的 `out["engine"]` 下标 → `out.get("engine")`（fail-closed 轮没有该键，曾把可控 invalid 升级成连接掐断）。

---

## 3. 能力现状快照（决定"哪些数敢用"）

来自 `tests/capability_profiles.json`（era `P3P2-2026-09-24`，缓存 sha16 `ac954c9494cde484`，264 条）：

| signal | role | td grade/status (AUC) | en grade/status (AUC) |
|---|---|---|---|
| hostility | behavior_tendency | C auxiliary (0.720) | C auxiliary (0.666) |
| cooperation | behavior_tendency | C auxiliary (0.700) | C auxiliary (0.794) |
| withdraw | behavior_tendency | D disabled (0.503) | D disabled (0.370) |
| confront | behavior_tendency | B auxiliary (0.683) | C auxiliary (0.554) |
| disclose | behavior_tendency | D disabled (0.481) | A→disabled (0.869，跑次不一致) |
| investigate | behavior_tendency | **R semantic_review (0.842)** | **R semantic_review (0.811)** |
| trust | situation_assessment | C auxiliary (0.684) | B auxiliary (0.768) |
| doubt | situation_assessment | C auxiliary (0.597) | C auxiliary (0.593) |
| danger | situation_assessment | D disabled (0.553) | D disabled (0.470) |
| trust_shift | state_shift | **C auxiliary (0.730)** | C auxiliary (0.740) |
| respect_shift | state_shift | C auxiliary (0.585) | C auxiliary (0.673) |
| doubt_shift | state_shift | **A active (0.773)** ★可写 | D disabled (0.373) |
| fondness_shift | state_shift | C auxiliary (0.702) | **A active (0.729)** ★可写 |
| alert_shift | state_shift | C auxiliary (0.671) | C auxiliary (0.582) |
| goal_shift | state_shift | D disabled (0.510) | D disabled (0.400) |

**四条硬事实**（改交互逻辑前必须记住）：
1. **typed-decisions 可写 Actor State 的只剩 `doubt_shift` 一个**（trust_shift/fondness_shift 因缓存重建 A→C，被档案门禁拒绝写入）。p3_experience 实测：50 次 commit 全是 doubt_shift，trust 恒 60。
2. **两检查点不可透明互换**：td 的 active 是 doubt_shift，en 的 active 是 fondness_shift。
3. **`investigate` 是 R 级**：AUC 很高（0.84/0.81）但方向稳定反向——**禁静默取反**。拿它做事件路由会"看起来特别可信地反向"。
4. **没有 emotion 信号**；最近的 `danger` 两检查点都是 D。

---

## 4. 新规划三类判断 × 现有能力映射（本分析的结论）

| 规划判断类型 | 最接近信号 | td | en | 结论 |
|---|---|---|---|---|
| **A. 关系状态判断** `trust_delta`/`affection_delta` | trust_shift / fondness_shift / doubt_shift | C / C / **A** | C / **A** / D | ⚠️ 接口可马上改，**但接口漂亮≠判断变准**：C 级信号返回的数不能标"可信" |
| **B. 情绪状态判断** `emotion∈[angry,sad,happy,fear,suspicious]`+intensity | 无 emotion；最近 danger=D | — | — | ❌ **从零开始**：需新造 benchmark 数据 |
| **C. 事件/剧情路由判断** `possible_trigger`+priority | investigate / confront / hostility | **R** / B / C | R / C / C | ⚠️ **必须先做 investigate 方向复核**（R 级），再谈接接口 |

**推论**：A 类是"把已有 C 级信号按新接口暴露"，要如实标注等级；B 类要立项造数据；C 类卡在一个语义问题上。**三类没有一类是"接一下现有能力就能上线"的。**

---

## 5. 新规划要点摘录（原文锚点，供后续施工对照）

本节保留历史规划原文；其中客户端直接提交 delta、未校准 confidence 等设计已由 §13 草案取代，不能直接照本节施工。旧代码保留，不在本轮删除行为或叙事功能。

- **三类判断字段**：A 关系（`trust_delta`, `affection_delta`, `emotion`, `confidence`）；B 情绪（标签 only，**不写文学性描述**）；C 路由（`possible_trigger` 候选如 `secret_reveal`/`conflict`/`comfort` + `priority`，**候选 only，LLM 决定**）。
- **删除/降级**：删 Laya 生成的剧情动作、自规划 NPC 目标、Laya 写的回复、复杂 agent 链；Proposal 降级为 **"State Delta + Confidence + Evidence"**。
- **保留**：`state → delta → commit → next turn read` 闭环；State Transition 校验/commit/history/rollback；测试框架（benchmark/validation/regression，目标调整为稳定性）。
- **§6 翻译缓存（原文）**："翻译缓存必须作为测试资产管理。固定：input.json / translation_cache.json / expected_output.json 三者共同组成测试基线。禁止：每次测试重新翻译导致输入漂移。"
  → **这是对 P3P2 选项 B（冻结缓存+版本控制）的 spec 级独立确认**——我方实测证据：temperature=0 重译仅 13% 逐字一致；缓存重建致 trust/fondness_shift A→C。
- **§7 架构图**：用户消息 → Laya Analyzer →（trust_delta / emotion / safety / event_hint / confidence）→ State Manager → 最新角色状态 → Claude/LLM → 最终剧情回复。
- **§8 API**：`POST /analyze`（in: `character_state`,`history`,`message`；out: `state_delta`,`signals`,`confidence`）；`POST /commit`（in: `character_id`,`delta`；out: 最新状态）。
- **§9 Phase-1 验收**：一条消息 → 结构化 JSON → 状态更新 → 下一轮读到新状态；**100 条固定样本**测格式错误率 / 状态一致性 / 重复输入漂移。集成价值：Claude 不再猜"NPC 是不是生气？"，而是收到 `trust -3 / emotion suspicious / event conflict`。
- **后续扩展**：respect, fear, loyalty, memory importance, safety judgement, lore consistency check。

---

## 6. 交互契约：现状 → 目标（改造落点核心）

### 6.1 现有路由契约（实测自 laya_bridge.py:2502–2789）

| 路由 | 方法 | 输入 | 输出/语义 |
|---|---|---|---|
| `/health` `/config` `/state` `/demo` `/history` | GET | — | 健康检查 / 配置 / Actor State / 演示页 / 最近 50 条历史 |
| `/decide` | POST | npc+玩家台词(+state) | 调纯分析核，只登记 Pending；**不写 Actor State、不写已提交历史**。响应带 `history_gate{stage:"proposed", committed:false}`；歧义轮 behavior=null |
| `/turn` | POST | 同上 | **兼容快路**：先纯分析，只有 `commit_state=true` 才显式提交状态与历史；`false` 两者都不写 |
| `/commit` | POST | **`turn_id`** | 兼容提交门：从服务端 Pending 取 Proposal，重新校验后提交 Actor State 与历史；客户端不能自带任意 delta |
| `/narrate` | POST | behavior 或让其先 decide；歧义接续传 `mode=upstream` + turn/session/actor | 普通分支保持行为→台词；页面歧义轮用 upstream 分支让 DeepSeek 自主回应，校验服务端 Pending 后生成，**不选 Laya 行为、不提交状态或决策历史**。未开启 LLM / 云端失败时接续明确失败，不回落台词池冒充成功 |
| `/world` | POST | 世界事件 | world_decide |
| `/reset` | POST | session_id/actor_id | 按桶清历史+Actor State（同桶同清） |
| `/predict` | POST | 任意 state+questions | 原始透传 ENGINE.predict（调 API 形状用） |

内部链路已经拆成：中文输入 → `translate_to_en`（**缓存优先，fail-closed**）→ 16 题（score/noul/choice）→ gates 覆盖 → `analyze_core()` 产出 Proposal → `validate_state_delta()` 纯校验 → 上游确认 → `commit_state()` 唯一写入 Actor State。Analyze 阶段只返回预演，不得创建状态桶。

### 6.2 目标契约（spec §8）

```
POST /analyze       {character_id, history, message, ...} → {analysis_id, state_proposal, signals, capability, base_state_version}
POST /commit_state  {analysis_id, expected_state_version} → 最新角色状态
```

### 6.3 新旧差异对照

| 维度 | 现状 | 目标 |
|---|---|---|
| 判断与提交 | `/turn` 兼容快路；`/decide`+`/commit(turn_id)` 已拆成纯分析与显式提交 | 新 API 继续沿用两步式；commit 引用服务端生成的 **analysis_id** |
| commit 语义 | 兼容路由按 turn_id 从 Pending 提交状态与历史 | `/commit_state` 按 `analysis_id + expected_state_version` 从 Pending 取已验证 Proposal，拒绝客户端自造 delta |
| 行为与台词 | choice 出行为分布，/narrate 出 LLM 台词 | **删**：Laya 不出行为/台词，只出判断 |
| Proposal | 复杂行为提案+理由+行动计划+状态变化 | **State Delta + Capability + Evidence**；单样本 confidence 须另行校准 |
| 台词 | /narrate（DeepSeek） | 全归 Claude/LLM，Laya 只喂结构化状态 |

### 6.4 文件级改造落点清单（供后续施工）

| 文件 | 动作 |
|---|---|
| `laya_bridge.py` | **地基已拆**：`analyze_core()` 纯分析、`validate_state_delta()` 纯校验、`commit_state()` 唯一写入；下一步新增 `/analyze` 与安全的 `/commit_state`，现有 `/decide` `/turn` `/commit` `/narrate` 作为兼容层 |
| `narra_config.json` | score/noul 段保留（判断原语）；choice 段降级为参考分布或删 |
| `laya-live-demo.html` | 未来改读 `signals`/`state_proposal`/`capability`；三栏中"行为/台词"区改由上游 LLM 承担，本轮未改 UI |
| `tests/` | §9 稳定性基线新目录（100 条固定样本；输入+缓存+期望输出三件套入库）；capability 档案机制原样保留 |
| `eras.json` | 新 era 声明（改造后第一次重跑时） |

---

## 7. 执行顺序建议（选项 B 已于 2026-09-24 落地，前提已满足）

1. **地基收口（本轮已实现）**：纯分析不写、`commit_state=false` 不写、服务端 Pending 后再提交；54/54 针对性断言通过，补日志、审查 diff、本地提交，不推送。
2. **独立任务先设计正式协议**：定稿 `/analyze + analysis_id + commit_state`，包括多 Agent 只读、上游确认/拒绝、过期、版本冲突与并发重复提交。本轮不实现新接口。协议确定后才建设 100 条稳定性资产与 API；benchmark 必须读取 Git 冻结缓存或临时副本。
3. **再暴露关系判断**：能力等级放在 `capability{grade,status}`，C 级可给 Claude 作辅助信息，但不得进入 `writable_delta`。没有经过单样本校准前不输出数值 confidence。
4. **B 情绪 / C 事件路由单独立项**：B 需新造 benchmark；C 需先解决 investigate 的 R 级方向复核。

**前提（已落地，2026-09-24 16:30）**：用户裁决**选项 B**——冻结缓存为不可再生基线并纳入版本控制：`tests/assets/translation_cache.json`（264 条，sha256 `ac954c94…52abc7`，与 eras.json `cache_frozen` 一致）。**此后所有等级以该缓存为准**：trust_shift/fondness_shift=C、td 可写信号仅 `doubt_shift` 为正式现状，不再追求回到 0.827。运行时缓存仍读 `_diag/translation_cache.json`，只能追加；合法增长须重新冻结入库 + 声明新 era（纪律见 `tests/assets/README.md`）。

---

## 8. 悬置决策与风险清单

| 项 | 状态 | 备注 |
|---|---|---|
| 缓存处置 A/B/C | **已决策：B（2026-09-24 16:30）**，verdict=`DECIDED_B_FROZEN_CACHE_BASELINE` | C 已排除（temperature=0 不可复现）；基线已入库 `tests/assets/translation_cache.json` |
| 桥服务运行状态 | **历史记录**（2026-09-24 16:50 工作区 venv） | 端口 8130；本轮未复核、未重启、未同步运行副本 |
| 下一步 | **已定：独立设计正式分析/提交协议** | 本轮不扩新信号，不实现 `/analyze` 或 `analysis_id` |
| capability worktree mismatch | **记录，暂不处理** | 只读核对曾报告 dataset hash / checkpoint id mismatch、缺 137/137 译文，另有 code_changed；原因未定位，不能直接归因模型能力。档案、冻结缓存、cases、runs 均不改，不重跑 capability 生成 |
| 当前兼容提交限制 | **正式协议尚未完成** | 仅覆盖顺序重复 turn_id 拒绝；无状态版本/CAS、TTL、并发原子提交、提交时重新核验 profile。旧 behavior 非空门仍保留 |
| 用户原始验收条件（30–50 轮试玩通过 P3P2） | **未达成** | 只有 doubt_shift 写状态，p3p2_playthrough/p3p2_acceptance 结果可预判，故未跑 |
| trust deadzone T=1.0 | 保留现状 | 措辞统一"部分缓解 neutral drift"，不再调 neutral band |
| E3 中性漂移（中性读成轻微负向） | **有意不修** | 要修得动模型校准，调阈值=凑参数 |
| `decision_history` 跨检查点方向不稳 | **未触及**（≠已验证无害） | 体验测试都没传 history |
| main 本地 ahead origin 9 提交 | 他人工作 | **只提交不推送**，push 前先 `git status -sb` |
| 翻译缓存保护 | **单点风险已解除**（冻结基线入库 `tests/assets/translation_cache.json`） | 运行时缓存仍只能追加；曾被空 dict 调用毁过一次（194→1 条），测试永不给 `_cached_translate` 传 `{}` |
| 回收站 2.6GB torch wheel | 待用户清空回收站 | 改名归档在 `_diag/download/` |

---

## 9. 证据文件索引

- `Laya接入报告.md` §18（P2.5）/ §19（P3 一阶段）/ §20（P3P2 失败链与修复）
- `tests/runs/eras.json`：era 声明 + `cache_frozen` + `p3p2_impact`
- `tests/capability_profiles.json`：15 signals × 2 检查点等级/状态/AUC
- `tests/p3p2_unit.py`（本轮 54/54）/ `tests/p0_acceptance.py`（20 断言，本轮未跑）/ `tests/replication/xlate_drift.py`（历史漂移量化）
- `tests/experience/p3_experience.json`、`p3p2_playthrough.json`（体验场景，不进判分指纹）
- `给外部审查的-P3P2验收说明.md`（外部审查交接）
- `tests/assets/README.md`：翻译缓存基线的冻结纪律（选项 B）
- `Laya定位书.md`：Codex 根据用户确认方向整理，本轮连同 README 和协作日志一并本地提交

---

## 10. 运行手册（接手第一件事：认路、把服务跑起来）

### 10.1 路径地图（三处副本，职责不同）

| 路径 | 角色 |
|---|---|
| `C:\Users\11\WorkBuddy\2026-09-23-11-11-35\laya-live\` | 历史 WorkBuddy 编辑/运行副本（无 .git）；本轮未同步 |
| `D:\Narraverse2.0-wt-laya`（分支 `laya-p2-safe`） | **本轮源码编辑与提交目录**；仅本地提交 |
| `D:\Narraverse2.0`（main） | 主仓，**常驻他人的未提交改动**。不要在里面做 laya 提交 |

### 10.2 启动 / 就绪 / 重启

```bash
# 启动（后台跑；无浏览器副作用，OPEN_BROWSER=1 才会开 /demo）
cd C:\Users\11\WorkBuddy\2026-09-23-11-11-35\laya-live
LAYA_MODEL=typed-decisions ./.venv/Scripts/python.exe -u laya_bridge.py serve

# 就绪判定（★ 冷启动 load_ms≈119s，端口就绪前探不到 ≠ 挂了，留足 2 分钟）
curl http://127.0.0.1:8130/health
```

- **重启前杀旧进程必须按命令行认**（PowerShell：`Get-CimInstance Win32_Process -Filter "Name='python.exe'"` → 认 `laya_bridge.py`），`Stop-Process -Id <pid> -Force`。Bash 里 `taskkill //PID` 不可用（MSYS 转义问题）。2026-09-24 实际撞过：占着 8130 的旧桥是**系统 Python `C:\Python311`** 起的。
- 检查点选择用 **`LAYA_MODEL=typed-decisions | english`**（不是 `LAYA_CHECKPOINT`）。模型在 `_models/`。**禁止同时加载两个检查点**（~840MB×2 → Segfault 无 traceback）。
- GPU：`.venv-cuda` + `LAYA_DEVICE=cuda`（长测试用；CPU venv 是 `./.venv/`）。
- **`.env` 是密钥权威**：桥启动时读入并**覆盖系统环境变量**（`DEEPSEEK_API_KEY` 等）。改 key 改 `.env`，不改 shell。key 失效是静默 401，先测 `https://api.deepseek.com/user/balance`。

---

## 11. 铁律守则（违反 = 数据作废或档案被污染，接手前必读）

### 数据资产
1. **翻译缓存不可再生**：冻结基线 `tests/assets/translation_cache.json`（264 条，sha256 `ac954c9494cde48427b80091daab20f1b5c74ddb1c7c5c9aa46940a20652abc7`）。运行时仍读 `_diag/translation_cache.json`，**只能追加、禁重译已有条目**；合法增长须重新冻结入库 + 在 eras.json 声明新 era。实测 `temperature=0` 重译逐字一致率仅 13%~24%，重建 = 全体 AUC 平移（trust/fondness_shift A→C 就是这么掉的）。**禁止为改善等级重预热；测试永不给 `_cached_translate` 传字面量 `{}`**（曾把缓存毁到 1 条）。
2. **目录分工**：`tests/cases/` = 判分基准（4 文件，进 `_dataset_fingerprint()`）；`tests/experience/` = 体验场景（不进指纹）。**改 cases 或 narra_config.json 后必须重建基线链**：`signalmetrics` → `capability` → `capability --check`。档案不 fresh 时状态层一条 delta 都不写（症状：`state_commits` 空、trust 冻住——那是门禁在工作，不是坏了）。

### 能力与状态层
3. 能力档案是"哪些 signal 能用"的唯一权威：**只有 `role=state_shift` 且 `status=active` 才写 Actor State**。当前 td 只剩 `doubt_shift` 可写；C 级=修正项（禁止单独定事实）；R 级（`investigate`）**禁静默取反**。
4. 状态链序固定：`proposal → deadzone → per-turn clamp → range clamp → Commit`（调试字段 `{old, proposal, after_deadzone, final_delta, new}` 必须保留）。**歧义轮不 commit**（behavior_is_null/awaiting_upstream → 整轮不写——事实认定权在上游）。
5. **不许为了让测试变绿调 `grade_signal` 阈值 / 放宽死区 / 改 max_tokens**——那是"参数凑出来的绿"。当前等级（trust/fondness=C）是用户裁决接受的正式现状。
6. **架构拆层不自动重建 capability**。`code_changed=true` 作为审查提示记录；worktree 的路径、checkpoint、dataset、运行缓存不匹配须另行只读定位。本轮禁止为消除告警重建档案或翻译缓存；真正变更评测条件须单独约定新基线。

### 实验纪律
7. **喂 Laya 一律英文**（typed-decisions 是英文 ModernBERT；中文静默失效：高置信+零准确）。玩家中文先翻英文。
8. 全部结果同号整齐 → 先做**反序配对**排除调用顺序漂移；小 Δ 判据前先**同态×3**定抖动下界；**变化量 vs 状态量并排比**（`*_shift` 是变化量，要状态量看 noul）；结论必须出自**预置判据的可执行脚本**（失败即结论）。
9. `decision_history` 轴跨检查点方向不稳 = **未触及**（≠已验证无害），体验测试都没传 history。E3 中性漂移（中性读成轻微负向）**有意不修**——要修得动模型校准。

### 代码模式
10. `decide()` 有多条返回路径（正常轮 / fail-closed 轮）——共享消费代码**一律 `.get` 不下标**（`out["engine"]` 事故，T10 钉住）。
11. 翻译 fail-closed：失败抛 `TranslationFailure` → 整轮 `status="invalid"`，**绝不回落中文**。空 content 按 `finish_reason` 区分 `length`（预算耗尽，配置问题）/ `stop`（真空回复）。
12. 架构不变式：`choice` 出分布（弱，conf 0.07~0.16）、`noul` 做覆盖（gates 强信号改写行为，带 `gated_by`）。改问题集三铁律：①instructions 反引号锚定 state 键名；②score.criteria 领域化档位描述；③choice 选项 3~5 词短标签。改完跑 `qcheck` + `langtest`。

---

## 12. 测试与验收命令速查

```bash
cd laya-live && PY=./.venv/Scripts/python.exe

$PY laya_bridge.py qcheck              # 问题集预算/温度桶/state 溢出（改配置后必跑）
$PY laya_bridge.py langtest            # 区分度+逐输入方向+死门限（改配置后必跑）
$PY laya_bridge.py capability          # 会生成档案；仅用于明确批准的新基线任务，本轮禁止执行
$PY laya_bridge.py capability --check  # 档案新鲜度核对

$PY tests/p3p2_unit.py                 # 本轮 54 PASS/0 FAIL；PowerShell 设置 PYTHONIOENCODING=utf-8
$PY tests/p0_acceptance.py             # P0 回归 20 断言（防路由层回归）
$PY tests/replication/xlate_drift.py   # 重译抖动量化（判据先定后验）

# GPU 侧（重基线，约 2 分钟/检查点；先确认没有别的桥在跑，防双载）
cd laya-live && LAYA_DEVICE=cuda LAYA_MODEL=typed-decisions ./.venv-cuda/Scripts/python.exe …signalmetrics

# 体验/试玩（跑模型，CPU 每轮 14–23s，建议 GPU）
$PY tests/p3_experience.py             # 最近结果 td: 6 PASS/10 FAIL（doubt_shift 单信号时代）
$PY tests/p3p2_playthrough.py          # 未跑（等扩信号后；结果目前可预判）
$PY tests/p3p2_acceptance.py           # 未跑（同上）
```
具体脚本参数以各文件顶部 argparse/docstring 为准。**新 era 重跑后旧 run 保留不删**。

---

## 13. `/analyze` + `/commit_state` 协议草案（独立任务定稿，尚未实现）

以下为设计输入，不是已完成协议。下一任务需明确 Pending 生命周期、Director 拒绝、超时失效、幂等语义和并发版本检查。Claude 可先查看 signals/auxiliary 形成草稿；上游确认后才提交，正式输出须与提交后状态一致。

### 13.1 POST /analyze

```jsonc
// 入参
{
  "session_id": "story-1",
  "character_id": "Lia",
  "history": [ /* 可选。注意 decision_history 轴未验证，默认不传 */ ],
  "message": "玩家台词（中文）"
}
// 出参 200
{
  "ok": true,
  "analysis_id": "服务端生成、一次性提交",
  "base_state_version": 12,
  "can_commit": true,         // 仅表示本次分析时可提交；提交时仍需重验
  "capability": {
    "checkpoint": "typed-decisions",
    "signals": {
      "trust_shift": { "grade": "C", "status": "auxiliary" },
      "doubt_shift": { "grade": "A", "status": "active" }
    }
  },
  "state_proposal": {         // ★ 只含过滤后可写信号（当前 td = doubt_shift 一项）
    "doubt_shift": { "target": "relationship.doubt", "delta": 3 }
  },
  "signals": {                // 全量原始读数（对应现有 raw_deltas_all_signals + noul）
    "trust_shift": {
      "raw": 2.1,
      "writable": false
    },
    "doubt_shift": {
      "raw": 3.4,
      "writable": true
    }
  },
  "evidence": { /* 问题读数、门限命中、gates 改写记录 */ }
}
// 翻译失败 → { "ok": false, "status": "invalid", "invalid_reason": "translation_failed" }
//   （不调 Laya、不产出 delta——fail-closed 语义原样保留）
```

- `capability.signals.*.grade/status` 是冻结 benchmark 得到的**能力可靠性档案**，不是这一条输入的 confidence。
- 只有在单样本置信度经过独立校准以后，才允许另加 `confidence: 0.xx`；当前阶段宁可不返回。
- `/analyze` 可以把 auxiliary 信号交给 Claude 参考，但 Pending 内的 `writable_delta` 只能包含 `active state_shift`。
- `/analyze` 调用多少次都不得修改 Actor State 或已提交历史；Pending 分析记录不等于状态提交。

### 13.2 POST /commit_state —— 只接受服务端 Proposal 的引用

```jsonc
// 入参
{
  "analysis_id": "来自 /analyze",
  "expected_state_version": 12
}
// 成功：服务端从 Pending 取自己的 Proposal，再做 capability gate、State Transition 与版本检查
// 旧版本：409 stale_state；未知/已提交 analysis_id：409 rejected；重复请求不得二次加减
```

- **禁止** `/commit_state {character_id, delta}`：客户端自带任意 delta 会绕过 capability profile、active 门禁与证据链。
- 现有 `/commit {turn_id}` 保留为兼容路由；新状态接口使用独立路径 `/commit_state`，避免名称撞义。
- `analysis_id` 必须绑定 session、character、基础状态版本、检查点能力档案和已验证 Proposal；提交时不采用客户端替换后的字段。

### 13.3 复用与红线

- **已完成的地基**：`analyze_core()` 只计算，`validate_state_delta()` 只校验，`commit_state()` 是唯一 Actor State 写入点；`/turn commit_state=false` 不再污染状态。
- **可直接复用**：`translate_to_en`（缓存+fail-closed）、问题集构建、`ENGINE.predict`、gates 覆盖、State Transition；`/analyze` 复用纯判断核，不复用任何隐式写状态包装层。
- **删除项**（spec §4）：Laya 生成剧情动作、自规划 NPC 目标、Laya 写台词、复杂 agent 链。`/narrate` 的台词职责移交上游 Claude/LLM；歧义轮 `awaiting_upstream` 语义不变。
- **冻结资产读取**：正式 benchmark 直接指定 `tests/assets/translation_cache.json`，或启动时复制到测试临时目录；不得把 `_diag/translation_cache.json` 当不可变基线。
- **第一版铁门**：100/100 JSON Schema 合法；100/100 `/analyze` 前后 Actor State 完全一致；翻译失败 0 次状态变化；只有 `active state_shift` 进入 `writable_delta`；同一冻结英文输入重复运行得到相同结构化结果；未知、过期或已提交 `analysis_id` 不得二次提交；成功提交后下一轮必须读到新状态。
- **测试资产**：输入+冻结译文+期望输出三件套入库（`tests/assets/` + 新 `tests/stability/`）；期望值明确区分精确值、方向范围与允许 unknown，避免为了绿灯硬写伪精度。
- **红线**：本轮不改 config/cases/档案/冻结缓存，不重建 capability。将来真正更改评测条件再单独批准新基线；不能用重生成掩盖 worktree mismatch。重复稳定性比较须固定状态快照、checkpoint、配置与运行条件，排除 ID/耗时等非判断字段；100 条验收尚未执行。

---

## 14. 协作与提交规则（写完代码往哪放、怎么提交）

1. **本轮流程**：在 `D:\Narraverse2.0-wt-laya` 编辑 → 补协作日志 → 审查 diff → 精确路径暂存 → 中文 Conventional Commit。
2. **绝对禁止**：`git add -A` / `git add .`；不碰别人的未提交改动。本轮 README、定位书、日志为同一 Codex 任务的改动，已获用户授权一并提交，不再误标为他人文件。
3. **本轮只本地提交，不 push**。main 不在改动范围。将来 push 须另有用户授权并先核对 `git status -sb`。
4. **`.env` 永不入库**；检查真实凭据，不把测试占位符或检测字面量当凭据；禁止输出真实密钥。
5. **网络间歇性断**：push 前先 `git ls-remote` 探测，失败不要循环重试，留本地等恢复。
6. Bash 里 git 要用 `D:/...` 形式路径（`/d/...` 报 no such directory）；diff 大量 "differ" 先怀疑行尾（autocrlf=true）。
7. 本快照的施工基线：`38dd466`（交接手册）← `4620160`（进度快照）← `9182cc3`（选项 B 落地）← `250316d` ← `9c17f43`（P3P2）← `15a1660`（P3 一阶段）。这是一条历史锚点，不代表未来移动分支的永久 HEAD。

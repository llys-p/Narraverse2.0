# 密钥
.env
.env.local

# 虚拟环境与模型权重（不入库）
.venv/
# .venv-cuda（CUDA 版 torch，约 5 GB）
.venv-*/
# .venv_broken_<ts> / .venv_parked_<ts>
.venv_*/
__pycache__/
*.pyc

# Laya 检查点（约 840 MB／个）
_models/
_hf_cache/

# 本地调试产物
_diag/
*.log
*.png

# torch CUDA 轮子（2.6 GB）。用 _fetch_cuda_torch.sh 现下，不要入库
*.whl

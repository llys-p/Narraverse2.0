# 审核任务：玩法与规则层收敛（a62002d..8e27833）

> 本文件是给 **Qoder**（审核方 AI）的独立提示词。要求 Qoder 以只读审查者身份，
> 对 `a62002d..HEAD`（当前 HEAD=8e27833）这一组**玩法层/规则层收敛**提交做一次
> 结构化代码审查，输出可执行的放行/驳回结论。请把「本文件标题起的全部内容」原文
> 交给 Qoder，无需额外解释。

---

## 你的角色

你是资深代码审查者，审查对象是 Narraverse × Laya 实时 Demo 的**玩法层**（剧情线档位 +
规则层事件裁决 + 叙事档位注入 + 剧本演示）的一组迭代。协议层（版本/锁/提交）不在
本轮申报范围；但规则裁决运行在协议提交路径里，**任何改动不得破坏既有的协议不变量**。

规则：
1. **必须读代码，不能只看提交说明。** 先 `git log --oneline a62002d..HEAD` 确认 9 个提交，
   再逐提交 `git show <sha>` 核实下面的每一个声明。
2. **只报告本次改动引入或未解决的相关风险**。改动范围之外的历史问题只作备注。
3. 每个问题必须给出：文件 + 行号 + 可复现路径，不接受只有观点没有证据。
4. 若某项改动确实消除了风险，认定它"已闭环"；若仍有绕过路径，请给出**具体绕过方法**。

## 背景：要守住的核心不变量（玩法层）

1. **双线真实可达、可持续推进**：信任线（玩家让渡/证据 → 疑点必回落）与决裂线（暴力
   升级 → 疑点必上升）都必须在真实模型下**单调可达**，不依赖模型心情；两条规则通道
   （证据 -2.8、暴力 +2.5）**互不打架**。
2. **三通道不失真**：规则不得把「质问/指控」当「让渡」、不得把「语言回落（放下…）」当
   「暴力」、不得把「带敌意的施压」当「合作」而软化疑点。任何豁免/护栏都必须能解释
   正向反例（压制 vs 不压制）。
3. **整页一致**：P2 分析模式与旧流（mode=upstream / 带 behavior 的 /narrate）两种叙事
   入口使用**同一档位判据**（trust: doubt<30；guard: 45≤doubt<70；break: doubt≥70；
   其余 probing）；前端 `_plotline` 与后端 `plot_stage` 判据一致。
4. **剧本演示无特权**：`?script=trust|break` 只复用公开协议调用（/analyze→/commit→/narrate），
   串行播放、无捷径、不污染用户默认 scope。
5. **P3-B harness 只产配对证据，不宣称质量通过**（呼应 tasks/P3-A-口径.md §5）。

## 本次提交改了什么（声明，待你核实）

| 风险（原报告/实测） | 声称的修复 | 涉及代码 |
|---|---|---|
| 决裂线卡死：模型对重复暴力台词判零/负增量，10 轮只能到 55-62 | 通道 C：暴力动作词 → 疑点无条件抬到保底 +2.5（对称证据 -2.8）；B（让渡）优先于 C；「放下…」不触发 | `laya_bridge.py` apply_rule_adjudication、_VIOLENCE_ESCALATION_WORDS/_FLOOR |
| 处女档误报信任：未提交状态 doubt 恰为 30，d<=30 让它开场就说"她开始松口" | trust 判据改严格 `d<30`；后端 `plot_stage` 与前端 `_plotline` 同步 | `laya_bridge.py` _PLOT_STAGES、`laya-live-demo.html` _plotline |
| 旧流叙事永远"试探阶段"（不随关系档位走） | `_narrate_stage_block` 注入两处 legacy 叙事（mode=upstream / 带 behavior 的 /narrate） | `laya_bridge.py` _narrate_stage_block + 两个 llm_narrate 调用点 |
| 证据词误判：质问「徽章哪来的/你有证据吗」被当让渡 → 疑点反降进信任档（P3-B 实测） | `_EVIDENCE_QUESTION_MARKERS` 质问豁免 | `laya_bridge.py` apply_rule_adjudication |
| 无问号指控句「你交信物/你身上那枚徽章」同样被当让渡 | `_EVIDENCE_ACCUSATION_FRAMES` 指控豁免 | 同上 |
| 让渡词覆盖缺漏：「掏出密令递过去，你自己看」不被识别（挽回探针实测） | `_EVIDENCE_STOP_WORDS` 补 掏出/递过去/出示/密令/纸条 | 同上 |
| 瞪视被 Laya 读成高合作 → 通道 A 打折 → 疑点被死区吃掉（30→30 平） | 通道 A 敌意护栏：hostility≥0.5 时不打折不软化 | 同上 |
| P3-B 对照一次性、不可复跑 | `tests/p3b_sig_compare.py` harness（进入 git；结果 JSON 写 `_diag/` 且被 gitignore） | `tests/p3b_sig_compare.py` |
| 剧本演示缺失 | `?script=trust|break` 串行播放两条弧线到剧情终点 | `laya-live-demo.html`（SCRIPT_LINES + runScr） |

## 重点核查清单（至少逐项过一遍）

1. **通道 C（暴力保底）**：
   - 保底 +2.5 的边界：模型已给 >2.5 时是否保留原值只打标记（不无脑抬）；负判定是否被无条件抬起
     （对称证据通道无条件压负），这是有意的——确认文档注释与代码一致。
   - 「放下…」排除会不会误伤真暴力（例："（放下刀）……"是让渡，但"（放下刀之前先划伤你）"之类混合句）？
     评估误伤面与可接受性。
   - 暴力词表与证据词表的**互斥性**：能否找到一个同时命中两者且语义中性的字符串？
2. **豁免体系（质问/指控）**：能否解释这组反例的**正/反**：
   - "（掏出密令递过去）你自己看。" → 应压制（evidence_handover）
   - "那纸密令，你从哪来的？" → 应不压制（原样返回）
   - "信物哪来的？" → 不压制；"（放下刀）名单在后巷棺底。" → 压制。
   - "你身上那枚徽章，哪来的？" vs "（翻出徽章递过去）这就是你要的徽章。" —— 前不压制、后压制。
   - 特别检查："出示" 无对象命令句（"出示证据"指使她）会不会漏压？
3. **敌意护栏**：cooperation≥0.5 且 hostility≥0.5 时整轮不打折。trade-off：既配合又施压的
   （"我能帮你，但你先放下刀"）会被拒绝软化——是否可接受？`host` 键缺失（旧档案/无该信号）默认 0.0，
   是否安全？
4. **档位判据**：doubt 恰为 30 / 45 / 70 的归属是否正确；reference_only 首轮（未提交快照=模板 doubt 30）
   叙事是否稳定落在"试探阶段"；前后端判据是否完全一致（html `_plotline` 与 py `_PLOT_STAGES`）。
5. **`_narrate_stage_block` 副作用**：对未建立状态桶的 scope 调用 `actor_state_snapshot` 是否
   **零写入、零版本推进**（确认不会创建桶/不动 generation）；两个调用点的 sid/aid 来源是否正确
   （mode=upstream 用分支内变量；带 behavior 分支现场兜底 default）。
6. **剧本演示**：是否存在任何绕过协议的捷径；`runScr` 串行 await 是否可靠（并发会 409——这正是
   设计而非缺陷）；scope 是否使用独立 `demo_scr_*`，完成时不回切，是否可接受；`speed` 下限 700ms 防抖。
7. **harness 纪律**：p3b_sig_compare.py 是否只有配对输出、无断言、无"通过/失败"结论；`_diag/` 是否
   被 gitignore（`git check-ignore demos/laya-live/_diag/p3b_sig_results.json`）。
8. **测试自证循环**：p2c 的规则用例是否**独立**断言行为（直接验证 delta 数值与标记），而不是
   "调用被测函数再断言其输出非空"；档位用例断言 `plot_stage(...)["key"]`，与 `_PLOT_STAGES` 共享
   同一常量——这是否构成自证？是否仍需接受（纯函数直接注入状态字典，属可接受的白盒测试）？给结论。

## 环境与命令（只读验证）

工作目录：`D:\Narraverse2.0-wt-laya\demos\laya-live`
- 提交清单：`git log --oneline a62002d..HEAD`（应为 9 个提交：a62002d cef84ad bc954b8 782fcbc
  6b8d3e3 719dd94 cd1eadb b2c0820 8e27833）
- 跑测试（进程内零模型，秒级）：
  `D:\Narraverse2.0-wt-laya\demos\laya-live\.venv-cuda\Scripts\python.exe tests/p2c_unit.py`
  （p2b1 / p2b2 / p3a / p3p2 / p1 同理；合计 286 项）——不要在引擎在线（8131 桥运行中）状态下同时跑测试。
- 浏览器手测（可选，付费）：`http://127.0.0.1:8131/demo?script=trust|break`（串行 5/10 轮弧线）。
- 不得修改任何文件、不得提交、不得启动/重启服务。

## 输出格式

1. **结论**：`放行` / `驳回` / `有条件放行`（必须二选一或明确"有条件"的具体条件）。
2. **问题清单**：按严重度排序（阻断/主要/次要）。每条含文件:行号、一句话风险、可复现方式。
3. **核实结果**：上面 8 类核查清单逐条 ✓ / ✗ / 部分，并注明证据。
4. **未覆盖的盲区**（备注，不作驳回理由）：硬决裂档（doubt≥70）证据压制未分档（挽回约需 17 轮）；
   暴力保底值 2.5 待用户调参；模型侧 3 处判读偏差（寒暄/示好微升疑点）未做提示词干预；力量/负重/角力
   口径、100 条稳定性资产未开。
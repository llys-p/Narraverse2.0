# 叙界 Narraverse · 夜间创作室设计系统

## 1. 视觉主题与氛围

叙界是内容优先的叙事工作区，不是 AI 控制台。整体采用暖黑纸面、低饱和苔绿与少量铜色信号，像夜间编辑室里的稿纸与书桌：安静、耐看、允许长时间阅读。

## 2. 色板与角色

- 画布：`#171614` 暖炭黑；嵌入 Denova 时继承宿主的深色环境，但不使用纯黑。
- 表面：`#211f1b` / `#292720`，只用来分隔实际层级。
- 主文字：`#eee9df`；正文次文字使用 `rgba(238,233,223,.74)`。
- 主信号：`#c99b58` 铜色，只用于主要动作、当前章节和需要注意的状态。
- 成功状态：`#8fae8d` 苔绿；错误状态：`#c77c72`；信息状态使用低饱和灰蓝，不使用霓虹蓝紫。
- 边界：优先 `1px` 细线或留白，不使用彩色左侧竖条。

## 3. 排版规则

- 中文正文使用系统字体栈：`-apple-system`、`PingFang SC`、`Microsoft YaHei`、`Noto Sans SC`。
- 西文和数字使用系统无衬线；等宽字体只用于技术字段、路径和版本号。
- 正文至少 `14px`，行高 `1.6–1.8`；标题用 `500–600` 字重，不用斜体。
- 叙事正文优先 `16–18px` 与 `1.8` 行高，状态信息保持紧凑但不抢正文注意力。

## 4. 组件样式

- 按钮：默认 6–8px 圆角、细边框、明确字重；主按钮为实色铜色，不使用渐变。
- 输入框：暖黑实面、细边框、聚焦时出现 2px 铜色外轮廓。
- 卡片：只在需要承载 z 轴关系时使用 8–10px 圆角；普通内容优先分隔线与留白。
- 标签：状态可用小圆角标签，避免全屏胶囊化；标签文字不承担主要信息。
- 弹窗：不做玻璃拟态，使用不透明暖黑表面与轻阴影；关闭和返回路径始终可见。

### 图标规则

- UI 图标统一使用底部内联的 Lucide SVG sprite（`n-icon-*`），采用 `currentColor` 继承文字/信号色。
- 图标只表达动作、导航和状态，不装饰叙事正文；正文中的 emoji 保留原样。
- 默认线宽为 `2`、圆角线帽、尺寸跟随文字 `1em`；独立图标按钮必须保留 `title` 或 `aria-label`。
- 来源：`lucide-icons/lucide`，采用仓库声明的 ISC / Feather MIT 许可；本地运行不加载远程字体或图标 CDN。

## 5. 布局原则

- 左侧是冒险/工作区索引，中间是叙事内容，右侧是可收起的状态上下文。
- 主视觉焦点只有一个：当前剧情或当前编辑内容。
- 空状态使用左对齐的短说明和一个明确动作，不使用巨大的发光图标或居中营销式 Hero。
- 移动端按“正文 → 输入 → 状态抽屉”顺序收缩，隐藏次要装饰，不压缩正文可读宽度。

## 6. 深度层级

- L0：页面画布，无阴影。
- L1：细边界与不同暖黑表面，用于区分工作区。
- L2：弹窗、抽屉等真正浮起的对象，使用低透明度暖黑阴影。
- 禁止外发光、霓虹阴影、渐变文字和全页 backdrop blur。

## 7. Do's / Don'ts

- Do：让剧情文本、章节标题、输入框和可恢复状态先被看到。
- Do：用铜色标出“现在该做什么”，用苔绿表达完成，不用多色装饰。
- Don't：紫蓝渐变、玻璃卡片、AI 星光图标、到处都是圆角胶囊、常驻“AI 正在……”状态。
- Don't：用左侧竖线、彩色光晕或巨大中心图标表达选中态。

## 8. 响应式行为

- `> 1100px`：三栏工作区；右栏保持可读宽度，不让正文被挤成窄列。
- `769–1100px`：收窄右栏与侧栏，优先保留正文和输入区。
- `≤ 768px`：侧栏转抽屉、右栏转状态区，正文单列；所有按钮保持可触达尺寸。
- 长标题、长剧情和长文件名使用换行或省略，不产生横向滚动。

## 9. Motion 哲学

- 动效只服务于状态变化、抽屉开合和按钮反馈；不为“AI 感”添加漂浮或闪烁。
- 使用 `120–220ms` 的 ease-out；hover 只做颜色/边框变化与最多 1px 位移。
- 尊重 `prefers-reduced-motion`，减少高频操作的动画。

## 参考 DNA

- 借鉴 Notion 的内容优先、`1px` whisper border 与低存在感阴影。
- 借鉴 Mintlify 的阅读行高、技术标签与边界克制，但不采用它的渐变 Hero 或全圆胶囊按钮。

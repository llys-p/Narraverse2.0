# Narraverse2.0 模块四现有版本改造总结

> 当前决策：**暂不开发 Module4 V2，不推倒重写。**
>
> 目标：基于现有 Module4 第一版继续修改，把它从“系统面板 / 调试工具”改成真正可玩的自由沙盒体验，同时尽量复用现有代码与流程。

---

## 1. 当前方向

现阶段不进入第二版的大规模开发。

不重新设计完整的多 Agent 世界、不立即接入 Obsidian、不重做整套世界架构。

当前优先目标是：

> **把现有 Module4 第一版改得更像“游戏世界”，而不是“后台控制面板”。**

也可以把这一阶段理解为：

**Module4 V1.5 改造。**

核心原则不是增加更多系统，而是：

> **把已经存在的系统从玩家面前藏到后台。**

---

# 2. 当前 Module4 已有能力

现有第一版已经具备一套完整基础流程：

```text
创建世界
↓
导入角色
↓
Daily Preview / 每日预演
↓
移动 / 行动
↓
Encounter / 相遇
↓
Judgment / 风险判断
↓
Facts / 世界事实
↓
Action Logs / 行动记录
↓
Settlement / 日结
```

因此现在没有必要把所有东西重新写一遍。

真正需要做的是重新安排这些功能的“位置”和“玩家看到的方式”。

---

# 3. 最大问题：当前太像系统面板

目前 Module4 的很多内部系统直接暴露在页面上。

玩家容易看到：

- Preview 状态
- NPC 日程
- Action Logs
- Judgment
- Facts
- 系统检查信息
- 调试状态
- 各种按钮

这些东西从开发和测试角度很有用，但不适合作为普通玩家的主体验。

新版第一版改造的目标是：

## 玩家只看到自己的世界

主界面应该重点显示：

```text
日期 / 时间
当前地点
当前场景
在场人物
剧情 / 对话
自由输入框
```

例如：

```text
9 月 4 日 · 18:20
公司 · 休息区

林雨坐在靠窗的位置。
桌上的咖啡已经凉了。

她似乎没有注意到你走进来。

你想做什么？

>
```

模块四 V1.5 的主界面应该始终让“当前世界”位于视觉中心，而不是把系统诊断信息摆在玩家脸上。

---

# 4. 主界面改造

第一版现有玩法不需要完全改变，但主界面需要重新包装。

建议主界面只保留：

## 顶部

- 日期
- 时间
- 当前地点
- 少量当前状态

## 中间

- 当前场景
- NPC 对话
- 行动结果
- 世界反馈

## 底部

一个自由输入框：

```text
你想做什么……
```

玩家可以输入：

- 我去找林雨
- 回家
- 去酒吧
- 问她发生了什么
- 等到晚上
- 偷听他们谈话
- 什么也不做
- 睡到明天
- 调查文件
- 给陈雪打电话

不再要求玩家先选择：

```text
移动
互动
调查
休息
检定
```

这些分类应该由系统在后台自己判断。

---

# 5. Action 改成统一自由入口

现有 Action 系统建议保留。

但玩家不再直接操作 Action 类型。

新的体验是：

```text
玩家自由输入
↓
系统解析玩家想做什么
↓
转换为现有 Action
↓
现有 Action.execute()
↓
世界状态变化
```

例如：

```text
“我去酒吧找林雨”
```

后台可以拆成：

```text
移动 → 酒吧
寻找 → 林雨
```

玩家不需要知道内部 Action 分类。

因此：

> **Action 保留，但隐藏。**

---

# 6. Encounter 自动运行

现有 Encounter 不需要删除。

但不应该让玩家主动点击“检测相遇”。

例如：

玩家来到酒吧。

系统后台自动检查：

```text
当前地点
当前时间
NPC 日程
NPC 是否在场
```

如果林雨也在那里：

```text
→ 自动触发 Encounter
→ 场景中出现林雨
```

玩家看到的是：

> 你推开酒吧的门，发现林雨正坐在角落的位置。

而不是：

> Encounter 检测成功：NPC 林雨。

因此：

> **Encounter 保留，但完全自动化。**

---

# 7. Judgment 降级为后台裁判

现有 Judgment 也不需要删除。

它适合处理：

- 高风险行为
- 冲突
- 偷窃
- 打架
- 潜入
- 欺骗
- 危险行动

但普通玩家不应该频繁看到“Judgment 系统”。

例如玩家输入：

> 我趁老板不注意拿走桌上的文件。

后台判断这是高风险行为。

于是：

```text
Action
↓
触发 Judgment
↓
根据当前状态计算结果
↓
生成最终行动结果
```

玩家只看到：

> 你的手刚碰到文件夹，走廊里突然传来脚步声。

因此：

> **Judgment 保留，但变成 Action 内部的风险判断器。**

---

# 8. Daily Preview 不删除，但降级

现有 Daily Preview 是第一版的重要基础，不建议现在直接删除。

但它不应该继续承担“完整模拟全世界”的职责。

建议改成：

## 轻量世界预演

每天只生成少量重要内容：

- NPC 大致计划
- 少量重要事件
- 关键地点变化
- 可能触发的关系事件
- 少量隐藏事件种子

例如：

```text
今日世界预演

林雨：
上午：公司
下午：可能与陈雪发生接触
晚上：计划独自回家

陈雪：
下午：准备和林雨谈一件事情

隐藏事件：
一份文件可能在下午被移动
```

这些内容主要给后台系统使用。

玩家默认看不到完整预演。

---

# 9. 没有传统主线，文本怎么继续？

这是自由沙盒最重要的问题之一。

如果完全没有推进机制，玩家确实可能出现：

> 今天干嘛？
>
> 不知道。
>
> 那睡觉吧。
>
> 第二天呢？
>
> 还是不知道。

最后 Module4 就会变成“发呆模拟器”。

因此：

## 没有传统主线 ≠ 没有世界推进

我们需要一个轻量的：

# 世界推进引擎

它不提供固定主线任务。

它负责让世界每天产生少量值得注意的变化。

---

# 10. 世界推进引擎

世界推进引擎的核心不是“每天生成剧情”。

而是维护：

## 未解决事件

例如：

```text
林雨和陈雪发生矛盾
```

第一天：

```text
两人发生争执
```

玩家不管。

第二天：

```text
关系继续恶化
```

仍然不管。

第三天：

```text
林雨决定主动找玩家谈
```

于是事件开始接触玩家。

也就是说：

> **事件不会因为玩家不碰就自动消失。**

它可能：

- 自己发展
- 升级
- 缓和
- 转变
- 影响其他 NPC
- 最终主动找到玩家

---

# 11. 事件链，而不是固定主线

未来文本展开依靠的是：

```text
人物目标
+
未解决事件
+
关系变化
+
世界事件
+
玩家行为
```

自然形成事件链。

例如：

```text
文件失踪
↓
林雨怀疑陈雪
↓
两人关系恶化
↓
陈雪开始调查
↓
发现老板曾接触文件
↓
林雨找玩家商量
↓
玩家决定是否参与
```

这条线不是开局提前写好的“主线任务”。

它是在世界运行中自然形成的。

因此玩家玩久之后，实际上会出现一些类似“主线”的长期故事。

区别只是：

> **这些故事是从世界里长出来的，而不是系统一开始强塞给玩家。**

---

# 12. 玩家什么都不做也可以

自由沙盒应该允许：

> 我今天什么都不干。

系统可以直接推进时间。

例如：

```text
玩家：
今天就在家待着。

↓

程序推进时间

↓

只结算重要事件

↓

第二天
```

世界可能发生：

- NPC 关系变化
- 某个事件升级
- 某个人给玩家发消息
- 地点出现变化
- 新事实产生

但不会为了表现“世界活着”而疯狂调用 AI。

---

# 13. Token 成本控制

第一版改造必须严格控制 AI 调用次数。

不能采用：

> 所有 NPC 每小时都跑一次 AI。

这会非常浪费 token，也会让系统越来越慢。

推荐原则：

## 普通动作

尽量：

```text
0～1 次 AI 调用
```

## 普通 NPC 对话

约：

```text
1 次调用
```

## 复杂冲突

约：

```text
1～3 次调用
```

## Daily Preview

每天：

```text
1 次轻量调用
```

大量确定性的事情交给程序完成。

例如：

- 时间推进
- 移动
- 地点连接
- 能量消耗
- NPC 是否在地点
- 已确定事件触发

这些不需要问模型。

---

# 14. NPC 第一版暂时不做真正多 Agent

之前讨论过 Module4 V2 可以让每个 NPC 成为独立 Agent。

这个方向仍然保留，但当前第一版不需要马上实现。

现阶段可以采用：

## 单模型，多角色视角

仍然使用同一个模型接口。

但每次 NPC 被触发时，只给模型这个 NPC 应该知道的信息。

例如：

```text
你现在是林雨。

你知道：
……

你不知道：
……

你与玩家关系：
……

你当前情绪：
……

当前场景：
……
```

这样玩家体感上已经可以得到：

> 不同人物拥有不同认知。

而不用现在就实现真正复杂的多 Agent 调度。

---

# 15. Facts 第一版需要稍微升级

现有 Facts 是值得保留的。

但为了以后支持秘密、调查、误会，最好开始区分：

## 世界事实

真正发生了什么。

例如：

```text
老板在 17:30 拿走文件。
```

## 玩家已知

玩家是否知道。

例如：

```text
玩家不知道。
```

## NPC 已知

例如：

```text
林雨：不知道
陈雪：怀疑
老板：知道
```

第一版不需要立刻建设非常复杂的认知系统。

但数据结构最好从现在开始预留这个方向。

---

# 16. 记忆与上下文

第一版不需要立即做复杂的长期记忆压缩。

可以先采用：

```text
当前场景
+
最近几轮对话
+
角色核心资料
+
最近重要 Facts
+
按需搜索相关历史
```

每次只取真正相关内容。

不把全部几十天的聊天记录一次性塞给 AI。

后续如果资料越来越大，再加入更复杂的：

- 摘要
- 重要记忆
- 主动知识检索
- Obsidian
- 向量搜索

---

# 17. 世界管理页面

被主界面隐藏掉的系统能力并不是删除。

而是集中到：

# 世界管理

例如：

```text
世界管理
├─ 今日预演
├─ NPC 状态
├─ NPC 日程
├─ 世界 Facts
├─ 事件链
├─ 行动日志
├─ 时间线
└─ 调试信息
```

玩家正常玩的时候完全可以不打开这里。

但开发、测试、整理设定时仍然可以查看。

---

# 18. 现阶段不做的内容

为了避免第一版再次膨胀，以下内容暂时不进入开发：

- 完整 Module4 V2
- 真正多 Agent 并行世界
- NPC 全天后台自主运行
- 完整长期记忆压缩系统
- Obsidian 主存储
- Obsidian Graph 自动增长
- 超大型地图模拟
- 完整经济系统
- 完整社会模拟
- 自动生成无限 NPC
- 所有 NPC 都拥有复杂长期计划

这些可以以后逐步加入。

---

# 19. 建议开发顺序

如果开始修改现有 Module4，推荐按照以下顺序。

## 第一阶段：先改玩家体验

1. 重做 Play View。
2. 隐藏调试信息。
3. 主界面改成场景 + 自由输入框。
4. Encounter 自动执行。
5. Judgment 转后台。
6. Action 统一由自由文本入口触发。

这一阶段完成后，Module4 的“感觉”就会明显改变。

## 第二阶段：增加世界推进

1. Daily Preview 轻量化。
2. 加入“未解决事件”。
3. 事件可以跨天存在。
4. 事件拥有简单的状态变化。
5. 玩家跳过时间时，只结算重要事件。

这一阶段解决：

> 没有主线以后，世界为什么还会继续产生内容？

## 第三阶段：NPC 差异化

1. NPC 独立当前目标。
2. NPC 独立当前情绪。
3. NPC 独立已知信息。
4. NPC 对话只读取自己的认知。
5. 关系和重要事实逐渐影响 NPC。

## 第四阶段：长期能力

以后再加入：

- 更好的记忆检索
- 真多 Agent
- Obsidian
- 知识图谱
- 更复杂的世界模拟

---

# 20. 这一版改造的最终目标

我们现在不是要把 Module4 做成一个巨大模拟器。

而是先做到：

> 玩家打开 Module4 后，看见一个世界，而不是一组系统。

玩家可以自由输入任何行为。

系统在后台继续使用现有：

```text
Preview
Action
Encounter
Judgment
Facts
Settlement
```

但这些名字和流程不再挡在玩家面前。

世界每天有少量事情变化。

NPC 有自己的状态。

没有固定主线，但存在不断发展的事件链。

玩家可以参与，也可以不参与。

最终形成：

> **一个简单、自由、会缓慢自己变化的 AI 沙盒世界。**

这就是现阶段 Module4 第一版最合适的改造目标。

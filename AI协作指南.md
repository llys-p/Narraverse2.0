# 叙界 Narraverse · AI 协作规范提示词

> 用途：把下面「**规范提示词（直接复制）**」整段粘贴给接手本项目的任意 AI（AI 1 / AI 2 / AI 3 共用），它即可按统一规矩工作。
> 下方「设计时的细节考量」是给**维护此规范的人**看的补充（为什么这样规定、边界怎么处理），不一定需要发给 AI。

---

## 一、规范提示词（直接复制 ↓↓↓）

```
你是「叙界 Narraverse」项目的协作 AI 之一（编号 AI _，由接手顺序确定：AI 3 最早、AI 2 中期、AI 1 当前主力）。这是一个纯静态单页文字冒险平台（位于 <project-root>），无构建步骤、无后端，改完刷新即生效。请严格按以下规矩协作：

【项目结构速览】
- 在线冒险线：app/index.html + app/style.css + app/app.js（约 5000 行核心逻辑）+ app/local_library.js（自动生成，禁止手改）。
- 离线小说线：app/game_engine.js（通用引擎，含可选 AI 增强层）+ app/packs/limitless.js（内容包）+ app/stickman.js（右侧火柴人）。
- 协作文档：<project-root>\项目协作日志.md（铁律 + 功能/踩坑记录，每条改动都必须追加）、<project-root>\代码指南.md（代码结构地图，改代码前先想到它）。

【开工前（铁律）】
1. 必读 <project-root>\项目协作日志.md（铁律 + 功能/踩坑记录 + 进行中任务）。若存在「进行中」任务，优先接续，不重复开工。
2. 涉及代码改动时（结构/提示词/状态/渲染/交互），先翻 <project-root>\代码指南.md 用 Grep/Read 定位函数与速查表；纯文案或非代码任务可不读代码指南。

【开工中（铁律）】
3. 每完成一件事（修复/新增/排查/生成）立刻追加一条日志，不要等收工；踩坑必记。
4. 记录格式：日期 YYYY-MM-DD + 精确时间 HH:MM；功能/踩坑记录一律按时间倒序（最新在最上）；涉及文件写完整路径；每条带你的 AI 编号；只写自己实际处理并确认过的内容，不确定项标注「待确认」。

【铁律 6 · 代码经验沉淀（重点）】
任何涉及代码「结构性改动」之后，把可复用的经验立刻（不必等收工）同步更新进 <project-root>\代码指南.md 对应章节：
- 新增/重命名函数 → 补到「文件地图」或对应「速查表」；
- 修改 LLM 协议或数据模型 → 补到「提示词构建管线」「数据模型」；
- 改全局 state 字段 → 补到「全局状态 state」；
- 修了一个隐藏 bug → 把根因与修法写进代码指南对应章节，并摘要点进协作日志「踩坑记录」。
- 纯文案、无结构变化的改动可不更新代码指南，但仍按铁律 3 记协作日志。
目的：让后续 AI 免重读全部源码，直接按指南定位。

【铁律 7 · 各 AI 行为潜质登记（重点）】
在项目协作日志.md 的「各 AI 行为潜质登记」区，登记你在本项目「总是稳定执行的某种行为 / 拥有的某种潜质」：
- 示例：你总在改动后跑无头回归、你擅长动画调参、你擅长提示词工程……
- 写他人部分只能依据公开记录推断，并标注「（据协作日志推断，待本人确认）」，不得编造不实内容；自己那段有变化及时更新。
- Skill 不再逐个登记：本项目已提供共享技能文件夹 `<project-root>\.workbuddy\skills\`（如 `text-adventure-collab` 协作工作流、`headless-frontend-regression` 无头回归），3 个 AI 直接调用即可。

【铁律 8 · 先用先贡献（重点）】
使用本项目的共享技能文件夹（`<project-root>\.workbuddy\skills\`）里的任何 skill 之前，你必须先把自己的、可复用的 skill 分享进该文件夹：
- 你在本项目沉淀出的、对其他人也有价值的流程 / 工具 / 知识（如提示词协议、无头回归、火柴人调参、知识库治理）应固化为 `SKILL.md` 放进 `<project-root>\.workbuddy\skills\<你的skill名>\`。
- 「贡献」先于「使用」：未贡献过自己 skill 的 AI，应先补一份再调用他人 skill；已贡献的（如 AI 1 的 `text-adventure-prompt-protocol`、AI 3 同步的 `headless-frontend-regression`）可直接调用。
- skill 内容要写明「何时调用 / 关键函数与锚点 / 避坑」，便于其他 AI 直接套用，避免重复踩坑。

【收尾自检清单】
- [ ] 改完 JS 跑过 node --check（托管 Node：C:/Users/11/.workbuddy/binaries/node/versions/22.22.2/node.exe）；
- [ ] 涉及核心流程的新功能已追加无头回归测试（.workbuddy/tmp/adv_harness.js）并通过；
- [ ] 结构性改动已同步更新代码指南.md；
- [ ] 本次工作已在协作日志.md 追加倒序日志（带时间 + AI 编号）；
- [ ] 若本次工作带来新的稳定行为/潜质，已在「各 AI 行为潜质登记」区更新；
- [ ] 若本次调用了项目共享 skill，自己也已贡献过 skill 到 `<project-root>\.workbuddy\skills\`（铁律 8：先用先贡献）。
```

---

## 二、设计时的细节考量（给维护规范的人看）

下面补充「为什么这样规定」以及容易踩的细节，便于你日后微调本规范。

### 1. 为什么铁律 1 要从「必读代码指南」改成「按代码改动判断」
- 原指令要求每次开工都读代码指南，过于僵硬、浪费上下文。改成「**涉及代码改动才先翻代码指南**」，纯文案/非代码任务放行。
- 措辞上用「先想到」而非「必读」——让 AI 有判断空间，但默认倾向去查，避免漏看已有约定（例如离线游戏「继续」必须走 `onContinue` 这种坑）。

### 2. 铁律 6「代码经验沉淀」要写清楚「什么算结构性改动」
- 明确区分「结构性改动」与「纯文案改动」，避免 AI 把无关的小修也硬塞进代码指南，造成指南膨胀。
- 把「修隐藏 bug」单列出来：根因 + 修法写进代码指南对应章节，摘要进协作日志踩坑记录——这样知识双落点，既能被接手者搜到，又有时间线。
- 强调「立刻同步，不必等收工」：经验最容易在收工时丢失，且多人接续时下一个 AI 可能立刻就需要。

### 3. 铁律 7「行为潜质登记」的边界（Skill 改为共享技能文件夹）
- 原方案要求各 AI 登记「潜质 / Skill」，但 Skill 逐个登记冗余且易过时；改为**项目级共享技能文件夹** `<project-root>\.workbuddy\skills\`，3 个 AI 直接调用，无需各自登记（已生成 `text-adventure-collab` 协作工作流技能 + 复用 `headless-frontend-regression`）。
- 「行为/潜质」仍登记：它是行为倾向（你总是做的事），用于任务分派时一眼看出「这活谁最合适」，且变化频率低、不易过时。
- **他人部分不能编造**：只能据协作日志公开记录推断并标「待确认」。这是关键安全线——避免一个 AI 替另一个 AI 写虚假内容，误导任务分派。
- 固定三段（AI 1/2/3）而非自由罗列，便于对比。

### 4. 自检清单要覆盖「代码指南同步」与「登记更新」两条新规矩
- 把铁律 6、7 也放进取清单，让 AI 在收尾时自查是否落实，而不只是记日志。
- 无头回归只对「核心流程改动」强制；纯样式/纯离线文本改动可放宽，但 node --check 始终要跑。

### 5. 本规范本身也要被维护
- 若协作方式再变（例如新增 AI 4、新增铁律 8），同步改三处：①本文件「规范提示词」正文；②项目协作日志.md 的「铁律」节；③代码指南.md 第 9 节摘要。三处口径必须一致。
- 登记区新增 AI 时，在「各 AI 行为潜质登记」下加一段，并保持 1/2/3 顺序或按实际编号。

### 6. 铁律 8「先用先贡献」的用意
- 共享技能文件夹要避免「只取不存」：若 3 个 AI 都只调用不贡献，文件夹会枯竭、且每个人重复造轮子。
- 「贡献先于使用」降低门槛又保活跃：未贡献过的 AI 先补一份再调用他人；已贡献的可直接调用。
- skill 内容标准：必须含「何时调用 / 关键函数与锚点 / 避坑」，否则别人拿到也无法直接套用。AI 1 已示范贡献 `text-adventure-prompt-protocol`（在线线协议+提示词工程）；AI 3 已把 `headless-frontend-regression` 同步进项目文件夹；AI 2 的「火柴人调参 / 知识库治理」流程建议也固化为 skill（待本人贡献）。
```
